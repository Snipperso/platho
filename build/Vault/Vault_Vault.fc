#pragma version =0.4.6;
#pragma allow-post-modification;
#pragma compute-asm-ltr;

;; Vault_Vault.headers.fc
;;
;; Header files for Vault
;; NOTE: declarations are sorted for optimal order
;;

;; __tact_context_get
(int, slice, int, slice) __tact_context_get() inline;

;; __tact_context_get_sender
slice __tact_context_get_sender() inline;

;; __tact_dict_get_slice_cell
cell __tact_dict_get_slice_cell(cell d, int kl, slice k) inline;

;; __tact_dict_set_slice_cell
(cell, ()) __tact_dict_set_slice_cell(cell d, int kl, slice k, cell v) inline;

;; __tact_dict_set_int_int
(cell, ()) __tact_dict_set_int_int(cell d, int kl, int k, int v, int vl) inline;

;; __tact_dict_get_int_cell
cell __tact_dict_get_int_cell(cell d, int kl, int k) inline;

;; __tact_dict_set_int_cell
(cell, ()) __tact_dict_set_int_cell(cell d, int kl, int k, cell v) inline;

;; __tact_dict_exists_slice
int __tact_dict_exists_slice(cell d, int kl, slice k) inline;

;; __tact_dict_exists_int
int __tact_dict_exists_int(cell d, int kl, int k) inline;

;; $AthTransferNotificationAck$_store
builder $AthTransferNotificationAck$_store(builder build_0, (int, int, int) v) inline;

;; $AthTransferNotificationAck$_store_cell
cell $AthTransferNotificationAck$_store_cell((int, int, int) v, builder b) inline;

;; $ATHTransferRequest$_store
builder $ATHTransferRequest$_store(builder build_0, (int, int, slice, slice) v) inline;

;; $ATHTransferRequest$_store_cell
cell $ATHTransferRequest$_store_cell((int, int, slice, slice) v, builder b) inline;

;; $ATHTransferRequest$_load_bounced
(slice, ((int, int))) $ATHTransferRequest$_load_bounced(slice sc_0) inline;

;; $ATHTransferRequestVaultProfileAvatar$_store
builder $ATHTransferRequestVaultProfileAvatar$_store(builder build_0, (int, int, slice, slice, int, slice, int, int, int, int, int) v) inline_ref;

;; $ATHTransferRequestVaultProfileAvatar$_store_cell
cell $ATHTransferRequestVaultProfileAvatar$_store_cell((int, int, slice, slice, int, slice, int, int, int, int, int) v, builder b) inline;

;; $ATHTransferRequestVaultProfileAvatar$_load_bounced
(slice, ((int, int))) $ATHTransferRequestVaultProfileAvatar$_load_bounced(slice sc_0) inline;

;; $ATHTransferRequestVaultMintUsername$_store
builder $ATHTransferRequestVaultMintUsername$_store(builder build_0, (int, int, slice, slice, int, slice, int, slice) v) inline;

;; $ATHTransferRequestVaultMintUsername$_store_cell
cell $ATHTransferRequestVaultMintUsername$_store_cell((int, int, slice, slice, int, slice, int, slice) v, builder b) inline;

;; $ATHTransferRequestVaultMintUsername$_load_bounced
(slice, ((int, int))) $ATHTransferRequestVaultMintUsername$_load_bounced(slice sc_0) inline;

;; $PublishPrivateFromVault$_store
builder $PublishPrivateFromVault$_store(builder build_0, (int, int, int, int, int, int, int, int, cell, cell, cell, int) v) inline_ref;

;; $PublishPrivateFromVault$_store_cell
cell $PublishPrivateFromVault$_store_cell((int, int, int, int, int, int, int, int, cell, cell, cell, int) v, builder b) inline;

;; $PublishPrivateFromVault$_load_bounced
(slice, ((int, int))) $PublishPrivateFromVault$_load_bounced(slice sc_0) inline;

;; $PublishPublicFromVault$_store
builder $PublishPublicFromVault$_store(builder build_0, (int, int, int, int, slice, int, int, cell, cell, int) v) inline;

;; $PublishPublicFromVault$_store_cell
cell $PublishPublicFromVault$_store_cell((int, int, int, int, slice, int, int, cell, cell, int) v, builder b) inline;

;; $PublishPublicFromVault$_load_bounced
(slice, ((int, int))) $PublishPublicFromVault$_load_bounced(slice sc_0) inline;

;; $PendingAthWithdrawal$_store
builder $PendingAthWithdrawal$_store(builder build_0, (slice, slice, slice, int, int, int) v) inline;

;; $PendingAthWithdrawal$_store_cell
cell $PendingAthWithdrawal$_store_cell((slice, slice, slice, int, int, int) v, builder b) inline;

;; $PendingAthWithdrawal$_load
(slice, ((slice, slice, slice, int, int, int))) $PendingAthWithdrawal$_load(slice sc_0) inline;

;; $PendingAthWithdrawal$_as_optional
tuple $PendingAthWithdrawal$_as_optional((slice, slice, slice, int, int, int) v) inline;

;; $PendingAthWithdrawal$_load_opt
tuple $PendingAthWithdrawal$_load_opt(cell cl) inline;

;; $PendingPublish$_store
builder $PendingPublish$_store(builder build_0, (slice, int, int, int, int, int, int, int) v) inline;

;; $PendingPublish$_store_cell
cell $PendingPublish$_store_cell((slice, int, int, int, int, int, int, int) v, builder b) inline;

;; $PendingPublish$_load
(slice, ((slice, int, int, int, int, int, int, int))) $PendingPublish$_load(slice sc_0) inline;

;; $PendingPublish$_as_optional
tuple $PendingPublish$_as_optional((slice, int, int, int, int, int, int, int) v) inline;

;; $PendingPublish$_load_opt
tuple $PendingPublish$_load_opt(cell cl) inline;

;; $PendingProfileAvatarPayment$_store
builder $PendingProfileAvatarPayment$_store(builder build_0, (slice, int, int) v) inline;

;; $PendingProfileAvatarPayment$_store_cell
cell $PendingProfileAvatarPayment$_store_cell((slice, int, int) v, builder b) inline;

;; $PendingProfileAvatarPayment$_load
(slice, ((slice, int, int))) $PendingProfileAvatarPayment$_load(slice sc_0) inline;

;; $PendingProfileAvatarPayment$_as_optional
tuple $PendingProfileAvatarPayment$_as_optional((slice, int, int) v) inline;

;; $PendingProfileAvatarPayment$_load_opt
tuple $PendingProfileAvatarPayment$_load_opt(cell cl) inline;

;; $PendingUsernameMintPayment$_store
builder $PendingUsernameMintPayment$_store(builder build_0, (slice, int, int) v) inline;

;; $PendingUsernameMintPayment$_store_cell
cell $PendingUsernameMintPayment$_store_cell((slice, int, int) v, builder b) inline;

;; $PendingUsernameMintPayment$_load
(slice, ((slice, int, int))) $PendingUsernameMintPayment$_load(slice sc_0) inline;

;; $PendingUsernameMintPayment$_as_optional
tuple $PendingUsernameMintPayment$_as_optional((slice, int, int) v) inline;

;; $PendingUsernameMintPayment$_load_opt
tuple $PendingUsernameMintPayment$_load_opt(cell cl) inline;

;; $ReceiveIntent$_store
builder $ReceiveIntent$_store(builder build_0, (slice, slice, int, int, int, int, int, int, int) v) inline;

;; $ReceiveIntent$_store_cell
cell $ReceiveIntent$_store_cell((slice, slice, int, int, int, int, int, int, int) v, builder b) inline;

;; $ReceiveIntent$_load
(slice, ((slice, slice, int, int, int, int, int, int, int))) $ReceiveIntent$_load(slice sc_0) inline;

;; $ReceiveIntent$_as_optional
tuple $ReceiveIntent$_as_optional((slice, slice, int, int, int, int, int, int, int) v) inline;

;; $ReceiveIntent$_load_opt
tuple $ReceiveIntent$_load_opt(cell cl) inline;

;; $KeyRecord$_store
builder $KeyRecord$_store(builder build_0, (slice, int, int, int, int, int, cell, int, int, int, int, int) v) inline_ref;

;; $KeyRecord$_store_cell
cell $KeyRecord$_store_cell((slice, int, int, int, int, int, cell, int, int, int, int, int) v, builder b) inline;

;; $KeyRecord$_load
(slice, ((slice, int, int, int, int, int, cell, int, int, int, int, int))) $KeyRecord$_load(slice sc_0) inline_ref;

;; $KeyRecord$_as_optional
tuple $KeyRecord$_as_optional((slice, int, int, int, int, int, cell, int, int, int, int, int) v) inline;

;; $KeyRecord$_load_opt
tuple $KeyRecord$_load_opt(cell cl) inline;

;; $UserState$_store
builder $UserState$_store(builder build_0, (int, int, int, int, int) v) inline;

;; $UserState$_store_cell
cell $UserState$_store_cell((int, int, int, int, int) v, builder b) inline;

;; $UserState$_load
(slice, ((int, int, int, int, int))) $UserState$_load(slice sc_0) inline;

;; $UserState$_as_optional
tuple $UserState$_as_optional((int, int, int, int, int) v) inline;

;; $UserState$_load_opt
tuple $UserState$_load_opt(cell cl) inline;

;; $Vault$_store
builder $Vault$_store(builder build_0, (slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) v) inline;

;; $Vault$_load
(slice, ((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int))) $Vault$_load(slice sc_0) inline;

;; $Context$_get_value
_ $Context$_get_value((int, slice, int, slice) v) inline;

;; $PendingAthWithdrawal$_not_null
((slice, slice, slice, int, int, int)) $PendingAthWithdrawal$_not_null(tuple v) inline;

;; $PendingPublish$_not_null
((slice, int, int, int, int, int, int, int)) $PendingPublish$_not_null(tuple v) inline;

;; $PendingProfileAvatarPayment$_not_null
((slice, int, int)) $PendingProfileAvatarPayment$_not_null(tuple v) inline;

;; $PendingUsernameMintPayment$_not_null
((slice, int, int)) $PendingUsernameMintPayment$_not_null(tuple v) inline;

;; $ReceiveIntent$_not_null
((slice, slice, int, int, int, int, int, int, int)) $ReceiveIntent$_not_null(tuple v) inline;

;; $KeyRecord$_not_null
((slice, int, int, int, int, int, cell, int, int, int, int, int)) $KeyRecord$_not_null(tuple v) inline;

;; $UserState$_not_null
((int, int, int, int, int)) $UserState$_not_null(tuple v) inline;

;; $VaultReceiveIntentView$_to_external
(int, slice, slice, int, int, int, int, int, int, int) $VaultReceiveIntentView$_to_external(((int, slice, slice, int, int, int, int, int, int, int)) v) inline;

;; $VaultKeyRecordView$_to_external
(int, slice, int, int, int, int, int, cell, int, int, int, int, int) $VaultKeyRecordView$_to_external(((int, slice, int, int, int, int, int, cell, int, int, int, int, int)) v) inline;

;; $VaultUserView$_to_external
(int, int, int, int, int, int) $VaultUserView$_to_external(((int, int, int, int, int, int)) v) inline;

;; $VaultPendingAthWithdrawalView$_to_external
(int, slice, slice, slice, int, int) $VaultPendingAthWithdrawalView$_to_external(((int, slice, slice, slice, int, int)) v) inline;

;; $VaultGlobalView$_to_external
(int, int, int, int, int, slice, slice, slice, slice, slice, int, int, int, int, int, int, int, int, int, int, int, int, int) $VaultGlobalView$_to_external(((int, int, int, int, int, slice, slice, slice, slice, slice, int, int, int, int, int, int, int, int, int, int, int, int, int)) v) inline;

;; $ATHWallet$init$_store
builder $ATHWallet$init$_store(builder build_0, (int, slice, slice) v) inline;

;; $Vault$init$_load
(slice, ((slice, slice, slice, int, int, int, int))) $Vault$init$_load(slice sc_0) inline;

;; $Vault$_contract_init
(slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $Vault$_contract_init(slice $vault_ath_wallet_address, slice $ath_master_address, slice $capsule_hub_address, int $genesis_config_hash, int $capsule_hub_bound, int $sealed, int $deployment_manifest_hash) impure inline;

;; $Vault$_contract_load
(slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $Vault$_contract_load() impure inline;

;; $Vault$_contract_store
() $Vault$_contract_store((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) v) impure inline;

;; $Cell$_fun_asSlice
slice $Cell$_fun_asSlice(cell $self) impure inline;

;; $global_newAddress
slice $global_newAddress(int $chain, int $hash) impure inline;

;; $global_contractAddressExt
slice $global_contractAddressExt(int $chain, cell $code, cell $data) impure inline;

;; $ATHWallet$_init_child
(cell, cell) $ATHWallet$_init_child(int $balance, slice $owner_address, slice $ath_master_address) inline_ref;

;; $MessageParameters$_constructor_to_value_bounce_mode_body
((int, cell, int, slice, int)) $MessageParameters$_constructor_to_value_bounce_mode_body(slice $to, int $value, int $bounce, int $mode, cell $body) inline;

;; $Vault$_fun_requireSealed
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_requireSealed((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref;

;; $Vault$_fun_requireUnsealed
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_requireUnsealed((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref;

;; $Vault$_fun_isBindingSet
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_isBindingSet((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $mask) impure inline_ref;

;; $Vault$_fun_isCapsuleHubBound
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_isCapsuleHubBound((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref;

;; $Vault$_fun_isProfileRegistryBound
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_isProfileRegistryBound((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref;

;; $Vault$_fun_isUsernameRegistryBound
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_isUsernameRegistryBound((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref;

;; $Vault$_fun_requireGenesisController
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_requireGenesisController((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref;

;; $UserState$_constructor_ton_balance_ath_balance_current_key_id_auth_pubkey_publish_nonce
((int, int, int, int, int)) $UserState$_constructor_ton_balance_ath_balance_current_key_id_auth_pubkey_publish_nonce(int $ton_balance, int $ath_balance, int $current_key_id, int $auth_pubkey, int $publish_nonce) inline;

;; $Vault$_fun_emptyUser
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), (int, int, int, int, int)) $Vault$_fun_emptyUser((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref;

;; $Vault$_fun_getUserOrEmpty
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), (int, int, int, int, int)) $Vault$_fun_getUserOrEmpty((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $owner) impure inline_ref;

;; $Vault$_fun_deriveAthWalletAddress
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), slice) $Vault$_fun_deriveAthWalletAddress((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $owner) impure inline_ref;

;; $Vault$_fun_isBasechainAddress
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_isBasechainAddress((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $owner) impure inline_ref;

;; $Vault$_fun_pendingPublishCount
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_pendingPublishCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref;

;; $Vault$_fun_pendingProfileAvatarPaymentCount
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_pendingProfileAvatarPaymentCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref;

;; $Vault$_fun_pendingUsernameMintPaymentCount
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_pendingUsernameMintPaymentCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref;

;; $Vault$_fun_incrementPendingPublishCount
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_incrementPendingPublishCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref;

;; $Vault$_fun_decrementPendingPublishCount
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_decrementPendingPublishCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref;

;; $Vault$_fun_incrementPendingProfileAvatarPaymentCount
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_incrementPendingProfileAvatarPaymentCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref;

;; $Vault$_fun_decrementPendingProfileAvatarPaymentCount
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_decrementPendingProfileAvatarPaymentCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref;

;; $Vault$_fun_incrementPendingUsernameMintPaymentCount
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_incrementPendingUsernameMintPaymentCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref;

;; $Vault$_fun_decrementPendingUsernameMintPaymentCount
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_decrementPendingUsernameMintPaymentCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref;

;; $Vault$_fun_computeAthDepositId
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_computeAthDepositId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $senderWallet, int $queryId) impure inline_ref;

;; $Vault$_fun_computeAthWithdrawalId
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_computeAthWithdrawalId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $ownerWallet, int $queryId) impure inline_ref;

;; $Vault$_fun_computeProfileAvatarPaymentId
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_computeProfileAvatarPaymentId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $ownerWallet, int $clientNonce, int $avatarHash) impure inline_ref;

;; $Vault$_fun_computeUsernameNameHash
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_computeUsernameNameHash((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $username) impure inline_ref;

;; $Vault$_fun_computeUsernameMintPaymentId
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_computeUsernameMintPaymentId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $ownerWallet, int $clientNonce, int $nameHash) impure inline_ref;

;; $Vault$_fun_profileRegistryAthWalletAddress
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), slice) $Vault$_fun_profileRegistryAthWalletAddress((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref;

;; $Vault$_fun_usernameRegistryAthWalletAddress
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), slice) $Vault$_fun_usernameRegistryAthWalletAddress((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref;

;; $Vault$_fun_profileAvatarTonCharge
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_profileAvatarTonCharge((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref;

;; $Vault$_fun_usernameMintTonCharge
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_usernameMintTonCharge((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref;

;; $Vault$_fun_usernamePriceForLength
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_usernamePriceForLength((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $nameLen) impure inline_ref;

;; $Vault$_fun_isValidUsernameBytes
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_isValidUsernameBytes((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $nameLen, slice $username) impure inline_ref;

;; $Vault$_fun_requireValidProfileAvatarPointer
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_requireValidProfileAvatarPointer((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $ownerWallet, int $avatarHash, int $avatarStreamId, int $avatarPartCount, int $mediaFormat) impure inline_ref;

;; $Vault$_fun_discountedFee
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_discountedFee((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $owner, int $fullFee) impure inline_ref;

;; $Vault$_fun_isAllowedPrivateSizeClass
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_isAllowedPrivateSizeClass((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $sizeClass) impure inline_ref;

;; $Vault$_fun_isPublishProfileValid
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_isPublishProfileValid((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $publishKind, int $sizeClass, int $cryptoSuite, int $header0, int $header1) impure inline_ref;

;; $Vault$_fun_privateUsefulBytes
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_privateUsefulBytes((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $sizeClass) impure inline_ref;

;; $Vault$_fun_privateBodyOverheadBytes
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_privateBodyOverheadBytes((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $cryptoSuite) impure inline_ref;

;; $Vault$_fun_privateBodyBytes
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_privateBodyBytes((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $sizeClass, int $cryptoSuite) impure inline_ref;

;; $Vault$_fun_privateBodyBits
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_privateBodyBits((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $sizeClass, int $cryptoSuite) impure inline_ref;

;; $Vault$_fun_privateBodyCells
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_privateBodyCells((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $sizeClass, int $cryptoSuite) impure inline_ref;

;; $Vault$_fun_privateBodyRefs
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_privateBodyRefs((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $sizeClass, int $cryptoSuite) impure inline_ref;

;; $Vault$_fun_isExactCapsuleCell
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_isExactCapsuleCell((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, cell $cell, int $expectedCells, int $expectedBits, int $expectedRefs) impure inline_ref;

;; $Vault$_fun_isPublicCapsuleCell
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_isPublicCapsuleCell((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, cell $cell, int $maxCells, int $maxBits, int $maxRefs, int $requireByteAligned) impure inline_ref;

;; $Vault$_fun_isPrivateCapsuleShapeValid
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_isPrivateCapsuleShapeValid((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $sizeClass, int $cryptoSuite, cell $header0, cell $header1, cell $body) impure inline_ref;

;; $Vault$_fun_isPublicCapsuleShapeValid
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_isPublicCapsuleShapeValid((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, cell $header, cell $body) impure inline_ref;

;; $Vault$_fun_privatePublishLocalExecReserve
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_privatePublishLocalExecReserve((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $sizeClass, int $cryptoSuite) impure inline_ref;

;; $Vault$_fun_privateCapsulehubExecReserve
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_privateCapsulehubExecReserve((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $sizeClass, int $cryptoSuite) impure inline_ref;

;; $Vault$_fun_publishLocalExecReserve
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_publishLocalExecReserve((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $publishKind, int $sizeClass, int $cryptoSuite) impure inline_ref;

;; $Vault$_fun_canonicalPublishMaxCharge
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_canonicalPublishMaxCharge((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $owner, int $publishKind, int $sizeClass, int $cryptoSuite) impure inline_ref;

;; $Vault$_fun_computePublishId
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_computePublishId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $owner, int $nonce, int $bodyHash, int $publishKind) impure inline_ref;

;; $Vault$_fun_capsulehubCallValue
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_capsulehubCallValue((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $maxCharge, int $localExecReserve) impure inline_ref;

;; $Vault$_fun_computePublishBounceId
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_computePublishBounceId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $publishId) impure inline_ref;

;; $Vault$_fun_computePublishBounceTag
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_computePublishBounceTag((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $publishId) impure inline_ref;

;; $Vault$_fun_protocolFeePaid
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_protocolFeePaid((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $owner, int $publishKind, int $sizeClass, int $cryptoSuite) impure inline_ref;

;; $Vault$_fun_refundPublishValue
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_refundPublishValue((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $owner, int $refundableAmount, int $amount) impure inline_ref;

;; $Vault$_fun_refundPendingPublish
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_refundPendingPublish((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, (slice, int, int, int, int, int, int, int) $pending, int $amount) impure inline_ref;

;; $Vault$_fun_refundPendingAthWithdrawalTon
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_refundPendingAthWithdrawalTon((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, (slice, slice, slice, int, int, int) $pending, int $amount) impure inline_ref;

;; $Vault$_fun_restorePendingProfileAvatarPayment
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_restorePendingProfileAvatarPayment((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $queryId, (slice, int, int) $pending) impure inline_ref;

;; $Vault$_fun_restorePendingUsernameMintPayment
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_restorePendingUsernameMintPayment((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $queryId, (slice, int, int) $pending) impure inline_ref;

;; $Vault$_fun_creditActivityAirdrop
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_creditActivityAirdrop((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $owner) impure inline_ref;

;; $Vault$_fun_keyRecordStorageEndowment
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_keyRecordStorageEndowment((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $cryptoSuiteMask) impure inline_ref;

;; $Vault$_fun_validatePqPubkeyCell
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_validatePqPubkeyCell((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $pqLen, int $cryptoSuiteMask, cell $pqPubkey) impure inline_ref;

;; $Vault$_fun_validateKeyProfile
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_validateKeyProfile((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $encPubkey, int $signPubkey, int $pqHash, int $pqLen, cell $pqPubkey, int $cryptoSuiteMask) impure inline_ref;

;; $Vault$_fun_computeKeyId
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_computeKeyId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $owner, int $keyGeneration, int $encPubkey, int $signPubkey, int $pqHash, int $pqLen, int $cryptoSuiteMask) impure inline_ref;

;; $Vault$_fun_computeReceiveIntentId
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_computeReceiveIntentId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $senderWallet, slice $recipientWallet, int $asset, int $amount, int $clientNonce) impure inline_ref;

;; $Vault$_fun_computeReceiveIntentCommitment
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_computeReceiveIntentCommitment((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $intentId, slice $recipientWallet, int $secret32) impure inline_ref;

;; $VaultUserView$_constructor_exists_ton_balance_ath_balance_current_key_id_auth_pubkey_publish_nonce
((int, int, int, int, int, int)) $VaultUserView$_constructor_exists_ton_balance_ath_balance_current_key_id_auth_pubkey_publish_nonce(int $exists, int $ton_balance, int $ath_balance, int $current_key_id, int $auth_pubkey, int $publish_nonce) inline;

;; $Vault$_fun_get_user
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), (int, int, int, int, int, int)) $Vault$_fun_get_user((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $owner) impure inline_ref;

;; $VaultKeyRecordView$_constructor_exists_owner_wallet_key_generation_enc_pubkey_sign_pubkey_pq_kem_pubkey_hash_pq_kem_pubkey_len_pq_kem_pubkey_crypto_suite_mask_created_at_created_lt_revoked_at_revoked_lt
((int, slice, int, int, int, int, int, cell, int, int, int, int, int)) $VaultKeyRecordView$_constructor_exists_owner_wallet_key_generation_enc_pubkey_sign_pubkey_pq_kem_pubkey_hash_pq_kem_pubkey_len_pq_kem_pubkey_crypto_suite_mask_created_at_created_lt_revoked_at_revoked_lt(int $exists, slice $owner_wallet, int $key_generation, int $enc_pubkey, int $sign_pubkey, int $pq_kem_pubkey_hash, int $pq_kem_pubkey_len, cell $pq_kem_pubkey, int $crypto_suite_mask, int $created_at, int $created_lt, int $revoked_at, int $revoked_lt) inline;

;; $Vault$_fun_get_key_record
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), (int, slice, int, int, int, int, int, cell, int, int, int, int, int)) $Vault$_fun_get_key_record((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $keyId) impure inline_ref;

;; $VaultReceiveIntentView$_constructor_exists_sender_wallet_recipient_wallet_asset_amount_commitment_client_nonce_settlement_reserve_ton_created_at_claimed
((int, slice, slice, int, int, int, int, int, int, int)) $VaultReceiveIntentView$_constructor_exists_sender_wallet_recipient_wallet_asset_amount_commitment_client_nonce_settlement_reserve_ton_created_at_claimed(int $exists, slice $sender_wallet, slice $recipient_wallet, int $asset, int $amount, int $commitment, int $client_nonce, int $settlement_reserve_ton, int $created_at, int $claimed) inline;

;; $Vault$_fun_get_receive_intent
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), (int, slice, slice, int, int, int, int, int, int, int)) $Vault$_fun_get_receive_intent((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $intentId) impure inline_ref;

;; $Vault$_fun_get_receive_intent_id
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_get_receive_intent_id((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $senderWallet, slice $recipientWallet, int $asset, int $amount, int $clientNonce) impure inline_ref;

;; $Vault$_fun_get_receive_intent_commitment
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_get_receive_intent_commitment((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $intentId, slice $recipientWallet, int $secret32) impure inline_ref;

;; $VaultPendingAthWithdrawalView$_constructor_exists_owner_wallet_recipient_recipient_ath_wallet_amount_created_at
((int, slice, slice, slice, int, int)) $VaultPendingAthWithdrawalView$_constructor_exists_owner_wallet_recipient_recipient_ath_wallet_amount_created_at(int $exists, slice $owner_wallet, slice $recipient, slice $recipient_ath_wallet, int $amount, int $created_at) inline;

;; $Vault$_fun_get_pending_ath_withdrawal
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), (int, slice, slice, slice, int, int)) $Vault$_fun_get_pending_ath_withdrawal((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $queryId) impure inline_ref;

;; $Vault$_fun_get_ath_withdrawal_id
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_get_ath_withdrawal_id((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $ownerWallet, int $queryId) impure inline_ref;

;; $Vault$_fun_get_pending_ath_withdrawal_for
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), (int, slice, slice, slice, int, int)) $Vault$_fun_get_pending_ath_withdrawal_for((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $ownerWallet, int $queryId) impure inline_ref;

;; $Vault$_fun_get_canonical_publish_charge
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_get_canonical_publish_charge((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $owner, int $publishKind, int $sizeClass, int $cryptoSuite) impure inline_ref;

;; $VaultGlobalView$_constructor_sealed_capsule_hub_bound_profile_registry_bound_username_registry_bound_deployment_manifest_hash_capsule_hub_address_profile_registry_address_username_registry_address_vault_ath_wallet_address_ath_master_address_user_count_key_record_count_receive_intent_count_pending_ath_withdrawal_count_pending_publish_count_pending_profile_avatar_payment_count_pending_username_mint_payment_count_processed_ath_deposit_count_pending_publish_stale_ttl_airdrop_remaining_ath_airdrop_distributed_ath_airdrop_reward_per_message_ath_airdrop_total_allocation_ath
((int, int, int, int, int, slice, slice, slice, slice, slice, int, int, int, int, int, int, int, int, int, int, int, int, int)) $VaultGlobalView$_constructor_sealed_capsule_hub_bound_profile_registry_bound_username_registry_bound_deployment_manifest_hash_capsule_hub_address_profile_registry_address_username_registry_address_vault_ath_wallet_address_ath_master_address_user_count_key_record_count_receive_intent_count_pending_ath_withdrawal_count_pending_publish_count_pending_profile_avatar_payment_count_pending_username_mint_payment_count_processed_ath_deposit_count_pending_publish_stale_ttl_airdrop_remaining_ath_airdrop_distributed_ath_airdrop_reward_per_message_ath_airdrop_total_allocation_ath(int $sealed, int $capsule_hub_bound, int $profile_registry_bound, int $username_registry_bound, int $deployment_manifest_hash, slice $capsule_hub_address, slice $profile_registry_address, slice $username_registry_address, slice $vault_ath_wallet_address, slice $ath_master_address, int $user_count, int $key_record_count, int $receive_intent_count, int $pending_ath_withdrawal_count, int $pending_publish_count, int $pending_profile_avatar_payment_count, int $pending_username_mint_payment_count, int $processed_ath_deposit_count, int $pending_publish_stale_ttl, int $airdrop_remaining_ath, int $airdrop_distributed_ath, int $airdrop_reward_per_message_ath, int $airdrop_total_allocation_ath) inline;

;; $Vault$_fun_get_global
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), (int, int, int, int, int, slice, slice, slice, slice, slice, int, int, int, int, int, int, int, int, int, int, int, int, int)) $Vault$_fun_get_global((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref;

;; $AthTransferNotificationAck$_constructor_query_id_sender_key_amount
((int, int, int)) $AthTransferNotificationAck$_constructor_query_id_sender_key_amount(int $query_id, int $sender_key, int $amount) inline;

;; $PendingAthWithdrawal$_constructor_owner_wallet_recipient_recipient_ath_wallet_amount_refundable_ton_amount_created_at
((slice, slice, slice, int, int, int)) $PendingAthWithdrawal$_constructor_owner_wallet_recipient_recipient_ath_wallet_amount_refundable_ton_amount_created_at(slice $owner_wallet, slice $recipient, slice $recipient_ath_wallet, int $amount, int $refundable_ton_amount, int $created_at) inline;

;; $ATHTransferRequest$_constructor_query_id_amount_recipient_response_destination
((int, int, slice, slice)) $ATHTransferRequest$_constructor_query_id_amount_recipient_response_destination(int $query_id, int $amount, slice $recipient, slice $response_destination) inline;

;; $KeyRecord$_constructor_owner_wallet_key_generation_enc_pubkey_sign_pubkey_pq_kem_pubkey_hash_pq_kem_pubkey_len_pq_kem_pubkey_crypto_suite_mask_created_at_created_lt_revoked_at_revoked_lt
((slice, int, int, int, int, int, cell, int, int, int, int, int)) $KeyRecord$_constructor_owner_wallet_key_generation_enc_pubkey_sign_pubkey_pq_kem_pubkey_hash_pq_kem_pubkey_len_pq_kem_pubkey_crypto_suite_mask_created_at_created_lt_revoked_at_revoked_lt(slice $owner_wallet, int $key_generation, int $enc_pubkey, int $sign_pubkey, int $pq_kem_pubkey_hash, int $pq_kem_pubkey_len, cell $pq_kem_pubkey, int $crypto_suite_mask, int $created_at, int $created_lt, int $revoked_at, int $revoked_lt) inline;

;; $PendingPublish$_constructor_owner_wallet_tombstone_refund_to_vault_nonce_publish_kind_body_hash_refundable_amount_created_at
((slice, int, int, int, int, int, int, int)) $PendingPublish$_constructor_owner_wallet_tombstone_refund_to_vault_nonce_publish_kind_body_hash_refundable_amount_created_at(slice $owner_wallet, int $tombstone, int $refund_to_vault, int $nonce, int $publish_kind, int $body_hash, int $refundable_amount, int $created_at) inline;

;; $ReceiveIntent$_constructor_sender_wallet_recipient_wallet_asset_amount_commitment_client_nonce_settlement_reserve_ton_created_at_claimed
((slice, slice, int, int, int, int, int, int, int)) $ReceiveIntent$_constructor_sender_wallet_recipient_wallet_asset_amount_commitment_client_nonce_settlement_reserve_ton_created_at_claimed(slice $sender_wallet, slice $recipient_wallet, int $asset, int $amount, int $commitment, int $client_nonce, int $settlement_reserve_ton, int $created_at, int $claimed) inline;

;; $PublishPrivateFromVault$_constructor_bounce_id_bounce_tag_publish_id_size_class_crypto_suite_header_0_hash_header_1_hash_body_hash_header_0_header_1_body_protocol_fee_paid
((int, int, int, int, int, int, int, int, cell, cell, cell, int)) $PublishPrivateFromVault$_constructor_bounce_id_bounce_tag_publish_id_size_class_crypto_suite_header_0_hash_header_1_hash_body_hash_header_0_header_1_body_protocol_fee_paid(int $bounce_id, int $bounce_tag, int $publish_id, int $size_class, int $crypto_suite, int $header_0_hash, int $header_1_hash, int $body_hash, cell $header_0, cell $header_1, cell $body, int $protocol_fee_paid) inline;

;; $PublishPublicFromVault$_constructor_bounce_id_bounce_tag_publish_id_marketing_note_author_wallet_header_hash_body_hash_header_body_protocol_fee_paid
((int, int, int, int, slice, int, int, cell, cell, int)) $PublishPublicFromVault$_constructor_bounce_id_bounce_tag_publish_id_marketing_note_author_wallet_header_hash_body_hash_header_body_protocol_fee_paid(int $bounce_id, int $bounce_tag, int $publish_id, int $marketing_note, slice $author_wallet, int $header_hash, int $body_hash, cell $header, cell $body, int $protocol_fee_paid) inline;

;; $PendingProfileAvatarPayment$_constructor_owner_wallet_amount_created_at
((slice, int, int)) $PendingProfileAvatarPayment$_constructor_owner_wallet_amount_created_at(slice $owner_wallet, int $amount, int $created_at) inline;

;; $ATHTransferRequestVaultProfileAvatar$_constructor_query_id_amount_recipient_response_destination_notify_value_owner_wallet_avatar_hash_avatar_entry_id_avatar_stream_id_avatar_part_count_media_format
((int, int, slice, slice, int, slice, int, int, int, int, int)) $ATHTransferRequestVaultProfileAvatar$_constructor_query_id_amount_recipient_response_destination_notify_value_owner_wallet_avatar_hash_avatar_entry_id_avatar_stream_id_avatar_part_count_media_format(int $query_id, int $amount, slice $recipient, slice $response_destination, int $notify_value, slice $owner_wallet, int $avatar_hash, int $avatar_entry_id, int $avatar_stream_id, int $avatar_part_count, int $media_format) inline;

;; $PendingUsernameMintPayment$_constructor_owner_wallet_amount_created_at
((slice, int, int)) $PendingUsernameMintPayment$_constructor_owner_wallet_amount_created_at(slice $owner_wallet, int $amount, int $created_at) inline;

;; $ATHTransferRequestVaultMintUsername$_constructor_query_id_amount_recipient_response_destination_notify_value_owner_wallet_username_len_username
((int, int, slice, slice, int, slice, int, slice)) $ATHTransferRequestVaultMintUsername$_constructor_query_id_amount_recipient_response_destination_notify_value_owner_wallet_username_len_username(int $query_id, int $amount, slice $recipient, slice $response_destination, int $notify_value, slice $owner_wallet, int $username_len, slice $username) inline;


;; Vault_Vault.stdlib.fc
global (int, slice, int, slice) __tact_context;
global slice __tact_context_sender;
global cell __tact_child_contract_codes;
global int __tact_randomized;

(cell, int) __tact_dict_delete(cell dict, int key_len, slice index) asm(index dict key_len) """
    DICTDEL
""";

(cell, int) __tact_dict_delete_int(cell dict, int key_len, int index) asm(index dict key_len) """
    DICTIDEL
""";

((cell), ()) __tact_dict_set_ref(cell dict, int key_len, slice index, cell value) asm(value index dict key_len) """
    DICTSETREF
""";

(slice, int) __tact_dict_get(cell dict, int key_len, slice index) asm(index dict key_len) """
    DICTGET NULLSWAPIFNOT
""";

(cell, int) __tact_dict_get_ref(cell dict, int key_len, slice index) asm(index dict key_len) """
    DICTGETREF NULLSWAPIFNOT
""";

(int, slice, int, slice) __tact_context_get() inline {
    return __tact_context;
}

slice __tact_context_get_sender() inline {
    return __tact_context_sender;
}

forall X0, X1, X2 -> tuple __tact_tuple_create_3((X0, X1, X2) v) asm """
    3 TUPLE
""";

forall X0, X1, X2 -> (X0, X1, X2) __tact_tuple_destroy_3(tuple v) asm """
    3 UNTUPLE
""";

forall X0, X1, X2, X3, X4 -> tuple __tact_tuple_create_5((X0, X1, X2, X3, X4) v) asm """
    5 TUPLE
""";

forall X0, X1, X2, X3, X4 -> (X0, X1, X2, X3, X4) __tact_tuple_destroy_5(tuple v) asm """
    5 UNTUPLE
""";

forall X0, X1, X2, X3, X4, X5 -> tuple __tact_tuple_create_6((X0, X1, X2, X3, X4, X5) v) asm """
    6 TUPLE
""";

forall X0, X1, X2, X3, X4, X5 -> (X0, X1, X2, X3, X4, X5) __tact_tuple_destroy_6(tuple v) asm """
    6 UNTUPLE
""";

forall X0, X1, X2, X3, X4, X5, X6, X7 -> tuple __tact_tuple_create_8((X0, X1, X2, X3, X4, X5, X6, X7) v) asm """
    8 TUPLE
""";

forall X0, X1, X2, X3, X4, X5, X6, X7 -> (X0, X1, X2, X3, X4, X5, X6, X7) __tact_tuple_destroy_8(tuple v) asm """
    8 UNTUPLE
""";

forall X0, X1, X2, X3, X4, X5, X6, X7, X8 -> tuple __tact_tuple_create_9((X0, X1, X2, X3, X4, X5, X6, X7, X8) v) asm """
    9 TUPLE
""";

forall X0, X1, X2, X3, X4, X5, X6, X7, X8 -> (X0, X1, X2, X3, X4, X5, X6, X7, X8) __tact_tuple_destroy_9(tuple v) asm """
    9 UNTUPLE
""";

forall X0, X1, X2, X3, X4, X5, X6, X7, X8, X9, X10, X11 -> tuple __tact_tuple_create_12((X0, X1, X2, X3, X4, X5, X6, X7, X8, X9, X10, X11) v) asm """
    12 TUPLE
""";

forall X0, X1, X2, X3, X4, X5, X6, X7, X8, X9, X10, X11 -> (X0, X1, X2, X3, X4, X5, X6, X7, X8, X9, X10, X11) __tact_tuple_destroy_12(tuple v) asm """
    12 UNTUPLE
""";

cell __tact_dict_get_slice_cell(cell d, int kl, slice k) inline {
    var (r, ok) = __tact_dict_get_ref(d, kl, k);
    if (ok) {
        return r;
    } else {
        return null();
    }
}

(cell, ()) __tact_dict_set_slice_cell(cell d, int kl, slice k, cell v) inline {
    if (null?(v)) {
        var (r, ok) = __tact_dict_delete(d, kl, k);
        return (r, ());
    } else {
        return __tact_dict_set_ref(d, kl, k, v);
    }
}

(cell, ()) __tact_dict_set_int_int(cell d, int kl, int k, int v, int vl) inline {
    if (null?(v)) {
        var (r, ok) = idict_delete?(d, kl, k);
        return (r, ());
    } else {
        return (idict_set_builder(d, kl, k, begin_cell().store_int(v, vl)), ());
    }
}

cell __tact_dict_get_int_cell(cell d, int kl, int k) inline {
    var (r, ok) = idict_get_ref?(d, kl, k);
    if (ok) {
        return r;
    } else {
        return null();
    }
}

(cell, ()) __tact_dict_set_int_cell(cell d, int kl, int k, cell v) inline {
    if (null?(v)) {
        var (r, ok) = idict_delete?(d, kl, k);
        return (r, ());
    } else {
        return (idict_set_ref(d, kl, k, v), ());
    }
}

int __tact_dict_exists_slice(cell d, int kl, slice k) inline {
    var (r, ok) = __tact_dict_get(d, kl, k);
    return ok;
}

int __tact_dict_exists_int(cell d, int kl, int k) inline {
    var (r, ok) = idict_get?(d, kl, k);
    return ok;
}

builder $global_beginCell() impure asm """
    NEWC
""";

cell $global_emptyCell() impure asm """
    <b b> PUSHREF // Pure Fift: "<b" creates a builder, "b>" turns it into a cell at compile time
""";

int $global_checkSignature(int $hash, slice $signature, int $publicKey) impure asm """
    CHKSIGNU
""";

int $global_contractHash(cell $code, cell $data) impure asm """
    s0 PUSH HASHCU // `data` hash
    s2 PUSH HASHCU // `code` hash
    SWAP2
    CDEPTH         // `data` depth
    SWAP
    CDEPTH         // `code` depth
    131380 INT     // (2 << 16) | (1 << 8) | 0x34

    // Group 2: Composition of the Builder
    NEWC
    24 STU  // store refs_descriptor | bits_descriptor | data
    16 STU  // store depth_descriptor for `code`
    16 STU  // store depth_descriptor for `data`
    256 STU // store `code` hash
    256 STU // store `data` hash

    // Group 3: SHA256 hash of the resulting Builder
    ONE HASHEXT_SHA256
""";

slice $Slice$_fun_asAddressUnsafe(slice $self) impure asm "NOP";

slice $Cell$_fun_beginParse(cell $self) impure asm """
    CTOS
""";

slice $Cell$_fun_asSlice(cell $self) impure inline {
    var ($self) = $self;
    return $Cell$_fun_beginParse($self);
}

cell $Builder$_fun_endCell(builder $self) impure asm """
    ENDC
""";

slice $global_newAddress(int $chain, int $hash) impure inline {
    return $Slice$_fun_asAddressUnsafe($Cell$_fun_asSlice($Builder$_fun_endCell(store_uint(store_int(store_uint($global_beginCell(), 4, 3), $chain, 8), $hash, 256))));
}

slice $global_contractAddressExt(int $chain, cell $code, cell $data) impure inline {
    int $hash = $global_contractHash($code, $data);
    return $global_newAddress($chain, $hash);
}

slice $global_myAddress() impure asm """
    MYADDR
""";

() $global_message((int, cell, int, slice, int) $params) impure asm """
    NEWC
    b{01} STSLICECONST  // store tag = $0 and ihr_disabled = true
    1 STI               // store `bounce`
    b{000} STSLICECONST // store bounced = false and src = addr_none
    STSLICE             // store `to`
    SWAP
    STGRAMS             // store `value`
    106 PUSHINT         // 1 + 4 + 4 + 64 + 32 + 1
    STZEROES
    // → Stack state
    // s0: Builder
    // s1: `body`
    // s2: `mode`
    STDICT
    ENDC
    SWAP
    SENDRAWMSG
""";

() $global_acceptMessage() impure asm """
    ACCEPT
""";

() $global_commit() impure asm """
    COMMIT
""";

(int, int) $global_parseStdAddress(slice $slice) impure asm """
    REWRITESTDADDR
""";

int $global_now() impure asm """
    NOW
""";

int $global_curLt() impure asm """
    LTIME
""";

builder $Builder$_fun_storeRef(cell $cell, builder $self) impure asm """
    STREF
""";

builder $Builder$_fun_storeSlice(builder $self, slice $slice) impure asm """
    STSLICER
""";

builder $Builder$_fun_storeAddress(builder $self, slice $address) impure asm """
    STSLICER
""";

(slice, cell) $Slice$_fun_loadRef(slice $self) impure asm( -> 1 0) """
    LDREF
""";

int $Slice$_fun_refs(slice $self) impure asm """
    SREFS
""";

int $Slice$_fun_bits(slice $self) impure asm """
    SBITS
""";

int $Slice$_fun_empty(slice $self) impure asm """
    SEMPTY
""";

int $Slice$_fun_refsEmpty(slice $self) impure asm """
    SREMPTY
""";

(slice, slice) $Slice$_fun_loadAddress(slice $self) impure asm( -> 1 0) """
    LDMSGADDR
""";

(int, int, int) $Cell$_fun_computeDataSize(cell $self, int $maxCells) impure asm """
    CDATASIZE
""";

int $Cell$_fun_hash(cell $self) impure asm """
    HASHCU
""";

slice $Address$_fun_asSlice(slice $self) impure asm "NOP";

;; Vault_Vault.storage.fc
;;
;; Type: Context
;; TLB: _ bounceable:bool sender:address value:int257 raw:^slice = Context
;;

_ $Context$_get_value((int, slice, int, slice) v) inline {
    var (v'bounceable, v'sender, v'value, v'raw) = v;
    return v'value;
}

;;
;; Type: MessageParameters
;; TLB: _ mode:int257 body:Maybe ^cell value:int257 to:address bounce:bool = MessageParameters
;;

((int, cell, int, slice, int)) $MessageParameters$_constructor_to_value_bounce_mode_body(slice $to, int $value, int $bounce, int $mode, cell $body) inline {
    return ($mode, $body, $value, $to, $bounce);
}

;;
;; Type: AthTransferNotificationAck
;; Header: 0x472d9d7e
;; TLB: ath_transfer_notification_ack#472d9d7e query_id:uint64 amount:uint128 sender_key:uint160 = AthTransferNotificationAck
;;

builder $AthTransferNotificationAck$_store(builder build_0, (int, int, int) v) inline {
    var (v'query_id, v'amount, v'sender_key) = v;
    build_0 = store_uint(build_0, 1194171774, 32);
    build_0 = build_0.store_uint(v'query_id, 64);
    build_0 = build_0.store_uint(v'amount, 128);
    build_0 = build_0.store_uint(v'sender_key, 160);
    return build_0;
}

cell $AthTransferNotificationAck$_store_cell((int, int, int) v, builder b) inline {
    return $AthTransferNotificationAck$_store(b, v).end_cell();
}

((int, int, int)) $AthTransferNotificationAck$_constructor_query_id_sender_key_amount(int $query_id, int $sender_key, int $amount) inline {
    return ($query_id, $amount, $sender_key);
}

;;
;; Type: ATHTransferRequest
;; Header: 0x41544810
;; TLB: ath_transfer_request#41544810 query_id:uint64 amount:uint128 recipient:address response_destination:address = ATHTransferRequest
;;

builder $ATHTransferRequest$_store(builder build_0, (int, int, slice, slice) v) inline {
    var (v'query_id, v'amount, v'recipient, v'response_destination) = v;
    build_0 = store_uint(build_0, 1096042512, 32);
    build_0 = build_0.store_uint(v'query_id, 64);
    build_0 = build_0.store_uint(v'amount, 128);
    build_0 = build_0.store_slice(v'recipient);
    build_0 = build_0.store_slice(v'response_destination);
    return build_0;
}

cell $ATHTransferRequest$_store_cell((int, int, slice, slice) v, builder b) inline {
    return $ATHTransferRequest$_store(b, v).end_cell();
}

(slice, ((int, int))) $ATHTransferRequest$_load_bounced(slice sc_0) inline {
    var v'query_id = sc_0~load_uint(64);
    var v'amount = sc_0~load_uint(128);
    return (sc_0, (v'query_id, v'amount));
}

((int, int, slice, slice)) $ATHTransferRequest$_constructor_query_id_amount_recipient_response_destination(int $query_id, int $amount, slice $recipient, slice $response_destination) inline {
    return ($query_id, $amount, $recipient, $response_destination);
}

;;
;; Type: ATHTransferRequestVaultProfileAvatar
;; Header: 0x4154481a
;; TLB: ath_transfer_request_vault_profile_avatar#4154481a query_id:uint64 amount:uint128 recipient:address response_destination:address notify_value:uint128 owner_wallet:address avatar_hash:uint256 avatar_entry_id:uint64 avatar_stream_id:uint128 avatar_part_count:uint16 media_format:uint8 = ATHTransferRequestVaultProfileAvatar
;;

builder $ATHTransferRequestVaultProfileAvatar$_store(builder build_0, (int, int, slice, slice, int, slice, int, int, int, int, int) v) inline_ref {
    var (v'query_id, v'amount, v'recipient, v'response_destination, v'notify_value, v'owner_wallet, v'avatar_hash, v'avatar_entry_id, v'avatar_stream_id, v'avatar_part_count, v'media_format) = v;
    build_0 = store_uint(build_0, 1096042522, 32);
    build_0 = build_0.store_uint(v'query_id, 64);
    build_0 = build_0.store_uint(v'amount, 128);
    build_0 = build_0.store_slice(v'recipient);
    build_0 = build_0.store_slice(v'response_destination);
    build_0 = build_0.store_uint(v'notify_value, 128);
    var build_1 = begin_cell();
    build_1 = build_1.store_slice(v'owner_wallet);
    build_1 = build_1.store_uint(v'avatar_hash, 256);
    build_1 = build_1.store_uint(v'avatar_entry_id, 64);
    build_1 = build_1.store_uint(v'avatar_stream_id, 128);
    build_1 = build_1.store_uint(v'avatar_part_count, 16);
    build_1 = build_1.store_uint(v'media_format, 8);
    build_0 = store_builder_ref(build_0, build_1);
    return build_0;
}

cell $ATHTransferRequestVaultProfileAvatar$_store_cell((int, int, slice, slice, int, slice, int, int, int, int, int) v, builder b) inline {
    return $ATHTransferRequestVaultProfileAvatar$_store(b, v).end_cell();
}

(slice, ((int, int))) $ATHTransferRequestVaultProfileAvatar$_load_bounced(slice sc_0) inline {
    var v'query_id = sc_0~load_uint(64);
    var v'amount = sc_0~load_uint(128);
    return (sc_0, (v'query_id, v'amount));
}

((int, int, slice, slice, int, slice, int, int, int, int, int)) $ATHTransferRequestVaultProfileAvatar$_constructor_query_id_amount_recipient_response_destination_notify_value_owner_wallet_avatar_hash_avatar_entry_id_avatar_stream_id_avatar_part_count_media_format(int $query_id, int $amount, slice $recipient, slice $response_destination, int $notify_value, slice $owner_wallet, int $avatar_hash, int $avatar_entry_id, int $avatar_stream_id, int $avatar_part_count, int $media_format) inline {
    return ($query_id, $amount, $recipient, $response_destination, $notify_value, $owner_wallet, $avatar_hash, $avatar_entry_id, $avatar_stream_id, $avatar_part_count, $media_format);
}

;;
;; Type: ATHTransferRequestVaultMintUsername
;; Header: 0x4154481c
;; TLB: ath_transfer_request_vault_mint_username#4154481c query_id:uint64 amount:uint128 recipient:address response_destination:address notify_value:uint128 owner_wallet:address username_len:uint8 username:remainder<slice> = ATHTransferRequestVaultMintUsername
;;

builder $ATHTransferRequestVaultMintUsername$_store(builder build_0, (int, int, slice, slice, int, slice, int, slice) v) inline {
    var (v'query_id, v'amount, v'recipient, v'response_destination, v'notify_value, v'owner_wallet, v'username_len, v'username) = v;
    build_0 = store_uint(build_0, 1096042524, 32);
    build_0 = build_0.store_uint(v'query_id, 64);
    build_0 = build_0.store_uint(v'amount, 128);
    build_0 = build_0.store_slice(v'recipient);
    build_0 = build_0.store_slice(v'response_destination);
    build_0 = build_0.store_uint(v'notify_value, 128);
    var build_1 = begin_cell();
    build_1 = build_1.store_slice(v'owner_wallet);
    build_1 = build_1.store_uint(v'username_len, 8);
    build_1 = build_1.store_slice(v'username);
    build_0 = store_builder_ref(build_0, build_1);
    return build_0;
}

cell $ATHTransferRequestVaultMintUsername$_store_cell((int, int, slice, slice, int, slice, int, slice) v, builder b) inline {
    return $ATHTransferRequestVaultMintUsername$_store(b, v).end_cell();
}

(slice, ((int, int))) $ATHTransferRequestVaultMintUsername$_load_bounced(slice sc_0) inline {
    var v'query_id = sc_0~load_uint(64);
    var v'amount = sc_0~load_uint(128);
    return (sc_0, (v'query_id, v'amount));
}

((int, int, slice, slice, int, slice, int, slice)) $ATHTransferRequestVaultMintUsername$_constructor_query_id_amount_recipient_response_destination_notify_value_owner_wallet_username_len_username(int $query_id, int $amount, slice $recipient, slice $response_destination, int $notify_value, slice $owner_wallet, int $username_len, slice $username) inline {
    return ($query_id, $amount, $recipient, $response_destination, $notify_value, $owner_wallet, $username_len, $username);
}

;;
;; Type: PublishPrivateFromVault
;; Header: 0xa4f862c0
;; TLB: publish_private_from_vault#a4f862c0 bounce_id:uint64 bounce_tag:uint160 publish_id:uint256 size_class:uint8 crypto_suite:uint8 header_0_hash:uint256 header_1_hash:uint256 body_hash:uint256 header_0:^cell header_1:^cell body:^cell protocol_fee_paid:uint128 = PublishPrivateFromVault
;;

builder $PublishPrivateFromVault$_store(builder build_0, (int, int, int, int, int, int, int, int, cell, cell, cell, int) v) inline_ref {
    var (v'bounce_id, v'bounce_tag, v'publish_id, v'size_class, v'crypto_suite, v'header_0_hash, v'header_1_hash, v'body_hash, v'header_0, v'header_1, v'body, v'protocol_fee_paid) = v;
    build_0 = store_uint(build_0, 2767741632, 32);
    build_0 = build_0.store_uint(v'bounce_id, 64);
    build_0 = build_0.store_uint(v'bounce_tag, 160);
    build_0 = build_0.store_uint(v'publish_id, 256);
    build_0 = build_0.store_uint(v'size_class, 8);
    build_0 = build_0.store_uint(v'crypto_suite, 8);
    build_0 = build_0.store_uint(v'header_0_hash, 256);
    var build_1 = begin_cell();
    build_1 = build_1.store_uint(v'header_1_hash, 256);
    build_1 = build_1.store_uint(v'body_hash, 256);
    build_1 = build_1.store_ref(v'header_0);
    build_1 = build_1.store_ref(v'header_1);
    build_1 = build_1.store_ref(v'body);
    build_1 = build_1.store_uint(v'protocol_fee_paid, 128);
    build_0 = store_builder_ref(build_0, build_1);
    return build_0;
}

cell $PublishPrivateFromVault$_store_cell((int, int, int, int, int, int, int, int, cell, cell, cell, int) v, builder b) inline {
    return $PublishPrivateFromVault$_store(b, v).end_cell();
}

(slice, ((int, int))) $PublishPrivateFromVault$_load_bounced(slice sc_0) inline {
    var v'bounce_id = sc_0~load_uint(64);
    var v'bounce_tag = sc_0~load_uint(160);
    return (sc_0, (v'bounce_id, v'bounce_tag));
}

((int, int, int, int, int, int, int, int, cell, cell, cell, int)) $PublishPrivateFromVault$_constructor_bounce_id_bounce_tag_publish_id_size_class_crypto_suite_header_0_hash_header_1_hash_body_hash_header_0_header_1_body_protocol_fee_paid(int $bounce_id, int $bounce_tag, int $publish_id, int $size_class, int $crypto_suite, int $header_0_hash, int $header_1_hash, int $body_hash, cell $header_0, cell $header_1, cell $body, int $protocol_fee_paid) inline {
    return ($bounce_id, $bounce_tag, $publish_id, $size_class, $crypto_suite, $header_0_hash, $header_1_hash, $body_hash, $header_0, $header_1, $body, $protocol_fee_paid);
}

;;
;; Type: PublishPublicFromVault
;; Header: 0x8c2a76b7
;; TLB: publish_public_from_vault#8c2a76b7 bounce_id:uint64 bounce_tag:uint160 publish_id:uint256 marketing_note:uint152 author_wallet:address header_hash:uint256 body_hash:uint256 header:^cell body:^cell protocol_fee_paid:uint128 = PublishPublicFromVault
;;

builder $PublishPublicFromVault$_store(builder build_0, (int, int, int, int, slice, int, int, cell, cell, int) v) inline {
    var (v'bounce_id, v'bounce_tag, v'publish_id, v'marketing_note, v'author_wallet, v'header_hash, v'body_hash, v'header, v'body, v'protocol_fee_paid) = v;
    build_0 = store_uint(build_0, 2351593143, 32);
    build_0 = build_0.store_uint(v'bounce_id, 64);
    build_0 = build_0.store_uint(v'bounce_tag, 160);
    build_0 = build_0.store_uint(v'publish_id, 256);
    build_0 = build_0.store_uint(v'marketing_note, 152);
    build_0 = build_0.store_slice(v'author_wallet);
    var build_1 = begin_cell();
    build_1 = build_1.store_uint(v'header_hash, 256);
    build_1 = build_1.store_uint(v'body_hash, 256);
    build_1 = build_1.store_ref(v'header);
    build_1 = build_1.store_ref(v'body);
    build_1 = build_1.store_uint(v'protocol_fee_paid, 128);
    build_0 = store_builder_ref(build_0, build_1);
    return build_0;
}

cell $PublishPublicFromVault$_store_cell((int, int, int, int, slice, int, int, cell, cell, int) v, builder b) inline {
    return $PublishPublicFromVault$_store(b, v).end_cell();
}

(slice, ((int, int))) $PublishPublicFromVault$_load_bounced(slice sc_0) inline {
    var v'bounce_id = sc_0~load_uint(64);
    var v'bounce_tag = sc_0~load_uint(160);
    return (sc_0, (v'bounce_id, v'bounce_tag));
}

((int, int, int, int, slice, int, int, cell, cell, int)) $PublishPublicFromVault$_constructor_bounce_id_bounce_tag_publish_id_marketing_note_author_wallet_header_hash_body_hash_header_body_protocol_fee_paid(int $bounce_id, int $bounce_tag, int $publish_id, int $marketing_note, slice $author_wallet, int $header_hash, int $body_hash, cell $header, cell $body, int $protocol_fee_paid) inline {
    return ($bounce_id, $bounce_tag, $publish_id, $marketing_note, $author_wallet, $header_hash, $body_hash, $header, $body, $protocol_fee_paid);
}

;;
;; Type: PendingAthWithdrawal
;; TLB: _ owner_wallet:address recipient:address recipient_ath_wallet:address amount:uint128 refundable_ton_amount:uint128 created_at:uint32 = PendingAthWithdrawal
;;

builder $PendingAthWithdrawal$_store(builder build_0, (slice, slice, slice, int, int, int) v) inline {
    var (v'owner_wallet, v'recipient, v'recipient_ath_wallet, v'amount, v'refundable_ton_amount, v'created_at) = v;
    build_0 = build_0.store_slice(v'owner_wallet);
    build_0 = build_0.store_slice(v'recipient);
    build_0 = build_0.store_slice(v'recipient_ath_wallet);
    build_0 = build_0.store_uint(v'amount, 128);
    var build_1 = begin_cell();
    build_1 = build_1.store_uint(v'refundable_ton_amount, 128);
    build_1 = build_1.store_uint(v'created_at, 32);
    build_0 = store_builder_ref(build_0, build_1);
    return build_0;
}

cell $PendingAthWithdrawal$_store_cell((slice, slice, slice, int, int, int) v, builder b) inline {
    return $PendingAthWithdrawal$_store(b, v).end_cell();
}

(slice, ((slice, slice, slice, int, int, int))) $PendingAthWithdrawal$_load(slice sc_0) inline {
    var v'owner_wallet = sc_0~load_msg_addr();
    var v'recipient = sc_0~load_msg_addr();
    var v'recipient_ath_wallet = sc_0~load_msg_addr();
    var v'amount = sc_0~load_uint(128);
    slice sc_1 = sc_0~load_ref().begin_parse();
    var v'refundable_ton_amount = sc_1~load_uint(128);
    var v'created_at = sc_1~load_uint(32);
    return (sc_0, (v'owner_wallet, v'recipient, v'recipient_ath_wallet, v'amount, v'refundable_ton_amount, v'created_at));
}

tuple $PendingAthWithdrawal$_as_optional((slice, slice, slice, int, int, int) v) inline {
    var (v'owner_wallet, v'recipient, v'recipient_ath_wallet, v'amount, v'refundable_ton_amount, v'created_at) = v;
    return __tact_tuple_create_6(v'owner_wallet, v'recipient, v'recipient_ath_wallet, v'amount, v'refundable_ton_amount, v'created_at);
}

tuple $PendingAthWithdrawal$_load_opt(cell cl) inline {
    if (null?(cl)) {
        return null();
    }
    var sc = cl.begin_parse();
    return $PendingAthWithdrawal$_as_optional(sc~$PendingAthWithdrawal$_load());
}

((slice, slice, slice, int, int, int)) $PendingAthWithdrawal$_not_null(tuple v) inline {
    var (slice vvv'owner_wallet, slice vvv'recipient, slice vvv'recipient_ath_wallet, int vvv'amount, int vvv'refundable_ton_amount, int vvv'created_at) = __tact_tuple_destroy_6(v);
    return (vvv'owner_wallet, vvv'recipient, vvv'recipient_ath_wallet, vvv'amount, vvv'refundable_ton_amount, vvv'created_at);
}

((slice, slice, slice, int, int, int)) $PendingAthWithdrawal$_constructor_owner_wallet_recipient_recipient_ath_wallet_amount_refundable_ton_amount_created_at(slice $owner_wallet, slice $recipient, slice $recipient_ath_wallet, int $amount, int $refundable_ton_amount, int $created_at) inline {
    return ($owner_wallet, $recipient, $recipient_ath_wallet, $amount, $refundable_ton_amount, $created_at);
}

;;
;; Type: PendingPublish
;; TLB: _ owner_wallet:address tombstone:bool refund_to_vault:bool nonce:uint64 publish_kind:uint8 body_hash:uint256 refundable_amount:uint128 created_at:uint32 = PendingPublish
;;

builder $PendingPublish$_store(builder build_0, (slice, int, int, int, int, int, int, int) v) inline {
    var (v'owner_wallet, v'tombstone, v'refund_to_vault, v'nonce, v'publish_kind, v'body_hash, v'refundable_amount, v'created_at) = v;
    build_0 = build_0.store_slice(v'owner_wallet);
    build_0 = build_0.store_int(v'tombstone, 1);
    build_0 = build_0.store_int(v'refund_to_vault, 1);
    build_0 = build_0.store_uint(v'nonce, 64);
    build_0 = build_0.store_uint(v'publish_kind, 8);
    build_0 = build_0.store_uint(v'body_hash, 256);
    build_0 = build_0.store_uint(v'refundable_amount, 128);
    build_0 = build_0.store_uint(v'created_at, 32);
    return build_0;
}

cell $PendingPublish$_store_cell((slice, int, int, int, int, int, int, int) v, builder b) inline {
    return $PendingPublish$_store(b, v).end_cell();
}

(slice, ((slice, int, int, int, int, int, int, int))) $PendingPublish$_load(slice sc_0) inline {
    var v'owner_wallet = sc_0~load_msg_addr();
    var v'tombstone = sc_0~load_int(1);
    var v'refund_to_vault = sc_0~load_int(1);
    var v'nonce = sc_0~load_uint(64);
    var v'publish_kind = sc_0~load_uint(8);
    var v'body_hash = sc_0~load_uint(256);
    var v'refundable_amount = sc_0~load_uint(128);
    var v'created_at = sc_0~load_uint(32);
    return (sc_0, (v'owner_wallet, v'tombstone, v'refund_to_vault, v'nonce, v'publish_kind, v'body_hash, v'refundable_amount, v'created_at));
}

tuple $PendingPublish$_as_optional((slice, int, int, int, int, int, int, int) v) inline {
    var (v'owner_wallet, v'tombstone, v'refund_to_vault, v'nonce, v'publish_kind, v'body_hash, v'refundable_amount, v'created_at) = v;
    return __tact_tuple_create_8(v'owner_wallet, v'tombstone, v'refund_to_vault, v'nonce, v'publish_kind, v'body_hash, v'refundable_amount, v'created_at);
}

tuple $PendingPublish$_load_opt(cell cl) inline {
    if (null?(cl)) {
        return null();
    }
    var sc = cl.begin_parse();
    return $PendingPublish$_as_optional(sc~$PendingPublish$_load());
}

((slice, int, int, int, int, int, int, int)) $PendingPublish$_not_null(tuple v) inline {
    var (slice vvv'owner_wallet, int vvv'tombstone, int vvv'refund_to_vault, int vvv'nonce, int vvv'publish_kind, int vvv'body_hash, int vvv'refundable_amount, int vvv'created_at) = __tact_tuple_destroy_8(v);
    return (vvv'owner_wallet, vvv'tombstone, vvv'refund_to_vault, vvv'nonce, vvv'publish_kind, vvv'body_hash, vvv'refundable_amount, vvv'created_at);
}

((slice, int, int, int, int, int, int, int)) $PendingPublish$_constructor_owner_wallet_tombstone_refund_to_vault_nonce_publish_kind_body_hash_refundable_amount_created_at(slice $owner_wallet, int $tombstone, int $refund_to_vault, int $nonce, int $publish_kind, int $body_hash, int $refundable_amount, int $created_at) inline {
    return ($owner_wallet, $tombstone, $refund_to_vault, $nonce, $publish_kind, $body_hash, $refundable_amount, $created_at);
}

;;
;; Type: PendingProfileAvatarPayment
;; TLB: _ owner_wallet:address amount:uint128 created_at:uint32 = PendingProfileAvatarPayment
;;

builder $PendingProfileAvatarPayment$_store(builder build_0, (slice, int, int) v) inline {
    var (v'owner_wallet, v'amount, v'created_at) = v;
    build_0 = build_0.store_slice(v'owner_wallet);
    build_0 = build_0.store_uint(v'amount, 128);
    build_0 = build_0.store_uint(v'created_at, 32);
    return build_0;
}

cell $PendingProfileAvatarPayment$_store_cell((slice, int, int) v, builder b) inline {
    return $PendingProfileAvatarPayment$_store(b, v).end_cell();
}

(slice, ((slice, int, int))) $PendingProfileAvatarPayment$_load(slice sc_0) inline {
    var v'owner_wallet = sc_0~load_msg_addr();
    var v'amount = sc_0~load_uint(128);
    var v'created_at = sc_0~load_uint(32);
    return (sc_0, (v'owner_wallet, v'amount, v'created_at));
}

tuple $PendingProfileAvatarPayment$_as_optional((slice, int, int) v) inline {
    var (v'owner_wallet, v'amount, v'created_at) = v;
    return __tact_tuple_create_3(v'owner_wallet, v'amount, v'created_at);
}

tuple $PendingProfileAvatarPayment$_load_opt(cell cl) inline {
    if (null?(cl)) {
        return null();
    }
    var sc = cl.begin_parse();
    return $PendingProfileAvatarPayment$_as_optional(sc~$PendingProfileAvatarPayment$_load());
}

((slice, int, int)) $PendingProfileAvatarPayment$_not_null(tuple v) inline {
    var (slice vvv'owner_wallet, int vvv'amount, int vvv'created_at) = __tact_tuple_destroy_3(v);
    return (vvv'owner_wallet, vvv'amount, vvv'created_at);
}

((slice, int, int)) $PendingProfileAvatarPayment$_constructor_owner_wallet_amount_created_at(slice $owner_wallet, int $amount, int $created_at) inline {
    return ($owner_wallet, $amount, $created_at);
}

;;
;; Type: PendingUsernameMintPayment
;; TLB: _ owner_wallet:address amount:uint128 created_at:uint32 = PendingUsernameMintPayment
;;

builder $PendingUsernameMintPayment$_store(builder build_0, (slice, int, int) v) inline {
    var (v'owner_wallet, v'amount, v'created_at) = v;
    build_0 = build_0.store_slice(v'owner_wallet);
    build_0 = build_0.store_uint(v'amount, 128);
    build_0 = build_0.store_uint(v'created_at, 32);
    return build_0;
}

cell $PendingUsernameMintPayment$_store_cell((slice, int, int) v, builder b) inline {
    return $PendingUsernameMintPayment$_store(b, v).end_cell();
}

(slice, ((slice, int, int))) $PendingUsernameMintPayment$_load(slice sc_0) inline {
    var v'owner_wallet = sc_0~load_msg_addr();
    var v'amount = sc_0~load_uint(128);
    var v'created_at = sc_0~load_uint(32);
    return (sc_0, (v'owner_wallet, v'amount, v'created_at));
}

tuple $PendingUsernameMintPayment$_as_optional((slice, int, int) v) inline {
    var (v'owner_wallet, v'amount, v'created_at) = v;
    return __tact_tuple_create_3(v'owner_wallet, v'amount, v'created_at);
}

tuple $PendingUsernameMintPayment$_load_opt(cell cl) inline {
    if (null?(cl)) {
        return null();
    }
    var sc = cl.begin_parse();
    return $PendingUsernameMintPayment$_as_optional(sc~$PendingUsernameMintPayment$_load());
}

((slice, int, int)) $PendingUsernameMintPayment$_not_null(tuple v) inline {
    var (slice vvv'owner_wallet, int vvv'amount, int vvv'created_at) = __tact_tuple_destroy_3(v);
    return (vvv'owner_wallet, vvv'amount, vvv'created_at);
}

((slice, int, int)) $PendingUsernameMintPayment$_constructor_owner_wallet_amount_created_at(slice $owner_wallet, int $amount, int $created_at) inline {
    return ($owner_wallet, $amount, $created_at);
}

;;
;; Type: ReceiveIntent
;; TLB: _ sender_wallet:address recipient_wallet:address asset:uint8 amount:uint128 commitment:uint256 client_nonce:uint64 settlement_reserve_ton:uint128 created_at:uint32 claimed:bool = ReceiveIntent
;;

builder $ReceiveIntent$_store(builder build_0, (slice, slice, int, int, int, int, int, int, int) v) inline {
    var (v'sender_wallet, v'recipient_wallet, v'asset, v'amount, v'commitment, v'client_nonce, v'settlement_reserve_ton, v'created_at, v'claimed) = v;
    build_0 = build_0.store_slice(v'sender_wallet);
    build_0 = build_0.store_slice(v'recipient_wallet);
    build_0 = build_0.store_uint(v'asset, 8);
    build_0 = build_0.store_uint(v'amount, 128);
    build_0 = build_0.store_uint(v'commitment, 256);
    build_0 = build_0.store_uint(v'client_nonce, 64);
    var build_1 = begin_cell();
    build_1 = build_1.store_uint(v'settlement_reserve_ton, 128);
    build_1 = build_1.store_uint(v'created_at, 32);
    build_1 = build_1.store_int(v'claimed, 1);
    build_0 = store_builder_ref(build_0, build_1);
    return build_0;
}

cell $ReceiveIntent$_store_cell((slice, slice, int, int, int, int, int, int, int) v, builder b) inline {
    return $ReceiveIntent$_store(b, v).end_cell();
}

(slice, ((slice, slice, int, int, int, int, int, int, int))) $ReceiveIntent$_load(slice sc_0) inline {
    var v'sender_wallet = sc_0~load_msg_addr();
    var v'recipient_wallet = sc_0~load_msg_addr();
    var v'asset = sc_0~load_uint(8);
    var v'amount = sc_0~load_uint(128);
    var v'commitment = sc_0~load_uint(256);
    var v'client_nonce = sc_0~load_uint(64);
    slice sc_1 = sc_0~load_ref().begin_parse();
    var v'settlement_reserve_ton = sc_1~load_uint(128);
    var v'created_at = sc_1~load_uint(32);
    var v'claimed = sc_1~load_int(1);
    return (sc_0, (v'sender_wallet, v'recipient_wallet, v'asset, v'amount, v'commitment, v'client_nonce, v'settlement_reserve_ton, v'created_at, v'claimed));
}

tuple $ReceiveIntent$_as_optional((slice, slice, int, int, int, int, int, int, int) v) inline {
    var (v'sender_wallet, v'recipient_wallet, v'asset, v'amount, v'commitment, v'client_nonce, v'settlement_reserve_ton, v'created_at, v'claimed) = v;
    return __tact_tuple_create_9(v'sender_wallet, v'recipient_wallet, v'asset, v'amount, v'commitment, v'client_nonce, v'settlement_reserve_ton, v'created_at, v'claimed);
}

tuple $ReceiveIntent$_load_opt(cell cl) inline {
    if (null?(cl)) {
        return null();
    }
    var sc = cl.begin_parse();
    return $ReceiveIntent$_as_optional(sc~$ReceiveIntent$_load());
}

((slice, slice, int, int, int, int, int, int, int)) $ReceiveIntent$_not_null(tuple v) inline {
    var (slice vvv'sender_wallet, slice vvv'recipient_wallet, int vvv'asset, int vvv'amount, int vvv'commitment, int vvv'client_nonce, int vvv'settlement_reserve_ton, int vvv'created_at, int vvv'claimed) = __tact_tuple_destroy_9(v);
    return (vvv'sender_wallet, vvv'recipient_wallet, vvv'asset, vvv'amount, vvv'commitment, vvv'client_nonce, vvv'settlement_reserve_ton, vvv'created_at, vvv'claimed);
}

((slice, slice, int, int, int, int, int, int, int)) $ReceiveIntent$_constructor_sender_wallet_recipient_wallet_asset_amount_commitment_client_nonce_settlement_reserve_ton_created_at_claimed(slice $sender_wallet, slice $recipient_wallet, int $asset, int $amount, int $commitment, int $client_nonce, int $settlement_reserve_ton, int $created_at, int $claimed) inline {
    return ($sender_wallet, $recipient_wallet, $asset, $amount, $commitment, $client_nonce, $settlement_reserve_ton, $created_at, $claimed);
}

;;
;; Type: KeyRecord
;; TLB: _ owner_wallet:address key_generation:uint32 enc_pubkey:uint256 sign_pubkey:uint256 pq_kem_pubkey_hash:uint256 pq_kem_pubkey_len:uint16 pq_kem_pubkey:^cell crypto_suite_mask:uint16 created_at:uint32 created_lt:uint64 revoked_at:uint32 revoked_lt:uint64 = KeyRecord
;;

builder $KeyRecord$_store(builder build_0, (slice, int, int, int, int, int, cell, int, int, int, int, int) v) inline_ref {
    var (v'owner_wallet, v'key_generation, v'enc_pubkey, v'sign_pubkey, v'pq_kem_pubkey_hash, v'pq_kem_pubkey_len, v'pq_kem_pubkey, v'crypto_suite_mask, v'created_at, v'created_lt, v'revoked_at, v'revoked_lt) = v;
    build_0 = build_0.store_slice(v'owner_wallet);
    build_0 = build_0.store_uint(v'key_generation, 32);
    build_0 = build_0.store_uint(v'enc_pubkey, 256);
    build_0 = build_0.store_uint(v'sign_pubkey, 256);
    var build_1 = begin_cell();
    build_1 = build_1.store_uint(v'pq_kem_pubkey_hash, 256);
    build_1 = build_1.store_uint(v'pq_kem_pubkey_len, 16);
    build_1 = build_1.store_ref(v'pq_kem_pubkey);
    build_1 = build_1.store_uint(v'crypto_suite_mask, 16);
    build_1 = build_1.store_uint(v'created_at, 32);
    build_1 = build_1.store_uint(v'created_lt, 64);
    build_1 = build_1.store_uint(v'revoked_at, 32);
    build_1 = build_1.store_uint(v'revoked_lt, 64);
    build_0 = store_builder_ref(build_0, build_1);
    return build_0;
}

cell $KeyRecord$_store_cell((slice, int, int, int, int, int, cell, int, int, int, int, int) v, builder b) inline {
    return $KeyRecord$_store(b, v).end_cell();
}

(slice, ((slice, int, int, int, int, int, cell, int, int, int, int, int))) $KeyRecord$_load(slice sc_0) inline_ref {
    var v'owner_wallet = sc_0~load_msg_addr();
    var v'key_generation = sc_0~load_uint(32);
    var v'enc_pubkey = sc_0~load_uint(256);
    var v'sign_pubkey = sc_0~load_uint(256);
    slice sc_1 = sc_0~load_ref().begin_parse();
    var v'pq_kem_pubkey_hash = sc_1~load_uint(256);
    var v'pq_kem_pubkey_len = sc_1~load_uint(16);
    var v'pq_kem_pubkey = sc_1~load_ref();
    var v'crypto_suite_mask = sc_1~load_uint(16);
    var v'created_at = sc_1~load_uint(32);
    var v'created_lt = sc_1~load_uint(64);
    var v'revoked_at = sc_1~load_uint(32);
    var v'revoked_lt = sc_1~load_uint(64);
    return (sc_0, (v'owner_wallet, v'key_generation, v'enc_pubkey, v'sign_pubkey, v'pq_kem_pubkey_hash, v'pq_kem_pubkey_len, v'pq_kem_pubkey, v'crypto_suite_mask, v'created_at, v'created_lt, v'revoked_at, v'revoked_lt));
}

tuple $KeyRecord$_as_optional((slice, int, int, int, int, int, cell, int, int, int, int, int) v) inline {
    var (v'owner_wallet, v'key_generation, v'enc_pubkey, v'sign_pubkey, v'pq_kem_pubkey_hash, v'pq_kem_pubkey_len, v'pq_kem_pubkey, v'crypto_suite_mask, v'created_at, v'created_lt, v'revoked_at, v'revoked_lt) = v;
    return __tact_tuple_create_12(v'owner_wallet, v'key_generation, v'enc_pubkey, v'sign_pubkey, v'pq_kem_pubkey_hash, v'pq_kem_pubkey_len, v'pq_kem_pubkey, v'crypto_suite_mask, v'created_at, v'created_lt, v'revoked_at, v'revoked_lt);
}

tuple $KeyRecord$_load_opt(cell cl) inline {
    if (null?(cl)) {
        return null();
    }
    var sc = cl.begin_parse();
    return $KeyRecord$_as_optional(sc~$KeyRecord$_load());
}

((slice, int, int, int, int, int, cell, int, int, int, int, int)) $KeyRecord$_not_null(tuple v) inline {
    var (slice vvv'owner_wallet, int vvv'key_generation, int vvv'enc_pubkey, int vvv'sign_pubkey, int vvv'pq_kem_pubkey_hash, int vvv'pq_kem_pubkey_len, cell vvv'pq_kem_pubkey, int vvv'crypto_suite_mask, int vvv'created_at, int vvv'created_lt, int vvv'revoked_at, int vvv'revoked_lt) = __tact_tuple_destroy_12(v);
    return (vvv'owner_wallet, vvv'key_generation, vvv'enc_pubkey, vvv'sign_pubkey, vvv'pq_kem_pubkey_hash, vvv'pq_kem_pubkey_len, vvv'pq_kem_pubkey, vvv'crypto_suite_mask, vvv'created_at, vvv'created_lt, vvv'revoked_at, vvv'revoked_lt);
}

((slice, int, int, int, int, int, cell, int, int, int, int, int)) $KeyRecord$_constructor_owner_wallet_key_generation_enc_pubkey_sign_pubkey_pq_kem_pubkey_hash_pq_kem_pubkey_len_pq_kem_pubkey_crypto_suite_mask_created_at_created_lt_revoked_at_revoked_lt(slice $owner_wallet, int $key_generation, int $enc_pubkey, int $sign_pubkey, int $pq_kem_pubkey_hash, int $pq_kem_pubkey_len, cell $pq_kem_pubkey, int $crypto_suite_mask, int $created_at, int $created_lt, int $revoked_at, int $revoked_lt) inline {
    return ($owner_wallet, $key_generation, $enc_pubkey, $sign_pubkey, $pq_kem_pubkey_hash, $pq_kem_pubkey_len, $pq_kem_pubkey, $crypto_suite_mask, $created_at, $created_lt, $revoked_at, $revoked_lt);
}

;;
;; Type: UserState
;; TLB: _ ton_balance:uint128 ath_balance:uint128 current_key_id:uint256 auth_pubkey:uint256 publish_nonce:uint64 = UserState
;;

builder $UserState$_store(builder build_0, (int, int, int, int, int) v) inline {
    var (v'ton_balance, v'ath_balance, v'current_key_id, v'auth_pubkey, v'publish_nonce) = v;
    build_0 = build_0.store_uint(v'ton_balance, 128);
    build_0 = build_0.store_uint(v'ath_balance, 128);
    build_0 = build_0.store_uint(v'current_key_id, 256);
    build_0 = build_0.store_uint(v'auth_pubkey, 256);
    build_0 = build_0.store_uint(v'publish_nonce, 64);
    return build_0;
}

cell $UserState$_store_cell((int, int, int, int, int) v, builder b) inline {
    return $UserState$_store(b, v).end_cell();
}

(slice, ((int, int, int, int, int))) $UserState$_load(slice sc_0) inline {
    var v'ton_balance = sc_0~load_uint(128);
    var v'ath_balance = sc_0~load_uint(128);
    var v'current_key_id = sc_0~load_uint(256);
    var v'auth_pubkey = sc_0~load_uint(256);
    var v'publish_nonce = sc_0~load_uint(64);
    return (sc_0, (v'ton_balance, v'ath_balance, v'current_key_id, v'auth_pubkey, v'publish_nonce));
}

tuple $UserState$_as_optional((int, int, int, int, int) v) inline {
    var (v'ton_balance, v'ath_balance, v'current_key_id, v'auth_pubkey, v'publish_nonce) = v;
    return __tact_tuple_create_5(v'ton_balance, v'ath_balance, v'current_key_id, v'auth_pubkey, v'publish_nonce);
}

tuple $UserState$_load_opt(cell cl) inline {
    if (null?(cl)) {
        return null();
    }
    var sc = cl.begin_parse();
    return $UserState$_as_optional(sc~$UserState$_load());
}

((int, int, int, int, int)) $UserState$_not_null(tuple v) inline {
    var (int vvv'ton_balance, int vvv'ath_balance, int vvv'current_key_id, int vvv'auth_pubkey, int vvv'publish_nonce) = __tact_tuple_destroy_5(v);
    return (vvv'ton_balance, vvv'ath_balance, vvv'current_key_id, vvv'auth_pubkey, vvv'publish_nonce);
}

((int, int, int, int, int)) $UserState$_constructor_ton_balance_ath_balance_current_key_id_auth_pubkey_publish_nonce(int $ton_balance, int $ath_balance, int $current_key_id, int $auth_pubkey, int $publish_nonce) inline {
    return ($ton_balance, $ath_balance, $current_key_id, $auth_pubkey, $publish_nonce);
}

;;
;; Type: VaultReceiveIntentView
;; TLB: _ exists:bool sender_wallet:address recipient_wallet:address asset:int257 amount:int257 commitment:int257 client_nonce:int257 settlement_reserve_ton:int257 created_at:int257 claimed:bool = VaultReceiveIntentView
;;

(int, slice, slice, int, int, int, int, int, int, int) $VaultReceiveIntentView$_to_external(((int, slice, slice, int, int, int, int, int, int, int)) v) inline {
    var (v'exists, v'sender_wallet, v'recipient_wallet, v'asset, v'amount, v'commitment, v'client_nonce, v'settlement_reserve_ton, v'created_at, v'claimed) = v; 
    return (v'exists, v'sender_wallet, v'recipient_wallet, v'asset, v'amount, v'commitment, v'client_nonce, v'settlement_reserve_ton, v'created_at, v'claimed);
}

((int, slice, slice, int, int, int, int, int, int, int)) $VaultReceiveIntentView$_constructor_exists_sender_wallet_recipient_wallet_asset_amount_commitment_client_nonce_settlement_reserve_ton_created_at_claimed(int $exists, slice $sender_wallet, slice $recipient_wallet, int $asset, int $amount, int $commitment, int $client_nonce, int $settlement_reserve_ton, int $created_at, int $claimed) inline {
    return ($exists, $sender_wallet, $recipient_wallet, $asset, $amount, $commitment, $client_nonce, $settlement_reserve_ton, $created_at, $claimed);
}

;;
;; Type: VaultKeyRecordView
;; TLB: _ exists:bool owner_wallet:address key_generation:int257 enc_pubkey:int257 sign_pubkey:int257 pq_kem_pubkey_hash:int257 pq_kem_pubkey_len:int257 pq_kem_pubkey:^cell crypto_suite_mask:int257 created_at:int257 created_lt:int257 revoked_at:int257 revoked_lt:int257 = VaultKeyRecordView
;;

(int, slice, int, int, int, int, int, cell, int, int, int, int, int) $VaultKeyRecordView$_to_external(((int, slice, int, int, int, int, int, cell, int, int, int, int, int)) v) inline {
    var (v'exists, v'owner_wallet, v'key_generation, v'enc_pubkey, v'sign_pubkey, v'pq_kem_pubkey_hash, v'pq_kem_pubkey_len, v'pq_kem_pubkey, v'crypto_suite_mask, v'created_at, v'created_lt, v'revoked_at, v'revoked_lt) = v; 
    return (v'exists, v'owner_wallet, v'key_generation, v'enc_pubkey, v'sign_pubkey, v'pq_kem_pubkey_hash, v'pq_kem_pubkey_len, v'pq_kem_pubkey, v'crypto_suite_mask, v'created_at, v'created_lt, v'revoked_at, v'revoked_lt);
}

((int, slice, int, int, int, int, int, cell, int, int, int, int, int)) $VaultKeyRecordView$_constructor_exists_owner_wallet_key_generation_enc_pubkey_sign_pubkey_pq_kem_pubkey_hash_pq_kem_pubkey_len_pq_kem_pubkey_crypto_suite_mask_created_at_created_lt_revoked_at_revoked_lt(int $exists, slice $owner_wallet, int $key_generation, int $enc_pubkey, int $sign_pubkey, int $pq_kem_pubkey_hash, int $pq_kem_pubkey_len, cell $pq_kem_pubkey, int $crypto_suite_mask, int $created_at, int $created_lt, int $revoked_at, int $revoked_lt) inline {
    return ($exists, $owner_wallet, $key_generation, $enc_pubkey, $sign_pubkey, $pq_kem_pubkey_hash, $pq_kem_pubkey_len, $pq_kem_pubkey, $crypto_suite_mask, $created_at, $created_lt, $revoked_at, $revoked_lt);
}

;;
;; Type: VaultUserView
;; TLB: _ exists:bool ton_balance:int257 ath_balance:int257 current_key_id:int257 auth_pubkey:int257 publish_nonce:int257 = VaultUserView
;;

(int, int, int, int, int, int) $VaultUserView$_to_external(((int, int, int, int, int, int)) v) inline {
    var (v'exists, v'ton_balance, v'ath_balance, v'current_key_id, v'auth_pubkey, v'publish_nonce) = v; 
    return (v'exists, v'ton_balance, v'ath_balance, v'current_key_id, v'auth_pubkey, v'publish_nonce);
}

((int, int, int, int, int, int)) $VaultUserView$_constructor_exists_ton_balance_ath_balance_current_key_id_auth_pubkey_publish_nonce(int $exists, int $ton_balance, int $ath_balance, int $current_key_id, int $auth_pubkey, int $publish_nonce) inline {
    return ($exists, $ton_balance, $ath_balance, $current_key_id, $auth_pubkey, $publish_nonce);
}

;;
;; Type: VaultPendingAthWithdrawalView
;; TLB: _ exists:bool owner_wallet:address recipient:address recipient_ath_wallet:address amount:int257 created_at:int257 = VaultPendingAthWithdrawalView
;;

(int, slice, slice, slice, int, int) $VaultPendingAthWithdrawalView$_to_external(((int, slice, slice, slice, int, int)) v) inline {
    var (v'exists, v'owner_wallet, v'recipient, v'recipient_ath_wallet, v'amount, v'created_at) = v; 
    return (v'exists, v'owner_wallet, v'recipient, v'recipient_ath_wallet, v'amount, v'created_at);
}

((int, slice, slice, slice, int, int)) $VaultPendingAthWithdrawalView$_constructor_exists_owner_wallet_recipient_recipient_ath_wallet_amount_created_at(int $exists, slice $owner_wallet, slice $recipient, slice $recipient_ath_wallet, int $amount, int $created_at) inline {
    return ($exists, $owner_wallet, $recipient, $recipient_ath_wallet, $amount, $created_at);
}

;;
;; Type: VaultGlobalView
;; TLB: _ sealed:bool capsule_hub_bound:bool profile_registry_bound:bool username_registry_bound:bool deployment_manifest_hash:int257 capsule_hub_address:address profile_registry_address:address username_registry_address:address vault_ath_wallet_address:address ath_master_address:address user_count:int257 key_record_count:int257 receive_intent_count:int257 pending_ath_withdrawal_count:int257 pending_publish_count:int257 pending_profile_avatar_payment_count:int257 pending_username_mint_payment_count:int257 processed_ath_deposit_count:int257 pending_publish_stale_ttl:int257 airdrop_remaining_ath:int257 airdrop_distributed_ath:int257 airdrop_reward_per_message_ath:int257 airdrop_total_allocation_ath:int257 = VaultGlobalView
;;

(int, int, int, int, int, slice, slice, slice, slice, slice, int, int, int, int, int, int, int, int, int, int, int, int, int) $VaultGlobalView$_to_external(((int, int, int, int, int, slice, slice, slice, slice, slice, int, int, int, int, int, int, int, int, int, int, int, int, int)) v) inline {
    var (v'sealed, v'capsule_hub_bound, v'profile_registry_bound, v'username_registry_bound, v'deployment_manifest_hash, v'capsule_hub_address, v'profile_registry_address, v'username_registry_address, v'vault_ath_wallet_address, v'ath_master_address, v'user_count, v'key_record_count, v'receive_intent_count, v'pending_ath_withdrawal_count, v'pending_publish_count, v'pending_profile_avatar_payment_count, v'pending_username_mint_payment_count, v'processed_ath_deposit_count, v'pending_publish_stale_ttl, v'airdrop_remaining_ath, v'airdrop_distributed_ath, v'airdrop_reward_per_message_ath, v'airdrop_total_allocation_ath) = v; 
    return (v'sealed, v'capsule_hub_bound, v'profile_registry_bound, v'username_registry_bound, v'deployment_manifest_hash, v'capsule_hub_address, v'profile_registry_address, v'username_registry_address, v'vault_ath_wallet_address, v'ath_master_address, v'user_count, v'key_record_count, v'receive_intent_count, v'pending_ath_withdrawal_count, v'pending_publish_count, v'pending_profile_avatar_payment_count, v'pending_username_mint_payment_count, v'processed_ath_deposit_count, v'pending_publish_stale_ttl, v'airdrop_remaining_ath, v'airdrop_distributed_ath, v'airdrop_reward_per_message_ath, v'airdrop_total_allocation_ath);
}

((int, int, int, int, int, slice, slice, slice, slice, slice, int, int, int, int, int, int, int, int, int, int, int, int, int)) $VaultGlobalView$_constructor_sealed_capsule_hub_bound_profile_registry_bound_username_registry_bound_deployment_manifest_hash_capsule_hub_address_profile_registry_address_username_registry_address_vault_ath_wallet_address_ath_master_address_user_count_key_record_count_receive_intent_count_pending_ath_withdrawal_count_pending_publish_count_pending_profile_avatar_payment_count_pending_username_mint_payment_count_processed_ath_deposit_count_pending_publish_stale_ttl_airdrop_remaining_ath_airdrop_distributed_ath_airdrop_reward_per_message_ath_airdrop_total_allocation_ath(int $sealed, int $capsule_hub_bound, int $profile_registry_bound, int $username_registry_bound, int $deployment_manifest_hash, slice $capsule_hub_address, slice $profile_registry_address, slice $username_registry_address, slice $vault_ath_wallet_address, slice $ath_master_address, int $user_count, int $key_record_count, int $receive_intent_count, int $pending_ath_withdrawal_count, int $pending_publish_count, int $pending_profile_avatar_payment_count, int $pending_username_mint_payment_count, int $processed_ath_deposit_count, int $pending_publish_stale_ttl, int $airdrop_remaining_ath, int $airdrop_distributed_ath, int $airdrop_reward_per_message_ath, int $airdrop_total_allocation_ath) inline {
    return ($sealed, $capsule_hub_bound, $profile_registry_bound, $username_registry_bound, $deployment_manifest_hash, $capsule_hub_address, $profile_registry_address, $username_registry_address, $vault_ath_wallet_address, $ath_master_address, $user_count, $key_record_count, $receive_intent_count, $pending_ath_withdrawal_count, $pending_publish_count, $pending_profile_avatar_payment_count, $pending_username_mint_payment_count, $processed_ath_deposit_count, $pending_publish_stale_ttl, $airdrop_remaining_ath, $airdrop_distributed_ath, $airdrop_reward_per_message_ath, $airdrop_total_allocation_ath);
}

;;
;; Type: ATHWallet
;; TLB: _ balance:uint128 owner_address:address ath_master_address:address pending_notifications:dict<int, ^PendingAthTransferNotification{sender_owner:address,response_destination:address,response_ack_value:uint64,amount:uint128,created_at:uint32}> processed_notifications:dict<int, int> pending_outgoing_transfers:dict<int, ^PendingAthOutgoingTransfer{recipient_wallet:address,response_destination:address,amount:uint128,created_at:uint32}> = ATHWallet
;;

builder $ATHWallet$init$_store(builder build_0, (int, slice, slice) v) inline {
    var (v'balance, v'owner_address, v'ath_master_address) = v;
    build_0 = build_0.store_int(v'balance, 257);
    build_0 = build_0.store_slice(v'owner_address);
    build_0 = build_0.store_slice(v'ath_master_address);
    return build_0;
}

cell $ATHWallet$_child_get_code() impure asm """
    B{} B>boc PUSHREF
""";

(cell, cell) $ATHWallet$_init_child(int $balance, slice $owner_address, slice $ath_master_address) inline_ref {
    ;; Build init code cell

    ;; Contract Code: ATHWallet
    cell init_code = $ATHWallet$_child_get_code();


    builder b = begin_cell();
    b = b.store_int(false, 1);
    b = $ATHWallet$init$_store(b, ($balance, $owner_address, $ath_master_address));
    return (init_code, b.end_cell());
}

;;
;; Type: Vault
;; TLB: _ vault_ath_wallet_address:address ath_master_address:address capsule_hub_address:address profile_registry_address:address username_registry_address:address binding_flags:uint8 sealed:bool deployment_manifest_hash:uint256 genesis_config_hash:uint256 users:dict<address, ^UserState{ton_balance:uint128,ath_balance:uint128,current_key_id:uint256,auth_pubkey:uint256,publish_nonce:uint64}> key_records:dict<int, ^KeyRecord{owner_wallet:address,key_generation:uint32,enc_pubkey:uint256,sign_pubkey:uint256,pq_kem_pubkey_hash:uint256,pq_kem_pubkey_len:uint16,pq_kem_pubkey:^cell,crypto_suite_mask:uint16,created_at:uint32,created_lt:uint64,revoked_at:uint32,revoked_lt:uint64}> receive_intents:dict<int, ^ReceiveIntent{sender_wallet:address,recipient_wallet:address,asset:uint8,amount:uint128,commitment:uint256,client_nonce:uint64,settlement_reserve_ton:uint128,created_at:uint32,claimed:bool}> processed_ath_deposits:dict<int, int> pending_ath_withdrawals:dict<int, ^PendingAthWithdrawal{owner_wallet:address,recipient:address,recipient_ath_wallet:address,amount:uint128,refundable_ton_amount:uint128,created_at:uint32}> pending_publishes:dict<int, ^PendingPublish{owner_wallet:address,tombstone:bool,refund_to_vault:bool,nonce:uint64,publish_kind:uint8,body_hash:uint256,refundable_amount:uint128,created_at:uint32}> pending_profile_avatar_payments:dict<int, ^PendingProfileAvatarPayment{owner_wallet:address,amount:uint128,created_at:uint32}> pending_username_mint_payments:dict<int, ^PendingUsernameMintPayment{owner_wallet:address,amount:uint128,created_at:uint32}> user_count:uint64 key_record_count:uint64 receive_intent_count:uint64 processed_ath_deposit_count:uint64 pending_ath_withdrawal_count:uint64 pending_publish_count:uint64 = Vault
;;

builder $Vault$_store(builder build_0, (slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) v) inline {
    var (v'vault_ath_wallet_address, v'ath_master_address, v'capsule_hub_address, v'profile_registry_address, v'username_registry_address, v'binding_flags, v'sealed, v'deployment_manifest_hash, v'genesis_config_hash, v'users, v'key_records, v'receive_intents, v'processed_ath_deposits, v'pending_ath_withdrawals, v'pending_publishes, v'pending_profile_avatar_payments, v'pending_username_mint_payments, v'user_count, v'key_record_count, v'receive_intent_count, v'processed_ath_deposit_count, v'pending_ath_withdrawal_count, v'pending_publish_count) = v;
    build_0 = build_0.store_slice(v'vault_ath_wallet_address);
    build_0 = build_0.store_slice(v'ath_master_address);
    build_0 = build_0.store_slice(v'capsule_hub_address);
    var build_1 = begin_cell();
    build_1 = build_1.store_slice(v'profile_registry_address);
    build_1 = build_1.store_slice(v'username_registry_address);
    build_1 = build_1.store_uint(v'binding_flags, 8);
    build_1 = build_1.store_int(v'sealed, 1);
    build_1 = build_1.store_uint(v'deployment_manifest_hash, 256);
    var build_2 = begin_cell();
    build_2 = build_2.store_uint(v'genesis_config_hash, 256);
    build_2 = build_2.store_dict(v'users);
    build_2 = build_2.store_dict(v'key_records);
    build_2 = build_2.store_dict(v'receive_intents);
    var build_3 = begin_cell();
    build_3 = build_3.store_dict(v'processed_ath_deposits);
    build_3 = build_3.store_dict(v'pending_ath_withdrawals);
    build_3 = build_3.store_dict(v'pending_publishes);
    var build_4 = begin_cell();
    build_4 = build_4.store_dict(v'pending_profile_avatar_payments);
    build_4 = build_4.store_dict(v'pending_username_mint_payments);
    build_4 = build_4.store_uint(v'user_count, 64);
    build_4 = build_4.store_uint(v'key_record_count, 64);
    build_4 = build_4.store_uint(v'receive_intent_count, 64);
    build_4 = build_4.store_uint(v'processed_ath_deposit_count, 64);
    build_4 = build_4.store_uint(v'pending_ath_withdrawal_count, 64);
    build_4 = build_4.store_uint(v'pending_publish_count, 64);
    build_3 = store_builder_ref(build_3, build_4);
    build_2 = store_builder_ref(build_2, build_3);
    build_1 = store_builder_ref(build_1, build_2);
    build_0 = store_builder_ref(build_0, build_1);
    return build_0;
}

(slice, ((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int))) $Vault$_load(slice sc_0) inline {
    var v'vault_ath_wallet_address = sc_0~load_msg_addr();
    var v'ath_master_address = sc_0~load_msg_addr();
    var v'capsule_hub_address = sc_0~load_msg_addr();
    slice sc_1 = sc_0~load_ref().begin_parse();
    var v'profile_registry_address = sc_1~load_msg_addr();
    var v'username_registry_address = sc_1~load_msg_addr();
    var v'binding_flags = sc_1~load_uint(8);
    var v'sealed = sc_1~load_int(1);
    var v'deployment_manifest_hash = sc_1~load_uint(256);
    slice sc_2 = sc_1~load_ref().begin_parse();
    var v'genesis_config_hash = sc_2~load_uint(256);
    var v'users = sc_2~load_dict();
    var v'key_records = sc_2~load_dict();
    var v'receive_intents = sc_2~load_dict();
    slice sc_3 = sc_2~load_ref().begin_parse();
    var v'processed_ath_deposits = sc_3~load_dict();
    var v'pending_ath_withdrawals = sc_3~load_dict();
    var v'pending_publishes = sc_3~load_dict();
    slice sc_4 = sc_3~load_ref().begin_parse();
    var v'pending_profile_avatar_payments = sc_4~load_dict();
    var v'pending_username_mint_payments = sc_4~load_dict();
    var v'user_count = sc_4~load_uint(64);
    var v'key_record_count = sc_4~load_uint(64);
    var v'receive_intent_count = sc_4~load_uint(64);
    var v'processed_ath_deposit_count = sc_4~load_uint(64);
    var v'pending_ath_withdrawal_count = sc_4~load_uint(64);
    var v'pending_publish_count = sc_4~load_uint(64);
    return (sc_0, (v'vault_ath_wallet_address, v'ath_master_address, v'capsule_hub_address, v'profile_registry_address, v'username_registry_address, v'binding_flags, v'sealed, v'deployment_manifest_hash, v'genesis_config_hash, v'users, v'key_records, v'receive_intents, v'processed_ath_deposits, v'pending_ath_withdrawals, v'pending_publishes, v'pending_profile_avatar_payments, v'pending_username_mint_payments, v'user_count, v'key_record_count, v'receive_intent_count, v'processed_ath_deposit_count, v'pending_ath_withdrawal_count, v'pending_publish_count));
}

(slice, ((slice, slice, slice, int, int, int, int))) $Vault$init$_load(slice sc_0) inline {
    var v'vault_ath_wallet_address = sc_0~load_msg_addr();
    var v'ath_master_address = sc_0~load_msg_addr();
    var v'capsule_hub_address = sc_0~load_msg_addr();
    slice sc_1 = sc_0~load_ref().begin_parse();
    var v'genesis_config_hash = sc_1~load_int(257);
    var v'capsule_hub_bound = sc_1~load_int(1);
    var v'sealed = sc_1~load_int(1);
    var v'deployment_manifest_hash = sc_1~load_int(257);
    return (sc_0, (v'vault_ath_wallet_address, v'ath_master_address, v'capsule_hub_address, v'genesis_config_hash, v'capsule_hub_bound, v'sealed, v'deployment_manifest_hash));
}

(slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $Vault$_contract_load() impure inline {
    slice $sc = get_data().begin_parse();
    int $loaded = $sc~load_int(1);
    if ($loaded) {
        return $sc~$Vault$_load();
    }
    else {
        (slice $vault_ath_wallet_address, slice $ath_master_address, slice $capsule_hub_address, int $genesis_config_hash, int $capsule_hub_bound, int $sealed, int $deployment_manifest_hash) = $sc~$Vault$init$_load();
        $sc.end_parse();
        return $Vault$_contract_init($vault_ath_wallet_address, $ath_master_address, $capsule_hub_address, $genesis_config_hash, $capsule_hub_bound, $sealed, $deployment_manifest_hash);
    }
}

() $Vault$_contract_store((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) v) impure inline {
    builder b = begin_cell();
    b = b.store_int(true, 1);
    b = $Vault$_store(b, v);
    set_data(b.end_cell());
}

;;
;; Contract Vault functions
;;

(slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $Vault$_contract_init(slice $vault_ath_wallet_address, slice $ath_master_address, slice $capsule_hub_address, int $genesis_config_hash, int $capsule_hub_bound, int $sealed, int $deployment_manifest_hash) impure inline {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = (null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null());
    $self'vault_ath_wallet_address = $vault_ath_wallet_address;
    $self'ath_master_address = $ath_master_address;
    $self'capsule_hub_address = $capsule_hub_address;
    $self'profile_registry_address = $capsule_hub_address;
    $self'username_registry_address = $capsule_hub_address;
    $self'binding_flags = 0;
    if ($capsule_hub_bound) {
        $self'binding_flags = 1;
    }
    $self'sealed = $sealed;
    $self'deployment_manifest_hash = $deployment_manifest_hash;
    $self'genesis_config_hash = $genesis_config_hash;
    if ($sealed) {
        ifnot ($self'deployment_manifest_hash) {
            $self'deployment_manifest_hash = $genesis_config_hash;
        }
        $self'genesis_config_hash = 15000000000000000;
    }
    $self'users = null();
    $self'key_records = null();
    $self'receive_intents = null();
    $self'processed_ath_deposits = null();
    $self'pending_ath_withdrawals = null();
    $self'pending_publishes = null();
    $self'pending_profile_avatar_payments = null();
    $self'pending_username_mint_payments = null();
    $self'user_count = 0;
    $self'key_record_count = 0;
    $self'receive_intent_count = 0;
    $self'processed_ath_deposit_count = 0;
    $self'pending_ath_withdrawal_count = 0;
    $self'pending_publish_count = 0;
    return ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_requireSealed((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    throw_unless(15000, $self'sealed);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_requireUnsealed((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    throw_unless(15001, (~ $self'sealed));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_isBindingSet((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $mask) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_127 = ((($self'binding_flags / $mask) % 2) == 1);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_127);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_isCapsuleHubBound((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_128 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isBindingSet(1);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_128);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_isProfileRegistryBound((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_129 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isBindingSet(2);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_129);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_isUsernameRegistryBound((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_130 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isBindingSet(4);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_130);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_requireGenesisController((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    throw_unless(15030, ($self'genesis_config_hash != 0));
    throw_unless(15031, ($Cell$_fun_hash($Builder$_fun_endCell($Builder$_fun_storeAddress($global_beginCell(), __tact_context_get_sender()))) == $self'genesis_config_hash));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), (int, int, int, int, int)) $Vault$_fun_emptyUser((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_131 = $UserState$_constructor_ton_balance_ath_balance_current_key_id_auth_pubkey_publish_nonce(0, 0, 0, 0, 0);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_131);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), (int, int, int, int, int)) $Vault$_fun_getUserOrEmpty((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $owner) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    tuple $value = $UserState$_load_opt(__tact_dict_get_slice_cell($self'users, 267, $owner));
    if (null?($value)) {
        var $fresh$ret_132 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_emptyUser();
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_132);
    }
    var $fresh$ret_133 = $UserState$_not_null($value);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_133);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), slice) $Vault$_fun_deriveAthWalletAddress((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $owner) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var ($owner_std'workchain, $owner_std'address) = $global_parseStdAddress($Address$_fun_asSlice($owner));
    var ($wallet_init'code, $wallet_init'data) = $ATHWallet$_init_child(0, $owner, $self'ath_master_address);
    var $fresh$ret_134 = $global_contractAddressExt($owner_std'workchain, $wallet_init'code, $wallet_init'data);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_134);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_isBasechainAddress((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $owner) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    slice $ownerSlice = $Address$_fun_asSlice($owner);
    var $fresh$ret_135 = ($ownerSlice~load_uint(11) == 1024);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_135);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_pendingPublishCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_136 = ($self'pending_publish_count % 4294967296);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_136);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_pendingProfileAvatarPaymentCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_137 = (($self'pending_publish_count / 4294967296) % 65536);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_137);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_pendingUsernameMintPaymentCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_138 = ($self'pending_publish_count / 281474976710656);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_138);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_incrementPendingPublishCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    $self'pending_publish_count = $self'pending_publish_count + 1;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_decrementPendingPublishCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    $self'pending_publish_count = $self'pending_publish_count - 1;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_incrementPendingProfileAvatarPaymentCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    throw_unless(16690, (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_pendingProfileAvatarPaymentCount() < 65535));
    $self'pending_publish_count = $self'pending_publish_count + 4294967296;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_decrementPendingProfileAvatarPaymentCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    $self'pending_publish_count = $self'pending_publish_count - 4294967296;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_incrementPendingUsernameMintPaymentCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    throw_unless(16790, (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_pendingUsernameMintPaymentCount() < 65535));
    $self'pending_publish_count = $self'pending_publish_count + 281474976710656;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_decrementPendingUsernameMintPaymentCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    $self'pending_publish_count = $self'pending_publish_count - 281474976710656;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_computeAthDepositId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $senderWallet, int $queryId) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_139 = $Cell$_fun_hash($Builder$_fun_endCell(store_uint($Builder$_fun_storeAddress(store_uint($global_beginCell(), 1094996041, 32), $senderWallet), $queryId, 64)));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_139);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_computeAthWithdrawalId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $ownerWallet, int $queryId) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_140 = ($Cell$_fun_hash($Builder$_fun_endCell(store_uint($Builder$_fun_storeAddress(store_uint($global_beginCell(), 1096239428, 32), $ownerWallet), $queryId, 64))) % 18446744073709551616);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_140);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_computeProfileAvatarPaymentId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $ownerWallet, int $clientNonce, int $avatarHash) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_141 = ($Cell$_fun_hash($Builder$_fun_endCell(store_uint(store_uint($Builder$_fun_storeAddress(store_uint($global_beginCell(), 1448100169, 32), $ownerWallet), $clientNonce, 64), $avatarHash, 256))) % 18446744073709551616);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_141);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_computeUsernameNameHash((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $username) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_142 = $Cell$_fun_hash($Builder$_fun_endCell($Builder$_fun_storeSlice(store_uint($global_beginCell(), 3318512854, 32), $username)));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_142);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_computeUsernameMintPaymentId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $ownerWallet, int $clientNonce, int $nameHash) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_143 = ($Cell$_fun_hash($Builder$_fun_endCell(store_uint(store_uint($Builder$_fun_storeAddress(store_uint($global_beginCell(), 1448431177, 32), $ownerWallet), $clientNonce, 64), $nameHash, 256))) % 18446744073709551616);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_143);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), slice) $Vault$_fun_profileRegistryAthWalletAddress((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_144 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_deriveAthWalletAddress($self'profile_registry_address);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_144);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), slice) $Vault$_fun_usernameRegistryAthWalletAddress((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_145 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_deriveAthWalletAddress($self'username_registry_address);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_145);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_profileAvatarTonCharge((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_146 = 61000000;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_146);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_usernameMintTonCharge((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_147 = 63000000;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_147);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_usernamePriceForLength((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $nameLen) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    throw_unless(16650, ($nameLen >= 4));
    throw_unless(16651, ($nameLen <= 16));
    if (($nameLen == 4)) {
        var $fresh$ret_148 = 10000000000000;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_148);
    }
    if (($nameLen == 5)) {
        var $fresh$ret_149 = 1000000000000;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_149);
    }
    var $fresh$ret_150 = 100000000000;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_150);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_isValidUsernameBytes((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $nameLen, slice $username) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    if (( (($nameLen < 4)) ? (true) : (($nameLen > 16)) )) {
        var $fresh$ret_151 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_151);
    }
    if (($Slice$_fun_bits($username) != ($nameLen * 8))) {
        var $fresh$ret_152 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_152);
    }
    ifnot ($Slice$_fun_refsEmpty($username)) {
        var $fresh$ret_153 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_153);
    }
    slice $cursor = $username;
    int $i = 0;
    while (($i < $nameLen)) {
        int $ch = $cursor~load_uint(8);
        int $lower = ( (($ch >= 97)) ? (($ch <= 122)) : (false) );
        int $digit = ( (($ch >= 48)) ? (($ch <= 57)) : (false) );
        int $separator = ( (($ch == 45)) ? (true) : (($ch == 95)) );
        ifnot (( (( ($lower) ? (true) : ($digit) )) ? (true) : ($separator) )) {
            var $fresh$ret_154 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_154);
        }
        $i = $i + 1;
    }
    var $fresh$ret_155 = $Slice$_fun_empty($cursor);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_155);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_requireValidProfileAvatarPointer((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $ownerWallet, int $avatarHash, int $avatarStreamId, int $avatarPartCount, int $mediaFormat) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    throw_unless(16640, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isBasechainAddress($ownerWallet));
    throw_unless(16641, ($avatarHash != 0));
    throw_unless(16642, ($avatarStreamId != 0));
    throw_unless(16643, ($avatarPartCount > 0));
    throw_unless(16644, ($avatarPartCount <= 16));
    throw_unless(16645, ($mediaFormat == 1));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_discountedFee((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $owner, int $fullFee) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    ifnot ($self'sealed) {
        var $fresh$ret_156 = $fullFee;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_156);
    }
    if (($self'genesis_config_hash > 0)) {
        var $fresh$ret_157 = $fullFee;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_157);
    }
    int $minFee = 0;
    if (($fullFee < $minFee)) {
        $minFee = $fullFee;
    }
    var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_getUserOrEmpty($owner);
    if (($user'ath_balance >= 10000000000000)) {
        var $fresh$ret_158 = $minFee;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_158);
    }
    int $remaining = (10000000000000 - $user'ath_balance);
    int $fee = (((($fullFee * $remaining) + 10000000000000) - 1) / 10000000000000);
    if (($fee < $minFee)) {
        var $fresh$ret_159 = $minFee;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_159);
    }
    var $fresh$ret_160 = $fee;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_160);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_isAllowedPrivateSizeClass((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $sizeClass) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_171 = ( (( (( (( (( (($sizeClass == 1)) ? (true) : (($sizeClass == 2)) )) ? (true) : (($sizeClass == 4)) )) ? (true) : (($sizeClass == 8)) )) ? (true) : (($sizeClass == 16)) )) ? (true) : (($sizeClass == 32)) );
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_171);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_isPublishProfileValid((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $publishKind, int $sizeClass, int $cryptoSuite, int $header0, int $header1) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    if (($publishKind == 1)) {
        ifnot ($header0) {
            var $fresh$ret_161 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_161);
        }
        ifnot ($header1) {
            var $fresh$ret_162 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_162);
        }
        ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isAllowedPrivateSizeClass($sizeClass)) {
            var $fresh$ret_163 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_163);
        }
        var $fresh$ret_164 = ($cryptoSuite == 2);
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_164);
    }
    if (($publishKind == 2)) {
        if (($sizeClass != 1)) {
            var $fresh$ret_165 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_165);
        }
        if ($cryptoSuite) {
            var $fresh$ret_166 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_166);
        }
        ifnot ($header0) {
            var $fresh$ret_167 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_167);
        }
        if ($header1) {
            var $fresh$ret_168 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_168);
        }
        var $fresh$ret_169 = true;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_169);
    }
    var $fresh$ret_170 = false;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_170);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_privateUsefulBytes((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $sizeClass) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    throw_unless(16326, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isAllowedPrivateSizeClass($sizeClass));
    var $fresh$ret_172 = ($sizeClass * 1024);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_172);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_privateBodyOverheadBytes((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $cryptoSuite) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    throw_unless(16327, ($cryptoSuite == 2));
    var $fresh$ret_173 = 1204;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_173);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_privateBodyBytes((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $sizeClass, int $cryptoSuite) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_174 = (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_privateBodyOverheadBytes($cryptoSuite) + ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_privateUsefulBytes($sizeClass));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_174);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_privateBodyBits((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $sizeClass, int $cryptoSuite) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_175 = (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_privateBodyBytes($sizeClass, $cryptoSuite) * 8);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_175);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_privateBodyCells((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $sizeClass, int $cryptoSuite) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_176 = ((($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_privateBodyBytes($sizeClass, $cryptoSuite) + 126) / 127);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_176);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_privateBodyRefs((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $sizeClass, int $cryptoSuite) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_177 = (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_privateBodyCells($sizeClass, $cryptoSuite) - 1);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_177);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_isExactCapsuleCell((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, cell $cell, int $expectedCells, int $expectedBits, int $expectedRefs) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    try {
        var ($size'cells, $size'bits, $size'refs) = $Cell$_fun_computeDataSize($cell, $expectedCells);
        var $fresh$ret_178 = ( (( (($size'cells == $expectedCells)) ? (($size'bits == $expectedBits)) : (false) )) ? (($size'refs == $expectedRefs)) : (false) );
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_178);
    } catch (_, $exitCode) {
        var $fresh$ret_179 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_179);
    }
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_isPublicCapsuleCell((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, cell $cell, int $maxCells, int $maxBits, int $maxRefs, int $requireByteAligned) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    try {
        var ($size'cells, $size'bits, $size'refs) = $Cell$_fun_computeDataSize($cell, $maxCells);
        ifnot (($size'cells <= $maxCells)) {
            var $fresh$ret_180 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_180);
        }
        ifnot (($size'bits > 0)) {
            var $fresh$ret_181 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_181);
        }
        ifnot (($size'bits <= $maxBits)) {
            var $fresh$ret_182 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_182);
        }
        ifnot (($size'refs <= $maxRefs)) {
            var $fresh$ret_183 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_183);
        }
        if (( ($requireByteAligned) ? ((~ (($size'bits % 8) == 0))) : (false) )) {
            var $fresh$ret_184 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_184);
        }
        var $fresh$ret_185 = true;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_185);
    } catch (_, $exitCode) {
        var $fresh$ret_186 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_186);
    }
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_isPrivateCapsuleShapeValid((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $sizeClass, int $cryptoSuite, cell $header0, cell $header1, cell $body) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isExactCapsuleCell($header0, 2, 1120, 1)) {
        var $fresh$ret_187 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_187);
    }
    ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isExactCapsuleCell($header1, 1, 240, 0)) {
        var $fresh$ret_188 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_188);
    }
    var $fresh$ret_189 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isExactCapsuleCell($body, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_privateBodyCells($sizeClass, $cryptoSuite), ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_privateBodyBits($sizeClass, $cryptoSuite), ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_privateBodyRefs($sizeClass, $cryptoSuite));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_189);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_isPublicCapsuleShapeValid((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, cell $header, cell $body) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isPublicCapsuleCell($header, 1, 576, 0, true)) {
        var $fresh$ret_190 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_190);
    }
    var $fresh$ret_191 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isPublicCapsuleCell($body, 9, 8192, 8, true);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_191);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_privatePublishLocalExecReserve((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $sizeClass, int $cryptoSuite) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    throw_unless(16323, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isAllowedPrivateSizeClass($sizeClass));
    throw_unless(16324, ($cryptoSuite == 2));
    if (($sizeClass == 1)) {
        var $fresh$ret_192 = 12000000;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_192);
    }
    if (($sizeClass == 2)) {
        var $fresh$ret_193 = 13800000;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_193);
    }
    if (($sizeClass == 4)) {
        var $fresh$ret_194 = 17300000;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_194);
    }
    if (($sizeClass == 8)) {
        var $fresh$ret_195 = 24400000;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_195);
    }
    if (($sizeClass == 16)) {
        var $fresh$ret_196 = 38900000;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_196);
    }
    var $fresh$ret_197 = 67600000;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_197);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_privateCapsulehubExecReserve((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $sizeClass, int $cryptoSuite) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    throw_unless(16329, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isAllowedPrivateSizeClass($sizeClass));
    throw_unless(16325, ($cryptoSuite == 2));
    if (($sizeClass == 1)) {
        var $fresh$ret_198 = 4200000;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_198);
    }
    if (($sizeClass == 2)) {
        var $fresh$ret_199 = 4300000;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_199);
    }
    if (($sizeClass == 4)) {
        var $fresh$ret_200 = 4500000;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_200);
    }
    if (($sizeClass == 8)) {
        var $fresh$ret_201 = 5000000;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_201);
    }
    if (($sizeClass == 16)) {
        var $fresh$ret_202 = 5800000;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_202);
    }
    var $fresh$ret_203 = 7600000;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_203);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_publishLocalExecReserve((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $publishKind, int $sizeClass, int $cryptoSuite) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    if (($publishKind == 2)) {
        throw_unless(16320, ($sizeClass == 1));
        throw_unless(16321, ($cryptoSuite == 0));
        var $fresh$ret_204 = 8700000;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_204);
    }
    throw_unless(16322, ($publishKind == 1));
    var $fresh$ret_205 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_privatePublishLocalExecReserve($sizeClass, $cryptoSuite);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_205);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_canonicalPublishMaxCharge((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $owner, int $publishKind, int $sizeClass, int $cryptoSuite) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    int $localExecReserve = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_publishLocalExecReserve($publishKind, $sizeClass, $cryptoSuite);
    if (($publishKind == 2)) {
        var $fresh$ret_206 = ((((($localExecReserve + ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_discountedFee($owner, 10000000)) + 2400000) + 1000000) + 7400000) + 30000000);
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_206);
    }
    throw_unless(16328, ($cryptoSuite == 2));
    var $fresh$ret_207 = ((((($localExecReserve + ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_discountedFee($owner, 10000000)) + ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_privateCapsulehubExecReserve($sizeClass, $cryptoSuite)) + 1000000) + 3300000) + 30000000);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_207);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_computePublishId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $owner, int $nonce, int $bodyHash, int $publishKind) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_208 = $Cell$_fun_hash($Builder$_fun_endCell(store_uint(store_uint(store_uint($Builder$_fun_storeAddress(store_uint($global_beginCell(), $self'deployment_manifest_hash, 256), $owner), $nonce, 64), $bodyHash, 256), $publishKind, 8)));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_208);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_capsulehubCallValue((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $maxCharge, int $localExecReserve) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    throw_unless(16340, ($maxCharge >= $localExecReserve));
    var $fresh$ret_209 = ($maxCharge - $localExecReserve);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_209);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_computePublishBounceId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $publishId) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_210 = ($publishId % 18446744073709551616);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_210);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_computePublishBounceTag((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $publishId) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_211 = ($Cell$_fun_hash($Builder$_fun_endCell(store_uint($global_beginCell(), $publishId, 256))) % 1461501637330902918203684832716283019655932542976);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_211);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_protocolFeePaid((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $owner, int $publishKind, int $sizeClass, int $cryptoSuite) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    if (($publishKind == 2)) {
        throw_unless(16350, ($sizeClass == 1));
        throw_unless(16351, ($cryptoSuite == 0));
        var $fresh$ret_212 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_discountedFee($owner, 10000000);
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_212);
    }
    throw_unless(16352, ($publishKind == 1));
    throw_unless(16353, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isAllowedPrivateSizeClass($sizeClass));
    throw_unless(16355, ($cryptoSuite == 2));
    var $fresh$ret_213 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_discountedFee($owner, 10000000);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_213);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_refundPublishValue((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $owner, int $refundableAmount, int $amount) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    int $refundAmount = 0;
    if (($amount > 4200000)) {
        $refundAmount = ($amount - 4200000);
    }
    if (($refundAmount > $refundableAmount)) {
        $refundAmount = $refundableAmount;
    }
    if (($refundAmount <= 0)) {
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), ());
    }
    $global_message($MessageParameters$_constructor_to_value_bounce_mode_body($owner, $refundAmount, false, 1, null()));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_refundPendingPublish((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, (slice, int, int, int, int, int, int, int) $pending, int $amount) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var (($pending'owner_wallet, $pending'tombstone, $pending'refund_to_vault, $pending'nonce, $pending'publish_kind, $pending'body_hash, $pending'refundable_amount, $pending'created_at)) = $pending;
    ifnot ($pending'refund_to_vault) {
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_refundPublishValue($pending'owner_wallet, $pending'refundable_amount, $amount);
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), ());
    }
    int $refundAmount = 0;
    if (($amount > 4200000)) {
        $refundAmount = ($amount - 4200000);
    }
    if (($refundAmount > $pending'refundable_amount)) {
        $refundAmount = $pending'refundable_amount;
    }
    if (($refundAmount <= 0)) {
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), ());
    }
    var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_getUserOrEmpty($pending'owner_wallet);
    $user'ton_balance = $user'ton_balance + $refundAmount;
    $self'users~__tact_dict_set_slice_cell(267, $pending'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_refundPendingAthWithdrawalTon((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, (slice, slice, slice, int, int, int) $pending, int $amount) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var (($pending'owner_wallet, $pending'recipient, $pending'recipient_ath_wallet, $pending'amount, $pending'refundable_ton_amount, $pending'created_at)) = $pending;
    int $refundAmount = 0;
    if (($amount > 2000000)) {
        $refundAmount = ($amount - 2000000);
    }
    if (($refundAmount > $pending'refundable_ton_amount)) {
        $refundAmount = $pending'refundable_ton_amount;
    }
    if (($refundAmount <= 0)) {
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), ());
    }
    var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_getUserOrEmpty($pending'owner_wallet);
    $user'ton_balance = $user'ton_balance + $refundAmount;
    $self'users~__tact_dict_set_slice_cell(267, $pending'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_restorePendingProfileAvatarPayment((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $queryId, (slice, int, int) $pending) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var (($pending'owner_wallet, $pending'amount, $pending'created_at)) = $pending;
    var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_getUserOrEmpty($pending'owner_wallet);
    $user'ath_balance = $user'ath_balance + $pending'amount;
    $self'users~__tact_dict_set_slice_cell(267, $pending'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
    $self'pending_profile_avatar_payments~__tact_dict_delete_int(257, $queryId);
    ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_decrementPendingProfileAvatarPaymentCount();
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_restorePendingUsernameMintPayment((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $queryId, (slice, int, int) $pending) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var (($pending'owner_wallet, $pending'amount, $pending'created_at)) = $pending;
    var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_getUserOrEmpty($pending'owner_wallet);
    $user'ath_balance = $user'ath_balance + $pending'amount;
    $self'users~__tact_dict_set_slice_cell(267, $pending'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
    $self'pending_username_mint_payments~__tact_dict_delete_int(257, $queryId);
    ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_decrementPendingUsernameMintPaymentCount();
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_creditActivityAirdrop((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $owner) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    if (($self'genesis_config_hash <= 0)) {
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), ());
    }
    throw_unless(16540, __tact_dict_exists_slice($self'users, 267, $owner));
    int $reward = 10000000000;
    if (($reward > $self'genesis_config_hash)) {
        $reward = $self'genesis_config_hash;
    }
    var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_getUserOrEmpty($owner);
    $user'ath_balance = $user'ath_balance + $reward;
    $self'genesis_config_hash = $self'genesis_config_hash - $reward;
    $self'users~__tact_dict_set_slice_cell(267, $owner, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_keyRecordStorageEndowment((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $cryptoSuiteMask) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    throw_unless(16100, ($cryptoSuiteMask == 2));
    var $fresh$ret_214 = 30000000;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_214);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_validatePqPubkeyCell((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $pqLen, int $cryptoSuiteMask, cell $pqPubkey) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    throw_unless(16107, ($cryptoSuiteMask == 2));
    var ($size'cells, $size'bits, $size'refs) = $Cell$_fun_computeDataSize($pqPubkey, 10);
    throw_unless(16108, ($size'cells == 10));
    throw_unless(16109, ($size'bits == 9472));
    throw_unless(16113, ($size'refs == 9));
    cell $cursor = $pqPubkey;
    int $remaining = $pqLen;
    while (($remaining > 0)) {
        int $chunkBytes = 127;
        if (($remaining == 1184)) {
            $chunkBytes = 41;
        } elseif (($remaining < $chunkBytes)) {
            $chunkBytes = $remaining;
        }
        slice $cursorSlice = $Cell$_fun_beginParse($cursor);
        throw_unless(16114, ($Slice$_fun_bits($cursorSlice) == ($chunkBytes * 8)));
        if (($remaining > $chunkBytes)) {
            throw_unless(16115, ($Slice$_fun_refs($cursorSlice) == 1));
            $cursor = $cursorSlice~$Slice$_fun_loadRef();
        } else {
            throw_unless(16116, ($Slice$_fun_refs($cursorSlice) == 0));
        }
        $remaining = $remaining - $chunkBytes;
    }
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), ()) $Vault$_fun_validateKeyProfile((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $encPubkey, int $signPubkey, int $pqHash, int $pqLen, cell $pqPubkey, int $cryptoSuiteMask) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    throw_unless(16101, ($encPubkey != 0));
    throw_unless(16102, ($signPubkey != 0));
    throw_unless(16107, ($cryptoSuiteMask == 2));
    throw_unless(16105, ($pqHash != 0));
    throw_unless(16106, ($pqLen == 1184));
    ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_validatePqPubkeyCell($pqLen, $cryptoSuiteMask, $pqPubkey);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_computeKeyId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $owner, int $keyGeneration, int $encPubkey, int $signPubkey, int $pqHash, int $pqLen, int $cryptoSuiteMask) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    cell $keyFields = $Builder$_fun_endCell(store_uint(store_uint(store_uint($global_beginCell(), $encPubkey, 256), $signPubkey, 256), $pqHash, 256));
    var $fresh$ret_215 = $Cell$_fun_hash($Builder$_fun_endCell($Builder$_fun_storeRef($keyFields, store_uint(store_uint(store_uint($Builder$_fun_storeAddress(store_uint($global_beginCell(), 1262836041, 32), $owner), $keyGeneration, 32), $pqLen, 16), $cryptoSuiteMask, 16))));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_215);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_computeReceiveIntentId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $senderWallet, slice $recipientWallet, int $asset, int $amount, int $clientNonce) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_216 = $Cell$_fun_hash($Builder$_fun_endCell(store_uint(store_uint(store_uint($Builder$_fun_storeAddress($Builder$_fun_storeAddress(store_uint($global_beginCell(), 1380141380, 32), $senderWallet), $recipientWallet), $asset, 8), $amount, 128), $clientNonce, 64)));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_216);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_computeReceiveIntentCommitment((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $intentId, slice $recipientWallet, int $secret32) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_217 = $Cell$_fun_hash($Builder$_fun_endCell(store_uint($Builder$_fun_storeAddress(store_uint(store_uint($global_beginCell(), 1380139853, 32), $intentId, 256), $recipientWallet), $secret32, 256)));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_217);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), (int, int, int, int, int, int)) $Vault$_fun_get_user((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $owner) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    tuple $userOpt = $UserState$_load_opt(__tact_dict_get_slice_cell($self'users, 267, $owner));
    if (null?($userOpt)) {
        var $fresh$ret_218 = $VaultUserView$_constructor_exists_ton_balance_ath_balance_current_key_id_auth_pubkey_publish_nonce(false, 0, 0, 0, 0, 0);
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_218);
    }
    var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce) = $UserState$_not_null($userOpt);
    var $fresh$ret_219 = $VaultUserView$_constructor_exists_ton_balance_ath_balance_current_key_id_auth_pubkey_publish_nonce(true, $user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_219);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), (int, slice, int, int, int, int, int, cell, int, int, int, int, int)) $Vault$_fun_get_key_record((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $keyId) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    tuple $recordOpt = $KeyRecord$_load_opt(__tact_dict_get_int_cell($self'key_records, 257, $keyId));
    if (null?($recordOpt)) {
        var $fresh$ret_220 = $VaultKeyRecordView$_constructor_exists_owner_wallet_key_generation_enc_pubkey_sign_pubkey_pq_kem_pubkey_hash_pq_kem_pubkey_len_pq_kem_pubkey_crypto_suite_mask_created_at_created_lt_revoked_at_revoked_lt(false, $global_myAddress(), 0, 0, 0, 0, 0, $global_emptyCell(), 0, 0, 0, 0, 0);
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_220);
    }
    var ($record'owner_wallet, $record'key_generation, $record'enc_pubkey, $record'sign_pubkey, $record'pq_kem_pubkey_hash, $record'pq_kem_pubkey_len, $record'pq_kem_pubkey, $record'crypto_suite_mask, $record'created_at, $record'created_lt, $record'revoked_at, $record'revoked_lt) = $KeyRecord$_not_null($recordOpt);
    var $fresh$ret_221 = $VaultKeyRecordView$_constructor_exists_owner_wallet_key_generation_enc_pubkey_sign_pubkey_pq_kem_pubkey_hash_pq_kem_pubkey_len_pq_kem_pubkey_crypto_suite_mask_created_at_created_lt_revoked_at_revoked_lt(true, $record'owner_wallet, $record'key_generation, $record'enc_pubkey, $record'sign_pubkey, $record'pq_kem_pubkey_hash, $record'pq_kem_pubkey_len, $record'pq_kem_pubkey, $record'crypto_suite_mask, $record'created_at, $record'created_lt, $record'revoked_at, $record'revoked_lt);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_221);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), (int, slice, slice, int, int, int, int, int, int, int)) $Vault$_fun_get_receive_intent((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $intentId) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    tuple $intentOpt = $ReceiveIntent$_load_opt(__tact_dict_get_int_cell($self'receive_intents, 257, $intentId));
    if (null?($intentOpt)) {
        var $fresh$ret_222 = $VaultReceiveIntentView$_constructor_exists_sender_wallet_recipient_wallet_asset_amount_commitment_client_nonce_settlement_reserve_ton_created_at_claimed(false, $global_myAddress(), $global_myAddress(), 0, 0, 0, 0, 0, 0, false);
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_222);
    }
    var ($intent'sender_wallet, $intent'recipient_wallet, $intent'asset, $intent'amount, $intent'commitment, $intent'client_nonce, $intent'settlement_reserve_ton, $intent'created_at, $intent'claimed) = $ReceiveIntent$_not_null($intentOpt);
    var $fresh$ret_223 = $VaultReceiveIntentView$_constructor_exists_sender_wallet_recipient_wallet_asset_amount_commitment_client_nonce_settlement_reserve_ton_created_at_claimed(true, $intent'sender_wallet, $intent'recipient_wallet, $intent'asset, $intent'amount, $intent'commitment, $intent'client_nonce, $intent'settlement_reserve_ton, $intent'created_at, $intent'claimed);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_223);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_get_receive_intent_id((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $senderWallet, slice $recipientWallet, int $asset, int $amount, int $clientNonce) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_224 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computeReceiveIntentId($senderWallet, $recipientWallet, $asset, $amount, $clientNonce);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_224);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_get_receive_intent_commitment((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $intentId, slice $recipientWallet, int $secret32) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_225 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computeReceiveIntentCommitment($intentId, $recipientWallet, $secret32);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_225);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), (int, slice, slice, slice, int, int)) $Vault$_fun_get_pending_ath_withdrawal((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, int $queryId) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    tuple $pendingOpt = $PendingAthWithdrawal$_load_opt(__tact_dict_get_int_cell($self'pending_ath_withdrawals, 257, $queryId));
    if (null?($pendingOpt)) {
        var $fresh$ret_226 = $VaultPendingAthWithdrawalView$_constructor_exists_owner_wallet_recipient_recipient_ath_wallet_amount_created_at(false, $global_myAddress(), $global_myAddress(), $global_myAddress(), 0, 0);
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_226);
    }
    var ($pending'owner_wallet, $pending'recipient, $pending'recipient_ath_wallet, $pending'amount, $pending'refundable_ton_amount, $pending'created_at) = $PendingAthWithdrawal$_not_null($pendingOpt);
    var $fresh$ret_227 = $VaultPendingAthWithdrawalView$_constructor_exists_owner_wallet_recipient_recipient_ath_wallet_amount_created_at(true, $pending'owner_wallet, $pending'recipient, $pending'recipient_ath_wallet, $pending'amount, $pending'created_at);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_227);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_get_ath_withdrawal_id((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $ownerWallet, int $queryId) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_228 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computeAthWithdrawalId($ownerWallet, $queryId);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_228);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), (int, slice, slice, slice, int, int)) $Vault$_fun_get_pending_ath_withdrawal_for((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $ownerWallet, int $queryId) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    int $withdrawalId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computeAthWithdrawalId($ownerWallet, $queryId);
    var $fresh$ret_229 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_get_pending_ath_withdrawal($withdrawalId);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_229);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), int) $Vault$_fun_get_canonical_publish_charge((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self, slice $owner, int $publishKind, int $sizeClass, int $cryptoSuite) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    var $fresh$ret_230 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_canonicalPublishMaxCharge($owner, $publishKind, $sizeClass, $cryptoSuite);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_230);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int), (int, int, int, int, int, slice, slice, slice, slice, slice, int, int, int, int, int, int, int, int, int, int, int, int, int)) $Vault$_fun_get_global((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, int) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)) = $self;
    int $airdropRemaining = 0;
    if ($self'sealed) {
        $airdropRemaining = $self'genesis_config_hash;
    }
    var $fresh$ret_231 = $VaultGlobalView$_constructor_sealed_capsule_hub_bound_profile_registry_bound_username_registry_bound_deployment_manifest_hash_capsule_hub_address_profile_registry_address_username_registry_address_vault_ath_wallet_address_ath_master_address_user_count_key_record_count_receive_intent_count_pending_ath_withdrawal_count_pending_publish_count_pending_profile_avatar_payment_count_pending_username_mint_payment_count_processed_ath_deposit_count_pending_publish_stale_ttl_airdrop_remaining_ath_airdrop_distributed_ath_airdrop_reward_per_message_ath_airdrop_total_allocation_ath($self'sealed, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isCapsuleHubBound(), ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isProfileRegistryBound(), ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isUsernameRegistryBound(), $self'deployment_manifest_hash, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'vault_ath_wallet_address, $self'ath_master_address, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'pending_ath_withdrawal_count, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_pendingPublishCount(), ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_pendingProfileAvatarPaymentCount(), ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_pendingUsernameMintPaymentCount(), $self'processed_ath_deposit_count, 86400, $airdropRemaining, (15000000000000000 - $airdropRemaining), 10000000000, 15000000000000000);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count), $fresh$ret_231);
}

;;
;; Get methods of a Contract Vault
;;

_ %get_user(slice $owner) method_id(91785) {
    slice $owner = $owner;
    var self = $Vault$_contract_load();
    var res = self~$Vault$_fun_get_user($owner);
    return $VaultUserView$_to_external(res);
}

_ %get_key_record(int $keyId) method_id(104356) {
    int $keyId = $keyId;
    var self = $Vault$_contract_load();
    var res = self~$Vault$_fun_get_key_record($keyId);
    return $VaultKeyRecordView$_to_external(res);
}

_ %get_receive_intent(int $intentId) method_id(118324) {
    int $intentId = $intentId;
    var self = $Vault$_contract_load();
    var res = self~$Vault$_fun_get_receive_intent($intentId);
    return $VaultReceiveIntentView$_to_external(res);
}

_ %get_receive_intent_id(slice $senderWallet, slice $recipientWallet, int $asset, int $amount, int $clientNonce) method_id(72648) {
    slice $senderWallet = $senderWallet;
    slice $recipientWallet = $recipientWallet;
    int $asset = $asset;
    int $amount = $amount;
    int $clientNonce = $clientNonce;
    var self = $Vault$_contract_load();
    var res = self~$Vault$_fun_get_receive_intent_id($senderWallet, $recipientWallet, $asset, $amount, $clientNonce);
    return res;
}

_ %get_receive_intent_commitment(int $intentId, slice $recipientWallet, int $secret32) method_id(104854) {
    int $intentId = $intentId;
    slice $recipientWallet = $recipientWallet;
    int $secret32 = $secret32;
    var self = $Vault$_contract_load();
    var res = self~$Vault$_fun_get_receive_intent_commitment($intentId, $recipientWallet, $secret32);
    return res;
}

_ %get_pending_ath_withdrawal(int $queryId) method_id(125951) {
    int $queryId = $queryId;
    var self = $Vault$_contract_load();
    var res = self~$Vault$_fun_get_pending_ath_withdrawal($queryId);
    return $VaultPendingAthWithdrawalView$_to_external(res);
}

_ %get_ath_withdrawal_id(slice $ownerWallet, int $queryId) method_id(123302) {
    slice $ownerWallet = $ownerWallet;
    int $queryId = $queryId;
    var self = $Vault$_contract_load();
    var res = self~$Vault$_fun_get_ath_withdrawal_id($ownerWallet, $queryId);
    return res;
}

_ %get_pending_ath_withdrawal_for(slice $ownerWallet, int $queryId) method_id(104521) {
    slice $ownerWallet = $ownerWallet;
    int $queryId = $queryId;
    var self = $Vault$_contract_load();
    var res = self~$Vault$_fun_get_pending_ath_withdrawal_for($ownerWallet, $queryId);
    return $VaultPendingAthWithdrawalView$_to_external(res);
}

_ %get_canonical_publish_charge(slice $owner, int $publishKind, int $sizeClass, int $cryptoSuite) method_id(129116) {
    slice $owner = $owner;
    int $publishKind = $publishKind;
    int $sizeClass = $sizeClass;
    int $cryptoSuite = $cryptoSuite;
    var self = $Vault$_contract_load();
    var res = self~$Vault$_fun_get_canonical_publish_charge($owner, $publishKind, $sizeClass, $cryptoSuite);
    return res;
}

_ %get_global() method_id(126899) {
    var self = $Vault$_contract_load();
    var res = self~$Vault$_fun_get_global();
    return $VaultGlobalView$_to_external(res);
}

;;
;; Routing of a Contract Vault
;;

;; message opcode reader utility: only binary receivers
;; Returns 32 bit message opcode, otherwise throws the "Invalid incoming message" exit code
(slice, int) ~load_opcode_internal(slice s) asm( -> 1 0) "32 LDUQ 130 THROWIFNOT";

() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    
    ;; Context
    var cs = in_msg_cell.begin_parse();
    cs~skip_bits(2);
    var msg_bounceable = cs~load_int(1);
    var msg_bounced = cs~load_int(1);
    slice msg_sender_addr = cs~load_msg_addr();
    __tact_context = (msg_bounceable, msg_sender_addr, msg_value, cs);
    __tact_context_sender = msg_sender_addr;
    
    ;; Load contract data
    var ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count) = $Vault$_contract_load();
    
    ;; Handle bounced messages
    if (msg_bounced) {
        ;; Skip 0xFFFFFFFF prefix of a bounced message
        in_msg~skip_bits(32);
        int op = 0;
        if (slice_bits(in_msg) >= 32) {
            op = in_msg~load_uint(32);
        }
        ;; Bounced handler for ATHTransferRequest message
        if (op == 0x41544810) {
            var ($msg'query_id, $msg'amount) = in_msg~$ATHTransferRequest$_load_bounced();
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireSealed();
            throw_unless(16060, ( equal_slices_bits(__tact_context_get_sender(), $self'vault_ath_wallet_address) ));
            throw_unless(16061, ($msg'amount > 0));
            tuple $pendingOpt = $PendingAthWithdrawal$_load_opt(__tact_dict_get_int_cell($self'pending_ath_withdrawals, 257, $msg'query_id));
            throw_unless(16062, (~ null?($pendingOpt)));
            var ($pending'owner_wallet, $pending'recipient, $pending'recipient_ath_wallet, $pending'amount, $pending'refundable_ton_amount, $pending'created_at) = $PendingAthWithdrawal$_not_null($pendingOpt);
            throw_unless(16063, ($pending'amount == $msg'amount));
            var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_getUserOrEmpty($pending'owner_wallet);
            $user'ath_balance = $user'ath_balance + $pending'amount;
            $self'users~__tact_dict_set_slice_cell(267, $pending'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_refundPendingAthWithdrawalTon(($pending'owner_wallet, $pending'recipient, $pending'recipient_ath_wallet, $pending'amount, $pending'refundable_ton_amount, $pending'created_at), $Context$_get_value(__tact_context_get()));
            $self'pending_ath_withdrawals~__tact_dict_delete_int(257, $msg'query_id);
            $self'pending_ath_withdrawal_count = $self'pending_ath_withdrawal_count - 1;
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        
        ;; Bounced handler for ATHTransferRequestVaultProfileAvatar message
        if (op == 0x4154481a) {
            var ($msg'query_id, $msg'amount) = in_msg~$ATHTransferRequestVaultProfileAvatar$_load_bounced();
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireSealed();
            throw_unless(16635, ( equal_slices_bits(__tact_context_get_sender(), $self'vault_ath_wallet_address) ));
            throw_unless(16636, ($msg'amount > 0));
            tuple $pendingProfileOpt = $PendingProfileAvatarPayment$_load_opt(__tact_dict_get_int_cell($self'pending_profile_avatar_payments, 257, $msg'query_id));
            throw_unless(16637, (~ null?($pendingProfileOpt)));
            var ($pendingProfile'owner_wallet, $pendingProfile'amount, $pendingProfile'created_at) = $PendingProfileAvatarPayment$_not_null($pendingProfileOpt);
            throw_unless(16638, ($pendingProfile'amount == $msg'amount));
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_restorePendingProfileAvatarPayment($msg'query_id, ($pendingProfile'owner_wallet, $pendingProfile'amount, $pendingProfile'created_at));
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        
        ;; Bounced handler for ATHTransferRequestVaultMintUsername message
        if (op == 0x4154481c) {
            var ($msg'query_id, $msg'amount) = in_msg~$ATHTransferRequestVaultMintUsername$_load_bounced();
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireSealed();
            throw_unless(16675, ( equal_slices_bits(__tact_context_get_sender(), $self'vault_ath_wallet_address) ));
            throw_unless(16676, ($msg'amount > 0));
            tuple $pendingUsernameOpt = $PendingUsernameMintPayment$_load_opt(__tact_dict_get_int_cell($self'pending_username_mint_payments, 257, $msg'query_id));
            throw_unless(16677, (~ null?($pendingUsernameOpt)));
            var ($pendingUsername'owner_wallet, $pendingUsername'amount, $pendingUsername'created_at) = $PendingUsernameMintPayment$_not_null($pendingUsernameOpt);
            throw_unless(16678, ($pendingUsername'amount == $msg'amount));
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_restorePendingUsernameMintPayment($msg'query_id, ($pendingUsername'owner_wallet, $pendingUsername'amount, $pendingUsername'created_at));
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        
        ;; Bounced handler for PublishPrivateFromVault message
        if (op == 0xa4f862c0) {
            var ($msg'bounce_id, $msg'bounce_tag) = in_msg~$PublishPrivateFromVault$_load_bounced();
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireSealed();
            throw_unless(16510, ( equal_slices_bits(__tact_context_get_sender(), $self'capsule_hub_address) ));
            tuple $pendingOpt = $PendingPublish$_load_opt(__tact_dict_get_int_cell($self'pending_publishes, 257, $msg'bounce_id));
            throw_unless(16511, (~ null?($pendingOpt)));
            var ($pending'owner_wallet, $pending'tombstone, $pending'refund_to_vault, $pending'nonce, $pending'publish_kind, $pending'body_hash, $pending'refundable_amount, $pending'created_at) = $PendingPublish$_not_null($pendingOpt);
            int $expectedPublishId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computePublishId($pending'owner_wallet, $pending'nonce, $pending'body_hash, $pending'publish_kind);
            throw_unless(16514, (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computePublishBounceTag($expectedPublishId) == $msg'bounce_tag));
            if ($pending'tombstone) {
                throw_unless(16512, ($global_now() <= $pending'created_at));
                throw_unless(16513, ($pending'publish_kind == 1));
                ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_refundPendingPublish(($pending'owner_wallet, $pending'tombstone, $pending'refund_to_vault, $pending'nonce, $pending'publish_kind, $pending'body_hash, $pending'refundable_amount, $pending'created_at), $Context$_get_value(__tact_context_get()));
                $self'pending_publishes~__tact_dict_delete_int(257, $msg'bounce_id);
            } else {
                ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_refundPendingPublish(($pending'owner_wallet, $pending'tombstone, $pending'refund_to_vault, $pending'nonce, $pending'publish_kind, $pending'body_hash, $pending'refundable_amount, $pending'created_at), $Context$_get_value(__tact_context_get()));
                $self'pending_publishes~__tact_dict_delete_int(257, $msg'bounce_id);
                ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_decrementPendingPublishCount();
            }
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        
        ;; Bounced handler for PublishPublicFromVault message
        if (op == 0x8c2a76b7) {
            var ($msg'bounce_id, $msg'bounce_tag) = in_msg~$PublishPublicFromVault$_load_bounced();
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireSealed();
            throw_unless(16520, ( equal_slices_bits(__tact_context_get_sender(), $self'capsule_hub_address) ));
            tuple $pendingOpt = $PendingPublish$_load_opt(__tact_dict_get_int_cell($self'pending_publishes, 257, $msg'bounce_id));
            throw_unless(16521, (~ null?($pendingOpt)));
            var ($pending'owner_wallet, $pending'tombstone, $pending'refund_to_vault, $pending'nonce, $pending'publish_kind, $pending'body_hash, $pending'refundable_amount, $pending'created_at) = $PendingPublish$_not_null($pendingOpt);
            int $expectedPublishId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computePublishId($pending'owner_wallet, $pending'nonce, $pending'body_hash, $pending'publish_kind);
            throw_unless(16524, (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computePublishBounceTag($expectedPublishId) == $msg'bounce_tag));
            if ($pending'tombstone) {
                throw_unless(16522, ($global_now() <= $pending'created_at));
                throw_unless(16523, ($pending'publish_kind == 2));
                ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_refundPendingPublish(($pending'owner_wallet, $pending'tombstone, $pending'refund_to_vault, $pending'nonce, $pending'publish_kind, $pending'body_hash, $pending'refundable_amount, $pending'created_at), $Context$_get_value(__tact_context_get()));
                $self'pending_publishes~__tact_dict_delete_int(257, $msg'bounce_id);
            } else {
                ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_refundPendingPublish(($pending'owner_wallet, $pending'tombstone, $pending'refund_to_vault, $pending'nonce, $pending'publish_kind, $pending'body_hash, $pending'refundable_amount, $pending'created_at), $Context$_get_value(__tact_context_get()));
                $self'pending_publishes~__tact_dict_delete_int(257, $msg'bounce_id);
                ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_decrementPendingPublishCount();
            }
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        
        return ();
    }
    int op = 0;
    int in_msg_length = slice_bits(in_msg);
    if (in_msg_length >= 32) {
        op = in_msg~load_uint(32);
    }
    ;; Receive BindDeploymentManifest message
    if (op == 0x90e2e0cb) {
        var $msg'deployment_manifest_hash = in_msg~load_uint(256);
        var $msg'counterpart_address = in_msg~load_msg_addr();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireUnsealed();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireGenesisController();
        throw_unless(15010, (~ ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isCapsuleHubBound()));
        throw_unless(15011, ($msg'deployment_manifest_hash != 0));
        throw_unless(15012, ( (($self'deployment_manifest_hash == 0)) ? (true) : (($self'deployment_manifest_hash == $msg'deployment_manifest_hash)) ));
        throw_unless(15018, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isBasechainAddress($msg'counterpart_address));
        throw_unless(15019, ( ~ equal_slices_bits($msg'counterpart_address, $global_myAddress()) ));
        $self'deployment_manifest_hash = $msg'deployment_manifest_hash;
        $self'capsule_hub_address = $msg'counterpart_address;
        $self'binding_flags = $self'binding_flags + 1;
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
        return ();
    }
    
    ;; Receive BindOfficialAthWallet message
    if (op == 0x18db2ccb) {
        var $msg'deployment_manifest_hash = in_msg~load_uint(256);
        var $msg'official_ath_wallet_address = in_msg~load_msg_addr();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireUnsealed();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireGenesisController();
        throw_unless(15013, ( equal_slices_bits($self'vault_ath_wallet_address, __tact_context_get_sender()) ));
        throw_unless(15014, ($msg'deployment_manifest_hash > 1));
        throw_unless(15015, ( (($self'deployment_manifest_hash == 0)) ? (true) : (($self'deployment_manifest_hash == $msg'deployment_manifest_hash)) ));
        throw_unless(15016, ( ~ equal_slices_bits($msg'official_ath_wallet_address, __tact_context_get_sender()) ));
        throw_unless(15017, ( equal_slices_bits($msg'official_ath_wallet_address, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_deriveAthWalletAddress($global_myAddress())) ));
        $self'deployment_manifest_hash = $msg'deployment_manifest_hash;
        $self'vault_ath_wallet_address = $msg'official_ath_wallet_address;
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
        return ();
    }
    
    ;; Receive BindProfileRegistry message
    if (op == 0x50a61103) {
        var $msg'deployment_manifest_hash = in_msg~load_uint(256);
        var $msg'profile_registry_address = in_msg~load_msg_addr();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireUnsealed();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireGenesisController();
        throw_unless(15025, ($msg'deployment_manifest_hash > 1));
        throw_unless(15026, (~ ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isProfileRegistryBound()));
        throw_unless(15027, ($self'deployment_manifest_hash == $msg'deployment_manifest_hash));
        throw_unless(15028, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isBasechainAddress($msg'profile_registry_address));
        throw_unless(15029, ( ~ equal_slices_bits($msg'profile_registry_address, $global_myAddress()) ));
        $self'profile_registry_address = $msg'profile_registry_address;
        $self'binding_flags = $self'binding_flags + 2;
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
        return ();
    }
    
    ;; Receive BindUsernameRegistry message
    if (op == 0x50a61104) {
        var $msg'deployment_manifest_hash = in_msg~load_uint(256);
        var $msg'username_registry_address = in_msg~load_msg_addr();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireUnsealed();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireGenesisController();
        throw_unless(15032, ($msg'deployment_manifest_hash > 1));
        throw_unless(15033, (~ ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isUsernameRegistryBound()));
        throw_unless(15034, ($self'deployment_manifest_hash == $msg'deployment_manifest_hash));
        throw_unless(15035, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isBasechainAddress($msg'username_registry_address));
        throw_unless(15036, ( ~ equal_slices_bits($msg'username_registry_address, $global_myAddress()) ));
        $self'username_registry_address = $msg'username_registry_address;
        $self'binding_flags = $self'binding_flags + 4;
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
        return ();
    }
    
    ;; Receive SealGenesis message
    if (op == 0x3a12d1ad) {
        var $msg'deployment_manifest_hash = in_msg~load_uint(256);
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireUnsealed();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireGenesisController();
        throw_unless(15020, ($msg'deployment_manifest_hash > 1));
        throw_unless(15021, ($self'deployment_manifest_hash == $msg'deployment_manifest_hash));
        throw_unless(15022, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isCapsuleHubBound());
        throw_unless(15023, ( ~ equal_slices_bits($self'vault_ath_wallet_address, __tact_context_get_sender()) ));
        throw_unless(15024, ( equal_slices_bits($self'vault_ath_wallet_address, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_deriveAthWalletAddress($global_myAddress())) ));
        throw_unless(15037, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isProfileRegistryBound());
        throw_unless(15038, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isUsernameRegistryBound());
        throw_unless(15039, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isBasechainAddress($self'profile_registry_address));
        throw_unless(15040, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isBasechainAddress($self'username_registry_address));
        throw_unless(15041, ( ~ equal_slices_bits($self'profile_registry_address, $global_myAddress()) ));
        throw_unless(15042, ( ~ equal_slices_bits($self'username_registry_address, $global_myAddress()) ));
        $self'genesis_config_hash = 15000000000000000;
        $self'sealed = true;
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
        return ();
    }
    
    ;; Receive DepositTon message
    if (op == 0x2aafbd98) {
        var $msg'amount = in_msg~load_uint(128);
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireSealed();
        throw_unless(16010, ($msg'amount > 0));
        int $exists = __tact_dict_exists_slice($self'users, 267, __tact_context_get_sender());
        ifnot ($exists) {
            throw_unless(16011, ($Context$_get_value(__tact_context_get()) >= (($msg'amount + 10000000) + 2000000)));
            $self'user_count = $self'user_count + 1;
        } else {
            throw_unless(16012, ($Context$_get_value(__tact_context_get()) >= ($msg'amount + 2000000)));
        }
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_getUserOrEmpty(__tact_context_get_sender());
        $user'ton_balance = $user'ton_balance + $msg'amount;
        $self'users~__tact_dict_set_slice_cell(267, __tact_context_get_sender(), $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
        return ();
    }
    
    ;; Receive AthTransferNotification message
    if (op == 0x472d9d7d) {
        var $msg'query_id = in_msg~load_uint(64);
        var $msg'sender_key = in_msg~load_uint(160);
        var $msg'amount = in_msg~load_uint(128);
        var $msg'sender_wallet = in_msg~load_msg_addr();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireSealed();
        throw_unless(16013, ( equal_slices_bits(__tact_context_get_sender(), $self'vault_ath_wallet_address) ));
        throw_unless(16014, ($msg'amount > 0));
        throw_unless(16074, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isBasechainAddress($msg'sender_wallet));
        int $depositId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computeAthDepositId($msg'sender_wallet, $msg'query_id);
        if (__tact_dict_exists_int($self'processed_ath_deposits, 257, $depositId)) {
            if (($Context$_get_value(__tact_context_get()) >= 1000000)) {
                $global_message($MessageParameters$_constructor_to_value_bounce_mode_body(__tact_context_get_sender(), 1000000, false, 0, $AthTransferNotificationAck$_store_cell($AthTransferNotificationAck$_constructor_query_id_sender_key_amount($msg'query_id, $msg'sender_key, $msg'amount), begin_cell())));
            }
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        int $exists = __tact_dict_exists_slice($self'users, 267, $msg'sender_wallet);
        ifnot ($exists) {
            throw_unless(16015, ($Context$_get_value(__tact_context_get()) >= 13000000));
            $self'user_count = $self'user_count + 1;
        } else {
            throw_unless(16016, ($Context$_get_value(__tact_context_get()) >= 1000000));
        }
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_getUserOrEmpty($msg'sender_wallet);
        $user'ath_balance = $user'ath_balance + $msg'amount;
        $self'users~__tact_dict_set_slice_cell(267, $msg'sender_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
        $self'processed_ath_deposits~__tact_dict_set_int_int(257, $depositId, $msg'amount, 257);
        $self'processed_ath_deposit_count = $self'processed_ath_deposit_count + 1;
        $global_message($MessageParameters$_constructor_to_value_bounce_mode_body(__tact_context_get_sender(), 1000000, false, 0, $AthTransferNotificationAck$_store_cell($AthTransferNotificationAck$_constructor_query_id_sender_key_amount($msg'query_id, $msg'sender_key, $msg'amount), begin_cell())));
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
        return ();
    }
    
    ;; Receive WithdrawTon message
    if (op == 0x484c1d72) {
        var $msg'amount = in_msg~load_uint(128);
        var $msg'recipient = in_msg~load_msg_addr();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireSealed();
        throw_unless(16020, ($msg'amount > 0));
        throw_unless(16018, ( ~ equal_slices_bits($msg'recipient, $global_myAddress()) ));
        throw_unless(16019, ($Context$_get_value(__tact_context_get()) >= 2000000));
        tuple $userOpt = $UserState$_load_opt(__tact_dict_get_slice_cell($self'users, 267, __tact_context_get_sender()));
        throw_unless(16021, (~ null?($userOpt)));
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce) = $UserState$_not_null($userOpt);
        throw_unless(16022, ($user'ton_balance >= $msg'amount));
        $user'ton_balance = $user'ton_balance - $msg'amount;
        $self'users~__tact_dict_set_slice_cell(267, __tact_context_get_sender(), $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
        $global_message($MessageParameters$_constructor_to_value_bounce_mode_body($msg'recipient, $msg'amount, false, 1, null()));
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
        return ();
    }
    
    ;; Receive WithdrawAth message
    if (op == 0xf9a44834) {
        var $msg'query_id = in_msg~load_uint(64);
        var $msg'amount = in_msg~load_uint(128);
        var $msg'recipient = in_msg~load_msg_addr();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireSealed();
        throw_unless(16023, ($msg'amount > 0));
        throw_unless(16017, ( ~ equal_slices_bits($msg'recipient, $global_myAddress()) ));
        throw_unless(16075, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isBasechainAddress(__tact_context_get_sender()));
        throw_unless(16076, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isBasechainAddress($msg'recipient));
        throw_unless(16027, ($Context$_get_value(__tact_context_get()) >= 40000000));
        tuple $userOpt = $UserState$_load_opt(__tact_dict_get_slice_cell($self'users, 267, __tact_context_get_sender()));
        throw_unless(16024, (~ null?($userOpt)));
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce) = $UserState$_not_null($userOpt);
        throw_unless(16025, ($user'ath_balance >= $msg'amount));
        int $withdrawalId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computeAthWithdrawalId(__tact_context_get_sender(), $msg'query_id);
        throw_unless(16026, (~ __tact_dict_exists_int($self'pending_ath_withdrawals, 257, $withdrawalId)));
        $user'ath_balance = $user'ath_balance - $msg'amount;
        $self'users~__tact_dict_set_slice_cell(267, __tact_context_get_sender(), $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
        slice $recipientAthWallet = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_deriveAthWalletAddress($msg'recipient);
        $self'pending_ath_withdrawals~__tact_dict_set_int_cell(257, $withdrawalId, $PendingAthWithdrawal$_store_cell($PendingAthWithdrawal$_constructor_owner_wallet_recipient_recipient_ath_wallet_amount_refundable_ton_amount_created_at(__tact_context_get_sender(), $msg'recipient, $recipientAthWallet, $msg'amount, $Context$_get_value(__tact_context_get()), $global_now()), begin_cell()));
        $self'pending_ath_withdrawal_count = $self'pending_ath_withdrawal_count + 1;
        $global_message($MessageParameters$_constructor_to_value_bounce_mode_body($self'vault_ath_wallet_address, 0, true, 64, $ATHTransferRequest$_store_cell($ATHTransferRequest$_constructor_query_id_amount_recipient_response_destination($withdrawalId, $msg'amount, $msg'recipient, $global_myAddress()), begin_cell())));
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
        return ();
    }
    
    ;; Receive ATHTransferAck message
    if (op == 0x41544811) {
        var $msg'query_id = in_msg~load_uint(64);
        var $msg'amount = in_msg~load_uint(128);
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireSealed();
        tuple $pendingUsernameOpt = $PendingUsernameMintPayment$_load_opt(__tact_dict_get_int_cell($self'pending_username_mint_payments, 257, $msg'query_id));
        if ((~ null?($pendingUsernameOpt))) {
            var ($pendingUsername'owner_wallet, $pendingUsername'amount, $pendingUsername'created_at) = $PendingUsernameMintPayment$_not_null($pendingUsernameOpt);
            throw_unless(16660, ($pendingUsername'amount == $msg'amount));
            if (( equal_slices_bits(__tact_context_get_sender(), ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_usernameRegistryAthWalletAddress()) )) {
                $self'pending_username_mint_payments~__tact_dict_delete_int(257, $msg'query_id);
                ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_decrementPendingUsernameMintPaymentCount();
            } else {
                throw_unless(16661, ( equal_slices_bits(__tact_context_get_sender(), $self'vault_ath_wallet_address) ));
                ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_restorePendingUsernameMintPayment($msg'query_id, ($pendingUsername'owner_wallet, $pendingUsername'amount, $pendingUsername'created_at));
            }
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        tuple $pendingProfileOpt = $PendingProfileAvatarPayment$_load_opt(__tact_dict_get_int_cell($self'pending_profile_avatar_payments, 257, $msg'query_id));
        if ((~ null?($pendingProfileOpt))) {
            var ($pendingProfile'owner_wallet, $pendingProfile'amount, $pendingProfile'created_at) = $PendingProfileAvatarPayment$_not_null($pendingProfileOpt);
            throw_unless(16620, ($pendingProfile'amount == $msg'amount));
            if (( equal_slices_bits(__tact_context_get_sender(), ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_profileRegistryAthWalletAddress()) )) {
                $self'pending_profile_avatar_payments~__tact_dict_delete_int(257, $msg'query_id);
                ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_decrementPendingProfileAvatarPaymentCount();
            } else {
                throw_unless(16621, ( equal_slices_bits(__tact_context_get_sender(), $self'vault_ath_wallet_address) ));
                ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_restorePendingProfileAvatarPayment($msg'query_id, ($pendingProfile'owner_wallet, $pendingProfile'amount, $pendingProfile'created_at));
            }
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        if (( equal_slices_bits(__tact_context_get_sender(), $self'vault_ath_wallet_address) )) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        tuple $pendingOpt = $PendingAthWithdrawal$_load_opt(__tact_dict_get_int_cell($self'pending_ath_withdrawals, 257, $msg'query_id));
        throw_unless(16028, (~ null?($pendingOpt)));
        var ($pending'owner_wallet, $pending'recipient, $pending'recipient_ath_wallet, $pending'amount, $pending'refundable_ton_amount, $pending'created_at) = $PendingAthWithdrawal$_not_null($pendingOpt);
        throw_unless(16029, ($pending'amount == $msg'amount));
        throw_unless(16030, ( equal_slices_bits(__tact_context_get_sender(), $pending'recipient_ath_wallet) ));
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_refundPendingAthWithdrawalTon(($pending'owner_wallet, $pending'recipient, $pending'recipient_ath_wallet, $pending'amount, $pending'refundable_ton_amount, $pending'created_at), $Context$_get_value(__tact_context_get()));
        $self'pending_ath_withdrawals~__tact_dict_delete_int(257, $msg'query_id);
        $self'pending_ath_withdrawal_count = $self'pending_ath_withdrawal_count - 1;
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
        return ();
    }
    
    ;; Receive ATHTransferFailed message
    if (op == 0x41544813) {
        var $msg'query_id = in_msg~load_uint(64);
        var $msg'amount = in_msg~load_uint(128);
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireSealed();
        tuple $pendingUsernameOpt = $PendingUsernameMintPayment$_load_opt(__tact_dict_get_int_cell($self'pending_username_mint_payments, 257, $msg'query_id));
        if ((~ null?($pendingUsernameOpt))) {
            var ($pendingUsername'owner_wallet, $pendingUsername'amount, $pendingUsername'created_at) = $PendingUsernameMintPayment$_not_null($pendingUsernameOpt);
            throw_unless(16670, ($pendingUsername'amount == $msg'amount));
            throw_unless(16671, ( equal_slices_bits(__tact_context_get_sender(), $self'vault_ath_wallet_address) ));
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_restorePendingUsernameMintPayment($msg'query_id, ($pendingUsername'owner_wallet, $pendingUsername'amount, $pendingUsername'created_at));
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        tuple $pendingProfileOpt = $PendingProfileAvatarPayment$_load_opt(__tact_dict_get_int_cell($self'pending_profile_avatar_payments, 257, $msg'query_id));
        if ((~ null?($pendingProfileOpt))) {
            var ($pendingProfile'owner_wallet, $pendingProfile'amount, $pendingProfile'created_at) = $PendingProfileAvatarPayment$_not_null($pendingProfileOpt);
            throw_unless(16630, ($pendingProfile'amount == $msg'amount));
            throw_unless(16631, ( equal_slices_bits(__tact_context_get_sender(), $self'vault_ath_wallet_address) ));
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_restorePendingProfileAvatarPayment($msg'query_id, ($pendingProfile'owner_wallet, $pendingProfile'amount, $pendingProfile'created_at));
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        throw_unless(16070, ( equal_slices_bits(__tact_context_get_sender(), $self'vault_ath_wallet_address) ));
        throw_unless(16071, ($msg'amount > 0));
        tuple $pendingOpt = $PendingAthWithdrawal$_load_opt(__tact_dict_get_int_cell($self'pending_ath_withdrawals, 257, $msg'query_id));
        throw_unless(16072, (~ null?($pendingOpt)));
        var ($pending'owner_wallet, $pending'recipient, $pending'recipient_ath_wallet, $pending'amount, $pending'refundable_ton_amount, $pending'created_at) = $PendingAthWithdrawal$_not_null($pendingOpt);
        throw_unless(16073, ($pending'amount == $msg'amount));
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_getUserOrEmpty($pending'owner_wallet);
        $user'ath_balance = $user'ath_balance + $pending'amount;
        $self'users~__tact_dict_set_slice_cell(267, $pending'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_refundPendingAthWithdrawalTon(($pending'owner_wallet, $pending'recipient, $pending'recipient_ath_wallet, $pending'amount, $pending'refundable_ton_amount, $pending'created_at), $Context$_get_value(__tact_context_get()));
        $self'pending_ath_withdrawals~__tact_dict_delete_int(257, $msg'query_id);
        $self'pending_ath_withdrawal_count = $self'pending_ath_withdrawal_count - 1;
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
        return ();
    }
    
    ;; Receive ProfileAvatarTonExcessRefund message
    if (op == 0x50a61121) {
        var $msg'query_id = in_msg~load_uint(64);
        var $msg'owner_wallet = in_msg~load_msg_addr();
        var $msg'amount = in_msg~load_uint(128);
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireSealed();
        throw_unless(16680, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isProfileRegistryBound());
        throw_unless(16681, ( equal_slices_bits(__tact_context_get_sender(), $self'profile_registry_address) ));
        throw_unless(16682, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isBasechainAddress($msg'owner_wallet));
        throw_unless(16683, ($msg'amount > 0));
        tuple $userOpt = $UserState$_load_opt(__tact_dict_get_slice_cell($self'users, 267, $msg'owner_wallet));
        throw_unless(16684, (~ null?($userOpt)));
        if (($Context$_get_value(__tact_context_get()) <= 2000000)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        int $creditAmount = ($Context$_get_value(__tact_context_get()) - 2000000);
        if (($creditAmount > $msg'amount)) {
            $creditAmount = $msg'amount;
        }
        if (($creditAmount <= 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce) = $UserState$_not_null($userOpt);
        $user'ton_balance = $user'ton_balance + $creditAmount;
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
        return ();
    }
    
    ;; Receive TopUpStorageReserve message
    if (op == 0x3215b5fd) {
        var $msg = empty_tuple();
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
        return ();
    }
    
    ;; Receive RegisterMessagingKeys message
    if (op == 0x52705eda) {
        var $msg'enc_pubkey = in_msg~load_uint(256);
        var $msg'sign_pubkey = in_msg~load_uint(256);
        var $msg'auth_pubkey = in_msg~load_uint(256);
        slice sc_1 = in_msg~load_ref().begin_parse();
        var $msg'pq_kem_pubkey_hash = sc_1~load_uint(256);
        var $msg'pq_kem_pubkey_len = sc_1~load_uint(16);
        var $msg'pq_kem_pubkey = sc_1~load_ref();
        var $msg'crypto_suite_mask = sc_1~load_uint(16);
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireSealed();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_validateKeyProfile($msg'enc_pubkey, $msg'sign_pubkey, $msg'pq_kem_pubkey_hash, $msg'pq_kem_pubkey_len, $msg'pq_kem_pubkey, $msg'crypto_suite_mask);
        throw_unless(16118, ($msg'auth_pubkey != 0));
        throw_unless(16119, ($msg'auth_pubkey != $msg'sign_pubkey));
        int $userExists = __tact_dict_exists_slice($self'users, 267, __tact_context_get_sender());
        int $required = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_keyRecordStorageEndowment($msg'crypto_suite_mask);
        ifnot ($userExists) {
            $required = $required + 10000000;
        }
        $required = $required + 2000000;
        throw_unless(16110, ($Context$_get_value(__tact_context_get()) >= $required));
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_getUserOrEmpty(__tact_context_get_sender());
        throw_unless(16111, ($user'current_key_id == 0));
        int $keyGeneration = 0;
        int $keyId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computeKeyId(__tact_context_get_sender(), $keyGeneration, $msg'enc_pubkey, $msg'sign_pubkey, $msg'pq_kem_pubkey_hash, $msg'pq_kem_pubkey_len, $msg'crypto_suite_mask);
        throw_unless(16112, (~ __tact_dict_exists_int($self'key_records, 257, $keyId)));
        ifnot ($userExists) {
            $self'user_count = $self'user_count + 1;
        }
        $self'key_records~__tact_dict_set_int_cell(257, $keyId, $KeyRecord$_store_cell($KeyRecord$_constructor_owner_wallet_key_generation_enc_pubkey_sign_pubkey_pq_kem_pubkey_hash_pq_kem_pubkey_len_pq_kem_pubkey_crypto_suite_mask_created_at_created_lt_revoked_at_revoked_lt(__tact_context_get_sender(), $keyGeneration, $msg'enc_pubkey, $msg'sign_pubkey, $msg'pq_kem_pubkey_hash, $msg'pq_kem_pubkey_len, $msg'pq_kem_pubkey, $msg'crypto_suite_mask, $global_now(), $global_curLt(), 0, 0), begin_cell()));
        $self'key_record_count = $self'key_record_count + 1;
        $user'current_key_id = $keyId;
        $user'auth_pubkey = $msg'auth_pubkey;
        $self'users~__tact_dict_set_slice_cell(267, __tact_context_get_sender(), $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
        return ();
    }
    
    ;; Receive CapsuleHubPublishAck message
    if (op == 0x874e576a) {
        var $msg'publish_id = in_msg~load_uint(256);
        var $msg'entry_id = in_msg~load_uint(64);
        var $msg'entry_uid = in_msg~load_uint(256);
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireSealed();
        throw_unless(16500, ( equal_slices_bits(__tact_context_get_sender(), $self'capsule_hub_address) ));
        int $bounceId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computePublishBounceId($msg'publish_id);
        tuple $pendingOpt = $PendingPublish$_load_opt(__tact_dict_get_int_cell($self'pending_publishes, 257, $bounceId));
        throw_unless(16501, (~ null?($pendingOpt)));
        var ($pending'owner_wallet, $pending'tombstone, $pending'refund_to_vault, $pending'nonce, $pending'publish_kind, $pending'body_hash, $pending'refundable_amount, $pending'created_at) = $PendingPublish$_not_null($pendingOpt);
        int $expectedPublishId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computePublishId($pending'owner_wallet, $pending'nonce, $pending'body_hash, $pending'publish_kind);
        throw_unless(16502, ($expectedPublishId == $msg'publish_id));
        if ($pending'tombstone) {
            throw_unless(16503, ($global_now() <= $pending'created_at));
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_refundPendingPublish(($pending'owner_wallet, $pending'tombstone, $pending'refund_to_vault, $pending'nonce, $pending'publish_kind, $pending'body_hash, $pending'refundable_amount, $pending'created_at), $Context$_get_value(__tact_context_get()));
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_creditActivityAirdrop($pending'owner_wallet);
            $self'pending_publishes~__tact_dict_delete_int(257, $bounceId);
        } else {
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_refundPendingPublish(($pending'owner_wallet, $pending'tombstone, $pending'refund_to_vault, $pending'nonce, $pending'publish_kind, $pending'body_hash, $pending'refundable_amount, $pending'created_at), $Context$_get_value(__tact_context_get()));
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_creditActivityAirdrop($pending'owner_wallet);
            $self'pending_publishes~__tact_dict_delete_int(257, $bounceId);
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_decrementPendingPublishCount();
        }
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
        return ();
    }
    
    ;; Receive PrunePendingPublish message
    if (op == 0x720bdd6d) {
        var $msg'publish_id = in_msg~load_uint(256);
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireSealed();
        throw_unless(16533, ($Context$_get_value(__tact_context_get()) >= 2000000));
        int $bounceId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computePublishBounceId($msg'publish_id);
        tuple $pendingOpt = $PendingPublish$_load_opt(__tact_dict_get_int_cell($self'pending_publishes, 257, $bounceId));
        throw_unless(16530, (~ null?($pendingOpt)));
        var ($pending'owner_wallet, $pending'tombstone, $pending'refund_to_vault, $pending'nonce, $pending'publish_kind, $pending'body_hash, $pending'refundable_amount, $pending'created_at) = $PendingPublish$_not_null($pendingOpt);
        int $expectedPublishId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computePublishId($pending'owner_wallet, $pending'nonce, $pending'body_hash, $pending'publish_kind);
        throw_unless(16531, ($expectedPublishId == $msg'publish_id));
        if ($pending'tombstone) {
            throw_unless(16534, ($global_now() >= $pending'created_at));
            $self'pending_publishes~__tact_dict_delete_int(257, $bounceId);
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        throw_unless(16532, ($global_now() >= ($pending'created_at + 86400)));
        int $tombstoneExpiresAt = ($global_now() + 86400);
        if (($tombstoneExpiresAt > 4294967295)) {
            $tombstoneExpiresAt = 4294967295;
        }
        $self'pending_publishes~__tact_dict_set_int_cell(257, $bounceId, $PendingPublish$_store_cell($PendingPublish$_constructor_owner_wallet_tombstone_refund_to_vault_nonce_publish_kind_body_hash_refundable_amount_created_at($pending'owner_wallet, true, $pending'refund_to_vault, $pending'nonce, $pending'publish_kind, $pending'body_hash, $pending'refundable_amount, $tombstoneExpiresAt), begin_cell()));
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_decrementPendingPublishCount();
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
        return ();
    }
    
    ;; Receive empty message
    if ((op == 0) & (in_msg_length <= 32)) {
        throw(16099);
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
        return ();
    }
    ;; Throw if not handled
    throw(130);
}

;; message opcode reader utility: only binary receivers
;; Returns 32 bit message opcode, otherwise throws the "Invalid incoming message" exit code
(slice, int) ~load_opcode_external(slice s) asm( -> 1 0) "32 LDUQ 130 THROWIFNOT";

() recv_external(slice in_msg) impure {
    ;; Load contract data
    var ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count) = $Vault$_contract_load();
    
    var op = in_msg~load_opcode_external();
    ;; Receive ReplaceMessagingKeys message
    if (op == 0x89d648bb) {
        var $msg'owner_wallet = in_msg~load_msg_addr();
        var $msg'signature = in_msg~load_bits(512);
        var $msg'signed_payload = in_msg~load_ref();
        tuple $userOpt = $UserState$_load_opt(__tact_dict_get_slice_cell($self'users, 267, $msg'owner_wallet));
        throw_unless(16120, (~ null?($userOpt)));
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce) = $UserState$_not_null($userOpt);
        throw_unless(16121, ($user'current_key_id != 0));
        throw_unless(16128, ($user'auth_pubkey != 0));
        throw_unless(16130, $global_checkSignature($Cell$_fun_hash($msg'signed_payload), $msg'signature, $user'auth_pubkey));
        slice $signedPayload = $Cell$_fun_beginParse($msg'signed_payload);
        int $signedDomain = $signedPayload~load_uint(32);
        throw_unless(16131, ($signedDomain == 1448233777));
        int $signedManifestHash = $signedPayload~load_uint(256);
        throw_unless(16132, ($signedManifestHash == $self'deployment_manifest_hash));
        int $signedVaultHash = $signedPayload~load_uint(256);
        slice $vaultAddressSlice = $Address$_fun_asSlice($global_myAddress());
        throw_unless(16133, ($vaultAddressSlice~load_uint(11) == 1024));
        throw_unless(16133, ($signedVaultHash == $vaultAddressSlice~load_uint(256)));
        int $signedOwnerHash = $signedPayload~load_uint(256);
        slice $ownerPrefix = $Address$_fun_asSlice($msg'owner_wallet);
        throw_unless(16134, ($ownerPrefix~load_uint(11) == 1024));
        throw_unless(16134, ($signedOwnerHash == $ownerPrefix~load_uint(256)));
        int $clientNonce = $signedPayload~load_uint(64);
        throw_unless(16135, ($clientNonce == $user'publish_nonce));
        throw_unless(16136, ($Slice$_fun_bits($signedPayload) == 0));
        throw_unless(16137, ($Slice$_fun_refs($signedPayload) == 1));
        int $required = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_keyRecordStorageEndowment(2);
        $required = $required + 2000000;
        throw_unless(16126, ($user'ton_balance >= $required));
        $global_acceptMessage();
        slice $actionPayload = $Cell$_fun_beginParse($signedPayload~$Slice$_fun_loadRef());
        int $encPubkey = $actionPayload~load_uint(256);
        int $signPubkey = $actionPayload~load_uint(256);
        int $pqKemPubkeyHash = $actionPayload~load_uint(256);
        int $pqKemPubkeyLen = $actionPayload~load_uint(16);
        cell $pqKemPubkey = $actionPayload~$Slice$_fun_loadRef();
        int $cryptoSuiteMask = $actionPayload~load_uint(16);
        throw_unless(16138, ($Slice$_fun_bits($actionPayload) == 0));
        throw_unless(16139, ($Slice$_fun_refs($actionPayload) == 0));
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_validateKeyProfile($encPubkey, $signPubkey, $pqKemPubkeyHash, $pqKemPubkeyLen, $pqKemPubkey, $cryptoSuiteMask);
        throw_unless(16129, ($signPubkey != $user'auth_pubkey));
        tuple $oldRecordOpt = $KeyRecord$_load_opt(__tact_dict_get_int_cell($self'key_records, 257, $user'current_key_id));
        throw_unless(16122, (~ null?($oldRecordOpt)));
        var ($oldRecord'owner_wallet, $oldRecord'key_generation, $oldRecord'enc_pubkey, $oldRecord'sign_pubkey, $oldRecord'pq_kem_pubkey_hash, $oldRecord'pq_kem_pubkey_len, $oldRecord'pq_kem_pubkey, $oldRecord'crypto_suite_mask, $oldRecord'created_at, $oldRecord'created_lt, $oldRecord'revoked_at, $oldRecord'revoked_lt) = $KeyRecord$_not_null($oldRecordOpt);
        throw_unless(16123, ( equal_slices_bits($oldRecord'owner_wallet, $msg'owner_wallet) ));
        throw_unless(16124, ($oldRecord'revoked_lt == 0));
        throw_unless(16125, ($oldRecord'key_generation < 4294967295));
        $oldRecord'revoked_at = $global_now();
        $oldRecord'revoked_lt = $global_curLt();
        $self'key_records~__tact_dict_set_int_cell(257, $user'current_key_id, $KeyRecord$_store_cell(($oldRecord'owner_wallet, $oldRecord'key_generation, $oldRecord'enc_pubkey, $oldRecord'sign_pubkey, $oldRecord'pq_kem_pubkey_hash, $oldRecord'pq_kem_pubkey_len, $oldRecord'pq_kem_pubkey, $oldRecord'crypto_suite_mask, $oldRecord'created_at, $oldRecord'created_lt, $oldRecord'revoked_at, $oldRecord'revoked_lt), begin_cell()));
        int $keyGeneration = ($oldRecord'key_generation + 1);
        int $newKeyId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computeKeyId($msg'owner_wallet, $keyGeneration, $encPubkey, $signPubkey, $pqKemPubkeyHash, $pqKemPubkeyLen, $cryptoSuiteMask);
        throw_unless(16127, (~ __tact_dict_exists_int($self'key_records, 257, $newKeyId)));
        $self'key_records~__tact_dict_set_int_cell(257, $newKeyId, $KeyRecord$_store_cell($KeyRecord$_constructor_owner_wallet_key_generation_enc_pubkey_sign_pubkey_pq_kem_pubkey_hash_pq_kem_pubkey_len_pq_kem_pubkey_crypto_suite_mask_created_at_created_lt_revoked_at_revoked_lt($msg'owner_wallet, $keyGeneration, $encPubkey, $signPubkey, $pqKemPubkeyHash, $pqKemPubkeyLen, $pqKemPubkey, $cryptoSuiteMask, $global_now(), $global_curLt(), 0, 0), begin_cell()));
        $self'key_record_count = $self'key_record_count + 1;
        $user'current_key_id = $newKeyId;
        $user'ton_balance = $user'ton_balance - $required;
        $user'publish_nonce = $user'publish_nonce + 1;
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
        return ();
    }
    
    ;; Receive CreateReceiveIntent message
    if (op == 0x7e1f5035) {
        var $msg'owner_wallet = in_msg~load_msg_addr();
        var $msg'signature = in_msg~load_bits(512);
        var $msg'signed_payload = in_msg~load_ref();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireSealed();
        tuple $userOpt = $UserState$_load_opt(__tact_dict_get_slice_cell($self'users, 267, $msg'owner_wallet));
        throw_unless(16214, (~ null?($userOpt)));
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce) = $UserState$_not_null($userOpt);
        throw_unless(16221, ($user'auth_pubkey != 0));
        throw_unless(16222, $global_checkSignature($Cell$_fun_hash($msg'signed_payload), $msg'signature, $user'auth_pubkey));
        slice $signedPayload = $Cell$_fun_beginParse($msg'signed_payload);
        int $signedDomain = $signedPayload~load_uint(32);
        throw_unless(16223, ($signedDomain == 1448231729));
        int $signedManifestHash = $signedPayload~load_uint(256);
        throw_unless(16224, ($signedManifestHash == $self'deployment_manifest_hash));
        int $signedVaultHash = $signedPayload~load_uint(256);
        slice $vaultAddressSlice = $Address$_fun_asSlice($global_myAddress());
        throw_unless(16225, ($vaultAddressSlice~load_uint(11) == 1024));
        throw_unless(16225, ($signedVaultHash == $vaultAddressSlice~load_uint(256)));
        int $signedAction = $signedPayload~load_uint(8);
        throw_unless(16226, ($signedAction == 1));
        int $signedOwnerHash = $signedPayload~load_uint(256);
        slice $ownerPrefix = $Address$_fun_asSlice($msg'owner_wallet);
        throw_unless(16227, ($ownerPrefix~load_uint(11) == 1024));
        throw_unless(16227, ($signedOwnerHash == $ownerPrefix~load_uint(256)));
        int $clientNonce = $signedPayload~load_uint(64);
        throw_unless(16233, ($clientNonce == $user'publish_nonce));
        $global_acceptMessage();
        $user'publish_nonce = $user'publish_nonce + 1;
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
        $global_commit();
        ifnot (($Slice$_fun_bits($signedPayload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($Slice$_fun_refs($signedPayload) == 1)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        slice $actionPayload = $Cell$_fun_beginParse($signedPayload~$Slice$_fun_loadRef());
        int $asset = $actionPayload~load_uint(8);
        int $amount = $actionPayload~load_uint(128);
        slice $recipientWallet = $actionPayload~$Slice$_fun_loadAddress();
        int $commitment = $actionPayload~load_uint(256);
        ifnot (($Slice$_fun_bits($actionPayload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($Slice$_fun_refs($actionPayload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (( (($asset == 1)) ? (true) : (($asset == 2)) )) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($amount > 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($commitment)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        int $tonReserve = 9000000;
        if (($asset == 1)) {
            ifnot (($user'ton_balance >= ($amount + $tonReserve))) {
                $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
                return ();
            }
            $user'ton_balance = $user'ton_balance - ($amount + $tonReserve);
        } else {
            ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isBasechainAddress($msg'owner_wallet)) {
                $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
                return ();
            }
            ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isBasechainAddress($recipientWallet)) {
                $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
                return ();
            }
            ifnot (($user'ath_balance >= $amount)) {
                $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
                return ();
            }
            ifnot (($user'ton_balance >= $tonReserve)) {
                $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
                return ();
            }
            $user'ath_balance = $user'ath_balance - $amount;
            $user'ton_balance = $user'ton_balance - $tonReserve;
        }
        int $intentId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computeReceiveIntentId($msg'owner_wallet, $recipientWallet, $asset, $amount, $clientNonce);
        if (__tact_dict_exists_int($self'receive_intents, 257, $intentId)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
        $self'receive_intents~__tact_dict_set_int_cell(257, $intentId, $ReceiveIntent$_store_cell($ReceiveIntent$_constructor_sender_wallet_recipient_wallet_asset_amount_commitment_client_nonce_settlement_reserve_ton_created_at_claimed($msg'owner_wallet, $recipientWallet, $asset, $amount, $commitment, $clientNonce, 2000000, $global_now(), false), begin_cell()));
        $self'receive_intent_count = $self'receive_intent_count + 1;
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
        return ();
    }
    
    ;; Receive ClaimReceiveIntent message
    if (op == 0x7e1f5036) {
        var $msg'owner_wallet = in_msg~load_msg_addr();
        var $msg'signature = in_msg~load_bits(512);
        var $msg'signed_payload = in_msg~load_ref();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireSealed();
        tuple $userOpt = $UserState$_load_opt(__tact_dict_get_slice_cell($self'users, 267, $msg'owner_wallet));
        throw_unless(16239, (~ null?($userOpt)));
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce) = $UserState$_not_null($userOpt);
        throw_unless(16240, ($user'auth_pubkey != 0));
        throw_unless(16241, $global_checkSignature($Cell$_fun_hash($msg'signed_payload), $msg'signature, $user'auth_pubkey));
        slice $signedPayload = $Cell$_fun_beginParse($msg'signed_payload);
        int $signedDomain = $signedPayload~load_uint(32);
        throw_unless(16242, ($signedDomain == 1448231729));
        int $signedManifestHash = $signedPayload~load_uint(256);
        throw_unless(16243, ($signedManifestHash == $self'deployment_manifest_hash));
        int $signedVaultHash = $signedPayload~load_uint(256);
        slice $vaultAddressSlice = $Address$_fun_asSlice($global_myAddress());
        throw_unless(16244, ($vaultAddressSlice~load_uint(11) == 1024));
        throw_unless(16244, ($signedVaultHash == $vaultAddressSlice~load_uint(256)));
        int $signedAction = $signedPayload~load_uint(8);
        throw_unless(16245, ($signedAction == 2));
        int $signedOwnerHash = $signedPayload~load_uint(256);
        slice $ownerPrefix = $Address$_fun_asSlice($msg'owner_wallet);
        throw_unless(16246, ($ownerPrefix~load_uint(11) == 1024));
        throw_unless(16246, ($signedOwnerHash == $ownerPrefix~load_uint(256)));
        int $clientNonce = $signedPayload~load_uint(64);
        throw_unless(16249, ($clientNonce == $user'publish_nonce));
        $global_acceptMessage();
        $user'publish_nonce = $user'publish_nonce + 1;
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
        $global_commit();
        ifnot (($Slice$_fun_bits($signedPayload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($Slice$_fun_refs($signedPayload) == 1)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        slice $actionPayload = $Cell$_fun_beginParse($signedPayload~$Slice$_fun_loadRef());
        int $intentId = $actionPayload~load_uint(256);
        int $secret32 = $actionPayload~load_uint(256);
        ifnot (($Slice$_fun_bits($actionPayload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($Slice$_fun_refs($actionPayload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        tuple $intentOpt = $ReceiveIntent$_load_opt(__tact_dict_get_int_cell($self'receive_intents, 257, $intentId));
        if (null?($intentOpt)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        var ($intent'sender_wallet, $intent'recipient_wallet, $intent'asset, $intent'amount, $intent'commitment, $intent'client_nonce, $intent'settlement_reserve_ton, $intent'created_at, $intent'claimed) = $ReceiveIntent$_not_null($intentOpt);
        if ($intent'claimed) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (( equal_slices_bits($msg'owner_wallet, $intent'recipient_wallet) )) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        if (($intent'asset == 2)) {
            ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isBasechainAddress($msg'owner_wallet)) {
                $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
                return ();
            }
        }
        int $expected = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computeReceiveIntentCommitment($intentId, $msg'owner_wallet, $secret32);
        ifnot (($expected == $intent'commitment)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        if (($intent'asset == 1)) {
            $user'ton_balance = $user'ton_balance + $intent'amount;
        } else {
            $user'ath_balance = $user'ath_balance + $intent'amount;
        }
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
        $self'receive_intents~__tact_dict_delete_int(257, $intentId);
        $self'receive_intent_count = $self'receive_intent_count - 1;
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
        return ();
    }
    
    ;; Receive CancelReceiveIntent message
    if (op == 0x7e1f5037) {
        var $msg'owner_wallet = in_msg~load_msg_addr();
        var $msg'signature = in_msg~load_bits(512);
        var $msg'signed_payload = in_msg~load_ref();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireSealed();
        tuple $userOpt = $UserState$_load_opt(__tact_dict_get_slice_cell($self'users, 267, $msg'owner_wallet));
        throw_unless(16252, (~ null?($userOpt)));
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce) = $UserState$_not_null($userOpt);
        throw_unless(16253, ($user'auth_pubkey != 0));
        throw_unless(16254, $global_checkSignature($Cell$_fun_hash($msg'signed_payload), $msg'signature, $user'auth_pubkey));
        slice $signedPayload = $Cell$_fun_beginParse($msg'signed_payload);
        int $signedDomain = $signedPayload~load_uint(32);
        throw_unless(16255, ($signedDomain == 1448231729));
        int $signedManifestHash = $signedPayload~load_uint(256);
        throw_unless(16256, ($signedManifestHash == $self'deployment_manifest_hash));
        int $signedVaultHash = $signedPayload~load_uint(256);
        slice $vaultAddressSlice = $Address$_fun_asSlice($global_myAddress());
        throw_unless(16257, ($vaultAddressSlice~load_uint(11) == 1024));
        throw_unless(16257, ($signedVaultHash == $vaultAddressSlice~load_uint(256)));
        int $signedAction = $signedPayload~load_uint(8);
        throw_unless(16258, ($signedAction == 3));
        int $signedOwnerHash = $signedPayload~load_uint(256);
        slice $ownerPrefix = $Address$_fun_asSlice($msg'owner_wallet);
        throw_unless(16259, ($ownerPrefix~load_uint(11) == 1024));
        throw_unless(16259, ($signedOwnerHash == $ownerPrefix~load_uint(256)));
        int $clientNonce = $signedPayload~load_uint(64);
        throw_unless(16262, ($clientNonce == $user'publish_nonce));
        $global_acceptMessage();
        $user'publish_nonce = $user'publish_nonce + 1;
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
        $global_commit();
        ifnot (($Slice$_fun_bits($signedPayload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($Slice$_fun_refs($signedPayload) == 1)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        slice $actionPayload = $Cell$_fun_beginParse($signedPayload~$Slice$_fun_loadRef());
        int $intentId = $actionPayload~load_uint(256);
        ifnot (($Slice$_fun_bits($actionPayload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($Slice$_fun_refs($actionPayload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        tuple $intentOpt = $ReceiveIntent$_load_opt(__tact_dict_get_int_cell($self'receive_intents, 257, $intentId));
        if (null?($intentOpt)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        var ($intent'sender_wallet, $intent'recipient_wallet, $intent'asset, $intent'amount, $intent'commitment, $intent'client_nonce, $intent'settlement_reserve_ton, $intent'created_at, $intent'claimed) = $ReceiveIntent$_not_null($intentOpt);
        ifnot (( equal_slices_bits($msg'owner_wallet, $intent'sender_wallet) )) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        if (($intent'asset == 1)) {
            $user'ton_balance = $user'ton_balance + $intent'amount;
        } else {
            $user'ath_balance = $user'ath_balance + $intent'amount;
        }
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
        $self'receive_intents~__tact_dict_delete_int(257, $intentId);
        $self'receive_intent_count = $self'receive_intent_count - 1;
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
        return ();
    }
    
    ;; Receive PublishPrivateFromVaultBalance message
    if (op == 0x7e1f5031) {
        var $msg'owner_wallet = in_msg~load_msg_addr();
        var $msg'signature = in_msg~load_bits(512);
        var $msg'signed_payload = in_msg~load_ref();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireSealed();
        tuple $userOpt = $UserState$_load_opt(__tact_dict_get_slice_cell($self'users, 267, $msg'owner_wallet));
        throw_unless(16451, (~ null?($userOpt)));
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce) = $UserState$_not_null($userOpt);
        throw_unless(16454, ($user'auth_pubkey != 0));
        throw_unless(16457, $global_checkSignature($Cell$_fun_hash($msg'signed_payload), $msg'signature, $user'auth_pubkey));
        slice $signedPayload = $Cell$_fun_beginParse($msg'signed_payload);
        int $signedDomain = $signedPayload~load_uint(32);
        throw_unless(16464, ($signedDomain == 1448100401));
        int $signedManifestHash = $signedPayload~load_uint(256);
        throw_unless(16465, ($signedManifestHash == $self'deployment_manifest_hash));
        int $signedVaultHash = $signedPayload~load_uint(256);
        slice $vaultAddressSlice = $Address$_fun_asSlice($global_myAddress());
        throw_unless(16466, ($vaultAddressSlice~load_uint(11) == 1024));
        throw_unless(16466, ($signedVaultHash == $vaultAddressSlice~load_uint(256)));
        int $signedPublishKind = $signedPayload~load_uint(8);
        throw_unless(16467, ($signedPublishKind == 1));
        int $signedOwnerHash = $signedPayload~load_uint(256);
        int $clientNonce = $signedPayload~load_uint(64);
        int $maxCharge = $signedPayload~load_uint(128);
        int $signedSizeClass = $signedPayload~load_uint(8);
        int $signedCryptoSuite = $signedPayload~load_uint(8);
        slice $ownerPrefix = $Address$_fun_asSlice($msg'owner_wallet);
        throw_unless(16450, ($ownerPrefix~load_uint(11) == 1024));
        throw_unless(16470, ($signedOwnerHash == $ownerPrefix~load_uint(256)));
        throw_unless(16453, ($clientNonce == $user'publish_nonce));
        throw_unless(16472, ($signedCryptoSuite == 2));
        int $localExecReserve = 0;
        if (($signedSizeClass == 1)) {
            $localExecReserve = 12000000;
        } elseif (($signedSizeClass == 2)) {
            $localExecReserve = 13800000;
        } elseif (($signedSizeClass == 4)) {
            $localExecReserve = 17300000;
        } elseif (($signedSizeClass == 8)) {
            $localExecReserve = 24400000;
        } elseif (($signedSizeClass == 16)) {
            $localExecReserve = 38900000;
        } elseif (($signedSizeClass == 32)) {
            $localExecReserve = 67600000;
        }
        throw_unless(16471, ($localExecReserve > 0));
        throw_unless(16462, ($maxCharge >= $localExecReserve));
        throw_unless(16463, ($user'ton_balance >= $maxCharge));
        $global_acceptMessage();
        $user'ton_balance = $user'ton_balance - $localExecReserve;
        $user'publish_nonce = $user'publish_nonce + 1;
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
        $global_commit();
        ifnot (($Slice$_fun_bits($signedPayload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($Slice$_fun_refs($signedPayload) == 1)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        cell $payloadCell = $signedPayload~$Slice$_fun_loadRef();
        slice $payloadPreview = $Cell$_fun_beginParse($payloadCell);
        ifnot (($Slice$_fun_bits($payloadPreview) == 784)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($Slice$_fun_refs($payloadPreview) == 3)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        slice $payload = $Cell$_fun_beginParse($payloadCell);
        int $parsedSizeClass = $payload~load_uint(8);
        int $parsedCryptoSuite = $payload~load_uint(8);
        ifnot (($parsedSizeClass == $signedSizeClass)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($parsedCryptoSuite == $signedCryptoSuite)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        int $sizeClass = $parsedSizeClass;
        int $cryptoSuite = $parsedCryptoSuite;
        int $header0Hash = $payload~load_uint(256);
        int $header1Hash = $payload~load_uint(256);
        int $bodyHash = $payload~load_uint(256);
        cell $header0 = $payload~$Slice$_fun_loadRef();
        cell $header1 = $payload~$Slice$_fun_loadRef();
        cell $body = $payload~$Slice$_fun_loadRef();
        ifnot (($Slice$_fun_bits($payload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($Slice$_fun_refs($payload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isPublishProfileValid(1, $sizeClass, $cryptoSuite, $header0Hash, $header1Hash)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($Cell$_fun_hash($header0) == $header0Hash)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($Cell$_fun_hash($header1) == $header1Hash)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($Cell$_fun_hash($body) == $bodyHash)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isPrivateCapsuleShapeValid($sizeClass, $cryptoSuite, $header0, $header1, $body)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        int $expectedMax = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_canonicalPublishMaxCharge($msg'owner_wallet, 1, $sizeClass, $cryptoSuite);
        ifnot (($maxCharge >= $expectedMax)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        int $protocolFee = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_protocolFeePaid($msg'owner_wallet, 1, $sizeClass, $cryptoSuite);
        int $callValue = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_capsulehubCallValue($maxCharge, $localExecReserve);
        int $publishId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computePublishId($msg'owner_wallet, $clientNonce, $bodyHash, 1);
        ifnot (($publishId)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        int $bounceId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computePublishBounceId($publishId);
        int $bounceTag = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computePublishBounceTag($publishId);
        if (__tact_dict_exists_int($self'pending_publishes, 257, $bounceId)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        $user'ton_balance = $user'ton_balance - ($maxCharge - $localExecReserve);
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
        $self'pending_publishes~__tact_dict_set_int_cell(257, $bounceId, $PendingPublish$_store_cell($PendingPublish$_constructor_owner_wallet_tombstone_refund_to_vault_nonce_publish_kind_body_hash_refundable_amount_created_at($msg'owner_wallet, false, true, $clientNonce, 1, $bodyHash, $callValue, $global_now()), begin_cell()));
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_incrementPendingPublishCount();
        $global_commit();
        $global_message($MessageParameters$_constructor_to_value_bounce_mode_body($self'capsule_hub_address, $callValue, true, 1, $PublishPrivateFromVault$_store_cell($PublishPrivateFromVault$_constructor_bounce_id_bounce_tag_publish_id_size_class_crypto_suite_header_0_hash_header_1_hash_body_hash_header_0_header_1_body_protocol_fee_paid($bounceId, $bounceTag, $publishId, $sizeClass, $cryptoSuite, $header0Hash, $header1Hash, $bodyHash, $header0, $header1, $body, $protocolFee), begin_cell())));
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
        return ();
    }
    
    ;; Receive PublishPublicFromVaultBalance message
    if (op == 0x7e1f5032) {
        var $msg'owner_wallet = in_msg~load_msg_addr();
        var $msg'signature = in_msg~load_bits(512);
        var $msg'signed_payload = in_msg~load_ref();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireSealed();
        tuple $userOpt = $UserState$_load_opt(__tact_dict_get_slice_cell($self'users, 267, $msg'owner_wallet));
        throw_unless(16481, (~ null?($userOpt)));
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce) = $UserState$_not_null($userOpt);
        throw_unless(16484, ($user'auth_pubkey != 0));
        throw_unless(16487, $global_checkSignature($Cell$_fun_hash($msg'signed_payload), $msg'signature, $user'auth_pubkey));
        slice $signedPayload = $Cell$_fun_beginParse($msg'signed_payload);
        int $signedDomain = $signedPayload~load_uint(32);
        throw_unless(16493, ($signedDomain == 1448100401));
        int $signedManifestHash = $signedPayload~load_uint(256);
        throw_unless(16494, ($signedManifestHash == $self'deployment_manifest_hash));
        slice $signedVaultAddress = $signedPayload~$Slice$_fun_loadAddress();
        throw_unless(16495, ( equal_slices_bits($signedVaultAddress, $global_myAddress()) ));
        int $signedPublishKind = $signedPayload~load_uint(8);
        throw_unless(16496, ($signedPublishKind == 2));
        slice $signedOwner = $signedPayload~$Slice$_fun_loadAddress();
        int $clientNonce = $signedPayload~load_uint(64);
        int $maxCharge = $signedPayload~load_uint(128);
        throw_unless(16499, ( equal_slices_bits($signedOwner, $msg'owner_wallet) ));
        slice $ownerPrefix = $Address$_fun_asSlice($msg'owner_wallet);
        throw_unless(16480, ($ownerPrefix~load_uint(11) == 1024));
        throw_unless(16483, ($clientNonce == $user'publish_nonce));
        int $localExecReserve = 8700000;
        throw_unless(16491, ($maxCharge >= $localExecReserve));
        throw_unless(16492, ($user'ton_balance >= $maxCharge));
        $global_acceptMessage();
        $user'ton_balance = $user'ton_balance - $localExecReserve;
        $user'publish_nonce = $user'publish_nonce + 1;
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
        $global_commit();
        ifnot (($Slice$_fun_bits($signedPayload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($Slice$_fun_refs($signedPayload) == 1)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        cell $payloadCell = $signedPayload~$Slice$_fun_loadRef();
        slice $payloadPreview = $Cell$_fun_beginParse($payloadCell);
        ifnot (($Slice$_fun_bits($payloadPreview) == 512)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($Slice$_fun_refs($payloadPreview) == 2)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        slice $payload = $Cell$_fun_beginParse($payloadCell);
        int $headerHash = $payload~load_uint(256);
        int $bodyHash = $payload~load_uint(256);
        cell $header = $payload~$Slice$_fun_loadRef();
        cell $body = $payload~$Slice$_fun_loadRef();
        ifnot (($Slice$_fun_bits($payload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($Slice$_fun_refs($payload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isPublishProfileValid(2, 1, 0, $headerHash, 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($Cell$_fun_hash($header) == $headerHash)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($Cell$_fun_hash($body) == $bodyHash)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isPublicCapsuleShapeValid($header, $body)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        int $expectedMax = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_canonicalPublishMaxCharge($msg'owner_wallet, 2, 1, 0);
        ifnot (($maxCharge >= $expectedMax)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        int $protocolFee = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_protocolFeePaid($msg'owner_wallet, 2, 1, 0);
        int $callValue = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_capsulehubCallValue($maxCharge, $localExecReserve);
        int $publishId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computePublishId($msg'owner_wallet, $clientNonce, $bodyHash, 2);
        ifnot (($publishId)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        int $bounceId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computePublishBounceId($publishId);
        int $bounceTag = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computePublishBounceTag($publishId);
        if (__tact_dict_exists_int($self'pending_publishes, 257, $bounceId)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        $user'ton_balance = $user'ton_balance - ($maxCharge - $localExecReserve);
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
        $self'pending_publishes~__tact_dict_set_int_cell(257, $bounceId, $PendingPublish$_store_cell($PendingPublish$_constructor_owner_wallet_tombstone_refund_to_vault_nonce_publish_kind_body_hash_refundable_amount_created_at($msg'owner_wallet, false, true, $clientNonce, 2, $bodyHash, $callValue, $global_now()), begin_cell()));
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_incrementPendingPublishCount();
        $global_commit();
        $global_message($MessageParameters$_constructor_to_value_bounce_mode_body($self'capsule_hub_address, $callValue, true, 1, $PublishPublicFromVault$_store_cell($PublishPublicFromVault$_constructor_bounce_id_bounce_tag_publish_id_marketing_note_author_wallet_header_hash_body_hash_header_body_protocol_fee_paid($bounceId, $bounceTag, $publishId, 2573421624129493433291659589718684717235138672, $msg'owner_wallet, $headerHash, $bodyHash, $header, $body, $protocolFee), begin_cell())));
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
        return ();
    }
    
    ;; Receive SetProfileAvatarFromVaultBalance message
    if (op == 0x7e1f5033) {
        var $msg'owner_wallet = in_msg~load_msg_addr();
        var $msg'signature = in_msg~load_bits(512);
        var $msg'signed_payload = in_msg~load_ref();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireSealed();
        throw_unless(16600, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isProfileRegistryBound());
        tuple $userOpt = $UserState$_load_opt(__tact_dict_get_slice_cell($self'users, 267, $msg'owner_wallet));
        throw_unless(16601, (~ null?($userOpt)));
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce) = $UserState$_not_null($userOpt);
        throw_unless(16602, ($user'auth_pubkey != 0));
        throw_unless(16604, $global_checkSignature($Cell$_fun_hash($msg'signed_payload), $msg'signature, $user'auth_pubkey));
        slice $signedPayload = $Cell$_fun_beginParse($msg'signed_payload);
        int $signedDomain = $signedPayload~load_uint(32);
        throw_unless(16605, ($signedDomain == 1448100145));
        int $signedManifestHash = $signedPayload~load_uint(256);
        throw_unless(16606, ($signedManifestHash == $self'deployment_manifest_hash));
        slice $signedVaultAddress = $signedPayload~$Slice$_fun_loadAddress();
        throw_unless(16607, ( equal_slices_bits($signedVaultAddress, $global_myAddress()) ));
        slice $signedOwner = $signedPayload~$Slice$_fun_loadAddress();
        int $clientNonce = $signedPayload~load_uint(64);
        int $maxTonCharge = $signedPayload~load_uint(128);
        throw_unless(16610, ( equal_slices_bits($signedOwner, $msg'owner_wallet) ));
        throw_unless(16611, ($clientNonce == $user'publish_nonce));
        $global_acceptMessage();
        $user'publish_nonce = $user'publish_nonce + 1;
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
        $global_commit();
        ifnot ($self'sealed) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isUsernameRegistryBound()) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        slice $ownerPrefix = $Address$_fun_asSlice($msg'owner_wallet);
        ifnot (($ownerPrefix~load_uint(11) == 1024)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($maxTonCharge >= 6000000)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($user'ton_balance >= 6000000)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        $user'ton_balance = $user'ton_balance - 6000000;
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
        $global_commit();
        ifnot (($Slice$_fun_bits($signedPayload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($Slice$_fun_refs($signedPayload) == 1)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        cell $avatarPayloadCell = $signedPayload~$Slice$_fun_loadRef();
        slice $avatarPayloadPreview = $Cell$_fun_beginParse($avatarPayloadCell);
        ifnot (($Slice$_fun_bits($avatarPayloadPreview) == 739)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($Slice$_fun_refs($avatarPayloadPreview) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        slice $avatarPayload = $Cell$_fun_beginParse($avatarPayloadCell);
        slice $signedProfileRegistry = $avatarPayload~$Slice$_fun_loadAddress();
        int $avatarHash = $avatarPayload~load_uint(256);
        int $avatarEntryId = $avatarPayload~load_uint(64);
        int $avatarStreamId = $avatarPayload~load_uint(128);
        int $avatarPartCount = $avatarPayload~load_uint(16);
        int $mediaFormat = $avatarPayload~load_uint(8);
        ifnot (( equal_slices_bits($signedProfileRegistry, $self'profile_registry_address) )) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_requireValidProfileAvatarPointer($msg'owner_wallet, $avatarHash, $avatarStreamId, $avatarPartCount, $mediaFormat);
        int $tonCharge = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_profileAvatarTonCharge();
        ifnot (($maxTonCharge >= $tonCharge)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        int $remainingTonCharge = ($tonCharge - 6000000);
        ifnot (($user'ton_balance >= $remainingTonCharge)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($user'ath_balance >= 100000000000)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        int $queryId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computeProfileAvatarPaymentId($msg'owner_wallet, $clientNonce, $avatarHash);
        if (__tact_dict_exists_int($self'pending_profile_avatar_payments, 257, $queryId)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        $user'ton_balance = $user'ton_balance - $remainingTonCharge;
        $user'ath_balance = $user'ath_balance - 100000000000;
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
        $self'pending_profile_avatar_payments~__tact_dict_set_int_cell(257, $queryId, $PendingProfileAvatarPayment$_store_cell($PendingProfileAvatarPayment$_constructor_owner_wallet_amount_created_at($msg'owner_wallet, 100000000000, $global_now()), begin_cell()));
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_incrementPendingProfileAvatarPaymentCount();
        $global_commit();
        $global_message($MessageParameters$_constructor_to_value_bounce_mode_body($self'vault_ath_wallet_address, 55000000, true, 1, $ATHTransferRequestVaultProfileAvatar$_store_cell($ATHTransferRequestVaultProfileAvatar$_constructor_query_id_amount_recipient_response_destination_notify_value_owner_wallet_avatar_hash_avatar_entry_id_avatar_stream_id_avatar_part_count_media_format($queryId, 100000000000, $self'profile_registry_address, $global_myAddress(), 30000000, $msg'owner_wallet, $avatarHash, $avatarEntryId, $avatarStreamId, $avatarPartCount, $mediaFormat), begin_cell())));
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
        return ();
    }
    
    ;; Receive MintUsernameFromVaultBalance message
    if (op == 0x7e1f5034) {
        var $msg'owner_wallet = in_msg~load_msg_addr();
        var $msg'signature = in_msg~load_bits(512);
        var $msg'signed_payload = in_msg~load_ref();
        tuple $userOpt = $UserState$_load_opt(__tact_dict_get_slice_cell($self'users, 267, $msg'owner_wallet));
        throw_unless(16701, (~ null?($userOpt)));
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce) = $UserState$_not_null($userOpt);
        throw_unless(16702, ($user'auth_pubkey != 0));
        throw_unless(16704, $global_checkSignature($Cell$_fun_hash($msg'signed_payload), $msg'signature, $user'auth_pubkey));
        slice $signedPayload = $Cell$_fun_beginParse($msg'signed_payload);
        int $signedDomain = $signedPayload~load_uint(32);
        throw_unless(16705, ($signedDomain == 1448431153));
        int $signedManifestHash = $signedPayload~load_uint(256);
        throw_unless(16706, ($signedManifestHash == $self'deployment_manifest_hash));
        slice $signedVaultAddress = $signedPayload~$Slice$_fun_loadAddress();
        throw_unless(16707, ( equal_slices_bits($signedVaultAddress, $global_myAddress()) ));
        slice $signedOwner = $signedPayload~$Slice$_fun_loadAddress();
        int $clientNonce = $signedPayload~load_uint(64);
        int $maxTonCharge = $signedPayload~load_uint(128);
        throw_unless(16710, ( equal_slices_bits($signedOwner, $msg'owner_wallet) ));
        throw_unless(16711, ($clientNonce == $user'publish_nonce));
        $global_acceptMessage();
        $user'publish_nonce = $user'publish_nonce + 1;
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
        $global_commit();
        slice $ownerPrefix = $Address$_fun_asSlice($msg'owner_wallet);
        ifnot (($ownerPrefix~load_uint(11) == 1024)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($maxTonCharge >= 6000000)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($user'ton_balance >= 6000000)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        $user'ton_balance = $user'ton_balance - 6000000;
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
        $global_commit();
        ifnot (($Slice$_fun_bits($signedPayload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($Slice$_fun_refs($signedPayload) == 1)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        cell $usernamePayloadCell = $signedPayload~$Slice$_fun_loadRef();
        slice $usernamePayloadPreview = $Cell$_fun_beginParse($usernamePayloadCell);
        ifnot (($Slice$_fun_bits($usernamePayloadPreview) >= 275)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($Slice$_fun_refs($usernamePayloadPreview) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        slice $usernamePayload = $Cell$_fun_beginParse($usernamePayloadCell);
        slice $signedUsernameRegistry = $usernamePayload~$Slice$_fun_loadAddress();
        int $usernameLen = $usernamePayload~load_uint(8);
        slice $username = $usernamePayload;
        ifnot (( equal_slices_bits($signedUsernameRegistry, $self'username_registry_address) )) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_isValidUsernameBytes($usernameLen, $username)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        int $price = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_usernamePriceForLength($usernameLen);
        int $tonCharge = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_usernameMintTonCharge();
        ifnot (($maxTonCharge >= $tonCharge)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        int $remainingTonCharge = ($tonCharge - 6000000);
        ifnot (($user'ton_balance >= $remainingTonCharge)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        ifnot (($user'ath_balance >= $price)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        int $nameHash = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computeUsernameNameHash($username);
        int $queryId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_computeUsernameMintPaymentId($msg'owner_wallet, $clientNonce, $nameHash);
        if (__tact_dict_exists_int($self'pending_username_mint_payments, 257, $queryId)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
            return ();
        }
        $user'ton_balance = $user'ton_balance - $remainingTonCharge;
        $user'ath_balance = $user'ath_balance - $price;
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce), begin_cell()));
        $self'pending_username_mint_payments~__tact_dict_set_int_cell(257, $queryId, $PendingUsernameMintPayment$_store_cell($PendingUsernameMintPayment$_constructor_owner_wallet_amount_created_at($msg'owner_wallet, $price, $global_now()), begin_cell()));
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count)~$Vault$_fun_incrementPendingUsernameMintPaymentCount();
        $global_commit();
        $global_message($MessageParameters$_constructor_to_value_bounce_mode_body($self'vault_ath_wallet_address, 57000000, true, 1, $ATHTransferRequestVaultMintUsername$_store_cell($ATHTransferRequestVaultMintUsername$_constructor_query_id_amount_recipient_response_destination_notify_value_owner_wallet_username_len_username($queryId, $price, $self'username_registry_address, $global_myAddress(), 32000000, $msg'owner_wallet, $usernameLen, $username), begin_cell())));
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'receive_intents, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'receive_intent_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count));
        return ();
    }
    
    ;; Throw if not handled
    throw(130);
}
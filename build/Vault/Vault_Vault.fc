#pragma version =0.4.6;
#pragma allow-post-modification;
#pragma compute-asm-ltr;

;; Vault_Vault.headers.fc
;;
;; Header files for Vault
;; NOTE: declarations are sorted for optimal order
;;

;; __tact_context_get
(int, slice, int, slice) __tact_context_get() inline;

;; __tact_context_get_sender
slice __tact_context_get_sender() inline;

;; __tact_dict_get_slice_cell
cell __tact_dict_get_slice_cell(cell d, int kl, slice k) inline;

;; __tact_dict_set_slice_cell
(cell, ()) __tact_dict_set_slice_cell(cell d, int kl, slice k, cell v) inline;

;; __tact_dict_get_uint_cell
cell __tact_dict_get_uint_cell(cell d, int kl, int k) inline;

;; __tact_dict_set_uint_cell
(cell, ()) __tact_dict_set_uint_cell(cell d, int kl, int k, cell v) inline;

;; __tact_dict_set_int_int
(cell, ()) __tact_dict_set_int_int(cell d, int kl, int k, int v, int vl) inline;

;; __tact_dict_get_int_cell
cell __tact_dict_get_int_cell(cell d, int kl, int k) inline;

;; __tact_dict_set_int_cell
(cell, ()) __tact_dict_set_int_cell(cell d, int kl, int k, cell v) inline;

;; __tact_dict_exists_slice
int __tact_dict_exists_slice(cell d, int kl, slice k) inline;

;; __tact_dict_exists_int
int __tact_dict_exists_int(cell d, int kl, int k) inline;

;; $AthTransferNotificationAck$_store
builder $AthTransferNotificationAck$_store(builder build_0, (int, int, int) v) inline;

;; $AthTransferNotificationAck$_store_cell
cell $AthTransferNotificationAck$_store_cell((int, int, int) v, builder b) inline;

;; $ATHTransferRequest$_store
builder $ATHTransferRequest$_store(builder build_0, (int, int, slice, slice) v) inline;

;; $ATHTransferRequest$_store_cell
cell $ATHTransferRequest$_store_cell((int, int, slice, slice) v, builder b) inline;

;; $ATHTransferRequest$_load_bounced
(slice, ((int, int))) $ATHTransferRequest$_load_bounced(slice sc_0) inline;

;; $ATHTransferRequestVaultProfileAvatar$_store
builder $ATHTransferRequestVaultProfileAvatar$_store(builder build_0, (int, int, slice, slice, int, slice, int, int, int, int, int) v) inline_ref;

;; $ATHTransferRequestVaultProfileAvatar$_store_cell
cell $ATHTransferRequestVaultProfileAvatar$_store_cell((int, int, slice, slice, int, slice, int, int, int, int, int) v, builder b) inline;

;; $ATHTransferRequestVaultProfileAvatar$_load_bounced
(slice, ((int, int))) $ATHTransferRequestVaultProfileAvatar$_load_bounced(slice sc_0) inline;

;; $ATHTransferRequestVaultMintUsername$_store
builder $ATHTransferRequestVaultMintUsername$_store(builder build_0, (int, int, slice, slice, int, slice, int, slice) v) inline;

;; $ATHTransferRequestVaultMintUsername$_store_cell
cell $ATHTransferRequestVaultMintUsername$_store_cell((int, int, slice, slice, int, slice, int, slice) v, builder b) inline;

;; $ATHTransferRequestVaultMintUsername$_load_bounced
(slice, ((int, int))) $ATHTransferRequestVaultMintUsername$_load_bounced(slice sc_0) inline;

;; $PublishBatchToHub$_store
builder $PublishBatchToHub$_store(builder build_0, (int, int, int, int, int, int, slice, cell, cell) v) inline;

;; $PublishBatchToHub$_store_cell
cell $PublishBatchToHub$_store_cell((int, int, int, int, int, int, slice, cell, cell) v, builder b) inline;

;; $PublishBatchToHub$_load_bounced
(slice, ((int, int))) $PublishBatchToHub$_load_bounced(slice sc_0) inline;

;; $PendingAthWithdrawal$_store
builder $PendingAthWithdrawal$_store(builder build_0, (slice, int, slice, slice, int, int, int) v) inline;

;; $PendingAthWithdrawal$_store_cell
cell $PendingAthWithdrawal$_store_cell((slice, int, slice, slice, int, int, int) v, builder b) inline;

;; $PendingAthWithdrawal$_load
(slice, ((slice, int, slice, slice, int, int, int))) $PendingAthWithdrawal$_load(slice sc_0) inline;

;; $PendingAthWithdrawal$_as_optional
tuple $PendingAthWithdrawal$_as_optional((slice, int, slice, slice, int, int, int) v) inline;

;; $PendingAthWithdrawal$_load_opt
tuple $PendingAthWithdrawal$_load_opt(cell cl) inline;

;; $PendingBatchPublish$_store
builder $PendingBatchPublish$_store(builder build_0, (slice, int, int, int, int, int, int, int, int) v) inline;

;; $PendingBatchPublish$_store_cell
cell $PendingBatchPublish$_store_cell((slice, int, int, int, int, int, int, int, int) v, builder b) inline;

;; $PendingBatchPublish$_load
(slice, ((slice, int, int, int, int, int, int, int, int))) $PendingBatchPublish$_load(slice sc_0) inline;

;; $PendingBatchPublish$_as_optional
tuple $PendingBatchPublish$_as_optional((slice, int, int, int, int, int, int, int, int) v) inline;

;; $PendingBatchPublish$_load_opt
tuple $PendingBatchPublish$_load_opt(cell cl) inline;

;; $PendingProfileAvatarPayment$_store
builder $PendingProfileAvatarPayment$_store(builder build_0, (slice, int, int, int) v) inline;

;; $PendingProfileAvatarPayment$_store_cell
cell $PendingProfileAvatarPayment$_store_cell((slice, int, int, int) v, builder b) inline;

;; $PendingProfileAvatarPayment$_load
(slice, ((slice, int, int, int))) $PendingProfileAvatarPayment$_load(slice sc_0) inline;

;; $PendingProfileAvatarPayment$_as_optional
tuple $PendingProfileAvatarPayment$_as_optional((slice, int, int, int) v) inline;

;; $PendingProfileAvatarPayment$_load_opt
tuple $PendingProfileAvatarPayment$_load_opt(cell cl) inline;

;; $PendingUsernameMintPayment$_store
builder $PendingUsernameMintPayment$_store(builder build_0, (slice, int, int, int) v) inline;

;; $PendingUsernameMintPayment$_store_cell
cell $PendingUsernameMintPayment$_store_cell((slice, int, int, int) v, builder b) inline;

;; $PendingUsernameMintPayment$_load
(slice, ((slice, int, int, int))) $PendingUsernameMintPayment$_load(slice sc_0) inline;

;; $PendingUsernameMintPayment$_as_optional
tuple $PendingUsernameMintPayment$_as_optional((slice, int, int, int) v) inline;

;; $PendingUsernameMintPayment$_load_opt
tuple $PendingUsernameMintPayment$_load_opt(cell cl) inline;

;; $KeyRecord$_store
builder $KeyRecord$_store(builder build_0, (slice, int, int, int, int, int, cell, int, int, int, int, int) v) inline_ref;

;; $KeyRecord$_store_cell
cell $KeyRecord$_store_cell((slice, int, int, int, int, int, cell, int, int, int, int, int) v, builder b) inline;

;; $KeyRecord$_load
(slice, ((slice, int, int, int, int, int, cell, int, int, int, int, int))) $KeyRecord$_load(slice sc_0) inline_ref;

;; $KeyRecord$_as_optional
tuple $KeyRecord$_as_optional((slice, int, int, int, int, int, cell, int, int, int, int, int) v) inline;

;; $KeyRecord$_load_opt
tuple $KeyRecord$_load_opt(cell cl) inline;

;; $ReceiptSlot$_store
builder $ReceiptSlot$_store(builder build_0, (int, int, int, int, int, int) v) inline;

;; $ReceiptSlot$_store_cell
cell $ReceiptSlot$_store_cell((int, int, int, int, int, int) v, builder b) inline;

;; $ReceiptSlot$_load
(slice, ((int, int, int, int, int, int))) $ReceiptSlot$_load(slice sc_0) inline;

;; $ReceiptSlot$_as_optional
tuple $ReceiptSlot$_as_optional((int, int, int, int, int, int) v) inline;

;; $ReceiptSlot$_load_opt
tuple $ReceiptSlot$_load_opt(cell cl) inline;

;; $UserState$_store
builder $UserState$_store(builder build_0, (int, int, int, int, int, int, cell) v) inline;

;; $UserState$_store_cell
cell $UserState$_store_cell((int, int, int, int, int, int, cell) v, builder b) inline;

;; $UserState$_load
(slice, ((int, int, int, int, int, int, cell))) $UserState$_load(slice sc_0) inline;

;; $UserState$_as_optional
tuple $UserState$_as_optional((int, int, int, int, int, int, cell) v) inline;

;; $UserState$_load_opt
tuple $UserState$_load_opt(cell cl) inline;

;; $Vault$_store
builder $Vault$_store(builder build_0, (slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) v) inline;

;; $Vault$_load
(slice, ((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell))) $Vault$_load(slice sc_0) inline;

;; $Context$_get_value
_ $Context$_get_value((int, slice, int, slice) v) inline;

;; $PendingAthWithdrawal$_not_null
((slice, int, slice, slice, int, int, int)) $PendingAthWithdrawal$_not_null(tuple v) inline;

;; $PendingBatchPublish$_not_null
((slice, int, int, int, int, int, int, int, int)) $PendingBatchPublish$_not_null(tuple v) inline;

;; $PendingProfileAvatarPayment$_not_null
((slice, int, int, int)) $PendingProfileAvatarPayment$_not_null(tuple v) inline;

;; $PendingUsernameMintPayment$_not_null
((slice, int, int, int)) $PendingUsernameMintPayment$_not_null(tuple v) inline;

;; $KeyRecord$_not_null
((slice, int, int, int, int, int, cell, int, int, int, int, int)) $KeyRecord$_not_null(tuple v) inline;

;; $ReceiptSlot$_not_null
((int, int, int, int, int, int)) $ReceiptSlot$_not_null(tuple v) inline;

;; $UserState$_not_null
((int, int, int, int, int, int, cell)) $UserState$_not_null(tuple v) inline;

;; $VaultKeyRecordView$_to_external
(int, slice, int, int, int, int, int, cell, int, int, int, int, int) $VaultKeyRecordView$_to_external(((int, slice, int, int, int, int, int, cell, int, int, int, int, int)) v) inline;

;; $VaultUserView$_to_external
(int, int, int, int, int, int, int) $VaultUserView$_to_external(((int, int, int, int, int, int, int)) v) inline;

;; $VaultStorageHealthView$_to_external
(int, int, int, int, int) $VaultStorageHealthView$_to_external(((int, int, int, int, int)) v) inline;

;; $VaultUserReceiptsView$_to_external
(int, int, cell) $VaultUserReceiptsView$_to_external(((int, int, cell)) v) inline;

;; $VaultPendingAthWithdrawalView$_to_external
(int, slice, slice, slice, int, int) $VaultPendingAthWithdrawalView$_to_external(((int, slice, slice, slice, int, int)) v) inline;

;; $VaultSuccessorView$_to_external
(int, int, slice, int) $VaultSuccessorView$_to_external(((int, int, slice, int)) v) inline;

;; $VaultPendingBatchPublishView$_to_external
(int, slice, int, int, int, int, int) $VaultPendingBatchPublishView$_to_external(((int, slice, int, int, int, int, int)) v) inline;

;; $VaultGlobalView$_to_external
(int, int, int, int, int, slice, slice, slice, slice, slice, int, int, int, int, int, int, int, int, int, int, int, int) $VaultGlobalView$_to_external(((int, int, int, int, int, slice, slice, slice, slice, slice, int, int, int, int, int, int, int, int, int, int, int, int)) v) inline;

;; $ATHWallet$init$_store
builder $ATHWallet$init$_store(builder build_0, (int, slice, slice) v) inline;

;; $Vault$init$_load
(slice, ((slice, slice, slice, int, int, int, int))) $Vault$init$_load(slice sc_0) inline;

;; $Vault$_contract_init
(slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $Vault$_contract_init(slice $vault_ath_wallet_address, slice $ath_master_address, slice $capsule_hub_address, int $genesis_config_hash, int $capsule_hub_bound, int $sealed, int $deployment_manifest_hash) impure inline;

;; $Vault$_contract_load
(slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $Vault$_contract_load() impure inline;

;; $Vault$_contract_store
() $Vault$_contract_store((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) v) impure inline;

;; $Cell$_fun_asSlice
slice $Cell$_fun_asSlice(cell $self) impure inline;

;; $global_newAddress
slice $global_newAddress(int $chain, int $hash) impure inline;

;; $global_contractAddressExt
slice $global_contractAddressExt(int $chain, cell $code, cell $data) impure inline;

;; $ATHWallet$_init_child
(cell, cell) $ATHWallet$_init_child(int $balance, slice $owner_address, slice $ath_master_address) inline_ref;

;; $MessageParameters$_constructor_to_value_bounce_mode_body
((int, cell, int, slice, int)) $MessageParameters$_constructor_to_value_bounce_mode_body(slice $to, int $value, int $bounce, int $mode, cell $body) inline;

;; $Vault$_fun_requireSealed
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_requireSealed((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline;

;; $Vault$_fun_requireEmptyEnvelopePadding
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_requireEmptyEnvelopePadding((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $padding) impure inline;

;; $Vault$_fun_requireUnsealed
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_requireUnsealed((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref;

;; $Vault$_fun_isBindingSet
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isBindingSet((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $mask) impure inline_ref;

;; $Vault$_fun_isCapsuleHubBound
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isCapsuleHubBound((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref;

;; $Vault$_fun_isProfileRegistryBound
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isProfileRegistryBound((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref;

;; $Vault$_fun_isUsernameRegistryBound
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isUsernameRegistryBound((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref;

;; $Vault$_fun_requireGenesisController
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_requireGenesisController((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref;

;; $UserState$_constructor_ton_balance_ath_balance_current_key_id_auth_pubkey_publish_nonce_last_active_receipts
((int, int, int, int, int, int, cell)) $UserState$_constructor_ton_balance_ath_balance_current_key_id_auth_pubkey_publish_nonce_last_active_receipts(int $ton_balance, int $ath_balance, int $current_key_id, int $auth_pubkey, int $publish_nonce, int $last_active, cell $receipts) inline;

;; $Vault$_fun_emptyUser
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, int, int, int, int, int, cell)) $Vault$_fun_emptyUser((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref;

;; $Vault$_fun_getUserOrEmpty
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, int, int, int, int, int, cell)) $Vault$_fun_getUserOrEmpty((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $owner) impure inline_ref;

;; $ReceiptSlot$_constructor_nonce_action_result_aux_part_count_at
((int, int, int, int, int, int)) $ReceiptSlot$_constructor_nonce_action_result_aux_part_count_at(int $nonce, int $action, int $result, int $aux, int $part_count, int $at) inline;

;; $Vault$_fun_writeReceipt
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, int, int, int, int, int, cell)) $Vault$_fun_writeReceipt((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, (int, int, int, int, int, int, cell) $user, int $nonce, int $action, int $result, int $aux, int $part_count) impure inline_ref;

;; $Vault$_fun_updateReceipt
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, int, int, int, int, int, cell)) $Vault$_fun_updateReceipt((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, (int, int, int, int, int, int, cell) $user, int $nonce, int $result, int $aux) impure inline_ref;

;; $Vault$_fun_deriveAthWalletAddress
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), slice) $Vault$_fun_deriveAthWalletAddress((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $owner) impure inline_ref;

;; $Vault$_fun_isBasechainAddress
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isBasechainAddress((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $owner) impure inline_ref;

;; $Vault$_fun_pendingPublishCount
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_pendingPublishCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref;

;; $Vault$_fun_pendingProfileAvatarPaymentCount
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_pendingProfileAvatarPaymentCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref;

;; $Vault$_fun_pendingUsernameMintPaymentCount
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_pendingUsernameMintPaymentCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref;

;; $Vault$_fun_incrementPendingPublishCount
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_incrementPendingPublishCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref;

;; $Vault$_fun_decrementPendingPublishCount
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_decrementPendingPublishCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref;

;; $Vault$_fun_incrementPendingProfileAvatarPaymentCount
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_incrementPendingProfileAvatarPaymentCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref;

;; $Vault$_fun_decrementPendingProfileAvatarPaymentCount
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_decrementPendingProfileAvatarPaymentCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref;

;; $Vault$_fun_incrementPendingUsernameMintPaymentCount
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_incrementPendingUsernameMintPaymentCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref;

;; $Vault$_fun_decrementPendingUsernameMintPaymentCount
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_decrementPendingUsernameMintPaymentCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref;

;; $Vault$_fun_computeAthDepositId
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_computeAthDepositId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $senderWallet, int $queryId) impure inline_ref;

;; $Vault$_fun_computeAthWithdrawalId
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_computeAthWithdrawalId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $ownerWallet, int $queryId) impure inline_ref;

;; $Vault$_fun_computeProfileAvatarPaymentId
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_computeProfileAvatarPaymentId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $ownerWallet, int $clientNonce, int $avatarHash) impure inline_ref;

;; $Vault$_fun_computeUsernameNameHash
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_computeUsernameNameHash((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $username) impure inline_ref;

;; $Vault$_fun_computeUsernameMintPaymentId
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_computeUsernameMintPaymentId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $ownerWallet, int $clientNonce, int $nameHash) impure inline_ref;

;; $Vault$_fun_profileRegistryAthWalletAddress
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), slice) $Vault$_fun_profileRegistryAthWalletAddress((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref;

;; $Vault$_fun_usernameRegistryAthWalletAddress
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), slice) $Vault$_fun_usernameRegistryAthWalletAddress((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref;

;; $Vault$_fun_profileAvatarTonCharge
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_profileAvatarTonCharge((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref;

;; $Vault$_fun_usernameMintTonCharge
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_usernameMintTonCharge((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref;

;; $Vault$_fun_usernamePriceForLength
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_usernamePriceForLength((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $nameLen) impure inline_ref;

;; $Vault$_fun_isValidUsernameBytes
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isValidUsernameBytes((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $nameLen, slice $username) impure inline_ref;

;; $Vault$_fun_isValidProfileAvatarPointer
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isValidProfileAvatarPointer((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $ownerWallet, int $avatarHash, int $avatarStreamId, int $avatarPartCount, int $mediaFormat) impure inline_ref;

;; $Vault$_fun_discountedFee
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_discountedFee((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $owner, int $fullFee) impure inline_ref;

;; $Vault$_fun_isAllowedPrivateSizeClass
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isAllowedPrivateSizeClass((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass) impure inline_ref;

;; $Vault$_fun_isAllowedPublicSizeClass
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isAllowedPublicSizeClass((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass) impure inline_ref;

;; $Vault$_fun_isPublishProfileValid
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isPublishProfileValid((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $publishKind, int $sizeClass, int $cryptoSuite, int $header0, int $header1) impure inline_ref;

;; $Vault$_fun_privateUsefulBytes
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_privateUsefulBytes((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass) impure inline_ref;

;; $Vault$_fun_privateBodyOverheadBytes
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_privateBodyOverheadBytes((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $cryptoSuite) impure inline_ref;

;; $Vault$_fun_privateBodyBytes
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_privateBodyBytes((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass, int $cryptoSuite) impure inline_ref;

;; $Vault$_fun_privateBodyBits
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_privateBodyBits((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass, int $cryptoSuite) impure inline_ref;

;; $Vault$_fun_privateBodyCells
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_privateBodyCells((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass, int $cryptoSuite) impure inline_ref;

;; $Vault$_fun_privateBodyRefs
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_privateBodyRefs((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass, int $cryptoSuite) impure inline_ref;

;; $Vault$_fun_introBodyBytes
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_introBodyBytes((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass) impure inline_ref;

;; $Vault$_fun_introBodyBits
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_introBodyBits((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass) impure inline_ref;

;; $Vault$_fun_introBodyCells
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_introBodyCells((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass) impure inline_ref;

;; $Vault$_fun_introBodyRefs
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_introBodyRefs((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass) impure inline_ref;

;; $Vault$_fun_publicBodyBytes
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_publicBodyBytes((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass) impure inline_ref;

;; $Vault$_fun_publicBodyBits
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_publicBodyBits((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass) impure inline_ref;

;; $Vault$_fun_publicBodyCells
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_publicBodyCells((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass) impure inline_ref;

;; $Vault$_fun_publicBodyRefs
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_publicBodyRefs((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass) impure inline_ref;

;; $Vault$_fun_isExactCapsuleCell
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isExactCapsuleCell((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, cell $cell, int $expectedCells, int $expectedBits, int $expectedRefs) impure inline_ref;

;; $Vault$_fun_isPublicCapsuleCell
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isPublicCapsuleCell((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, cell $cell, int $maxCells, int $maxBits, int $maxRefs, int $requireByteAligned) impure inline_ref;

;; $Vault$_fun_isConvCapsuleShapeValid
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isConvCapsuleShapeValid((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass, int $cryptoSuite, cell $header0, cell $header1, cell $body) impure inline_ref;

;; $Vault$_fun_isIntroCapsuleShapeValid
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isIntroCapsuleShapeValid((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass, int $cryptoSuite, cell $header0, cell $header1, cell $body) impure inline_ref;

;; $Vault$_fun_isPublicCapsuleShapeValid
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isPublicCapsuleShapeValid((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass, cell $header, cell $body) impure inline_ref;

;; $Vault$_fun_computePublishBounceId
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_computePublishBounceId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $publishId) impure inline_ref;

;; $Vault$_fun_computePublishBounceTag
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_computePublishBounceTag((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $publishId) impure inline_ref;

;; $Vault$_fun_batchStorageReserve
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_batchStorageReserve((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $kind) impure inline_ref;

;; $Vault$_fun_batchHubPartGas
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_batchHubPartGas((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $kind) impure inline_ref;

;; $Vault$_fun_batchVaultPartGas
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_batchVaultPartGas((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $kind) impure inline_ref;

;; $Vault$_fun_computeBatchPublishId
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_computeBatchPublishId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $owner, int $nonce, int $partsRootHash, int $kind, int $partCount) impure inline_ref;

;; $Vault$_fun_batchChargeFloor
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_batchChargeFloor((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $partCount) impure inline;

;; $Vault$_fun_refundPendingAthWithdrawalTon
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_refundPendingAthWithdrawalTon((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, (slice, int, slice, slice, int, int, int) $pending, int $amount) impure inline_ref;

;; $Vault$_fun_restorePendingProfileAvatarPayment
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_restorePendingProfileAvatarPayment((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $queryId, (slice, int, int, int) $pending) impure inline_ref;

;; $Vault$_fun_restorePendingUsernameMintPayment
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_restorePendingUsernameMintPayment((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $queryId, (slice, int, int, int) $pending) impure inline_ref;

;; $Vault$_fun_creditActivityAirdrop
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_creditActivityAirdrop((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $owner, int $count) impure inline_ref;

;; $Vault$_fun_keyRecordStorageEndowment
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_keyRecordStorageEndowment((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $cryptoSuiteMask) impure inline_ref;

;; $Vault$_fun_vaultStorageReserve
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_vaultStorageReserve((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref;

;; $Vault$_fun_validatePqPubkeyCell
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_validatePqPubkeyCell((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $pqLen, int $cryptoSuiteMask, cell $pqPubkey) impure inline_ref;

;; $Vault$_fun_isValidPqPubkeyCell
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isValidPqPubkeyCell((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $pqLen, int $cryptoSuiteMask, cell $pqPubkey) impure inline_ref;

;; $Vault$_fun_isValidKeyProfile
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isValidKeyProfile((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $encPubkey, int $signPubkey, int $pqHash, int $pqLen, cell $pqPubkey, int $cryptoSuiteMask) impure inline_ref;

;; $Vault$_fun_validateKeyProfile
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_validateKeyProfile((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $encPubkey, int $signPubkey, int $pqHash, int $pqLen, cell $pqPubkey, int $cryptoSuiteMask) impure inline_ref;

;; $Vault$_fun_computeKeyId
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_computeKeyId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $owner, int $keyGeneration, int $encPubkey, int $signPubkey, int $pqHash, int $pqLen, int $cryptoSuiteMask) impure inline_ref;

;; $Vault$_fun_creditBatchSettlementRefund
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, int, int, int, int, int, cell)) $Vault$_fun_creditBatchSettlementRefund((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, (int, int, int, int, int, int, cell) $user, int $refundableAmount) impure inline_ref;

;; $VaultUserView$_constructor_exists_ton_balance_ath_balance_current_key_id_auth_pubkey_publish_nonce_last_active
((int, int, int, int, int, int, int)) $VaultUserView$_constructor_exists_ton_balance_ath_balance_current_key_id_auth_pubkey_publish_nonce_last_active(int $exists, int $ton_balance, int $ath_balance, int $current_key_id, int $auth_pubkey, int $publish_nonce, int $last_active) inline;

;; $Vault$_fun_get_user
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, int, int, int, int, int, int)) $Vault$_fun_get_user((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $owner) impure inline_ref;

;; $VaultStorageHealthView$_constructor_balance_storage_reserve_user_count_key_record_count_surplus
((int, int, int, int, int)) $VaultStorageHealthView$_constructor_balance_storage_reserve_user_count_key_record_count_surplus(int $balance, int $storage_reserve, int $user_count, int $key_record_count, int $surplus) inline;

;; $Vault$_fun_get_storage_health
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, int, int, int, int)) $Vault$_fun_get_storage_health((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref;

;; $VaultUserReceiptsView$_constructor_exists_publish_nonce_receipts
((int, int, cell)) $VaultUserReceiptsView$_constructor_exists_publish_nonce_receipts(int $exists, int $publish_nonce, cell $receipts) inline;

;; $Vault$_fun_get_user_receipts
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, int, cell)) $Vault$_fun_get_user_receipts((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $owner) impure inline_ref;

;; $Vault$_fun_diag_forward_fee
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_diag_forward_fee((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $cells, int $bits) impure inline_ref;

;; $Vault$_fun_diag_compute_fee
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_diag_compute_fee((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $gas) impure inline_ref;

;; $Vault$_fun_diag_ext_hard_import
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_diag_ext_hard_import((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref;

;; $VaultKeyRecordView$_constructor_exists_owner_wallet_key_generation_enc_pubkey_sign_pubkey_pq_kem_pubkey_hash_pq_kem_pubkey_len_pq_kem_pubkey_crypto_suite_mask_created_at_created_lt_revoked_at_revoked_lt
((int, slice, int, int, int, int, int, cell, int, int, int, int, int)) $VaultKeyRecordView$_constructor_exists_owner_wallet_key_generation_enc_pubkey_sign_pubkey_pq_kem_pubkey_hash_pq_kem_pubkey_len_pq_kem_pubkey_crypto_suite_mask_created_at_created_lt_revoked_at_revoked_lt(int $exists, slice $owner_wallet, int $key_generation, int $enc_pubkey, int $sign_pubkey, int $pq_kem_pubkey_hash, int $pq_kem_pubkey_len, cell $pq_kem_pubkey, int $crypto_suite_mask, int $created_at, int $created_lt, int $revoked_at, int $revoked_lt) inline;

;; $Vault$_fun_get_key_record
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, slice, int, int, int, int, int, cell, int, int, int, int, int)) $Vault$_fun_get_key_record((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $keyId) impure inline_ref;

;; $VaultPendingBatchPublishView$_constructor_exists_owner_wallet_nonce_publish_kind_part_count_created_at_tombstone
((int, slice, int, int, int, int, int)) $VaultPendingBatchPublishView$_constructor_exists_owner_wallet_nonce_publish_kind_part_count_created_at_tombstone(int $exists, slice $owner_wallet, int $nonce, int $publish_kind, int $part_count, int $created_at, int $tombstone) inline;

;; $Vault$_fun_get_pending_batch_publish
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, slice, int, int, int, int, int)) $Vault$_fun_get_pending_batch_publish((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $bounceId) impure inline_ref;

;; $VaultPendingAthWithdrawalView$_constructor_exists_owner_wallet_recipient_recipient_ath_wallet_amount_created_at
((int, slice, slice, slice, int, int)) $VaultPendingAthWithdrawalView$_constructor_exists_owner_wallet_recipient_recipient_ath_wallet_amount_created_at(int $exists, slice $owner_wallet, slice $recipient, slice $recipient_ath_wallet, int $amount, int $created_at) inline;

;; $Vault$_fun_get_pending_ath_withdrawal
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, slice, slice, slice, int, int)) $Vault$_fun_get_pending_ath_withdrawal((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $queryId) impure inline_ref;

;; $Vault$_fun_get_ath_withdrawal_id
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_get_ath_withdrawal_id((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $ownerWallet, int $queryId) impure inline_ref;

;; $Vault$_fun_get_pending_ath_withdrawal_for
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, slice, slice, slice, int, int)) $Vault$_fun_get_pending_ath_withdrawal_for((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $ownerWallet, int $queryId) impure inline_ref;

;; $VaultSuccessorView$_constructor_announced_successor_manifest_hash_successor_vault_announced_at
((int, int, slice, int)) $VaultSuccessorView$_constructor_announced_successor_manifest_hash_successor_vault_announced_at(int $announced, int $successor_manifest_hash, slice $successor_vault, int $announced_at) inline;

;; $Vault$_fun_get_successor
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, int, slice, int)) $Vault$_fun_get_successor((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref;

;; $VaultGlobalView$_constructor_sealed_capsule_hub_bound_profile_registry_bound_username_registry_bound_deployment_manifest_hash_capsule_hub_address_profile_registry_address_username_registry_address_vault_ath_wallet_address_ath_master_address_user_count_key_record_count_pending_ath_withdrawal_count_pending_publish_count_pending_profile_avatar_payment_count_pending_username_mint_payment_count_processed_ath_deposit_count_pending_publish_stale_ttl_airdrop_remaining_ath_airdrop_distributed_ath_airdrop_reward_per_message_ath_airdrop_total_allocation_ath
((int, int, int, int, int, slice, slice, slice, slice, slice, int, int, int, int, int, int, int, int, int, int, int, int)) $VaultGlobalView$_constructor_sealed_capsule_hub_bound_profile_registry_bound_username_registry_bound_deployment_manifest_hash_capsule_hub_address_profile_registry_address_username_registry_address_vault_ath_wallet_address_ath_master_address_user_count_key_record_count_pending_ath_withdrawal_count_pending_publish_count_pending_profile_avatar_payment_count_pending_username_mint_payment_count_processed_ath_deposit_count_pending_publish_stale_ttl_airdrop_remaining_ath_airdrop_distributed_ath_airdrop_reward_per_message_ath_airdrop_total_allocation_ath(int $sealed, int $capsule_hub_bound, int $profile_registry_bound, int $username_registry_bound, int $deployment_manifest_hash, slice $capsule_hub_address, slice $profile_registry_address, slice $username_registry_address, slice $vault_ath_wallet_address, slice $ath_master_address, int $user_count, int $key_record_count, int $pending_ath_withdrawal_count, int $pending_publish_count, int $pending_profile_avatar_payment_count, int $pending_username_mint_payment_count, int $processed_ath_deposit_count, int $pending_publish_stale_ttl, int $airdrop_remaining_ath, int $airdrop_distributed_ath, int $airdrop_reward_per_message_ath, int $airdrop_total_allocation_ath) inline;

;; $Vault$_fun_get_global
((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, int, int, int, int, slice, slice, slice, slice, slice, int, int, int, int, int, int, int, int, int, int, int, int)) $Vault$_fun_get_global((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref;

;; $AthTransferNotificationAck$_constructor_query_id_sender_key_amount
((int, int, int)) $AthTransferNotificationAck$_constructor_query_id_sender_key_amount(int $query_id, int $sender_key, int $amount) inline;

;; $KeyRecord$_constructor_owner_wallet_key_generation_enc_pubkey_sign_pubkey_pq_kem_pubkey_hash_pq_kem_pubkey_len_pq_kem_pubkey_crypto_suite_mask_created_at_created_lt_revoked_at_revoked_lt
((slice, int, int, int, int, int, cell, int, int, int, int, int)) $KeyRecord$_constructor_owner_wallet_key_generation_enc_pubkey_sign_pubkey_pq_kem_pubkey_hash_pq_kem_pubkey_len_pq_kem_pubkey_crypto_suite_mask_created_at_created_lt_revoked_at_revoked_lt(slice $owner_wallet, int $key_generation, int $enc_pubkey, int $sign_pubkey, int $pq_kem_pubkey_hash, int $pq_kem_pubkey_len, cell $pq_kem_pubkey, int $crypto_suite_mask, int $created_at, int $created_lt, int $revoked_at, int $revoked_lt) inline;

;; $PendingBatchPublish$_constructor_owner_wallet_tombstone_refund_to_vault_nonce_publish_kind_part_count_publish_id_refundable_amount_created_at
((slice, int, int, int, int, int, int, int, int)) $PendingBatchPublish$_constructor_owner_wallet_tombstone_refund_to_vault_nonce_publish_kind_part_count_publish_id_refundable_amount_created_at(slice $owner_wallet, int $tombstone, int $refund_to_vault, int $nonce, int $publish_kind, int $part_count, int $publish_id, int $refundable_amount, int $created_at) inline;

;; $PendingUsernameMintPayment$_constructor_owner_wallet_tombstone_amount_created_at
((slice, int, int, int)) $PendingUsernameMintPayment$_constructor_owner_wallet_tombstone_amount_created_at(slice $owner_wallet, int $tombstone, int $amount, int $created_at) inline;

;; $PendingProfileAvatarPayment$_constructor_owner_wallet_tombstone_amount_created_at
((slice, int, int, int)) $PendingProfileAvatarPayment$_constructor_owner_wallet_tombstone_amount_created_at(slice $owner_wallet, int $tombstone, int $amount, int $created_at) inline;

;; $PendingAthWithdrawal$_constructor_owner_wallet_tombstone_recipient_recipient_ath_wallet_amount_refundable_ton_amount_created_at
((slice, int, slice, slice, int, int, int)) $PendingAthWithdrawal$_constructor_owner_wallet_tombstone_recipient_recipient_ath_wallet_amount_refundable_ton_amount_created_at(slice $owner_wallet, int $tombstone, slice $recipient, slice $recipient_ath_wallet, int $amount, int $refundable_ton_amount, int $created_at) inline;

;; $ATHTransferRequest$_constructor_query_id_amount_recipient_response_destination
((int, int, slice, slice)) $ATHTransferRequest$_constructor_query_id_amount_recipient_response_destination(int $query_id, int $amount, slice $recipient, slice $response_destination) inline;

;; $PublishBatchToHub$_constructor_bounce_id_bounce_tag_publish_id_publish_kind_part_count_protocol_fee_total_author_wallet_parts_marketing
((int, int, int, int, int, int, slice, cell, cell)) $PublishBatchToHub$_constructor_bounce_id_bounce_tag_publish_id_publish_kind_part_count_protocol_fee_total_author_wallet_parts_marketing(int $bounce_id, int $bounce_tag, int $publish_id, int $publish_kind, int $part_count, int $protocol_fee_total, slice $author_wallet, cell $parts, cell $marketing) inline;

;; $ATHTransferRequestVaultProfileAvatar$_constructor_query_id_amount_recipient_response_destination_notify_value_owner_wallet_avatar_hash_avatar_entry_id_avatar_stream_id_avatar_part_count_media_format
((int, int, slice, slice, int, slice, int, int, int, int, int)) $ATHTransferRequestVaultProfileAvatar$_constructor_query_id_amount_recipient_response_destination_notify_value_owner_wallet_avatar_hash_avatar_entry_id_avatar_stream_id_avatar_part_count_media_format(int $query_id, int $amount, slice $recipient, slice $response_destination, int $notify_value, slice $owner_wallet, int $avatar_hash, int $avatar_entry_id, int $avatar_stream_id, int $avatar_part_count, int $media_format) inline;

;; $ATHTransferRequestVaultMintUsername$_constructor_query_id_amount_recipient_response_destination_notify_value_owner_wallet_username_len_username
((int, int, slice, slice, int, slice, int, slice)) $ATHTransferRequestVaultMintUsername$_constructor_query_id_amount_recipient_response_destination_notify_value_owner_wallet_username_len_username(int $query_id, int $amount, slice $recipient, slice $response_destination, int $notify_value, slice $owner_wallet, int $username_len, slice $username) inline;


;; Vault_Vault.stdlib.fc
global (int, slice, int, slice) __tact_context;
global slice __tact_context_sender;
global cell __tact_child_contract_codes;
global int __tact_randomized;

(cell, int) __tact_dict_delete(cell dict, int key_len, slice index) asm(index dict key_len) """
    DICTDEL
""";

(cell, int) __tact_dict_delete_int(cell dict, int key_len, int index) asm(index dict key_len) """
    DICTIDEL
""";

((cell), ()) __tact_dict_set_ref(cell dict, int key_len, slice index, cell value) asm(value index dict key_len) """
    DICTSETREF
""";

(slice, int) __tact_dict_get(cell dict, int key_len, slice index) asm(index dict key_len) """
    DICTGET NULLSWAPIFNOT
""";

(cell, int) __tact_dict_get_ref(cell dict, int key_len, slice index) asm(index dict key_len) """
    DICTGETREF NULLSWAPIFNOT
""";

(int, slice, int, slice) __tact_context_get() inline {
    return __tact_context;
}

slice __tact_context_get_sender() inline {
    return __tact_context_sender;
}

forall X0, X1, X2, X3 -> tuple __tact_tuple_create_4((X0, X1, X2, X3) v) asm """
    4 TUPLE
""";

forall X0, X1, X2, X3 -> (X0, X1, X2, X3) __tact_tuple_destroy_4(tuple v) asm """
    4 UNTUPLE
""";

forall X0, X1, X2, X3, X4, X5 -> tuple __tact_tuple_create_6((X0, X1, X2, X3, X4, X5) v) asm """
    6 TUPLE
""";

forall X0, X1, X2, X3, X4, X5 -> (X0, X1, X2, X3, X4, X5) __tact_tuple_destroy_6(tuple v) asm """
    6 UNTUPLE
""";

forall X0, X1, X2, X3, X4, X5, X6 -> tuple __tact_tuple_create_7((X0, X1, X2, X3, X4, X5, X6) v) asm """
    7 TUPLE
""";

forall X0, X1, X2, X3, X4, X5, X6 -> (X0, X1, X2, X3, X4, X5, X6) __tact_tuple_destroy_7(tuple v) asm """
    7 UNTUPLE
""";

forall X0, X1, X2, X3, X4, X5, X6, X7, X8 -> tuple __tact_tuple_create_9((X0, X1, X2, X3, X4, X5, X6, X7, X8) v) asm """
    9 TUPLE
""";

forall X0, X1, X2, X3, X4, X5, X6, X7, X8 -> (X0, X1, X2, X3, X4, X5, X6, X7, X8) __tact_tuple_destroy_9(tuple v) asm """
    9 UNTUPLE
""";

forall X0, X1, X2, X3, X4, X5, X6, X7, X8, X9, X10, X11 -> tuple __tact_tuple_create_12((X0, X1, X2, X3, X4, X5, X6, X7, X8, X9, X10, X11) v) asm """
    12 TUPLE
""";

forall X0, X1, X2, X3, X4, X5, X6, X7, X8, X9, X10, X11 -> (X0, X1, X2, X3, X4, X5, X6, X7, X8, X9, X10, X11) __tact_tuple_destroy_12(tuple v) asm """
    12 UNTUPLE
""";

cell __tact_dict_get_slice_cell(cell d, int kl, slice k) inline {
    var (r, ok) = __tact_dict_get_ref(d, kl, k);
    if (ok) {
        return r;
    } else {
        return null();
    }
}

(cell, ()) __tact_dict_set_slice_cell(cell d, int kl, slice k, cell v) inline {
    if (null?(v)) {
        var (r, ok) = __tact_dict_delete(d, kl, k);
        return (r, ());
    } else {
        return __tact_dict_set_ref(d, kl, k, v);
    }
}

cell __tact_dict_get_uint_cell(cell d, int kl, int k) inline {
    var (r, ok) = udict_get_ref?(d, kl, k);
    if (ok) {
        return r;
    } else {
        return null();
    }
}

(cell, ()) __tact_dict_set_uint_cell(cell d, int kl, int k, cell v) inline {
    if (null?(v)) {
        var (r, ok) = udict_delete?(d, kl, k);
        return (r, ());
    } else {
        return (udict_set_ref(d, kl, k, v), ());
    }
}

(cell, ()) __tact_dict_set_int_int(cell d, int kl, int k, int v, int vl) inline {
    if (null?(v)) {
        var (r, ok) = idict_delete?(d, kl, k);
        return (r, ());
    } else {
        return (idict_set_builder(d, kl, k, begin_cell().store_int(v, vl)), ());
    }
}

cell __tact_dict_get_int_cell(cell d, int kl, int k) inline {
    var (r, ok) = idict_get_ref?(d, kl, k);
    if (ok) {
        return r;
    } else {
        return null();
    }
}

(cell, ()) __tact_dict_set_int_cell(cell d, int kl, int k, cell v) inline {
    if (null?(v)) {
        var (r, ok) = idict_delete?(d, kl, k);
        return (r, ());
    } else {
        return (idict_set_ref(d, kl, k, v), ());
    }
}

int __tact_dict_exists_slice(cell d, int kl, slice k) inline {
    var (r, ok) = __tact_dict_get(d, kl, k);
    return ok;
}

int __tact_dict_exists_int(cell d, int kl, int k) inline {
    var (r, ok) = idict_get?(d, kl, k);
    return ok;
}

cell $Builder$_fun_endCell(builder $self) impure asm """
    ENDC
""";

builder $Builder$_fun_storeBool(int $value, builder $self) impure asm """
    1 STI
""";

builder $global_beginCell() impure asm """
    NEWC
""";

cell $global_emptyCell() impure asm """
    <b b> PUSHREF // Pure Fift: "<b" creates a builder, "b>" turns it into a cell at compile time
""";

int $global_checkSignature(int $hash, slice $signature, int $publicKey) impure asm """
    CHKSIGNU
""";

int $global_contractHash(cell $code, cell $data) impure asm """
    s0 PUSH HASHCU // `data` hash
    s2 PUSH HASHCU // `code` hash
    SWAP2
    CDEPTH         // `data` depth
    SWAP
    CDEPTH         // `code` depth
    131380 INT     // (2 << 16) | (1 << 8) | 0x34

    // Group 2: Composition of the Builder
    NEWC
    24 STU  // store refs_descriptor | bits_descriptor | data
    16 STU  // store depth_descriptor for `code`
    16 STU  // store depth_descriptor for `data`
    256 STU // store `code` hash
    256 STU // store `data` hash

    // Group 3: SHA256 hash of the resulting Builder
    ONE HASHEXT_SHA256
""";

slice $Slice$_fun_asAddressUnsafe(slice $self) impure asm "NOP";

slice $Cell$_fun_beginParse(cell $self) impure asm """
    CTOS
""";

slice $Cell$_fun_asSlice(cell $self) impure inline {
    var ($self) = $self;
    return $Cell$_fun_beginParse($self);
}

slice $global_newAddress(int $chain, int $hash) impure inline {
    return $Slice$_fun_asAddressUnsafe($Cell$_fun_asSlice($Builder$_fun_endCell(store_uint(store_int(store_uint($global_beginCell(), 4, 3), $chain, 8), $hash, 256))));
}

slice $global_contractAddressExt(int $chain, cell $code, cell $data) impure inline {
    int $hash = $global_contractHash($code, $data);
    return $global_newAddress($chain, $hash);
}

slice $global_myAddress() impure asm """
    MYADDR
""";

int $global_myBalance() impure asm """
    BALANCE FIRST
""";

int $global_getComputeFee(int $gasUsed, int $isMasterchain) impure asm """
    GETGASFEE
""";

int $global_getForwardFee(int $cells, int $bits, int $isMasterchain) impure asm """
    GETFORWARDFEE
""";

() $global_message((int, cell, int, slice, int) $params) impure asm """
    NEWC
    b{01} STSLICECONST  // store tag = $0 and ihr_disabled = true
    1 STI               // store `bounce`
    b{000} STSLICECONST // store bounced = false and src = addr_none
    STSLICE             // store `to`
    SWAP
    STGRAMS             // store `value`
    106 PUSHINT         // 1 + 4 + 4 + 64 + 32 + 1
    STZEROES
    // → Stack state
    // s0: Builder
    // s1: `body`
    // s2: `mode`
    STDICT
    ENDC
    SWAP
    SENDRAWMSG
""";

() $global_acceptMessage() impure asm """
    ACCEPT
""";

() $global_commit() impure asm """
    COMMIT
""";

(int, int) $global_parseStdAddress(slice $slice) impure asm """
    REWRITESTDADDR
""";

int $global_now() impure asm """
    NOW
""";

int $global_curLt() impure asm """
    LTIME
""";

builder $Builder$_fun_storeRef(cell $cell, builder $self) impure asm """
    STREF
""";

builder $Builder$_fun_storeSlice(builder $self, slice $slice) impure asm """
    STSLICER
""";

builder $Builder$_fun_storeAddress(builder $self, slice $address) impure asm """
    STSLICER
""";

(slice, cell) $Slice$_fun_loadRef(slice $self) impure asm( -> 1 0) """
    LDREF
""";

(slice, int) $Slice$_fun_loadBool(slice $self) impure asm( -> 1 0) """
    1 LDI
""";

(slice, ()) $Slice$_fun_skipBits(slice $self, int $l) impure asm """
    SDSKIPFIRST
""";

int $Slice$_fun_refs(slice $self) impure asm """
    SREFS
""";

int $Slice$_fun_bits(slice $self) impure asm """
    SBITS
""";

int $Slice$_fun_empty(slice $self) impure asm """
    SEMPTY
""";

int $Slice$_fun_refsEmpty(slice $self) impure asm """
    SREMPTY
""";

(slice, slice) $Slice$_fun_loadAddress(slice $self) impure asm( -> 1 0) """
    LDMSGADDR
""";

(int, int, int) $Cell$_fun_computeDataSize(cell $self, int $maxCells) impure asm """
    CDATASIZE
""";

int $Cell$_fun_hash(cell $self) impure asm """
    HASHCU
""";

slice $Address$_fun_asSlice(slice $self) impure asm "NOP";

;; Vault_Vault.storage.fc
;;
;; Type: Context
;; TLB: _ bounceable:bool sender:address value:int257 raw:^slice = Context
;;

_ $Context$_get_value((int, slice, int, slice) v) inline {
    var (v'bounceable, v'sender, v'value, v'raw) = v;
    return v'value;
}

;;
;; Type: MessageParameters
;; TLB: _ mode:int257 body:Maybe ^cell value:int257 to:address bounce:bool = MessageParameters
;;

((int, cell, int, slice, int)) $MessageParameters$_constructor_to_value_bounce_mode_body(slice $to, int $value, int $bounce, int $mode, cell $body) inline {
    return ($mode, $body, $value, $to, $bounce);
}

;;
;; Type: AthTransferNotificationAck
;; Header: 0x472d9d7e
;; TLB: ath_transfer_notification_ack#472d9d7e query_id:uint64 amount:uint128 sender_key:uint160 = AthTransferNotificationAck
;;

builder $AthTransferNotificationAck$_store(builder build_0, (int, int, int) v) inline {
    var (v'query_id, v'amount, v'sender_key) = v;
    build_0 = store_uint(build_0, 1194171774, 32);
    build_0 = build_0.store_uint(v'query_id, 64);
    build_0 = build_0.store_uint(v'amount, 128);
    build_0 = build_0.store_uint(v'sender_key, 160);
    return build_0;
}

cell $AthTransferNotificationAck$_store_cell((int, int, int) v, builder b) inline {
    return $AthTransferNotificationAck$_store(b, v).end_cell();
}

((int, int, int)) $AthTransferNotificationAck$_constructor_query_id_sender_key_amount(int $query_id, int $sender_key, int $amount) inline {
    return ($query_id, $amount, $sender_key);
}

;;
;; Type: ATHTransferRequest
;; Header: 0x41544810
;; TLB: ath_transfer_request#41544810 query_id:uint64 amount:uint128 recipient:address response_destination:address = ATHTransferRequest
;;

builder $ATHTransferRequest$_store(builder build_0, (int, int, slice, slice) v) inline {
    var (v'query_id, v'amount, v'recipient, v'response_destination) = v;
    build_0 = store_uint(build_0, 1096042512, 32);
    build_0 = build_0.store_uint(v'query_id, 64);
    build_0 = build_0.store_uint(v'amount, 128);
    build_0 = build_0.store_slice(v'recipient);
    build_0 = build_0.store_slice(v'response_destination);
    return build_0;
}

cell $ATHTransferRequest$_store_cell((int, int, slice, slice) v, builder b) inline {
    return $ATHTransferRequest$_store(b, v).end_cell();
}

(slice, ((int, int))) $ATHTransferRequest$_load_bounced(slice sc_0) inline {
    var v'query_id = sc_0~load_uint(64);
    var v'amount = sc_0~load_uint(128);
    return (sc_0, (v'query_id, v'amount));
}

((int, int, slice, slice)) $ATHTransferRequest$_constructor_query_id_amount_recipient_response_destination(int $query_id, int $amount, slice $recipient, slice $response_destination) inline {
    return ($query_id, $amount, $recipient, $response_destination);
}

;;
;; Type: ATHTransferRequestVaultProfileAvatar
;; Header: 0x4154481a
;; TLB: ath_transfer_request_vault_profile_avatar#4154481a query_id:uint64 amount:uint128 recipient:address response_destination:address notify_value:uint128 owner_wallet:address avatar_hash:uint256 avatar_entry_id:uint64 avatar_stream_id:uint128 avatar_part_count:uint16 media_format:uint8 = ATHTransferRequestVaultProfileAvatar
;;

builder $ATHTransferRequestVaultProfileAvatar$_store(builder build_0, (int, int, slice, slice, int, slice, int, int, int, int, int) v) inline_ref {
    var (v'query_id, v'amount, v'recipient, v'response_destination, v'notify_value, v'owner_wallet, v'avatar_hash, v'avatar_entry_id, v'avatar_stream_id, v'avatar_part_count, v'media_format) = v;
    build_0 = store_uint(build_0, 1096042522, 32);
    build_0 = build_0.store_uint(v'query_id, 64);
    build_0 = build_0.store_uint(v'amount, 128);
    build_0 = build_0.store_slice(v'recipient);
    build_0 = build_0.store_slice(v'response_destination);
    build_0 = build_0.store_uint(v'notify_value, 128);
    var build_1 = begin_cell();
    build_1 = build_1.store_slice(v'owner_wallet);
    build_1 = build_1.store_uint(v'avatar_hash, 256);
    build_1 = build_1.store_uint(v'avatar_entry_id, 64);
    build_1 = build_1.store_uint(v'avatar_stream_id, 128);
    build_1 = build_1.store_uint(v'avatar_part_count, 16);
    build_1 = build_1.store_uint(v'media_format, 8);
    build_0 = store_builder_ref(build_0, build_1);
    return build_0;
}

cell $ATHTransferRequestVaultProfileAvatar$_store_cell((int, int, slice, slice, int, slice, int, int, int, int, int) v, builder b) inline {
    return $ATHTransferRequestVaultProfileAvatar$_store(b, v).end_cell();
}

(slice, ((int, int))) $ATHTransferRequestVaultProfileAvatar$_load_bounced(slice sc_0) inline {
    var v'query_id = sc_0~load_uint(64);
    var v'amount = sc_0~load_uint(128);
    return (sc_0, (v'query_id, v'amount));
}

((int, int, slice, slice, int, slice, int, int, int, int, int)) $ATHTransferRequestVaultProfileAvatar$_constructor_query_id_amount_recipient_response_destination_notify_value_owner_wallet_avatar_hash_avatar_entry_id_avatar_stream_id_avatar_part_count_media_format(int $query_id, int $amount, slice $recipient, slice $response_destination, int $notify_value, slice $owner_wallet, int $avatar_hash, int $avatar_entry_id, int $avatar_stream_id, int $avatar_part_count, int $media_format) inline {
    return ($query_id, $amount, $recipient, $response_destination, $notify_value, $owner_wallet, $avatar_hash, $avatar_entry_id, $avatar_stream_id, $avatar_part_count, $media_format);
}

;;
;; Type: ATHTransferRequestVaultMintUsername
;; Header: 0x4154481c
;; TLB: ath_transfer_request_vault_mint_username#4154481c query_id:uint64 amount:uint128 recipient:address response_destination:address notify_value:uint128 owner_wallet:address username_len:uint8 username:remainder<slice> = ATHTransferRequestVaultMintUsername
;;

builder $ATHTransferRequestVaultMintUsername$_store(builder build_0, (int, int, slice, slice, int, slice, int, slice) v) inline {
    var (v'query_id, v'amount, v'recipient, v'response_destination, v'notify_value, v'owner_wallet, v'username_len, v'username) = v;
    build_0 = store_uint(build_0, 1096042524, 32);
    build_0 = build_0.store_uint(v'query_id, 64);
    build_0 = build_0.store_uint(v'amount, 128);
    build_0 = build_0.store_slice(v'recipient);
    build_0 = build_0.store_slice(v'response_destination);
    build_0 = build_0.store_uint(v'notify_value, 128);
    var build_1 = begin_cell();
    build_1 = build_1.store_slice(v'owner_wallet);
    build_1 = build_1.store_uint(v'username_len, 8);
    build_1 = build_1.store_slice(v'username);
    build_0 = store_builder_ref(build_0, build_1);
    return build_0;
}

cell $ATHTransferRequestVaultMintUsername$_store_cell((int, int, slice, slice, int, slice, int, slice) v, builder b) inline {
    return $ATHTransferRequestVaultMintUsername$_store(b, v).end_cell();
}

(slice, ((int, int))) $ATHTransferRequestVaultMintUsername$_load_bounced(slice sc_0) inline {
    var v'query_id = sc_0~load_uint(64);
    var v'amount = sc_0~load_uint(128);
    return (sc_0, (v'query_id, v'amount));
}

((int, int, slice, slice, int, slice, int, slice)) $ATHTransferRequestVaultMintUsername$_constructor_query_id_amount_recipient_response_destination_notify_value_owner_wallet_username_len_username(int $query_id, int $amount, slice $recipient, slice $response_destination, int $notify_value, slice $owner_wallet, int $username_len, slice $username) inline {
    return ($query_id, $amount, $recipient, $response_destination, $notify_value, $owner_wallet, $username_len, $username);
}

;;
;; Type: PublishBatchToHub
;; Header: 0xa4f862d1
;; TLB: publish_batch_to_hub#a4f862d1 bounce_id:uint64 bounce_tag:uint160 publish_id:uint256 publish_kind:uint8 part_count:uint8 protocol_fee_total:uint128 author_wallet:address parts:^cell marketing:Maybe ^cell = PublishBatchToHub
;;

builder $PublishBatchToHub$_store(builder build_0, (int, int, int, int, int, int, slice, cell, cell) v) inline {
    var (v'bounce_id, v'bounce_tag, v'publish_id, v'publish_kind, v'part_count, v'protocol_fee_total, v'author_wallet, v'parts, v'marketing) = v;
    build_0 = store_uint(build_0, 2767741649, 32);
    build_0 = build_0.store_uint(v'bounce_id, 64);
    build_0 = build_0.store_uint(v'bounce_tag, 160);
    build_0 = build_0.store_uint(v'publish_id, 256);
    build_0 = build_0.store_uint(v'publish_kind, 8);
    build_0 = build_0.store_uint(v'part_count, 8);
    build_0 = build_0.store_uint(v'protocol_fee_total, 128);
    build_0 = build_0.store_slice(v'author_wallet);
    build_0 = build_0.store_ref(v'parts);
    build_0 = build_0.store_maybe_ref(v'marketing);
    return build_0;
}

cell $PublishBatchToHub$_store_cell((int, int, int, int, int, int, slice, cell, cell) v, builder b) inline {
    return $PublishBatchToHub$_store(b, v).end_cell();
}

(slice, ((int, int))) $PublishBatchToHub$_load_bounced(slice sc_0) inline {
    var v'bounce_id = sc_0~load_uint(64);
    var v'bounce_tag = sc_0~load_uint(160);
    return (sc_0, (v'bounce_id, v'bounce_tag));
}

((int, int, int, int, int, int, slice, cell, cell)) $PublishBatchToHub$_constructor_bounce_id_bounce_tag_publish_id_publish_kind_part_count_protocol_fee_total_author_wallet_parts_marketing(int $bounce_id, int $bounce_tag, int $publish_id, int $publish_kind, int $part_count, int $protocol_fee_total, slice $author_wallet, cell $parts, cell $marketing) inline {
    return ($bounce_id, $bounce_tag, $publish_id, $publish_kind, $part_count, $protocol_fee_total, $author_wallet, $parts, $marketing);
}

;;
;; Type: PendingAthWithdrawal
;; TLB: _ owner_wallet:address tombstone:bool recipient:address recipient_ath_wallet:address amount:uint128 refundable_ton_amount:uint128 created_at:uint64 = PendingAthWithdrawal
;;

builder $PendingAthWithdrawal$_store(builder build_0, (slice, int, slice, slice, int, int, int) v) inline {
    var (v'owner_wallet, v'tombstone, v'recipient, v'recipient_ath_wallet, v'amount, v'refundable_ton_amount, v'created_at) = v;
    build_0 = build_0.store_slice(v'owner_wallet);
    build_0 = build_0.store_int(v'tombstone, 1);
    build_0 = build_0.store_slice(v'recipient);
    build_0 = build_0.store_slice(v'recipient_ath_wallet);
    build_0 = build_0.store_uint(v'amount, 128);
    var build_1 = begin_cell();
    build_1 = build_1.store_uint(v'refundable_ton_amount, 128);
    build_1 = build_1.store_uint(v'created_at, 64);
    build_0 = store_builder_ref(build_0, build_1);
    return build_0;
}

cell $PendingAthWithdrawal$_store_cell((slice, int, slice, slice, int, int, int) v, builder b) inline {
    return $PendingAthWithdrawal$_store(b, v).end_cell();
}

(slice, ((slice, int, slice, slice, int, int, int))) $PendingAthWithdrawal$_load(slice sc_0) inline {
    var v'owner_wallet = sc_0~load_msg_addr();
    var v'tombstone = sc_0~load_int(1);
    var v'recipient = sc_0~load_msg_addr();
    var v'recipient_ath_wallet = sc_0~load_msg_addr();
    var v'amount = sc_0~load_uint(128);
    slice sc_1 = sc_0~load_ref().begin_parse();
    var v'refundable_ton_amount = sc_1~load_uint(128);
    var v'created_at = sc_1~load_uint(64);
    return (sc_0, (v'owner_wallet, v'tombstone, v'recipient, v'recipient_ath_wallet, v'amount, v'refundable_ton_amount, v'created_at));
}

tuple $PendingAthWithdrawal$_as_optional((slice, int, slice, slice, int, int, int) v) inline {
    var (v'owner_wallet, v'tombstone, v'recipient, v'recipient_ath_wallet, v'amount, v'refundable_ton_amount, v'created_at) = v;
    return __tact_tuple_create_7(v'owner_wallet, v'tombstone, v'recipient, v'recipient_ath_wallet, v'amount, v'refundable_ton_amount, v'created_at);
}

tuple $PendingAthWithdrawal$_load_opt(cell cl) inline {
    if (null?(cl)) {
        return null();
    }
    var sc = cl.begin_parse();
    return $PendingAthWithdrawal$_as_optional(sc~$PendingAthWithdrawal$_load());
}

((slice, int, slice, slice, int, int, int)) $PendingAthWithdrawal$_not_null(tuple v) inline {
    var (slice vvv'owner_wallet, int vvv'tombstone, slice vvv'recipient, slice vvv'recipient_ath_wallet, int vvv'amount, int vvv'refundable_ton_amount, int vvv'created_at) = __tact_tuple_destroy_7(v);
    return (vvv'owner_wallet, vvv'tombstone, vvv'recipient, vvv'recipient_ath_wallet, vvv'amount, vvv'refundable_ton_amount, vvv'created_at);
}

((slice, int, slice, slice, int, int, int)) $PendingAthWithdrawal$_constructor_owner_wallet_tombstone_recipient_recipient_ath_wallet_amount_refundable_ton_amount_created_at(slice $owner_wallet, int $tombstone, slice $recipient, slice $recipient_ath_wallet, int $amount, int $refundable_ton_amount, int $created_at) inline {
    return ($owner_wallet, $tombstone, $recipient, $recipient_ath_wallet, $amount, $refundable_ton_amount, $created_at);
}

;;
;; Type: PendingBatchPublish
;; TLB: _ owner_wallet:address tombstone:bool refund_to_vault:bool nonce:uint64 publish_kind:uint8 part_count:uint8 publish_id:uint256 refundable_amount:uint128 created_at:uint64 = PendingBatchPublish
;;

builder $PendingBatchPublish$_store(builder build_0, (slice, int, int, int, int, int, int, int, int) v) inline {
    var (v'owner_wallet, v'tombstone, v'refund_to_vault, v'nonce, v'publish_kind, v'part_count, v'publish_id, v'refundable_amount, v'created_at) = v;
    build_0 = build_0.store_slice(v'owner_wallet);
    build_0 = build_0.store_int(v'tombstone, 1);
    build_0 = build_0.store_int(v'refund_to_vault, 1);
    build_0 = build_0.store_uint(v'nonce, 64);
    build_0 = build_0.store_uint(v'publish_kind, 8);
    build_0 = build_0.store_uint(v'part_count, 8);
    build_0 = build_0.store_uint(v'publish_id, 256);
    build_0 = build_0.store_uint(v'refundable_amount, 128);
    build_0 = build_0.store_uint(v'created_at, 64);
    return build_0;
}

cell $PendingBatchPublish$_store_cell((slice, int, int, int, int, int, int, int, int) v, builder b) inline {
    return $PendingBatchPublish$_store(b, v).end_cell();
}

(slice, ((slice, int, int, int, int, int, int, int, int))) $PendingBatchPublish$_load(slice sc_0) inline {
    var v'owner_wallet = sc_0~load_msg_addr();
    var v'tombstone = sc_0~load_int(1);
    var v'refund_to_vault = sc_0~load_int(1);
    var v'nonce = sc_0~load_uint(64);
    var v'publish_kind = sc_0~load_uint(8);
    var v'part_count = sc_0~load_uint(8);
    var v'publish_id = sc_0~load_uint(256);
    var v'refundable_amount = sc_0~load_uint(128);
    var v'created_at = sc_0~load_uint(64);
    return (sc_0, (v'owner_wallet, v'tombstone, v'refund_to_vault, v'nonce, v'publish_kind, v'part_count, v'publish_id, v'refundable_amount, v'created_at));
}

tuple $PendingBatchPublish$_as_optional((slice, int, int, int, int, int, int, int, int) v) inline {
    var (v'owner_wallet, v'tombstone, v'refund_to_vault, v'nonce, v'publish_kind, v'part_count, v'publish_id, v'refundable_amount, v'created_at) = v;
    return __tact_tuple_create_9(v'owner_wallet, v'tombstone, v'refund_to_vault, v'nonce, v'publish_kind, v'part_count, v'publish_id, v'refundable_amount, v'created_at);
}

tuple $PendingBatchPublish$_load_opt(cell cl) inline {
    if (null?(cl)) {
        return null();
    }
    var sc = cl.begin_parse();
    return $PendingBatchPublish$_as_optional(sc~$PendingBatchPublish$_load());
}

((slice, int, int, int, int, int, int, int, int)) $PendingBatchPublish$_not_null(tuple v) inline {
    var (slice vvv'owner_wallet, int vvv'tombstone, int vvv'refund_to_vault, int vvv'nonce, int vvv'publish_kind, int vvv'part_count, int vvv'publish_id, int vvv'refundable_amount, int vvv'created_at) = __tact_tuple_destroy_9(v);
    return (vvv'owner_wallet, vvv'tombstone, vvv'refund_to_vault, vvv'nonce, vvv'publish_kind, vvv'part_count, vvv'publish_id, vvv'refundable_amount, vvv'created_at);
}

((slice, int, int, int, int, int, int, int, int)) $PendingBatchPublish$_constructor_owner_wallet_tombstone_refund_to_vault_nonce_publish_kind_part_count_publish_id_refundable_amount_created_at(slice $owner_wallet, int $tombstone, int $refund_to_vault, int $nonce, int $publish_kind, int $part_count, int $publish_id, int $refundable_amount, int $created_at) inline {
    return ($owner_wallet, $tombstone, $refund_to_vault, $nonce, $publish_kind, $part_count, $publish_id, $refundable_amount, $created_at);
}

;;
;; Type: PendingProfileAvatarPayment
;; TLB: _ owner_wallet:address tombstone:bool amount:uint128 created_at:uint64 = PendingProfileAvatarPayment
;;

builder $PendingProfileAvatarPayment$_store(builder build_0, (slice, int, int, int) v) inline {
    var (v'owner_wallet, v'tombstone, v'amount, v'created_at) = v;
    build_0 = build_0.store_slice(v'owner_wallet);
    build_0 = build_0.store_int(v'tombstone, 1);
    build_0 = build_0.store_uint(v'amount, 128);
    build_0 = build_0.store_uint(v'created_at, 64);
    return build_0;
}

cell $PendingProfileAvatarPayment$_store_cell((slice, int, int, int) v, builder b) inline {
    return $PendingProfileAvatarPayment$_store(b, v).end_cell();
}

(slice, ((slice, int, int, int))) $PendingProfileAvatarPayment$_load(slice sc_0) inline {
    var v'owner_wallet = sc_0~load_msg_addr();
    var v'tombstone = sc_0~load_int(1);
    var v'amount = sc_0~load_uint(128);
    var v'created_at = sc_0~load_uint(64);
    return (sc_0, (v'owner_wallet, v'tombstone, v'amount, v'created_at));
}

tuple $PendingProfileAvatarPayment$_as_optional((slice, int, int, int) v) inline {
    var (v'owner_wallet, v'tombstone, v'amount, v'created_at) = v;
    return __tact_tuple_create_4(v'owner_wallet, v'tombstone, v'amount, v'created_at);
}

tuple $PendingProfileAvatarPayment$_load_opt(cell cl) inline {
    if (null?(cl)) {
        return null();
    }
    var sc = cl.begin_parse();
    return $PendingProfileAvatarPayment$_as_optional(sc~$PendingProfileAvatarPayment$_load());
}

((slice, int, int, int)) $PendingProfileAvatarPayment$_not_null(tuple v) inline {
    var (slice vvv'owner_wallet, int vvv'tombstone, int vvv'amount, int vvv'created_at) = __tact_tuple_destroy_4(v);
    return (vvv'owner_wallet, vvv'tombstone, vvv'amount, vvv'created_at);
}

((slice, int, int, int)) $PendingProfileAvatarPayment$_constructor_owner_wallet_tombstone_amount_created_at(slice $owner_wallet, int $tombstone, int $amount, int $created_at) inline {
    return ($owner_wallet, $tombstone, $amount, $created_at);
}

;;
;; Type: PendingUsernameMintPayment
;; TLB: _ owner_wallet:address tombstone:bool amount:uint128 created_at:uint64 = PendingUsernameMintPayment
;;

builder $PendingUsernameMintPayment$_store(builder build_0, (slice, int, int, int) v) inline {
    var (v'owner_wallet, v'tombstone, v'amount, v'created_at) = v;
    build_0 = build_0.store_slice(v'owner_wallet);
    build_0 = build_0.store_int(v'tombstone, 1);
    build_0 = build_0.store_uint(v'amount, 128);
    build_0 = build_0.store_uint(v'created_at, 64);
    return build_0;
}

cell $PendingUsernameMintPayment$_store_cell((slice, int, int, int) v, builder b) inline {
    return $PendingUsernameMintPayment$_store(b, v).end_cell();
}

(slice, ((slice, int, int, int))) $PendingUsernameMintPayment$_load(slice sc_0) inline {
    var v'owner_wallet = sc_0~load_msg_addr();
    var v'tombstone = sc_0~load_int(1);
    var v'amount = sc_0~load_uint(128);
    var v'created_at = sc_0~load_uint(64);
    return (sc_0, (v'owner_wallet, v'tombstone, v'amount, v'created_at));
}

tuple $PendingUsernameMintPayment$_as_optional((slice, int, int, int) v) inline {
    var (v'owner_wallet, v'tombstone, v'amount, v'created_at) = v;
    return __tact_tuple_create_4(v'owner_wallet, v'tombstone, v'amount, v'created_at);
}

tuple $PendingUsernameMintPayment$_load_opt(cell cl) inline {
    if (null?(cl)) {
        return null();
    }
    var sc = cl.begin_parse();
    return $PendingUsernameMintPayment$_as_optional(sc~$PendingUsernameMintPayment$_load());
}

((slice, int, int, int)) $PendingUsernameMintPayment$_not_null(tuple v) inline {
    var (slice vvv'owner_wallet, int vvv'tombstone, int vvv'amount, int vvv'created_at) = __tact_tuple_destroy_4(v);
    return (vvv'owner_wallet, vvv'tombstone, vvv'amount, vvv'created_at);
}

((slice, int, int, int)) $PendingUsernameMintPayment$_constructor_owner_wallet_tombstone_amount_created_at(slice $owner_wallet, int $tombstone, int $amount, int $created_at) inline {
    return ($owner_wallet, $tombstone, $amount, $created_at);
}

;;
;; Type: KeyRecord
;; TLB: _ owner_wallet:address key_generation:uint32 enc_pubkey:uint256 sign_pubkey:uint256 pq_kem_pubkey_hash:uint256 pq_kem_pubkey_len:uint16 pq_kem_pubkey:^cell crypto_suite_mask:uint16 created_at:uint64 created_lt:uint64 revoked_at:uint64 revoked_lt:uint64 = KeyRecord
;;

builder $KeyRecord$_store(builder build_0, (slice, int, int, int, int, int, cell, int, int, int, int, int) v) inline_ref {
    var (v'owner_wallet, v'key_generation, v'enc_pubkey, v'sign_pubkey, v'pq_kem_pubkey_hash, v'pq_kem_pubkey_len, v'pq_kem_pubkey, v'crypto_suite_mask, v'created_at, v'created_lt, v'revoked_at, v'revoked_lt) = v;
    build_0 = build_0.store_slice(v'owner_wallet);
    build_0 = build_0.store_uint(v'key_generation, 32);
    build_0 = build_0.store_uint(v'enc_pubkey, 256);
    build_0 = build_0.store_uint(v'sign_pubkey, 256);
    var build_1 = begin_cell();
    build_1 = build_1.store_uint(v'pq_kem_pubkey_hash, 256);
    build_1 = build_1.store_uint(v'pq_kem_pubkey_len, 16);
    build_1 = build_1.store_ref(v'pq_kem_pubkey);
    build_1 = build_1.store_uint(v'crypto_suite_mask, 16);
    build_1 = build_1.store_uint(v'created_at, 64);
    build_1 = build_1.store_uint(v'created_lt, 64);
    build_1 = build_1.store_uint(v'revoked_at, 64);
    build_1 = build_1.store_uint(v'revoked_lt, 64);
    build_0 = store_builder_ref(build_0, build_1);
    return build_0;
}

cell $KeyRecord$_store_cell((slice, int, int, int, int, int, cell, int, int, int, int, int) v, builder b) inline {
    return $KeyRecord$_store(b, v).end_cell();
}

(slice, ((slice, int, int, int, int, int, cell, int, int, int, int, int))) $KeyRecord$_load(slice sc_0) inline_ref {
    var v'owner_wallet = sc_0~load_msg_addr();
    var v'key_generation = sc_0~load_uint(32);
    var v'enc_pubkey = sc_0~load_uint(256);
    var v'sign_pubkey = sc_0~load_uint(256);
    slice sc_1 = sc_0~load_ref().begin_parse();
    var v'pq_kem_pubkey_hash = sc_1~load_uint(256);
    var v'pq_kem_pubkey_len = sc_1~load_uint(16);
    var v'pq_kem_pubkey = sc_1~load_ref();
    var v'crypto_suite_mask = sc_1~load_uint(16);
    var v'created_at = sc_1~load_uint(64);
    var v'created_lt = sc_1~load_uint(64);
    var v'revoked_at = sc_1~load_uint(64);
    var v'revoked_lt = sc_1~load_uint(64);
    return (sc_0, (v'owner_wallet, v'key_generation, v'enc_pubkey, v'sign_pubkey, v'pq_kem_pubkey_hash, v'pq_kem_pubkey_len, v'pq_kem_pubkey, v'crypto_suite_mask, v'created_at, v'created_lt, v'revoked_at, v'revoked_lt));
}

tuple $KeyRecord$_as_optional((slice, int, int, int, int, int, cell, int, int, int, int, int) v) inline {
    var (v'owner_wallet, v'key_generation, v'enc_pubkey, v'sign_pubkey, v'pq_kem_pubkey_hash, v'pq_kem_pubkey_len, v'pq_kem_pubkey, v'crypto_suite_mask, v'created_at, v'created_lt, v'revoked_at, v'revoked_lt) = v;
    return __tact_tuple_create_12(v'owner_wallet, v'key_generation, v'enc_pubkey, v'sign_pubkey, v'pq_kem_pubkey_hash, v'pq_kem_pubkey_len, v'pq_kem_pubkey, v'crypto_suite_mask, v'created_at, v'created_lt, v'revoked_at, v'revoked_lt);
}

tuple $KeyRecord$_load_opt(cell cl) inline {
    if (null?(cl)) {
        return null();
    }
    var sc = cl.begin_parse();
    return $KeyRecord$_as_optional(sc~$KeyRecord$_load());
}

((slice, int, int, int, int, int, cell, int, int, int, int, int)) $KeyRecord$_not_null(tuple v) inline {
    var (slice vvv'owner_wallet, int vvv'key_generation, int vvv'enc_pubkey, int vvv'sign_pubkey, int vvv'pq_kem_pubkey_hash, int vvv'pq_kem_pubkey_len, cell vvv'pq_kem_pubkey, int vvv'crypto_suite_mask, int vvv'created_at, int vvv'created_lt, int vvv'revoked_at, int vvv'revoked_lt) = __tact_tuple_destroy_12(v);
    return (vvv'owner_wallet, vvv'key_generation, vvv'enc_pubkey, vvv'sign_pubkey, vvv'pq_kem_pubkey_hash, vvv'pq_kem_pubkey_len, vvv'pq_kem_pubkey, vvv'crypto_suite_mask, vvv'created_at, vvv'created_lt, vvv'revoked_at, vvv'revoked_lt);
}

((slice, int, int, int, int, int, cell, int, int, int, int, int)) $KeyRecord$_constructor_owner_wallet_key_generation_enc_pubkey_sign_pubkey_pq_kem_pubkey_hash_pq_kem_pubkey_len_pq_kem_pubkey_crypto_suite_mask_created_at_created_lt_revoked_at_revoked_lt(slice $owner_wallet, int $key_generation, int $enc_pubkey, int $sign_pubkey, int $pq_kem_pubkey_hash, int $pq_kem_pubkey_len, cell $pq_kem_pubkey, int $crypto_suite_mask, int $created_at, int $created_lt, int $revoked_at, int $revoked_lt) inline {
    return ($owner_wallet, $key_generation, $enc_pubkey, $sign_pubkey, $pq_kem_pubkey_hash, $pq_kem_pubkey_len, $pq_kem_pubkey, $crypto_suite_mask, $created_at, $created_lt, $revoked_at, $revoked_lt);
}

;;
;; Type: ReceiptSlot
;; TLB: _ nonce:uint64 action:uint8 result:uint8 aux:uint64 part_count:uint8 at:uint64 = ReceiptSlot
;;

builder $ReceiptSlot$_store(builder build_0, (int, int, int, int, int, int) v) inline {
    var (v'nonce, v'action, v'result, v'aux, v'part_count, v'at) = v;
    build_0 = build_0.store_uint(v'nonce, 64);
    build_0 = build_0.store_uint(v'action, 8);
    build_0 = build_0.store_uint(v'result, 8);
    build_0 = build_0.store_uint(v'aux, 64);
    build_0 = build_0.store_uint(v'part_count, 8);
    build_0 = build_0.store_uint(v'at, 64);
    return build_0;
}

cell $ReceiptSlot$_store_cell((int, int, int, int, int, int) v, builder b) inline {
    return $ReceiptSlot$_store(b, v).end_cell();
}

(slice, ((int, int, int, int, int, int))) $ReceiptSlot$_load(slice sc_0) inline {
    var v'nonce = sc_0~load_uint(64);
    var v'action = sc_0~load_uint(8);
    var v'result = sc_0~load_uint(8);
    var v'aux = sc_0~load_uint(64);
    var v'part_count = sc_0~load_uint(8);
    var v'at = sc_0~load_uint(64);
    return (sc_0, (v'nonce, v'action, v'result, v'aux, v'part_count, v'at));
}

tuple $ReceiptSlot$_as_optional((int, int, int, int, int, int) v) inline {
    var (v'nonce, v'action, v'result, v'aux, v'part_count, v'at) = v;
    return __tact_tuple_create_6(v'nonce, v'action, v'result, v'aux, v'part_count, v'at);
}

tuple $ReceiptSlot$_load_opt(cell cl) inline {
    if (null?(cl)) {
        return null();
    }
    var sc = cl.begin_parse();
    return $ReceiptSlot$_as_optional(sc~$ReceiptSlot$_load());
}

((int, int, int, int, int, int)) $ReceiptSlot$_not_null(tuple v) inline {
    var (int vvv'nonce, int vvv'action, int vvv'result, int vvv'aux, int vvv'part_count, int vvv'at) = __tact_tuple_destroy_6(v);
    return (vvv'nonce, vvv'action, vvv'result, vvv'aux, vvv'part_count, vvv'at);
}

((int, int, int, int, int, int)) $ReceiptSlot$_constructor_nonce_action_result_aux_part_count_at(int $nonce, int $action, int $result, int $aux, int $part_count, int $at) inline {
    return ($nonce, $action, $result, $aux, $part_count, $at);
}

;;
;; Type: UserState
;; TLB: _ ton_balance:uint128 ath_balance:uint128 current_key_id:uint256 auth_pubkey:uint256 publish_nonce:uint64 last_active:uint64 receipts:dict<uint8, ^ReceiptSlot{nonce:uint64,action:uint8,result:uint8,aux:uint64,part_count:uint8,at:uint64}> = UserState
;;

builder $UserState$_store(builder build_0, (int, int, int, int, int, int, cell) v) inline {
    var (v'ton_balance, v'ath_balance, v'current_key_id, v'auth_pubkey, v'publish_nonce, v'last_active, v'receipts) = v;
    build_0 = build_0.store_uint(v'ton_balance, 128);
    build_0 = build_0.store_uint(v'ath_balance, 128);
    build_0 = build_0.store_uint(v'current_key_id, 256);
    build_0 = build_0.store_uint(v'auth_pubkey, 256);
    build_0 = build_0.store_uint(v'publish_nonce, 64);
    build_0 = build_0.store_uint(v'last_active, 64);
    build_0 = build_0.store_dict(v'receipts);
    return build_0;
}

cell $UserState$_store_cell((int, int, int, int, int, int, cell) v, builder b) inline {
    return $UserState$_store(b, v).end_cell();
}

(slice, ((int, int, int, int, int, int, cell))) $UserState$_load(slice sc_0) inline {
    var v'ton_balance = sc_0~load_uint(128);
    var v'ath_balance = sc_0~load_uint(128);
    var v'current_key_id = sc_0~load_uint(256);
    var v'auth_pubkey = sc_0~load_uint(256);
    var v'publish_nonce = sc_0~load_uint(64);
    var v'last_active = sc_0~load_uint(64);
    var v'receipts = sc_0~load_dict();
    return (sc_0, (v'ton_balance, v'ath_balance, v'current_key_id, v'auth_pubkey, v'publish_nonce, v'last_active, v'receipts));
}

tuple $UserState$_as_optional((int, int, int, int, int, int, cell) v) inline {
    var (v'ton_balance, v'ath_balance, v'current_key_id, v'auth_pubkey, v'publish_nonce, v'last_active, v'receipts) = v;
    return __tact_tuple_create_7(v'ton_balance, v'ath_balance, v'current_key_id, v'auth_pubkey, v'publish_nonce, v'last_active, v'receipts);
}

tuple $UserState$_load_opt(cell cl) inline {
    if (null?(cl)) {
        return null();
    }
    var sc = cl.begin_parse();
    return $UserState$_as_optional(sc~$UserState$_load());
}

((int, int, int, int, int, int, cell)) $UserState$_not_null(tuple v) inline {
    var (int vvv'ton_balance, int vvv'ath_balance, int vvv'current_key_id, int vvv'auth_pubkey, int vvv'publish_nonce, int vvv'last_active, cell vvv'receipts) = __tact_tuple_destroy_7(v);
    return (vvv'ton_balance, vvv'ath_balance, vvv'current_key_id, vvv'auth_pubkey, vvv'publish_nonce, vvv'last_active, vvv'receipts);
}

((int, int, int, int, int, int, cell)) $UserState$_constructor_ton_balance_ath_balance_current_key_id_auth_pubkey_publish_nonce_last_active_receipts(int $ton_balance, int $ath_balance, int $current_key_id, int $auth_pubkey, int $publish_nonce, int $last_active, cell $receipts) inline {
    return ($ton_balance, $ath_balance, $current_key_id, $auth_pubkey, $publish_nonce, $last_active, $receipts);
}

;;
;; Type: VaultKeyRecordView
;; TLB: _ exists:bool owner_wallet:address key_generation:int257 enc_pubkey:int257 sign_pubkey:int257 pq_kem_pubkey_hash:int257 pq_kem_pubkey_len:int257 pq_kem_pubkey:^cell crypto_suite_mask:int257 created_at:int257 created_lt:int257 revoked_at:int257 revoked_lt:int257 = VaultKeyRecordView
;;

(int, slice, int, int, int, int, int, cell, int, int, int, int, int) $VaultKeyRecordView$_to_external(((int, slice, int, int, int, int, int, cell, int, int, int, int, int)) v) inline {
    var (v'exists, v'owner_wallet, v'key_generation, v'enc_pubkey, v'sign_pubkey, v'pq_kem_pubkey_hash, v'pq_kem_pubkey_len, v'pq_kem_pubkey, v'crypto_suite_mask, v'created_at, v'created_lt, v'revoked_at, v'revoked_lt) = v; 
    return (v'exists, v'owner_wallet, v'key_generation, v'enc_pubkey, v'sign_pubkey, v'pq_kem_pubkey_hash, v'pq_kem_pubkey_len, v'pq_kem_pubkey, v'crypto_suite_mask, v'created_at, v'created_lt, v'revoked_at, v'revoked_lt);
}

((int, slice, int, int, int, int, int, cell, int, int, int, int, int)) $VaultKeyRecordView$_constructor_exists_owner_wallet_key_generation_enc_pubkey_sign_pubkey_pq_kem_pubkey_hash_pq_kem_pubkey_len_pq_kem_pubkey_crypto_suite_mask_created_at_created_lt_revoked_at_revoked_lt(int $exists, slice $owner_wallet, int $key_generation, int $enc_pubkey, int $sign_pubkey, int $pq_kem_pubkey_hash, int $pq_kem_pubkey_len, cell $pq_kem_pubkey, int $crypto_suite_mask, int $created_at, int $created_lt, int $revoked_at, int $revoked_lt) inline {
    return ($exists, $owner_wallet, $key_generation, $enc_pubkey, $sign_pubkey, $pq_kem_pubkey_hash, $pq_kem_pubkey_len, $pq_kem_pubkey, $crypto_suite_mask, $created_at, $created_lt, $revoked_at, $revoked_lt);
}

;;
;; Type: VaultUserView
;; TLB: _ exists:bool ton_balance:int257 ath_balance:int257 current_key_id:int257 auth_pubkey:int257 publish_nonce:int257 last_active:int257 = VaultUserView
;;

(int, int, int, int, int, int, int) $VaultUserView$_to_external(((int, int, int, int, int, int, int)) v) inline {
    var (v'exists, v'ton_balance, v'ath_balance, v'current_key_id, v'auth_pubkey, v'publish_nonce, v'last_active) = v; 
    return (v'exists, v'ton_balance, v'ath_balance, v'current_key_id, v'auth_pubkey, v'publish_nonce, v'last_active);
}

((int, int, int, int, int, int, int)) $VaultUserView$_constructor_exists_ton_balance_ath_balance_current_key_id_auth_pubkey_publish_nonce_last_active(int $exists, int $ton_balance, int $ath_balance, int $current_key_id, int $auth_pubkey, int $publish_nonce, int $last_active) inline {
    return ($exists, $ton_balance, $ath_balance, $current_key_id, $auth_pubkey, $publish_nonce, $last_active);
}

;;
;; Type: VaultStorageHealthView
;; TLB: _ balance:int257 storage_reserve:int257 user_count:int257 key_record_count:int257 surplus:int257 = VaultStorageHealthView
;;

(int, int, int, int, int) $VaultStorageHealthView$_to_external(((int, int, int, int, int)) v) inline {
    var (v'balance, v'storage_reserve, v'user_count, v'key_record_count, v'surplus) = v; 
    return (v'balance, v'storage_reserve, v'user_count, v'key_record_count, v'surplus);
}

((int, int, int, int, int)) $VaultStorageHealthView$_constructor_balance_storage_reserve_user_count_key_record_count_surplus(int $balance, int $storage_reserve, int $user_count, int $key_record_count, int $surplus) inline {
    return ($balance, $storage_reserve, $user_count, $key_record_count, $surplus);
}

;;
;; Type: VaultUserReceiptsView
;; TLB: _ exists:bool publish_nonce:int257 receipts:dict<uint8, ^ReceiptSlot{nonce:uint64,action:uint8,result:uint8,aux:uint64,part_count:uint8,at:uint64}> = VaultUserReceiptsView
;;

(int, int, cell) $VaultUserReceiptsView$_to_external(((int, int, cell)) v) inline {
    var (v'exists, v'publish_nonce, v'receipts) = v; 
    return (v'exists, v'publish_nonce, v'receipts);
}

((int, int, cell)) $VaultUserReceiptsView$_constructor_exists_publish_nonce_receipts(int $exists, int $publish_nonce, cell $receipts) inline {
    return ($exists, $publish_nonce, $receipts);
}

;;
;; Type: VaultPendingAthWithdrawalView
;; TLB: _ exists:bool owner_wallet:address recipient:address recipient_ath_wallet:address amount:int257 created_at:int257 = VaultPendingAthWithdrawalView
;;

(int, slice, slice, slice, int, int) $VaultPendingAthWithdrawalView$_to_external(((int, slice, slice, slice, int, int)) v) inline {
    var (v'exists, v'owner_wallet, v'recipient, v'recipient_ath_wallet, v'amount, v'created_at) = v; 
    return (v'exists, v'owner_wallet, v'recipient, v'recipient_ath_wallet, v'amount, v'created_at);
}

((int, slice, slice, slice, int, int)) $VaultPendingAthWithdrawalView$_constructor_exists_owner_wallet_recipient_recipient_ath_wallet_amount_created_at(int $exists, slice $owner_wallet, slice $recipient, slice $recipient_ath_wallet, int $amount, int $created_at) inline {
    return ($exists, $owner_wallet, $recipient, $recipient_ath_wallet, $amount, $created_at);
}

;;
;; Type: VaultSuccessorView
;; TLB: _ announced:bool successor_manifest_hash:int257 successor_vault:address announced_at:int257 = VaultSuccessorView
;;

(int, int, slice, int) $VaultSuccessorView$_to_external(((int, int, slice, int)) v) inline {
    var (v'announced, v'successor_manifest_hash, v'successor_vault, v'announced_at) = v; 
    return (v'announced, v'successor_manifest_hash, v'successor_vault, v'announced_at);
}

((int, int, slice, int)) $VaultSuccessorView$_constructor_announced_successor_manifest_hash_successor_vault_announced_at(int $announced, int $successor_manifest_hash, slice $successor_vault, int $announced_at) inline {
    return ($announced, $successor_manifest_hash, $successor_vault, $announced_at);
}

;;
;; Type: VaultPendingBatchPublishView
;; TLB: _ exists:bool owner_wallet:address nonce:int257 publish_kind:int257 part_count:int257 created_at:int257 tombstone:bool = VaultPendingBatchPublishView
;;

(int, slice, int, int, int, int, int) $VaultPendingBatchPublishView$_to_external(((int, slice, int, int, int, int, int)) v) inline {
    var (v'exists, v'owner_wallet, v'nonce, v'publish_kind, v'part_count, v'created_at, v'tombstone) = v; 
    return (v'exists, v'owner_wallet, v'nonce, v'publish_kind, v'part_count, v'created_at, v'tombstone);
}

((int, slice, int, int, int, int, int)) $VaultPendingBatchPublishView$_constructor_exists_owner_wallet_nonce_publish_kind_part_count_created_at_tombstone(int $exists, slice $owner_wallet, int $nonce, int $publish_kind, int $part_count, int $created_at, int $tombstone) inline {
    return ($exists, $owner_wallet, $nonce, $publish_kind, $part_count, $created_at, $tombstone);
}

;;
;; Type: VaultGlobalView
;; TLB: _ sealed:bool capsule_hub_bound:bool profile_registry_bound:bool username_registry_bound:bool deployment_manifest_hash:int257 capsule_hub_address:address profile_registry_address:address username_registry_address:address vault_ath_wallet_address:address ath_master_address:address user_count:int257 key_record_count:int257 pending_ath_withdrawal_count:int257 pending_publish_count:int257 pending_profile_avatar_payment_count:int257 pending_username_mint_payment_count:int257 processed_ath_deposit_count:int257 pending_publish_stale_ttl:int257 airdrop_remaining_ath:int257 airdrop_distributed_ath:int257 airdrop_reward_per_message_ath:int257 airdrop_total_allocation_ath:int257 = VaultGlobalView
;;

(int, int, int, int, int, slice, slice, slice, slice, slice, int, int, int, int, int, int, int, int, int, int, int, int) $VaultGlobalView$_to_external(((int, int, int, int, int, slice, slice, slice, slice, slice, int, int, int, int, int, int, int, int, int, int, int, int)) v) inline {
    var (v'sealed, v'capsule_hub_bound, v'profile_registry_bound, v'username_registry_bound, v'deployment_manifest_hash, v'capsule_hub_address, v'profile_registry_address, v'username_registry_address, v'vault_ath_wallet_address, v'ath_master_address, v'user_count, v'key_record_count, v'pending_ath_withdrawal_count, v'pending_publish_count, v'pending_profile_avatar_payment_count, v'pending_username_mint_payment_count, v'processed_ath_deposit_count, v'pending_publish_stale_ttl, v'airdrop_remaining_ath, v'airdrop_distributed_ath, v'airdrop_reward_per_message_ath, v'airdrop_total_allocation_ath) = v; 
    return (v'sealed, v'capsule_hub_bound, v'profile_registry_bound, v'username_registry_bound, v'deployment_manifest_hash, v'capsule_hub_address, v'profile_registry_address, v'username_registry_address, v'vault_ath_wallet_address, v'ath_master_address, v'user_count, v'key_record_count, v'pending_ath_withdrawal_count, v'pending_publish_count, v'pending_profile_avatar_payment_count, v'pending_username_mint_payment_count, v'processed_ath_deposit_count, v'pending_publish_stale_ttl, v'airdrop_remaining_ath, v'airdrop_distributed_ath, v'airdrop_reward_per_message_ath, v'airdrop_total_allocation_ath);
}

((int, int, int, int, int, slice, slice, slice, slice, slice, int, int, int, int, int, int, int, int, int, int, int, int)) $VaultGlobalView$_constructor_sealed_capsule_hub_bound_profile_registry_bound_username_registry_bound_deployment_manifest_hash_capsule_hub_address_profile_registry_address_username_registry_address_vault_ath_wallet_address_ath_master_address_user_count_key_record_count_pending_ath_withdrawal_count_pending_publish_count_pending_profile_avatar_payment_count_pending_username_mint_payment_count_processed_ath_deposit_count_pending_publish_stale_ttl_airdrop_remaining_ath_airdrop_distributed_ath_airdrop_reward_per_message_ath_airdrop_total_allocation_ath(int $sealed, int $capsule_hub_bound, int $profile_registry_bound, int $username_registry_bound, int $deployment_manifest_hash, slice $capsule_hub_address, slice $profile_registry_address, slice $username_registry_address, slice $vault_ath_wallet_address, slice $ath_master_address, int $user_count, int $key_record_count, int $pending_ath_withdrawal_count, int $pending_publish_count, int $pending_profile_avatar_payment_count, int $pending_username_mint_payment_count, int $processed_ath_deposit_count, int $pending_publish_stale_ttl, int $airdrop_remaining_ath, int $airdrop_distributed_ath, int $airdrop_reward_per_message_ath, int $airdrop_total_allocation_ath) inline {
    return ($sealed, $capsule_hub_bound, $profile_registry_bound, $username_registry_bound, $deployment_manifest_hash, $capsule_hub_address, $profile_registry_address, $username_registry_address, $vault_ath_wallet_address, $ath_master_address, $user_count, $key_record_count, $pending_ath_withdrawal_count, $pending_publish_count, $pending_profile_avatar_payment_count, $pending_username_mint_payment_count, $processed_ath_deposit_count, $pending_publish_stale_ttl, $airdrop_remaining_ath, $airdrop_distributed_ath, $airdrop_reward_per_message_ath, $airdrop_total_allocation_ath);
}

;;
;; Type: ATHWallet
;; TLB: _ balance:uint128 owner_address:address ath_master_address:address pending_notifications:dict<int, ^PendingAthTransferNotification{sender_owner:address,response_destination:address,response_ack_value:uint64,amount:uint128,created_at:uint64}> processed_notifications:dict<int, int> pending_outgoing_transfers:dict<int, ^PendingAthOutgoingTransfer{recipient_wallet:address,response_destination:address,amount:uint128,created_at:uint64}> = ATHWallet
;;

builder $ATHWallet$init$_store(builder build_0, (int, slice, slice) v) inline {
    var (v'balance, v'owner_address, v'ath_master_address) = v;
    build_0 = build_0.store_int(v'balance, 257);
    build_0 = build_0.store_slice(v'owner_address);
    build_0 = build_0.store_slice(v'ath_master_address);
    return build_0;
}

cell $ATHWallet$_child_get_code() impure asm """
    B{} B>boc PUSHREF
""";

(cell, cell) $ATHWallet$_init_child(int $balance, slice $owner_address, slice $ath_master_address) inline_ref {
    ;; Build init code cell

    ;; Contract Code: ATHWallet
    cell init_code = $ATHWallet$_child_get_code();


    builder b = begin_cell();
    b = b.store_int(false, 1);
    b = $ATHWallet$init$_store(b, ($balance, $owner_address, $ath_master_address));
    return (init_code, b.end_cell());
}

;;
;; Type: Vault
;; TLB: _ vault_ath_wallet_address:address ath_master_address:address capsule_hub_address:address profile_registry_address:address username_registry_address:address binding_flags:uint8 sealed:bool deployment_manifest_hash:uint256 genesis_config_hash:uint256 users:dict<address, ^UserState{ton_balance:uint128,ath_balance:uint128,current_key_id:uint256,auth_pubkey:uint256,publish_nonce:uint64,last_active:uint64,receipts:dict<uint8, ^ReceiptSlot{nonce:uint64,action:uint8,result:uint8,aux:uint64,part_count:uint8,at:uint64}>}> key_records:dict<int, ^KeyRecord{owner_wallet:address,key_generation:uint32,enc_pubkey:uint256,sign_pubkey:uint256,pq_kem_pubkey_hash:uint256,pq_kem_pubkey_len:uint16,pq_kem_pubkey:^cell,crypto_suite_mask:uint16,created_at:uint64,created_lt:uint64,revoked_at:uint64,revoked_lt:uint64}> processed_ath_deposits:dict<int, int> pending_ath_withdrawals:dict<int, ^PendingAthWithdrawal{owner_wallet:address,tombstone:bool,recipient:address,recipient_ath_wallet:address,amount:uint128,refundable_ton_amount:uint128,created_at:uint64}> pending_batch_publishes:dict<int, ^PendingBatchPublish{owner_wallet:address,tombstone:bool,refund_to_vault:bool,nonce:uint64,publish_kind:uint8,part_count:uint8,publish_id:uint256,refundable_amount:uint128,created_at:uint64}> pending_profile_avatar_payments:dict<int, ^PendingProfileAvatarPayment{owner_wallet:address,tombstone:bool,amount:uint128,created_at:uint64}> pending_username_mint_payments:dict<int, ^PendingUsernameMintPayment{owner_wallet:address,tombstone:bool,amount:uint128,created_at:uint64}> user_count:uint64 key_record_count:uint64 processed_ath_deposit_count:uint64 pending_ath_withdrawal_count:uint64 pending_publish_count:uint64 genesis_ext:^cell = Vault
;;

builder $Vault$_store(builder build_0, (slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) v) inline {
    var (v'vault_ath_wallet_address, v'ath_master_address, v'capsule_hub_address, v'profile_registry_address, v'username_registry_address, v'binding_flags, v'sealed, v'deployment_manifest_hash, v'genesis_config_hash, v'users, v'key_records, v'processed_ath_deposits, v'pending_ath_withdrawals, v'pending_batch_publishes, v'pending_profile_avatar_payments, v'pending_username_mint_payments, v'user_count, v'key_record_count, v'processed_ath_deposit_count, v'pending_ath_withdrawal_count, v'pending_publish_count, v'genesis_ext) = v;
    build_0 = build_0.store_slice(v'vault_ath_wallet_address);
    build_0 = build_0.store_slice(v'ath_master_address);
    build_0 = build_0.store_slice(v'capsule_hub_address);
    var build_1 = begin_cell();
    build_1 = build_1.store_slice(v'profile_registry_address);
    build_1 = build_1.store_slice(v'username_registry_address);
    build_1 = build_1.store_uint(v'binding_flags, 8);
    build_1 = build_1.store_int(v'sealed, 1);
    build_1 = build_1.store_uint(v'deployment_manifest_hash, 256);
    var build_2 = begin_cell();
    build_2 = build_2.store_uint(v'genesis_config_hash, 256);
    build_2 = build_2.store_dict(v'users);
    build_2 = build_2.store_dict(v'key_records);
    build_2 = build_2.store_dict(v'processed_ath_deposits);
    var build_3 = begin_cell();
    build_3 = build_3.store_dict(v'pending_ath_withdrawals);
    build_3 = build_3.store_dict(v'pending_batch_publishes);
    build_3 = build_3.store_dict(v'pending_profile_avatar_payments);
    var build_4 = begin_cell();
    build_4 = build_4.store_dict(v'pending_username_mint_payments);
    build_4 = build_4.store_uint(v'user_count, 64);
    build_4 = build_4.store_uint(v'key_record_count, 64);
    build_4 = build_4.store_uint(v'processed_ath_deposit_count, 64);
    build_4 = build_4.store_uint(v'pending_ath_withdrawal_count, 64);
    build_4 = build_4.store_uint(v'pending_publish_count, 64);
    build_4 = build_4.store_ref(v'genesis_ext);
    build_3 = store_builder_ref(build_3, build_4);
    build_2 = store_builder_ref(build_2, build_3);
    build_1 = store_builder_ref(build_1, build_2);
    build_0 = store_builder_ref(build_0, build_1);
    return build_0;
}

(slice, ((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell))) $Vault$_load(slice sc_0) inline {
    var v'vault_ath_wallet_address = sc_0~load_msg_addr();
    var v'ath_master_address = sc_0~load_msg_addr();
    var v'capsule_hub_address = sc_0~load_msg_addr();
    slice sc_1 = sc_0~load_ref().begin_parse();
    var v'profile_registry_address = sc_1~load_msg_addr();
    var v'username_registry_address = sc_1~load_msg_addr();
    var v'binding_flags = sc_1~load_uint(8);
    var v'sealed = sc_1~load_int(1);
    var v'deployment_manifest_hash = sc_1~load_uint(256);
    slice sc_2 = sc_1~load_ref().begin_parse();
    var v'genesis_config_hash = sc_2~load_uint(256);
    var v'users = sc_2~load_dict();
    var v'key_records = sc_2~load_dict();
    var v'processed_ath_deposits = sc_2~load_dict();
    slice sc_3 = sc_2~load_ref().begin_parse();
    var v'pending_ath_withdrawals = sc_3~load_dict();
    var v'pending_batch_publishes = sc_3~load_dict();
    var v'pending_profile_avatar_payments = sc_3~load_dict();
    slice sc_4 = sc_3~load_ref().begin_parse();
    var v'pending_username_mint_payments = sc_4~load_dict();
    var v'user_count = sc_4~load_uint(64);
    var v'key_record_count = sc_4~load_uint(64);
    var v'processed_ath_deposit_count = sc_4~load_uint(64);
    var v'pending_ath_withdrawal_count = sc_4~load_uint(64);
    var v'pending_publish_count = sc_4~load_uint(64);
    var v'genesis_ext = sc_4~load_ref();
    return (sc_0, (v'vault_ath_wallet_address, v'ath_master_address, v'capsule_hub_address, v'profile_registry_address, v'username_registry_address, v'binding_flags, v'sealed, v'deployment_manifest_hash, v'genesis_config_hash, v'users, v'key_records, v'processed_ath_deposits, v'pending_ath_withdrawals, v'pending_batch_publishes, v'pending_profile_avatar_payments, v'pending_username_mint_payments, v'user_count, v'key_record_count, v'processed_ath_deposit_count, v'pending_ath_withdrawal_count, v'pending_publish_count, v'genesis_ext));
}

(slice, ((slice, slice, slice, int, int, int, int))) $Vault$init$_load(slice sc_0) inline {
    var v'vault_ath_wallet_address = sc_0~load_msg_addr();
    var v'ath_master_address = sc_0~load_msg_addr();
    var v'capsule_hub_address = sc_0~load_msg_addr();
    slice sc_1 = sc_0~load_ref().begin_parse();
    var v'genesis_config_hash = sc_1~load_int(257);
    var v'capsule_hub_bound = sc_1~load_int(1);
    var v'sealed = sc_1~load_int(1);
    var v'deployment_manifest_hash = sc_1~load_int(257);
    return (sc_0, (v'vault_ath_wallet_address, v'ath_master_address, v'capsule_hub_address, v'genesis_config_hash, v'capsule_hub_bound, v'sealed, v'deployment_manifest_hash));
}

(slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $Vault$_contract_load() impure inline {
    slice $sc = get_data().begin_parse();
    int $loaded = $sc~load_int(1);
    if ($loaded) {
        return $sc~$Vault$_load();
    }
    else {
        (slice $vault_ath_wallet_address, slice $ath_master_address, slice $capsule_hub_address, int $genesis_config_hash, int $capsule_hub_bound, int $sealed, int $deployment_manifest_hash) = $sc~$Vault$init$_load();
        $sc.end_parse();
        return $Vault$_contract_init($vault_ath_wallet_address, $ath_master_address, $capsule_hub_address, $genesis_config_hash, $capsule_hub_bound, $sealed, $deployment_manifest_hash);
    }
}

() $Vault$_contract_store((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) v) impure inline {
    builder b = begin_cell();
    b = b.store_int(true, 1);
    b = $Vault$_store(b, v);
    set_data(b.end_cell());
}

;;
;; Contract Vault functions
;;

(slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $Vault$_contract_init(slice $vault_ath_wallet_address, slice $ath_master_address, slice $capsule_hub_address, int $genesis_config_hash, int $capsule_hub_bound, int $sealed, int $deployment_manifest_hash) impure inline {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = (null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null());
    $self'vault_ath_wallet_address = $vault_ath_wallet_address;
    $self'ath_master_address = $ath_master_address;
    $self'capsule_hub_address = $capsule_hub_address;
    $self'profile_registry_address = $capsule_hub_address;
    $self'username_registry_address = $capsule_hub_address;
    $self'binding_flags = 0;
    if ($capsule_hub_bound) {
        $self'binding_flags = 1;
    }
    $self'sealed = $sealed;
    $self'deployment_manifest_hash = $deployment_manifest_hash;
    $self'genesis_config_hash = $genesis_config_hash;
    $self'genesis_ext = $Builder$_fun_endCell($Builder$_fun_storeBool(false, store_uint($global_beginCell(), $genesis_config_hash, 256)));
    if ($sealed) {
        ifnot ($self'deployment_manifest_hash) {
            $self'deployment_manifest_hash = $genesis_config_hash;
        }
        $self'genesis_config_hash = 15000000000000000;
    }
    $self'users = null();
    $self'key_records = null();
    $self'processed_ath_deposits = null();
    $self'pending_ath_withdrawals = null();
    $self'pending_batch_publishes = null();
    $self'pending_profile_avatar_payments = null();
    $self'pending_username_mint_payments = null();
    $self'user_count = 0;
    $self'key_record_count = 0;
    $self'processed_ath_deposit_count = 0;
    $self'pending_ath_withdrawal_count = 0;
    $self'pending_publish_count = 0;
    return ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_requireSealed((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    throw_unless(15000, $self'sealed);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_requireEmptyEnvelopePadding((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $padding) impure inline {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    throw_unless(16900, ($Slice$_fun_bits($padding) == 0));
    throw_unless(16901, ($Slice$_fun_refs($padding) == 0));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_requireUnsealed((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    throw_unless(15001, (~ $self'sealed));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isBindingSet((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $mask) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_156 = ((($self'binding_flags / $mask) % 2) == 1);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_156);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isCapsuleHubBound((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_157 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isBindingSet(1);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_157);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isProfileRegistryBound((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_158 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isBindingSet(2);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_158);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isUsernameRegistryBound((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_159 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isBindingSet(4);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_159);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_requireGenesisController((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    throw_unless(15030, ($self'genesis_config_hash != 0));
    throw_unless(15031, ($Cell$_fun_hash($Builder$_fun_endCell($Builder$_fun_storeAddress($global_beginCell(), __tact_context_get_sender()))) == $self'genesis_config_hash));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, int, int, int, int, int, cell)) $Vault$_fun_emptyUser((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_160 = $UserState$_constructor_ton_balance_ath_balance_current_key_id_auth_pubkey_publish_nonce_last_active_receipts(0, 0, 0, 0, 0, 0, null());
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_160);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, int, int, int, int, int, cell)) $Vault$_fun_getUserOrEmpty((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $owner) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    tuple $value = $UserState$_load_opt(__tact_dict_get_slice_cell($self'users, 267, $owner));
    if (null?($value)) {
        var $fresh$ret_161 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_emptyUser();
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_161);
    }
    var $fresh$ret_162 = $UserState$_not_null($value);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_162);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, int, int, int, int, int, cell)) $Vault$_fun_writeReceipt((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, (int, int, int, int, int, int, cell) $user, int $nonce, int $action, int $result, int $aux, int $part_count) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var (($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts)) = $user;
    $user'receipts~__tact_dict_set_uint_cell(8, ($nonce % 20), $ReceiptSlot$_store_cell($ReceiptSlot$_constructor_nonce_action_result_aux_part_count_at($nonce, $action, $result, $aux, $part_count, $global_now()), begin_cell()));
    $user'last_active = $global_now();
    var $fresh$ret_163 = ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_163);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, int, int, int, int, int, cell)) $Vault$_fun_updateReceipt((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, (int, int, int, int, int, int, cell) $user, int $nonce, int $result, int $aux) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var (($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts)) = $user;
    int $idx = ($nonce % 20);
    tuple $slotOpt = $ReceiptSlot$_load_opt(__tact_dict_get_uint_cell($user'receipts, 8, $idx));
    if ((~ null?($slotOpt))) {
        var ($slot'nonce, $slot'action, $slot'result, $slot'aux, $slot'part_count, $slot'at) = $ReceiptSlot$_not_null($slotOpt);
        if (($slot'nonce == $nonce)) {
            $slot'result = $result;
            $slot'aux = $aux;
            $user'receipts~__tact_dict_set_uint_cell(8, $idx, $ReceiptSlot$_store_cell(($slot'nonce, $slot'action, $slot'result, $slot'aux, $slot'part_count, $slot'at), begin_cell()));
        }
    }
    var $fresh$ret_164 = ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_164);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), slice) $Vault$_fun_deriveAthWalletAddress((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $owner) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var ($owner_std'workchain, $owner_std'address) = $global_parseStdAddress($Address$_fun_asSlice($owner));
    var ($wallet_init'code, $wallet_init'data) = $ATHWallet$_init_child(0, $owner, $self'ath_master_address);
    var $fresh$ret_165 = $global_contractAddressExt($owner_std'workchain, $wallet_init'code, $wallet_init'data);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_165);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isBasechainAddress((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $owner) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    slice $ownerSlice = $Address$_fun_asSlice($owner);
    var $fresh$ret_166 = ($ownerSlice~load_uint(11) == 1024);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_166);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_pendingPublishCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_167 = ($self'pending_publish_count % 4294967296);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_167);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_pendingProfileAvatarPaymentCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_168 = (($self'pending_publish_count / 4294967296) % 65536);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_168);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_pendingUsernameMintPaymentCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_169 = ($self'pending_publish_count / 281474976710656);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_169);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_incrementPendingPublishCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    $self'pending_publish_count = $self'pending_publish_count + 1;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_decrementPendingPublishCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    $self'pending_publish_count = $self'pending_publish_count - 1;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_incrementPendingProfileAvatarPaymentCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    throw_unless(16690, (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_pendingProfileAvatarPaymentCount() < 65535));
    $self'pending_publish_count = $self'pending_publish_count + 4294967296;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_decrementPendingProfileAvatarPaymentCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    $self'pending_publish_count = $self'pending_publish_count - 4294967296;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_incrementPendingUsernameMintPaymentCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    throw_unless(16790, (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_pendingUsernameMintPaymentCount() < 65535));
    $self'pending_publish_count = $self'pending_publish_count + 281474976710656;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_decrementPendingUsernameMintPaymentCount((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    $self'pending_publish_count = $self'pending_publish_count - 281474976710656;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_computeAthDepositId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $senderWallet, int $queryId) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_170 = $Cell$_fun_hash($Builder$_fun_endCell(store_uint($Builder$_fun_storeAddress(store_uint($global_beginCell(), 1094996041, 32), $senderWallet), $queryId, 64)));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_170);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_computeAthWithdrawalId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $ownerWallet, int $queryId) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_171 = ($Cell$_fun_hash($Builder$_fun_endCell(store_uint($Builder$_fun_storeAddress(store_uint($global_beginCell(), 1096239428, 32), $ownerWallet), $queryId, 64))) % 18446744073709551616);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_171);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_computeProfileAvatarPaymentId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $ownerWallet, int $clientNonce, int $avatarHash) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_172 = ($Cell$_fun_hash($Builder$_fun_endCell(store_uint(store_uint($Builder$_fun_storeAddress(store_uint($global_beginCell(), 1448100169, 32), $ownerWallet), $clientNonce, 64), $avatarHash, 256))) % 18446744073709551616);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_172);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_computeUsernameNameHash((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $username) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_173 = $Cell$_fun_hash($Builder$_fun_endCell($Builder$_fun_storeSlice(store_uint($global_beginCell(), 3318512854, 32), $username)));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_173);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_computeUsernameMintPaymentId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $ownerWallet, int $clientNonce, int $nameHash) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_174 = ($Cell$_fun_hash($Builder$_fun_endCell(store_uint(store_uint($Builder$_fun_storeAddress(store_uint($global_beginCell(), 1448431177, 32), $ownerWallet), $clientNonce, 64), $nameHash, 256))) % 18446744073709551616);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_174);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), slice) $Vault$_fun_profileRegistryAthWalletAddress((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_175 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_deriveAthWalletAddress($self'profile_registry_address);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_175);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), slice) $Vault$_fun_usernameRegistryAthWalletAddress((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_176 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_deriveAthWalletAddress($self'username_registry_address);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_176);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_profileAvatarTonCharge((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_177 = 115000000;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_177);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_usernameMintTonCharge((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_178 = 1000000000;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_178);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_usernamePriceForLength((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $nameLen) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    throw_unless(16650, ($nameLen >= 4));
    throw_unless(16651, ($nameLen <= 16));
    if (($nameLen == 4)) {
        var $fresh$ret_179 = 10000000000000;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_179);
    }
    if (($nameLen == 5)) {
        var $fresh$ret_180 = 1000000000000;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_180);
    }
    var $fresh$ret_181 = 100000000000;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_181);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isValidUsernameBytes((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $nameLen, slice $username) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    if (( (($nameLen < 4)) ? (true) : (($nameLen > 16)) )) {
        var $fresh$ret_182 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_182);
    }
    if (($Slice$_fun_bits($username) != ($nameLen * 8))) {
        var $fresh$ret_183 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_183);
    }
    ifnot ($Slice$_fun_refsEmpty($username)) {
        var $fresh$ret_184 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_184);
    }
    slice $cursor = $username;
    int $i = 0;
    while (($i < $nameLen)) {
        int $ch = $cursor~load_uint(8);
        int $lower = ( (($ch >= 97)) ? (($ch <= 122)) : (false) );
        int $digit = ( (($ch >= 48)) ? (($ch <= 57)) : (false) );
        int $separator = ( (($ch == 45)) ? (true) : (($ch == 95)) );
        ifnot (( (( ($lower) ? (true) : ($digit) )) ? (true) : ($separator) )) {
            var $fresh$ret_185 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_185);
        }
        $i = $i + 1;
    }
    var $fresh$ret_186 = $Slice$_fun_empty($cursor);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_186);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isValidProfileAvatarPointer((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $ownerWallet, int $avatarHash, int $avatarStreamId, int $avatarPartCount, int $mediaFormat) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_187 = ( (( (( (( (( (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isBasechainAddress($ownerWallet)) ? (($avatarHash != 0)) : (false) )) ? (($avatarStreamId != 0)) : (false) )) ? (($avatarPartCount > 0)) : (false) )) ? (($avatarPartCount <= 2)) : (false) )) ? (($mediaFormat == 1)) : (false) );
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_187);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_discountedFee((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $owner, int $fullFee) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    ifnot ($self'sealed) {
        var $fresh$ret_188 = $fullFee;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_188);
    }
    if (($self'genesis_config_hash > 0)) {
        var $fresh$ret_189 = $fullFee;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_189);
    }
    int $minFee = 0;
    if (($fullFee < $minFee)) {
        $minFee = $fullFee;
    }
    var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_getUserOrEmpty($owner);
    if (($user'ath_balance >= 10000000000000)) {
        var $fresh$ret_190 = $minFee;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_190);
    }
    int $remaining = (10000000000000 - $user'ath_balance);
    int $fee = (((($fullFee * $remaining) + 10000000000000) - 1) / 10000000000000);
    if (($fee < $minFee)) {
        var $fresh$ret_191 = $minFee;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_191);
    }
    var $fresh$ret_192 = $fee;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_192);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isAllowedPrivateSizeClass((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_204 = ( (( (( (( (( (($sizeClass == 1)) ? (true) : (($sizeClass == 2)) )) ? (true) : (($sizeClass == 4)) )) ? (true) : (($sizeClass == 8)) )) ? (true) : (($sizeClass == 16)) )) ? (true) : (($sizeClass == 32)) );
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_204);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isAllowedPublicSizeClass((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_205 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isAllowedPrivateSizeClass($sizeClass);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_205);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isPublishProfileValid((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $publishKind, int $sizeClass, int $cryptoSuite, int $header0, int $header1) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    if (( (( (($publishKind == 1)) ? (true) : (($publishKind == 3)) )) ? (true) : (($publishKind == 4)) )) {
        ifnot ($header0) {
            var $fresh$ret_193 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_193);
        }
        ifnot ($header1) {
            var $fresh$ret_194 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_194);
        }
        ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isAllowedPrivateSizeClass($sizeClass)) {
            var $fresh$ret_195 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_195);
        }
        if (( (($publishKind == 4)) ? (($sizeClass > 8)) : (false) )) {
            var $fresh$ret_196 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_196);
        }
        var $fresh$ret_197 = ($cryptoSuite == 2);
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_197);
    }
    if (($publishKind == 2)) {
        ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isAllowedPublicSizeClass($sizeClass)) {
            var $fresh$ret_198 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_198);
        }
        if ($cryptoSuite) {
            var $fresh$ret_199 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_199);
        }
        ifnot ($header0) {
            var $fresh$ret_200 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_200);
        }
        if ($header1) {
            var $fresh$ret_201 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_201);
        }
        var $fresh$ret_202 = true;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_202);
    }
    var $fresh$ret_203 = false;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_203);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_privateUsefulBytes((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    throw_unless(16326, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isAllowedPrivateSizeClass($sizeClass));
    var $fresh$ret_206 = ($sizeClass * 1024);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_206);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_privateBodyOverheadBytes((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $cryptoSuite) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    throw_unless(16327, ($cryptoSuite == 2));
    var $fresh$ret_207 = 1204;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_207);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_privateBodyBytes((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass, int $cryptoSuite) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_208 = (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_privateBodyOverheadBytes($cryptoSuite) + ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_privateUsefulBytes($sizeClass));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_208);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_privateBodyBits((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass, int $cryptoSuite) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_209 = (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_privateBodyBytes($sizeClass, $cryptoSuite) * 8);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_209);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_privateBodyCells((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass, int $cryptoSuite) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_210 = ((($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_privateBodyBytes($sizeClass, $cryptoSuite) + 126) / 127);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_210);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_privateBodyRefs((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass, int $cryptoSuite) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_211 = (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_privateBodyCells($sizeClass, $cryptoSuite) - 1);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_211);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_introBodyBytes((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_212 = (2388 + ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_privateUsefulBytes($sizeClass));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_212);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_introBodyBits((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_213 = (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_introBodyBytes($sizeClass) * 8);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_213);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_introBodyCells((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_214 = ((($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_introBodyBytes($sizeClass) + 126) / 127);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_214);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_introBodyRefs((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_215 = (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_introBodyCells($sizeClass) - 1);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_215);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_publicBodyBytes((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    throw_unless(16330, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isAllowedPublicSizeClass($sizeClass));
    var $fresh$ret_216 = ($sizeClass * 1024);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_216);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_publicBodyBits((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_217 = (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_publicBodyBytes($sizeClass) * 8);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_217);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_publicBodyCells((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_218 = ((($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_publicBodyBytes($sizeClass) + 126) / 127);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_218);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_publicBodyRefs((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_219 = (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_publicBodyCells($sizeClass) - 1);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_219);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isExactCapsuleCell((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, cell $cell, int $expectedCells, int $expectedBits, int $expectedRefs) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    try {
        var ($size'cells, $size'bits, $size'refs) = $Cell$_fun_computeDataSize($cell, $expectedCells);
        var $fresh$ret_220 = ( (( (($size'cells == $expectedCells)) ? (($size'bits == $expectedBits)) : (false) )) ? (($size'refs == $expectedRefs)) : (false) );
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_220);
    } catch (_, $exitCode) {
        var $fresh$ret_221 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_221);
    }
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isPublicCapsuleCell((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, cell $cell, int $maxCells, int $maxBits, int $maxRefs, int $requireByteAligned) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    try {
        var ($size'cells, $size'bits, $size'refs) = $Cell$_fun_computeDataSize($cell, $maxCells);
        ifnot (($size'cells <= $maxCells)) {
            var $fresh$ret_222 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_222);
        }
        ifnot (($size'bits > 0)) {
            var $fresh$ret_223 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_223);
        }
        ifnot (($size'bits <= $maxBits)) {
            var $fresh$ret_224 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_224);
        }
        ifnot (($size'refs <= $maxRefs)) {
            var $fresh$ret_225 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_225);
        }
        if (( ($requireByteAligned) ? ((~ (($size'bits % 8) == 0))) : (false) )) {
            var $fresh$ret_226 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_226);
        }
        var $fresh$ret_227 = true;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_227);
    } catch (_, $exitCode) {
        var $fresh$ret_228 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_228);
    }
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isConvCapsuleShapeValid((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass, int $cryptoSuite, cell $header0, cell $header1, cell $body) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isExactCapsuleCell($header0, 1, 320, 0)) {
        var $fresh$ret_229 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_229);
    }
    ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isExactCapsuleCell($header1, 1, 240, 0)) {
        var $fresh$ret_230 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_230);
    }
    var $fresh$ret_231 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isExactCapsuleCell($body, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_privateBodyCells($sizeClass, $cryptoSuite), ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_privateBodyBits($sizeClass, $cryptoSuite), ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_privateBodyRefs($sizeClass, $cryptoSuite));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_231);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isIntroCapsuleShapeValid((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass, int $cryptoSuite, cell $header0, cell $header1, cell $body) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isExactCapsuleCell($header0, 1, 336, 0)) {
        var $fresh$ret_232 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_232);
    }
    ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isExactCapsuleCell($header1, 1, 240, 0)) {
        var $fresh$ret_233 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_233);
    }
    var $fresh$ret_234 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isExactCapsuleCell($body, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_introBodyCells($sizeClass), ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_introBodyBits($sizeClass), ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_introBodyRefs($sizeClass));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_234);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isPublicCapsuleShapeValid((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $sizeClass, cell $header, cell $body) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isPublicCapsuleCell($header, 1, 576, 0, true)) {
        var $fresh$ret_235 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_235);
    }
    var $fresh$ret_236 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isPublicCapsuleCell($body, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_publicBodyCells($sizeClass), ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_publicBodyBits($sizeClass), ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_publicBodyRefs($sizeClass), true);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_236);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_computePublishBounceId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $publishId) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_237 = ($publishId % 18446744073709551616);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_237);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_computePublishBounceTag((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $publishId) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_238 = ($Cell$_fun_hash($Builder$_fun_endCell(store_uint($global_beginCell(), $publishId, 256))) % 1461501637330902918203684832716283019655932542976);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_238);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_batchStorageReserve((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $kind) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    if (($kind == 2)) {
        var $fresh$ret_239 = 10400000;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_239);
    }
    if (($kind == 3)) {
        var $fresh$ret_240 = 3000000;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_240);
    }
    if (($kind == 4)) {
        var $fresh$ret_241 = 201000000;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_241);
    }
    throw_unless(16360, ($kind == 1));
    var $fresh$ret_242 = 4300000;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_242);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_batchHubPartGas((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $kind) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    if (($kind == 2)) {
        var $fresh$ret_243 = 180000;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_243);
    }
    if (($kind == 3)) {
        var $fresh$ret_244 = 120000;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_244);
    }
    if (($kind == 4)) {
        var $fresh$ret_245 = 150000;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_245);
    }
    throw_unless(16360, ($kind == 1));
    var $fresh$ret_246 = 170000;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_246);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_batchVaultPartGas((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $kind) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    if (($kind == 2)) {
        var $fresh$ret_247 = 90000;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_247);
    }
    if (($kind == 3)) {
        var $fresh$ret_248 = 90000;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_248);
    }
    if (($kind == 4)) {
        var $fresh$ret_249 = 90000;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_249);
    }
    throw_unless(16360, ($kind == 1));
    var $fresh$ret_250 = 90000;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_250);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_computeBatchPublishId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $owner, int $nonce, int $partsRootHash, int $kind, int $partCount) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_251 = $Cell$_fun_hash($Builder$_fun_endCell(store_uint(store_uint(store_uint(store_uint($Builder$_fun_storeAddress(store_uint(store_uint($global_beginCell(), 1112557873, 32), $self'deployment_manifest_hash, 256), $owner), $nonce, 64), $partsRootHash, 256), $kind, 8), $partCount, 8)));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_251);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_batchChargeFloor((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $partCount) impure inline {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_252 = (92700000 + (6200000 * $partCount));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_252);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_refundPendingAthWithdrawalTon((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, (slice, int, slice, slice, int, int, int) $pending, int $amount) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var (($pending'owner_wallet, $pending'tombstone, $pending'recipient, $pending'recipient_ath_wallet, $pending'amount, $pending'refundable_ton_amount, $pending'created_at)) = $pending;
    int $refundAmount = 0;
    if (($amount > 2000000)) {
        $refundAmount = ($amount - 2000000);
    }
    if (($refundAmount > $pending'refundable_ton_amount)) {
        $refundAmount = $pending'refundable_ton_amount;
    }
    if (($refundAmount <= 0)) {
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), ());
    }
    var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_getUserOrEmpty($pending'owner_wallet);
    $user'ton_balance = $user'ton_balance + $refundAmount;
    $self'users~__tact_dict_set_slice_cell(267, $pending'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), begin_cell()));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_restorePendingProfileAvatarPayment((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $queryId, (slice, int, int, int) $pending) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var (($pending'owner_wallet, $pending'tombstone, $pending'amount, $pending'created_at)) = $pending;
    var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_getUserOrEmpty($pending'owner_wallet);
    $user'ath_balance = $user'ath_balance + $pending'amount;
    $self'users~__tact_dict_set_slice_cell(267, $pending'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), begin_cell()));
    $self'pending_profile_avatar_payments~__tact_dict_delete_int(257, $queryId);
    ifnot ($pending'tombstone) {
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_decrementPendingProfileAvatarPaymentCount();
    }
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_restorePendingUsernameMintPayment((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $queryId, (slice, int, int, int) $pending) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var (($pending'owner_wallet, $pending'tombstone, $pending'amount, $pending'created_at)) = $pending;
    var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_getUserOrEmpty($pending'owner_wallet);
    $user'ath_balance = $user'ath_balance + $pending'amount;
    $self'users~__tact_dict_set_slice_cell(267, $pending'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), begin_cell()));
    $self'pending_username_mint_payments~__tact_dict_delete_int(257, $queryId);
    ifnot ($pending'tombstone) {
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_decrementPendingUsernameMintPaymentCount();
    }
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_creditActivityAirdrop((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $owner, int $count) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    if (($self'genesis_config_hash <= 0)) {
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), ());
    }
    throw_unless(16540, __tact_dict_exists_slice($self'users, 267, $owner));
    int $reward = (10000000000 * $count);
    if (($reward > $self'genesis_config_hash)) {
        $reward = $self'genesis_config_hash;
    }
    var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_getUserOrEmpty($owner);
    $user'ath_balance = $user'ath_balance + $reward;
    $self'genesis_config_hash = $self'genesis_config_hash - $reward;
    $self'users~__tact_dict_set_slice_cell(267, $owner, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), begin_cell()));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_keyRecordStorageEndowment((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $cryptoSuiteMask) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    throw_unless(16100, ($cryptoSuiteMask == 2));
    var $fresh$ret_253 = 30000000;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_253);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_vaultStorageReserve((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_254 = (($self'user_count * 10000000) + ($self'key_record_count * 30000000));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_254);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_validatePqPubkeyCell((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $pqLen, int $cryptoSuiteMask, cell $pqPubkey) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    throw_unless(16107, ($cryptoSuiteMask == 2));
    var ($size'cells, $size'bits, $size'refs) = $Cell$_fun_computeDataSize($pqPubkey, 10);
    throw_unless(16108, ($size'cells == 10));
    throw_unless(16109, ($size'bits == 9472));
    throw_unless(16113, ($size'refs == 9));
    cell $cursor = $pqPubkey;
    int $remaining = $pqLen;
    while (($remaining > 0)) {
        int $chunkBytes = 127;
        if (($remaining == 1184)) {
            $chunkBytes = 41;
        } elseif (($remaining < $chunkBytes)) {
            $chunkBytes = $remaining;
        }
        slice $cursorSlice = $Cell$_fun_beginParse($cursor);
        throw_unless(16114, ($Slice$_fun_bits($cursorSlice) == ($chunkBytes * 8)));
        if (($remaining > $chunkBytes)) {
            throw_unless(16115, ($Slice$_fun_refs($cursorSlice) == 1));
            $cursor = $cursorSlice~$Slice$_fun_loadRef();
        } else {
            throw_unless(16116, ($Slice$_fun_refs($cursorSlice) == 0));
        }
        $remaining = $remaining - $chunkBytes;
    }
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isValidPqPubkeyCell((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $pqLen, int $cryptoSuiteMask, cell $pqPubkey) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    if (($cryptoSuiteMask != 2)) {
        var $fresh$ret_255 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_255);
    }
    var ($size'cells, $size'bits, $size'refs) = $Cell$_fun_computeDataSize($pqPubkey, 10);
    if (($size'cells != 10)) {
        var $fresh$ret_256 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_256);
    }
    if (($size'bits != 9472)) {
        var $fresh$ret_257 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_257);
    }
    if (($size'refs != 9)) {
        var $fresh$ret_258 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_258);
    }
    cell $cursor = $pqPubkey;
    int $remaining = $pqLen;
    while (($remaining > 0)) {
        int $chunkBytes = 127;
        if (($remaining == 1184)) {
            $chunkBytes = 41;
        } elseif (($remaining < $chunkBytes)) {
            $chunkBytes = $remaining;
        }
        slice $cursorSlice = $Cell$_fun_beginParse($cursor);
        if (($Slice$_fun_bits($cursorSlice) != ($chunkBytes * 8))) {
            var $fresh$ret_259 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_259);
        }
        if (($remaining > $chunkBytes)) {
            if (($Slice$_fun_refs($cursorSlice) != 1)) {
                var $fresh$ret_260 = false;
                return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_260);
            }
            $cursor = $cursorSlice~$Slice$_fun_loadRef();
        } elseif ($Slice$_fun_refs($cursorSlice)) {
            var $fresh$ret_261 = false;
            return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_261);
        }
        $remaining = $remaining - $chunkBytes;
    }
    var $fresh$ret_262 = true;
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_262);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_isValidKeyProfile((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $encPubkey, int $signPubkey, int $pqHash, int $pqLen, cell $pqPubkey, int $cryptoSuiteMask) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    ifnot ($encPubkey) {
        var $fresh$ret_263 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_263);
    }
    ifnot ($signPubkey) {
        var $fresh$ret_264 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_264);
    }
    if (($cryptoSuiteMask != 2)) {
        var $fresh$ret_265 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_265);
    }
    ifnot ($pqHash) {
        var $fresh$ret_266 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_266);
    }
    if (($pqLen != 1184)) {
        var $fresh$ret_267 = false;
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_267);
    }
    var $fresh$ret_268 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isValidPqPubkeyCell($pqLen, $cryptoSuiteMask, $pqPubkey);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_268);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), ()) $Vault$_fun_validateKeyProfile((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $encPubkey, int $signPubkey, int $pqHash, int $pqLen, cell $pqPubkey, int $cryptoSuiteMask) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    throw_unless(16101, ($encPubkey != 0));
    throw_unless(16102, ($signPubkey != 0));
    throw_unless(16107, ($cryptoSuiteMask == 2));
    throw_unless(16105, ($pqHash != 0));
    throw_unless(16106, ($pqLen == 1184));
    ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_validatePqPubkeyCell($pqLen, $cryptoSuiteMask, $pqPubkey);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), ());
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_computeKeyId((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $owner, int $keyGeneration, int $encPubkey, int $signPubkey, int $pqHash, int $pqLen, int $cryptoSuiteMask) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    cell $keyFields = $Builder$_fun_endCell(store_uint(store_uint(store_uint($global_beginCell(), $encPubkey, 256), $signPubkey, 256), $pqHash, 256));
    var $fresh$ret_269 = $Cell$_fun_hash($Builder$_fun_endCell($Builder$_fun_storeRef($keyFields, store_uint(store_uint(store_uint($Builder$_fun_storeAddress(store_uint($global_beginCell(), 1262836041, 32), $owner), $keyGeneration, 32), $pqLen, 16), $cryptoSuiteMask, 16))));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_269);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, int, int, int, int, int, cell)) $Vault$_fun_creditBatchSettlementRefund((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, (int, int, int, int, int, int, cell) $user, int $refundableAmount) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var (($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts)) = $user;
    int $ackReserve = $global_getComputeFee(9000, false);
    int $refundAmount = 0;
    if (($Context$_get_value(__tact_context_get()) > $ackReserve)) {
        $refundAmount = ($Context$_get_value(__tact_context_get()) - $ackReserve);
    }
    if (($refundAmount > $refundableAmount)) {
        $refundAmount = $refundableAmount;
    }
    if (($refundAmount > 0)) {
        $user'ton_balance = $user'ton_balance + $refundAmount;
    }
    var $fresh$ret_270 = ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_270);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, int, int, int, int, int, int)) $Vault$_fun_get_user((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $owner) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    tuple $userOpt = $UserState$_load_opt(__tact_dict_get_slice_cell($self'users, 267, $owner));
    if (null?($userOpt)) {
        var $fresh$ret_271 = $VaultUserView$_constructor_exists_ton_balance_ath_balance_current_key_id_auth_pubkey_publish_nonce_last_active(false, 0, 0, 0, 0, 0, 0);
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_271);
    }
    var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = $UserState$_not_null($userOpt);
    var $fresh$ret_272 = $VaultUserView$_constructor_exists_ton_balance_ath_balance_current_key_id_auth_pubkey_publish_nonce_last_active(true, $user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_272);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, int, int, int, int)) $Vault$_fun_get_storage_health((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    int $reserve = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_vaultStorageReserve();
    var $fresh$ret_273 = $VaultStorageHealthView$_constructor_balance_storage_reserve_user_count_key_record_count_surplus($global_myBalance(), $reserve, $self'user_count, $self'key_record_count, ($global_myBalance() - $reserve));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_273);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, int, cell)) $Vault$_fun_get_user_receipts((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $owner) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    tuple $userOpt = $UserState$_load_opt(__tact_dict_get_slice_cell($self'users, 267, $owner));
    if (null?($userOpt)) {
        var $fresh$ret_274 = $VaultUserReceiptsView$_constructor_exists_publish_nonce_receipts(false, 0, null());
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_274);
    }
    var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = $UserState$_not_null($userOpt);
    var $fresh$ret_275 = $VaultUserReceiptsView$_constructor_exists_publish_nonce_receipts(true, $user'publish_nonce, $user'receipts);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_275);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_diag_forward_fee((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $cells, int $bits) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_276 = $global_getForwardFee($cells, $bits, false);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_276);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_diag_compute_fee((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $gas) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_277 = $global_getComputeFee($gas, false);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_277);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_diag_ext_hard_import((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_278 = $global_getForwardFee(8192, 524280, false);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_278);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, slice, int, int, int, int, int, cell, int, int, int, int, int)) $Vault$_fun_get_key_record((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $keyId) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    tuple $recordOpt = $KeyRecord$_load_opt(__tact_dict_get_int_cell($self'key_records, 257, $keyId));
    if (null?($recordOpt)) {
        var $fresh$ret_279 = $VaultKeyRecordView$_constructor_exists_owner_wallet_key_generation_enc_pubkey_sign_pubkey_pq_kem_pubkey_hash_pq_kem_pubkey_len_pq_kem_pubkey_crypto_suite_mask_created_at_created_lt_revoked_at_revoked_lt(false, $global_myAddress(), 0, 0, 0, 0, 0, $global_emptyCell(), 0, 0, 0, 0, 0);
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_279);
    }
    var ($record'owner_wallet, $record'key_generation, $record'enc_pubkey, $record'sign_pubkey, $record'pq_kem_pubkey_hash, $record'pq_kem_pubkey_len, $record'pq_kem_pubkey, $record'crypto_suite_mask, $record'created_at, $record'created_lt, $record'revoked_at, $record'revoked_lt) = $KeyRecord$_not_null($recordOpt);
    var $fresh$ret_280 = $VaultKeyRecordView$_constructor_exists_owner_wallet_key_generation_enc_pubkey_sign_pubkey_pq_kem_pubkey_hash_pq_kem_pubkey_len_pq_kem_pubkey_crypto_suite_mask_created_at_created_lt_revoked_at_revoked_lt(true, $record'owner_wallet, $record'key_generation, $record'enc_pubkey, $record'sign_pubkey, $record'pq_kem_pubkey_hash, $record'pq_kem_pubkey_len, $record'pq_kem_pubkey, $record'crypto_suite_mask, $record'created_at, $record'created_lt, $record'revoked_at, $record'revoked_lt);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_280);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, slice, int, int, int, int, int)) $Vault$_fun_get_pending_batch_publish((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $bounceId) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    tuple $pendingOpt = $PendingBatchPublish$_load_opt(__tact_dict_get_int_cell($self'pending_batch_publishes, 257, $bounceId));
    if (null?($pendingOpt)) {
        var $fresh$ret_281 = $VaultPendingBatchPublishView$_constructor_exists_owner_wallet_nonce_publish_kind_part_count_created_at_tombstone(false, $global_myAddress(), 0, 0, 0, 0, false);
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_281);
    }
    var ($pending'owner_wallet, $pending'tombstone, $pending'refund_to_vault, $pending'nonce, $pending'publish_kind, $pending'part_count, $pending'publish_id, $pending'refundable_amount, $pending'created_at) = $PendingBatchPublish$_not_null($pendingOpt);
    var $fresh$ret_282 = $VaultPendingBatchPublishView$_constructor_exists_owner_wallet_nonce_publish_kind_part_count_created_at_tombstone(true, $pending'owner_wallet, $pending'nonce, $pending'publish_kind, $pending'part_count, $pending'created_at, $pending'tombstone);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_282);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, slice, slice, slice, int, int)) $Vault$_fun_get_pending_ath_withdrawal((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, int $queryId) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    tuple $pendingOpt = $PendingAthWithdrawal$_load_opt(__tact_dict_get_int_cell($self'pending_ath_withdrawals, 257, $queryId));
    if (null?($pendingOpt)) {
        var $fresh$ret_283 = $VaultPendingAthWithdrawalView$_constructor_exists_owner_wallet_recipient_recipient_ath_wallet_amount_created_at(false, $global_myAddress(), $global_myAddress(), $global_myAddress(), 0, 0);
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_283);
    }
    var ($pending'owner_wallet, $pending'tombstone, $pending'recipient, $pending'recipient_ath_wallet, $pending'amount, $pending'refundable_ton_amount, $pending'created_at) = $PendingAthWithdrawal$_not_null($pendingOpt);
    var $fresh$ret_284 = $VaultPendingAthWithdrawalView$_constructor_exists_owner_wallet_recipient_recipient_ath_wallet_amount_created_at(true, $pending'owner_wallet, $pending'recipient, $pending'recipient_ath_wallet, $pending'amount, $pending'created_at);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_284);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), int) $Vault$_fun_get_ath_withdrawal_id((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $ownerWallet, int $queryId) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    var $fresh$ret_285 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_computeAthWithdrawalId($ownerWallet, $queryId);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_285);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, slice, slice, slice, int, int)) $Vault$_fun_get_pending_ath_withdrawal_for((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self, slice $ownerWallet, int $queryId) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    int $withdrawalId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_computeAthWithdrawalId($ownerWallet, $queryId);
    var $fresh$ret_286 = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_get_pending_ath_withdrawal($withdrawalId);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_286);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, int, slice, int)) $Vault$_fun_get_successor((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    slice $ext = $Cell$_fun_beginParse($self'genesis_ext);
    $ext~load_uint(256);
    ifnot ($ext~$Slice$_fun_loadBool()) {
        var $fresh$ret_287 = $VaultSuccessorView$_constructor_announced_successor_manifest_hash_successor_vault_announced_at(false, 0, $global_myAddress(), 0);
        return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_287);
    }
    var $fresh$ret_288 = $VaultSuccessorView$_constructor_announced_successor_manifest_hash_successor_vault_announced_at(true, $ext~load_uint(256), $ext~$Slice$_fun_loadAddress(), $ext~load_uint(64));
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_288);
}

((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell), (int, int, int, int, int, slice, slice, slice, slice, slice, int, int, int, int, int, int, int, int, int, int, int, int)) $Vault$_fun_get_global((slice, slice, slice, slice, slice, int, int, int, int, cell, cell, cell, cell, cell, cell, cell, int, int, int, int, int, cell) $self) impure inline_ref {
    var (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)) = $self;
    int $airdropRemaining = 0;
    if ($self'sealed) {
        $airdropRemaining = $self'genesis_config_hash;
    }
    var $fresh$ret_289 = $VaultGlobalView$_constructor_sealed_capsule_hub_bound_profile_registry_bound_username_registry_bound_deployment_manifest_hash_capsule_hub_address_profile_registry_address_username_registry_address_vault_ath_wallet_address_ath_master_address_user_count_key_record_count_pending_ath_withdrawal_count_pending_publish_count_pending_profile_avatar_payment_count_pending_username_mint_payment_count_processed_ath_deposit_count_pending_publish_stale_ttl_airdrop_remaining_ath_airdrop_distributed_ath_airdrop_reward_per_message_ath_airdrop_total_allocation_ath($self'sealed, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isCapsuleHubBound(), ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isProfileRegistryBound(), ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isUsernameRegistryBound(), $self'deployment_manifest_hash, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'vault_ath_wallet_address, $self'ath_master_address, $self'user_count, $self'key_record_count, $self'pending_ath_withdrawal_count, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_pendingPublishCount(), ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_pendingProfileAvatarPaymentCount(), ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_pendingUsernameMintPaymentCount(), $self'processed_ath_deposit_count, 86400, $airdropRemaining, (15000000000000000 - $airdropRemaining), 10000000000, 15000000000000000);
    return (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext), $fresh$ret_289);
}

;;
;; Get methods of a Contract Vault
;;

_ %get_user(slice $owner) method_id(91785) {
    slice $owner = $owner;
    var self = $Vault$_contract_load();
    var res = self~$Vault$_fun_get_user($owner);
    return $VaultUserView$_to_external(res);
}

_ %get_storage_health() method_id(119233) {
    var self = $Vault$_contract_load();
    var res = self~$Vault$_fun_get_storage_health();
    return $VaultStorageHealthView$_to_external(res);
}

_ %get_user_receipts(slice $owner) method_id(79094) {
    slice $owner = $owner;
    var self = $Vault$_contract_load();
    var res = self~$Vault$_fun_get_user_receipts($owner);
    return $VaultUserReceiptsView$_to_external(res);
}

_ %diag_forward_fee(int $cells, int $bits) method_id(127218) {
    int $cells = $cells;
    int $bits = $bits;
    var self = $Vault$_contract_load();
    var res = self~$Vault$_fun_diag_forward_fee($cells, $bits);
    return res;
}

_ %diag_compute_fee(int $gas) method_id(107587) {
    int $gas = $gas;
    var self = $Vault$_contract_load();
    var res = self~$Vault$_fun_diag_compute_fee($gas);
    return res;
}

_ %diag_ext_hard_import() method_id(107995) {
    var self = $Vault$_contract_load();
    var res = self~$Vault$_fun_diag_ext_hard_import();
    return res;
}

_ %get_key_record(int $keyId) method_id(104356) {
    int $keyId = $keyId;
    var self = $Vault$_contract_load();
    var res = self~$Vault$_fun_get_key_record($keyId);
    return $VaultKeyRecordView$_to_external(res);
}

_ %get_pending_batch_publish(int $bounceId) method_id(94421) {
    int $bounceId = $bounceId;
    var self = $Vault$_contract_load();
    var res = self~$Vault$_fun_get_pending_batch_publish($bounceId);
    return $VaultPendingBatchPublishView$_to_external(res);
}

_ %get_pending_ath_withdrawal(int $queryId) method_id(125951) {
    int $queryId = $queryId;
    var self = $Vault$_contract_load();
    var res = self~$Vault$_fun_get_pending_ath_withdrawal($queryId);
    return $VaultPendingAthWithdrawalView$_to_external(res);
}

_ %get_ath_withdrawal_id(slice $ownerWallet, int $queryId) method_id(123302) {
    slice $ownerWallet = $ownerWallet;
    int $queryId = $queryId;
    var self = $Vault$_contract_load();
    var res = self~$Vault$_fun_get_ath_withdrawal_id($ownerWallet, $queryId);
    return res;
}

_ %get_pending_ath_withdrawal_for(slice $ownerWallet, int $queryId) method_id(104521) {
    slice $ownerWallet = $ownerWallet;
    int $queryId = $queryId;
    var self = $Vault$_contract_load();
    var res = self~$Vault$_fun_get_pending_ath_withdrawal_for($ownerWallet, $queryId);
    return $VaultPendingAthWithdrawalView$_to_external(res);
}

_ %get_successor() method_id(129519) {
    var self = $Vault$_contract_load();
    var res = self~$Vault$_fun_get_successor();
    return $VaultSuccessorView$_to_external(res);
}

_ %get_global() method_id(126899) {
    var self = $Vault$_contract_load();
    var res = self~$Vault$_fun_get_global();
    return $VaultGlobalView$_to_external(res);
}

;;
;; Routing of a Contract Vault
;;

;; message opcode reader utility: only binary receivers
;; Returns 32 bit message opcode, otherwise throws the "Invalid incoming message" exit code
(slice, int) ~load_opcode_internal(slice s) asm( -> 1 0) "32 LDUQ 130 THROWIFNOT";

() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    
    ;; Context
    var cs = in_msg_cell.begin_parse();
    cs~skip_bits(2);
    var msg_bounceable = cs~load_int(1);
    var msg_bounced = cs~load_int(1);
    slice msg_sender_addr = cs~load_msg_addr();
    __tact_context = (msg_bounceable, msg_sender_addr, msg_value, cs);
    __tact_context_sender = msg_sender_addr;
    
    ;; Load contract data
    var ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext) = $Vault$_contract_load();
    
    ;; Handle bounced messages
    if (msg_bounced) {
        ;; Skip 0xFFFFFFFF prefix of a bounced message
        in_msg~skip_bits(32);
        int op = 0;
        if (slice_bits(in_msg) >= 32) {
            op = in_msg~load_uint(32);
        }
        ;; Bounced handler for ATHTransferRequest message
        if (op == 0x41544810) {
            var ($msg'query_id, $msg'amount) = in_msg~$ATHTransferRequest$_load_bounced();
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireSealed();
            throw_unless(16060, ( equal_slices_bits(__tact_context_get_sender(), $self'vault_ath_wallet_address) ));
            throw_unless(16061, ($msg'amount > 0));
            tuple $pendingOpt = $PendingAthWithdrawal$_load_opt(__tact_dict_get_int_cell($self'pending_ath_withdrawals, 257, $msg'query_id));
            throw_unless(16062, (~ null?($pendingOpt)));
            var ($pending'owner_wallet, $pending'tombstone, $pending'recipient, $pending'recipient_ath_wallet, $pending'amount, $pending'refundable_ton_amount, $pending'created_at) = $PendingAthWithdrawal$_not_null($pendingOpt);
            throw_unless(16063, ($pending'amount == $msg'amount));
            var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_getUserOrEmpty($pending'owner_wallet);
            $user'ath_balance = $user'ath_balance + $pending'amount;
            $self'users~__tact_dict_set_slice_cell(267, $pending'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), begin_cell()));
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_refundPendingAthWithdrawalTon(($pending'owner_wallet, $pending'tombstone, $pending'recipient, $pending'recipient_ath_wallet, $pending'amount, $pending'refundable_ton_amount, $pending'created_at), $Context$_get_value(__tact_context_get()));
            $self'pending_ath_withdrawals~__tact_dict_delete_int(257, $msg'query_id);
            ifnot ($pending'tombstone) {
                $self'pending_ath_withdrawal_count = $self'pending_ath_withdrawal_count - 1;
            }
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        
        ;; Bounced handler for ATHTransferRequestVaultProfileAvatar message
        if (op == 0x4154481a) {
            var ($msg'query_id, $msg'amount) = in_msg~$ATHTransferRequestVaultProfileAvatar$_load_bounced();
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireSealed();
            throw_unless(16635, ( equal_slices_bits(__tact_context_get_sender(), $self'vault_ath_wallet_address) ));
            throw_unless(16636, ($msg'amount > 0));
            tuple $pendingProfileOpt = $PendingProfileAvatarPayment$_load_opt(__tact_dict_get_int_cell($self'pending_profile_avatar_payments, 257, $msg'query_id));
            throw_unless(16637, (~ null?($pendingProfileOpt)));
            var ($pendingProfile'owner_wallet, $pendingProfile'tombstone, $pendingProfile'amount, $pendingProfile'created_at) = $PendingProfileAvatarPayment$_not_null($pendingProfileOpt);
            throw_unless(16638, ($pendingProfile'amount == $msg'amount));
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_restorePendingProfileAvatarPayment($msg'query_id, ($pendingProfile'owner_wallet, $pendingProfile'tombstone, $pendingProfile'amount, $pendingProfile'created_at));
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        
        ;; Bounced handler for ATHTransferRequestVaultMintUsername message
        if (op == 0x4154481c) {
            var ($msg'query_id, $msg'amount) = in_msg~$ATHTransferRequestVaultMintUsername$_load_bounced();
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireSealed();
            throw_unless(16675, ( equal_slices_bits(__tact_context_get_sender(), $self'vault_ath_wallet_address) ));
            throw_unless(16676, ($msg'amount > 0));
            tuple $pendingUsernameOpt = $PendingUsernameMintPayment$_load_opt(__tact_dict_get_int_cell($self'pending_username_mint_payments, 257, $msg'query_id));
            throw_unless(16677, (~ null?($pendingUsernameOpt)));
            var ($pendingUsername'owner_wallet, $pendingUsername'tombstone, $pendingUsername'amount, $pendingUsername'created_at) = $PendingUsernameMintPayment$_not_null($pendingUsernameOpt);
            throw_unless(16678, ($pendingUsername'amount == $msg'amount));
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_restorePendingUsernameMintPayment($msg'query_id, ($pendingUsername'owner_wallet, $pendingUsername'tombstone, $pendingUsername'amount, $pendingUsername'created_at));
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        
        ;; Bounced handler for PublishBatchToHub message
        if (op == 0xa4f862d1) {
            var ($msg'bounce_id, $msg'bounce_tag) = in_msg~$PublishBatchToHub$_load_bounced();
            if (( ~ equal_slices_bits(__tact_context_get_sender(), $self'capsule_hub_address) )) {
                $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
                return ();
            }
            tuple $pendingOpt = $PendingBatchPublish$_load_opt(__tact_dict_get_int_cell($self'pending_batch_publishes, 257, $msg'bounce_id));
            if (null?($pendingOpt)) {
                $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
                return ();
            }
            var ($pending'owner_wallet, $pending'tombstone, $pending'refund_to_vault, $pending'nonce, $pending'publish_kind, $pending'part_count, $pending'publish_id, $pending'refundable_amount, $pending'created_at) = $PendingBatchPublish$_not_null($pendingOpt);
            if ((($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_computePublishBounceTag($pending'publish_id) != $msg'bounce_tag)) {
                $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
                return ();
            }
            var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_creditBatchSettlementRefund(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_getUserOrEmpty($pending'owner_wallet), $pending'refundable_amount);
            ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_updateReceipt(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), $pending'nonce, 2, 18446744073709551615);
            $self'users~__tact_dict_set_slice_cell(267, $pending'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), begin_cell()));
            ifnot ($pending'tombstone) {
                ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_decrementPendingPublishCount();
            }
            $self'pending_batch_publishes~__tact_dict_delete_int(257, $msg'bounce_id);
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        
        return ();
    }
    int op = 0;
    int in_msg_length = slice_bits(in_msg);
    if (in_msg_length >= 32) {
        op = in_msg~load_uint(32);
    }
    ;; Receive BindDeploymentManifest message
    if (op == 0x90e2e0cb) {
        var $msg'deployment_manifest_hash = in_msg~load_uint(256);
        var $msg'counterpart_address = in_msg~load_msg_addr();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireUnsealed();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireGenesisController();
        throw_unless(15010, (~ ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isCapsuleHubBound()));
        throw_unless(15011, ($msg'deployment_manifest_hash != 0));
        throw_unless(15012, ( (($self'deployment_manifest_hash == 0)) ? (true) : (($self'deployment_manifest_hash == $msg'deployment_manifest_hash)) ));
        throw_unless(15018, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isBasechainAddress($msg'counterpart_address));
        throw_unless(15019, ( ~ equal_slices_bits($msg'counterpart_address, $global_myAddress()) ));
        $self'deployment_manifest_hash = $msg'deployment_manifest_hash;
        $self'capsule_hub_address = $msg'counterpart_address;
        $self'binding_flags = $self'binding_flags + 1;
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
        return ();
    }
    
    ;; Receive BindOfficialAthWallet message
    if (op == 0x18db2ccb) {
        var $msg'deployment_manifest_hash = in_msg~load_uint(256);
        var $msg'official_ath_wallet_address = in_msg~load_msg_addr();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireUnsealed();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireGenesisController();
        throw_unless(15013, ( equal_slices_bits($self'vault_ath_wallet_address, __tact_context_get_sender()) ));
        throw_unless(15014, ($msg'deployment_manifest_hash > 1));
        throw_unless(15015, ( (($self'deployment_manifest_hash == 0)) ? (true) : (($self'deployment_manifest_hash == $msg'deployment_manifest_hash)) ));
        throw_unless(15016, ( ~ equal_slices_bits($msg'official_ath_wallet_address, __tact_context_get_sender()) ));
        throw_unless(15017, ( equal_slices_bits($msg'official_ath_wallet_address, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_deriveAthWalletAddress($global_myAddress())) ));
        $self'deployment_manifest_hash = $msg'deployment_manifest_hash;
        $self'vault_ath_wallet_address = $msg'official_ath_wallet_address;
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
        return ();
    }
    
    ;; Receive BindProfileRegistry message
    if (op == 0x50a61103) {
        var $msg'deployment_manifest_hash = in_msg~load_uint(256);
        var $msg'profile_registry_address = in_msg~load_msg_addr();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireUnsealed();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireGenesisController();
        throw_unless(15025, ($msg'deployment_manifest_hash > 1));
        throw_unless(15026, (~ ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isProfileRegistryBound()));
        throw_unless(15027, ($self'deployment_manifest_hash == $msg'deployment_manifest_hash));
        throw_unless(15028, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isBasechainAddress($msg'profile_registry_address));
        throw_unless(15029, ( ~ equal_slices_bits($msg'profile_registry_address, $global_myAddress()) ));
        $self'profile_registry_address = $msg'profile_registry_address;
        $self'binding_flags = $self'binding_flags + 2;
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
        return ();
    }
    
    ;; Receive BindUsernameRegistry message
    if (op == 0x50a61104) {
        var $msg'deployment_manifest_hash = in_msg~load_uint(256);
        var $msg'username_registry_address = in_msg~load_msg_addr();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireUnsealed();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireGenesisController();
        throw_unless(15032, ($msg'deployment_manifest_hash > 1));
        throw_unless(15033, (~ ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isUsernameRegistryBound()));
        throw_unless(15034, ($self'deployment_manifest_hash == $msg'deployment_manifest_hash));
        throw_unless(15035, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isBasechainAddress($msg'username_registry_address));
        throw_unless(15036, ( ~ equal_slices_bits($msg'username_registry_address, $global_myAddress()) ));
        $self'username_registry_address = $msg'username_registry_address;
        $self'binding_flags = $self'binding_flags + 4;
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
        return ();
    }
    
    ;; Receive SealGenesis message
    if (op == 0x3a12d1ad) {
        var $msg'deployment_manifest_hash = in_msg~load_uint(256);
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireUnsealed();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireGenesisController();
        throw_unless(15020, ($msg'deployment_manifest_hash > 1));
        throw_unless(15021, ($self'deployment_manifest_hash == $msg'deployment_manifest_hash));
        throw_unless(15022, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isCapsuleHubBound());
        throw_unless(15023, ( ~ equal_slices_bits($self'vault_ath_wallet_address, __tact_context_get_sender()) ));
        throw_unless(15024, ( equal_slices_bits($self'vault_ath_wallet_address, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_deriveAthWalletAddress($global_myAddress())) ));
        throw_unless(15037, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isProfileRegistryBound());
        throw_unless(15038, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isUsernameRegistryBound());
        throw_unless(15039, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isBasechainAddress($self'profile_registry_address));
        throw_unless(15040, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isBasechainAddress($self'username_registry_address));
        throw_unless(15041, ( ~ equal_slices_bits($self'profile_registry_address, $global_myAddress()) ));
        throw_unless(15042, ( ~ equal_slices_bits($self'username_registry_address, $global_myAddress()) ));
        $self'genesis_config_hash = 15000000000000000;
        $self'sealed = true;
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
        return ();
    }
    
    ;; Receive AnnounceSuccessorManifest message
    if (op == 0x5355434d) {
        var $msg'successor_manifest_hash = in_msg~load_uint(256);
        var $msg'successor_vault = in_msg~load_msg_addr();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireSealed();
        slice $ext = $Cell$_fun_beginParse($self'genesis_ext);
        int $controllerHash = $ext~load_uint(256);
        throw_unless(15050, ($controllerHash != 0));
        throw_unless(15051, ($Cell$_fun_hash($Builder$_fun_endCell($Builder$_fun_storeAddress($global_beginCell(), __tact_context_get_sender()))) == $controllerHash));
        $self'genesis_ext = $Builder$_fun_endCell(store_uint($Builder$_fun_storeAddress(store_uint($Builder$_fun_storeBool(true, store_uint($global_beginCell(), $controllerHash, 256)), $msg'successor_manifest_hash, 256), $msg'successor_vault), $global_now(), 64));
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
        return ();
    }
    
    ;; Receive DepositTon message
    if (op == 0x2aafbd98) {
        var $msg'amount = in_msg~load_uint(128);
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireSealed();
        throw_unless(16010, ($msg'amount > 0));
        int $exists = __tact_dict_exists_slice($self'users, 267, __tact_context_get_sender());
        ifnot ($exists) {
            throw_unless(16011, ($Context$_get_value(__tact_context_get()) >= (($msg'amount + 10000000) + 2000000)));
            $self'user_count = $self'user_count + 1;
        } else {
            throw_unless(16012, ($Context$_get_value(__tact_context_get()) >= ($msg'amount + 2000000)));
        }
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_getUserOrEmpty(__tact_context_get_sender());
        $user'ton_balance = $user'ton_balance + $msg'amount;
        $user'last_active = $global_now();
        $self'users~__tact_dict_set_slice_cell(267, __tact_context_get_sender(), $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), begin_cell()));
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
        return ();
    }
    
    ;; Receive EvictDormantUser message
    if (op == 0x45564454) {
        var $msg'owner_wallet = in_msg~load_msg_addr();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireSealed();
        tuple $userOpt = $UserState$_load_opt(__tact_dict_get_slice_cell($self'users, 267, $msg'owner_wallet));
        throw_unless(16900, (~ null?($userOpt)));
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = $UserState$_not_null($userOpt);
        throw_unless(16901, ($user'ton_balance == 0));
        throw_unless(16902, ($user'ath_balance == 0));
        throw_unless(16903, (($global_now() - $user'last_active) > 63072000));
        if ($user'current_key_id) {
            $self'key_records~__tact_dict_delete_int(257, $user'current_key_id);
            $self'key_record_count = $self'key_record_count - 1;
        }
        $self'users~__tact_dict_delete(267, $msg'owner_wallet);
        $self'user_count = $self'user_count - 1;
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
        return ();
    }
    
    ;; Receive AthTransferNotification message
    if (op == 0x472d9d7d) {
        var $msg'query_id = in_msg~load_uint(64);
        var $msg'sender_key = in_msg~load_uint(160);
        var $msg'amount = in_msg~load_uint(128);
        var $msg'sender_wallet = in_msg~load_msg_addr();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireSealed();
        throw_unless(16013, ( equal_slices_bits(__tact_context_get_sender(), $self'vault_ath_wallet_address) ));
        throw_unless(16014, ($msg'amount > 0));
        throw_unless(16074, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isBasechainAddress($msg'sender_wallet));
        int $depositId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_computeAthDepositId($msg'sender_wallet, $msg'query_id);
        if (__tact_dict_exists_int($self'processed_ath_deposits, 257, $depositId)) {
            if (($Context$_get_value(__tact_context_get()) >= 1000000)) {
                $global_message($MessageParameters$_constructor_to_value_bounce_mode_body(__tact_context_get_sender(), 1000000, false, 0, $AthTransferNotificationAck$_store_cell($AthTransferNotificationAck$_constructor_query_id_sender_key_amount($msg'query_id, $msg'sender_key, $msg'amount), begin_cell())));
            }
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        int $exists = __tact_dict_exists_slice($self'users, 267, $msg'sender_wallet);
        ifnot ($exists) {
            throw_unless(16015, ($Context$_get_value(__tact_context_get()) >= 13000000));
            $self'user_count = $self'user_count + 1;
        } else {
            throw_unless(16016, ($Context$_get_value(__tact_context_get()) >= 1000000));
        }
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_getUserOrEmpty($msg'sender_wallet);
        $user'ath_balance = $user'ath_balance + $msg'amount;
        $self'users~__tact_dict_set_slice_cell(267, $msg'sender_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), begin_cell()));
        $self'processed_ath_deposits~__tact_dict_set_int_int(257, $depositId, $msg'amount, 257);
        $self'processed_ath_deposit_count = $self'processed_ath_deposit_count + 1;
        $global_message($MessageParameters$_constructor_to_value_bounce_mode_body(__tact_context_get_sender(), 1000000, false, 0, $AthTransferNotificationAck$_store_cell($AthTransferNotificationAck$_constructor_query_id_sender_key_amount($msg'query_id, $msg'sender_key, $msg'amount), begin_cell())));
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
        return ();
    }
    
    ;; Receive ATHTransferAck message
    if (op == 0x41544811) {
        var $msg'query_id = in_msg~load_uint(64);
        var $msg'amount = in_msg~load_uint(128);
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireSealed();
        tuple $pendingUsernameOpt = $PendingUsernameMintPayment$_load_opt(__tact_dict_get_int_cell($self'pending_username_mint_payments, 257, $msg'query_id));
        if ((~ null?($pendingUsernameOpt))) {
            var ($pendingUsername'owner_wallet, $pendingUsername'tombstone, $pendingUsername'amount, $pendingUsername'created_at) = $PendingUsernameMintPayment$_not_null($pendingUsernameOpt);
            throw_unless(16660, ($pendingUsername'amount == $msg'amount));
            if (( equal_slices_bits(__tact_context_get_sender(), ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_usernameRegistryAthWalletAddress()) )) {
                $self'pending_username_mint_payments~__tact_dict_delete_int(257, $msg'query_id);
                ifnot ($pendingUsername'tombstone) {
                    ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_decrementPendingUsernameMintPaymentCount();
                }
            } else {
                throw_unless(16661, ( equal_slices_bits(__tact_context_get_sender(), $self'vault_ath_wallet_address) ));
                ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_restorePendingUsernameMintPayment($msg'query_id, ($pendingUsername'owner_wallet, $pendingUsername'tombstone, $pendingUsername'amount, $pendingUsername'created_at));
            }
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        tuple $pendingProfileOpt = $PendingProfileAvatarPayment$_load_opt(__tact_dict_get_int_cell($self'pending_profile_avatar_payments, 257, $msg'query_id));
        if ((~ null?($pendingProfileOpt))) {
            var ($pendingProfile'owner_wallet, $pendingProfile'tombstone, $pendingProfile'amount, $pendingProfile'created_at) = $PendingProfileAvatarPayment$_not_null($pendingProfileOpt);
            throw_unless(16620, ($pendingProfile'amount == $msg'amount));
            if (( equal_slices_bits(__tact_context_get_sender(), ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_profileRegistryAthWalletAddress()) )) {
                $self'pending_profile_avatar_payments~__tact_dict_delete_int(257, $msg'query_id);
                ifnot ($pendingProfile'tombstone) {
                    ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_decrementPendingProfileAvatarPaymentCount();
                }
            } else {
                throw_unless(16621, ( equal_slices_bits(__tact_context_get_sender(), $self'vault_ath_wallet_address) ));
                ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_restorePendingProfileAvatarPayment($msg'query_id, ($pendingProfile'owner_wallet, $pendingProfile'tombstone, $pendingProfile'amount, $pendingProfile'created_at));
            }
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        if (( equal_slices_bits(__tact_context_get_sender(), $self'vault_ath_wallet_address) )) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        tuple $pendingOpt = $PendingAthWithdrawal$_load_opt(__tact_dict_get_int_cell($self'pending_ath_withdrawals, 257, $msg'query_id));
        throw_unless(16028, (~ null?($pendingOpt)));
        var ($pending'owner_wallet, $pending'tombstone, $pending'recipient, $pending'recipient_ath_wallet, $pending'amount, $pending'refundable_ton_amount, $pending'created_at) = $PendingAthWithdrawal$_not_null($pendingOpt);
        throw_unless(16029, ($pending'amount == $msg'amount));
        throw_unless(16030, ( equal_slices_bits(__tact_context_get_sender(), $pending'recipient_ath_wallet) ));
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_refundPendingAthWithdrawalTon(($pending'owner_wallet, $pending'tombstone, $pending'recipient, $pending'recipient_ath_wallet, $pending'amount, $pending'refundable_ton_amount, $pending'created_at), $Context$_get_value(__tact_context_get()));
        $self'pending_ath_withdrawals~__tact_dict_delete_int(257, $msg'query_id);
        ifnot ($pending'tombstone) {
            $self'pending_ath_withdrawal_count = $self'pending_ath_withdrawal_count - 1;
        }
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
        return ();
    }
    
    ;; Receive ATHTransferFailed message
    if (op == 0x41544813) {
        var $msg'query_id = in_msg~load_uint(64);
        var $msg'amount = in_msg~load_uint(128);
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireSealed();
        tuple $pendingUsernameOpt = $PendingUsernameMintPayment$_load_opt(__tact_dict_get_int_cell($self'pending_username_mint_payments, 257, $msg'query_id));
        if ((~ null?($pendingUsernameOpt))) {
            var ($pendingUsername'owner_wallet, $pendingUsername'tombstone, $pendingUsername'amount, $pendingUsername'created_at) = $PendingUsernameMintPayment$_not_null($pendingUsernameOpt);
            throw_unless(16670, ($pendingUsername'amount == $msg'amount));
            throw_unless(16671, ( equal_slices_bits(__tact_context_get_sender(), $self'vault_ath_wallet_address) ));
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_restorePendingUsernameMintPayment($msg'query_id, ($pendingUsername'owner_wallet, $pendingUsername'tombstone, $pendingUsername'amount, $pendingUsername'created_at));
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        tuple $pendingProfileOpt = $PendingProfileAvatarPayment$_load_opt(__tact_dict_get_int_cell($self'pending_profile_avatar_payments, 257, $msg'query_id));
        if ((~ null?($pendingProfileOpt))) {
            var ($pendingProfile'owner_wallet, $pendingProfile'tombstone, $pendingProfile'amount, $pendingProfile'created_at) = $PendingProfileAvatarPayment$_not_null($pendingProfileOpt);
            throw_unless(16630, ($pendingProfile'amount == $msg'amount));
            throw_unless(16631, ( equal_slices_bits(__tact_context_get_sender(), $self'vault_ath_wallet_address) ));
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_restorePendingProfileAvatarPayment($msg'query_id, ($pendingProfile'owner_wallet, $pendingProfile'tombstone, $pendingProfile'amount, $pendingProfile'created_at));
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        throw_unless(16070, ( equal_slices_bits(__tact_context_get_sender(), $self'vault_ath_wallet_address) ));
        throw_unless(16071, ($msg'amount > 0));
        tuple $pendingOpt = $PendingAthWithdrawal$_load_opt(__tact_dict_get_int_cell($self'pending_ath_withdrawals, 257, $msg'query_id));
        throw_unless(16072, (~ null?($pendingOpt)));
        var ($pending'owner_wallet, $pending'tombstone, $pending'recipient, $pending'recipient_ath_wallet, $pending'amount, $pending'refundable_ton_amount, $pending'created_at) = $PendingAthWithdrawal$_not_null($pendingOpt);
        throw_unless(16073, ($pending'amount == $msg'amount));
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_getUserOrEmpty($pending'owner_wallet);
        $user'ath_balance = $user'ath_balance + $pending'amount;
        $self'users~__tact_dict_set_slice_cell(267, $pending'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), begin_cell()));
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_refundPendingAthWithdrawalTon(($pending'owner_wallet, $pending'tombstone, $pending'recipient, $pending'recipient_ath_wallet, $pending'amount, $pending'refundable_ton_amount, $pending'created_at), $Context$_get_value(__tact_context_get()));
        $self'pending_ath_withdrawals~__tact_dict_delete_int(257, $msg'query_id);
        ifnot ($pending'tombstone) {
            $self'pending_ath_withdrawal_count = $self'pending_ath_withdrawal_count - 1;
        }
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
        return ();
    }
    
    ;; Receive ProfileAvatarTonExcessRefund message
    if (op == 0x50a61121) {
        var $msg'query_id = in_msg~load_uint(64);
        var $msg'owner_wallet = in_msg~load_msg_addr();
        var $msg'amount = in_msg~load_uint(128);
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireSealed();
        throw_unless(16680, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isProfileRegistryBound());
        throw_unless(16681, ( equal_slices_bits(__tact_context_get_sender(), $self'profile_registry_address) ));
        throw_unless(16682, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isBasechainAddress($msg'owner_wallet));
        throw_unless(16683, ($msg'amount > 0));
        tuple $userOpt = $UserState$_load_opt(__tact_dict_get_slice_cell($self'users, 267, $msg'owner_wallet));
        throw_unless(16684, (~ null?($userOpt)));
        if (($Context$_get_value(__tact_context_get()) <= 2000000)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        int $creditAmount = ($Context$_get_value(__tact_context_get()) - 2000000);
        if (($creditAmount > $msg'amount)) {
            $creditAmount = $msg'amount;
        }
        if (($creditAmount <= 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = $UserState$_not_null($userOpt);
        $user'ton_balance = $user'ton_balance + $creditAmount;
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), begin_cell()));
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
        return ();
    }
    
    ;; Receive TopUpStorageReserve message
    if (op == 0x3215b5fd) {
        var $msg = empty_tuple();
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
        return ();
    }
    
    ;; Receive RegisterMessagingKeys message
    if (op == 0x52705eda) {
        var $msg'enc_pubkey = in_msg~load_uint(256);
        var $msg'sign_pubkey = in_msg~load_uint(256);
        var $msg'auth_pubkey = in_msg~load_uint(256);
        slice sc_1 = in_msg~load_ref().begin_parse();
        var $msg'pq_kem_pubkey_hash = sc_1~load_uint(256);
        var $msg'pq_kem_pubkey_len = sc_1~load_uint(16);
        var $msg'pq_kem_pubkey = sc_1~load_ref();
        var $msg'crypto_suite_mask = sc_1~load_uint(16);
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireSealed();
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_validateKeyProfile($msg'enc_pubkey, $msg'sign_pubkey, $msg'pq_kem_pubkey_hash, $msg'pq_kem_pubkey_len, $msg'pq_kem_pubkey, $msg'crypto_suite_mask);
        throw_unless(16118, ($msg'auth_pubkey != 0));
        throw_unless(16119, ($msg'auth_pubkey != $msg'sign_pubkey));
        int $userExists = __tact_dict_exists_slice($self'users, 267, __tact_context_get_sender());
        int $required = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_keyRecordStorageEndowment($msg'crypto_suite_mask);
        ifnot ($userExists) {
            $required = $required + 10000000;
        }
        $required = $required + 2000000;
        throw_unless(16110, ($Context$_get_value(__tact_context_get()) >= $required));
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_getUserOrEmpty(__tact_context_get_sender());
        throw_unless(16111, ($user'current_key_id == 0));
        int $keyGeneration = 0;
        int $keyId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_computeKeyId(__tact_context_get_sender(), $keyGeneration, $msg'enc_pubkey, $msg'sign_pubkey, $msg'pq_kem_pubkey_hash, $msg'pq_kem_pubkey_len, $msg'crypto_suite_mask);
        throw_unless(16112, (~ __tact_dict_exists_int($self'key_records, 257, $keyId)));
        ifnot ($userExists) {
            $self'user_count = $self'user_count + 1;
        }
        $self'key_records~__tact_dict_set_int_cell(257, $keyId, $KeyRecord$_store_cell($KeyRecord$_constructor_owner_wallet_key_generation_enc_pubkey_sign_pubkey_pq_kem_pubkey_hash_pq_kem_pubkey_len_pq_kem_pubkey_crypto_suite_mask_created_at_created_lt_revoked_at_revoked_lt(__tact_context_get_sender(), $keyGeneration, $msg'enc_pubkey, $msg'sign_pubkey, $msg'pq_kem_pubkey_hash, $msg'pq_kem_pubkey_len, $msg'pq_kem_pubkey, $msg'crypto_suite_mask, $global_now(), $global_curLt(), 0, 0), begin_cell()));
        $self'key_record_count = $self'key_record_count + 1;
        $user'current_key_id = $keyId;
        $user'auth_pubkey = $msg'auth_pubkey;
        $user'last_active = $global_now();
        $self'users~__tact_dict_set_slice_cell(267, __tact_context_get_sender(), $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), begin_cell()));
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
        return ();
    }
    
    ;; Receive CapsuleHubBatchAck message
    if (op == 0x874e5771) {
        var $msg'publish_id = in_msg~load_uint(256);
        var $msg'first_entry_id = in_msg~load_uint(64);
        var $msg'part_count = in_msg~load_uint(8);
        var $msg'batch_uid = in_msg~load_uint(256);
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireSealed();
        throw_unless(16550, ( equal_slices_bits(__tact_context_get_sender(), $self'capsule_hub_address) ));
        int $bounceId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_computePublishBounceId($msg'publish_id);
        tuple $pendingOpt = $PendingBatchPublish$_load_opt(__tact_dict_get_int_cell($self'pending_batch_publishes, 257, $bounceId));
        throw_unless(16551, (~ null?($pendingOpt)));
        var ($pending'owner_wallet, $pending'tombstone, $pending'refund_to_vault, $pending'nonce, $pending'publish_kind, $pending'part_count, $pending'publish_id, $pending'refundable_amount, $pending'created_at) = $PendingBatchPublish$_not_null($pendingOpt);
        throw_unless(16552, ($pending'publish_id == $msg'publish_id));
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_creditActivityAirdrop($pending'owner_wallet, $pending'part_count);
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_creditBatchSettlementRefund(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_getUserOrEmpty($pending'owner_wallet), $pending'refundable_amount);
        ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_updateReceipt(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), $pending'nonce, 1, $msg'first_entry_id);
        $self'users~__tact_dict_set_slice_cell(267, $pending'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), begin_cell()));
        ifnot ($pending'tombstone) {
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_decrementPendingPublishCount();
        }
        $self'pending_batch_publishes~__tact_dict_delete_int(257, $bounceId);
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
        return ();
    }
    
    ;; Receive PruneBatchPublish message
    if (op == 0x720bdd6e) {
        var $msg'publish_id = in_msg~load_uint(256);
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireSealed();
        throw_unless(16563, ($Context$_get_value(__tact_context_get()) >= 2000000));
        int $bounceId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_computePublishBounceId($msg'publish_id);
        tuple $pendingOpt = $PendingBatchPublish$_load_opt(__tact_dict_get_int_cell($self'pending_batch_publishes, 257, $bounceId));
        throw_unless(16560, (~ null?($pendingOpt)));
        var ($pending'owner_wallet, $pending'tombstone, $pending'refund_to_vault, $pending'nonce, $pending'publish_kind, $pending'part_count, $pending'publish_id, $pending'refundable_amount, $pending'created_at) = $PendingBatchPublish$_not_null($pendingOpt);
        throw_unless(16561, ($pending'publish_id == $msg'publish_id));
        if ($pending'tombstone) {
            throw_unless(16564, ($global_now() >= $pending'created_at));
            $self'pending_batch_publishes~__tact_dict_delete_int(257, $bounceId);
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        throw_unless(16562, ($global_now() >= ($pending'created_at + 86400)));
        int $tombstoneExpiresAt = ($global_now() + 86400);
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_updateReceipt(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_getUserOrEmpty($pending'owner_wallet), $pending'nonce, 3, 18446744073709551615);
        $self'users~__tact_dict_set_slice_cell(267, $pending'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), begin_cell()));
        $self'pending_batch_publishes~__tact_dict_set_int_cell(257, $bounceId, $PendingBatchPublish$_store_cell($PendingBatchPublish$_constructor_owner_wallet_tombstone_refund_to_vault_nonce_publish_kind_part_count_publish_id_refundable_amount_created_at($pending'owner_wallet, true, $pending'refund_to_vault, $pending'nonce, $pending'publish_kind, $pending'part_count, $pending'publish_id, $pending'refundable_amount, $tombstoneExpiresAt), begin_cell()));
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_decrementPendingPublishCount();
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
        return ();
    }
    
    ;; Receive PruneStuckAthPending message
    if (op == 0x720bdd6f) {
        var $msg'kind = in_msg~load_uint(8);
        var $msg'query_id = in_msg~load_uint(64);
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireSealed();
        throw_unless(16573, ($Context$_get_value(__tact_context_get()) >= 2000000));
        int $tombstoneExpiresAt = ($global_now() + 86400);
        if (($msg'kind == 1)) {
            tuple $opt = $PendingUsernameMintPayment$_load_opt(__tact_dict_get_int_cell($self'pending_username_mint_payments, 257, $msg'query_id));
            throw_unless(16570, (~ null?($opt)));
            var ($p'owner_wallet, $p'tombstone, $p'amount, $p'created_at) = $PendingUsernameMintPayment$_not_null($opt);
            if ($p'tombstone) {
                throw_unless(16574, ($global_now() >= $p'created_at));
                $self'pending_username_mint_payments~__tact_dict_delete_int(257, $msg'query_id);
                $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
                return ();
            }
            throw_unless(16571, ($global_now() >= ($p'created_at + 86400)));
            $self'pending_username_mint_payments~__tact_dict_set_int_cell(257, $msg'query_id, $PendingUsernameMintPayment$_store_cell($PendingUsernameMintPayment$_constructor_owner_wallet_tombstone_amount_created_at($p'owner_wallet, true, $p'amount, $tombstoneExpiresAt), begin_cell()));
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_decrementPendingUsernameMintPaymentCount();
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        if (($msg'kind == 2)) {
            tuple $opt = $PendingProfileAvatarPayment$_load_opt(__tact_dict_get_int_cell($self'pending_profile_avatar_payments, 257, $msg'query_id));
            throw_unless(16570, (~ null?($opt)));
            var ($p'owner_wallet, $p'tombstone, $p'amount, $p'created_at) = $PendingProfileAvatarPayment$_not_null($opt);
            if ($p'tombstone) {
                throw_unless(16574, ($global_now() >= $p'created_at));
                $self'pending_profile_avatar_payments~__tact_dict_delete_int(257, $msg'query_id);
                $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
                return ();
            }
            throw_unless(16571, ($global_now() >= ($p'created_at + 86400)));
            $self'pending_profile_avatar_payments~__tact_dict_set_int_cell(257, $msg'query_id, $PendingProfileAvatarPayment$_store_cell($PendingProfileAvatarPayment$_constructor_owner_wallet_tombstone_amount_created_at($p'owner_wallet, true, $p'amount, $tombstoneExpiresAt), begin_cell()));
            ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_decrementPendingProfileAvatarPaymentCount();
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        if (($msg'kind == 3)) {
            tuple $opt = $PendingAthWithdrawal$_load_opt(__tact_dict_get_int_cell($self'pending_ath_withdrawals, 257, $msg'query_id));
            throw_unless(16570, (~ null?($opt)));
            var ($p'owner_wallet, $p'tombstone, $p'recipient, $p'recipient_ath_wallet, $p'amount, $p'refundable_ton_amount, $p'created_at) = $PendingAthWithdrawal$_not_null($opt);
            if ($p'tombstone) {
                throw_unless(16574, ($global_now() >= $p'created_at));
                $self'pending_ath_withdrawals~__tact_dict_delete_int(257, $msg'query_id);
                $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
                return ();
            }
            throw_unless(16571, ($global_now() >= ($p'created_at + 86400)));
            $self'pending_ath_withdrawals~__tact_dict_set_int_cell(257, $msg'query_id, $PendingAthWithdrawal$_store_cell($PendingAthWithdrawal$_constructor_owner_wallet_tombstone_recipient_recipient_ath_wallet_amount_refundable_ton_amount_created_at($p'owner_wallet, true, $p'recipient, $p'recipient_ath_wallet, $p'amount, $p'refundable_ton_amount, $tombstoneExpiresAt), begin_cell()));
            $self'pending_ath_withdrawal_count = $self'pending_ath_withdrawal_count - 1;
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        throw_unless(16572, false);
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
        return ();
    }
    
    ;; Receive empty message
    if ((op == 0) & (in_msg_length <= 32)) {
        throw(16099);
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
        return ();
    }
    ;; Throw if not handled
    throw(130);
}

;; message opcode reader utility: only binary receivers
;; Returns 32 bit message opcode, otherwise throws the "Invalid incoming message" exit code
(slice, int) ~load_opcode_external(slice s) asm( -> 1 0) "32 LDUQ 130 THROWIFNOT";

() recv_external(slice in_msg) impure {
    ;; Load contract data
    var ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext) = $Vault$_contract_load();
    
    var op = in_msg~load_opcode_external();
    ;; Receive WithdrawTonFromVaultBalance message
    if (op == 0x7e1f5038) {
        var $msg'owner_wallet = in_msg~load_msg_addr();
        var $msg'signature = in_msg~load_bits(512);
        var $msg'signed_payload = in_msg~load_ref();
        var $msg'envelope_padding = in_msg;
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireEmptyEnvelopePadding($msg'envelope_padding);
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireSealed();
        throw_unless(16800, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isBasechainAddress($msg'owner_wallet));
        tuple $userOpt = $UserState$_load_opt(__tact_dict_get_slice_cell($self'users, 267, $msg'owner_wallet));
        throw_unless(16801, (~ null?($userOpt)));
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = $UserState$_not_null($userOpt);
        throw_unless(16802, ($user'auth_pubkey != 0));
        throw_unless(16803, $global_checkSignature($Cell$_fun_hash($msg'signed_payload), $msg'signature, $user'auth_pubkey));
        slice $signedPayload = $Cell$_fun_beginParse($msg'signed_payload);
        int $signedDomain = $signedPayload~load_uint(32);
        throw_unless(16804, ($signedDomain == 1448367921));
        int $signedManifestHash = $signedPayload~load_uint(256);
        throw_unless(16805, ($signedManifestHash == $self'deployment_manifest_hash));
        slice $signedVaultAddress = $signedPayload~$Slice$_fun_loadAddress();
        throw_unless(16806, ( equal_slices_bits($signedVaultAddress, $global_myAddress()) ));
        slice $signedOwner = $signedPayload~$Slice$_fun_loadAddress();
        throw_unless(16807, ( equal_slices_bits($signedOwner, $msg'owner_wallet) ));
        int $clientNonce = $signedPayload~load_uint(64);
        throw_unless(16808, ($clientNonce == $user'publish_nonce));
        throw_unless(16816, ($user'ton_balance >= 2000000));
        $global_acceptMessage();
        $user'ton_balance = $user'ton_balance - 2000000;
        $user'publish_nonce = $user'publish_nonce + 1;
        ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_writeReceipt(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), ($user'publish_nonce - 1), 1, 0, 0, 0);
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), begin_cell()));
        $global_commit();
        ifnot (($Slice$_fun_bits($signedPayload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($Slice$_fun_refs($signedPayload) == 1)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        slice $actionPayload = $Cell$_fun_beginParse($signedPayload~$Slice$_fun_loadRef());
        int $amount = $actionPayload~load_uint(128);
        slice $recipient = $actionPayload~$Slice$_fun_loadAddress();
        ifnot (($Slice$_fun_bits($actionPayload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($Slice$_fun_refs($actionPayload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($amount > 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (( ~ equal_slices_bits($recipient, $global_myAddress()) )) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isBasechainAddress($recipient)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($user'ton_balance >= $amount)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($global_myBalance() >= ($amount + ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_vaultStorageReserve()))) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        $user'ton_balance = $user'ton_balance - $amount;
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), begin_cell()));
        $global_message($MessageParameters$_constructor_to_value_bounce_mode_body($recipient, $amount, false, 1, null()));
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
        return ();
    }
    
    ;; Receive WithdrawAthFromVaultBalance message
    if (op == 0x7e1f5039) {
        var $msg'owner_wallet = in_msg~load_msg_addr();
        var $msg'signature = in_msg~load_bits(512);
        var $msg'signed_payload = in_msg~load_ref();
        var $msg'envelope_padding = in_msg;
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireEmptyEnvelopePadding($msg'envelope_padding);
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireSealed();
        throw_unless(16075, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isBasechainAddress($msg'owner_wallet));
        tuple $userOpt = $UserState$_load_opt(__tact_dict_get_slice_cell($self'users, 267, $msg'owner_wallet));
        throw_unless(16024, (~ null?($userOpt)));
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = $UserState$_not_null($userOpt);
        throw_unless(16031, ($user'auth_pubkey != 0));
        throw_unless(16032, $global_checkSignature($Cell$_fun_hash($msg'signed_payload), $msg'signature, $user'auth_pubkey));
        slice $signedPayload = $Cell$_fun_beginParse($msg'signed_payload);
        int $signedDomain = $signedPayload~load_uint(32);
        throw_unless(16033, ($signedDomain == 1448558897));
        int $signedManifestHash = $signedPayload~load_uint(256);
        throw_unless(16034, ($signedManifestHash == $self'deployment_manifest_hash));
        slice $signedVaultAddress = $signedPayload~$Slice$_fun_loadAddress();
        throw_unless(16035, ( equal_slices_bits($signedVaultAddress, $global_myAddress()) ));
        slice $signedOwner = $signedPayload~$Slice$_fun_loadAddress();
        throw_unless(16036, ( equal_slices_bits($signedOwner, $msg'owner_wallet) ));
        int $clientNonce = $signedPayload~load_uint(64);
        throw_unless(16037, ($clientNonce == $user'publish_nonce));
        throw_unless(16027, ($user'ton_balance >= 58000000));
        $global_acceptMessage();
        $user'publish_nonce = $user'publish_nonce + 1;
        ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_writeReceipt(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), ($user'publish_nonce - 1), 2, 0, 0, 0);
        $user'ton_balance = $user'ton_balance - 58000000;
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), begin_cell()));
        $global_commit();
        ifnot (($Slice$_fun_bits($signedPayload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($Slice$_fun_refs($signedPayload) == 1)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        slice $actionPayload = $Cell$_fun_beginParse($signedPayload~$Slice$_fun_loadRef());
        int $amount = $actionPayload~load_uint(128);
        slice $recipient = $actionPayload~$Slice$_fun_loadAddress();
        ifnot (($Slice$_fun_bits($actionPayload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($Slice$_fun_refs($actionPayload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($amount > 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (( ~ equal_slices_bits($recipient, $global_myAddress()) )) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isBasechainAddress($recipient)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($user'ath_balance >= $amount)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        int $withdrawalId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_computeAthWithdrawalId($msg'owner_wallet, $clientNonce);
        if (__tact_dict_exists_int($self'pending_ath_withdrawals, 257, $withdrawalId)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($global_myBalance() >= (58000000 + ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_vaultStorageReserve()))) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        $user'ath_balance = $user'ath_balance - $amount;
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), begin_cell()));
        slice $recipientAthWallet = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_deriveAthWalletAddress($recipient);
        $self'pending_ath_withdrawals~__tact_dict_set_int_cell(257, $withdrawalId, $PendingAthWithdrawal$_store_cell($PendingAthWithdrawal$_constructor_owner_wallet_tombstone_recipient_recipient_ath_wallet_amount_refundable_ton_amount_created_at($msg'owner_wallet, false, $recipient, $recipientAthWallet, $amount, 58000000, $global_now()), begin_cell()));
        $self'pending_ath_withdrawal_count = $self'pending_ath_withdrawal_count + 1;
        $global_message($MessageParameters$_constructor_to_value_bounce_mode_body($self'vault_ath_wallet_address, 58000000, true, 0, $ATHTransferRequest$_store_cell($ATHTransferRequest$_constructor_query_id_amount_recipient_response_destination($withdrawalId, $amount, $recipient, $global_myAddress()), begin_cell())));
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
        return ();
    }
    
    ;; Receive ReplaceMessagingKeys message
    if (op == 0x89d648bb) {
        var $msg'owner_wallet = in_msg~load_msg_addr();
        var $msg'signature = in_msg~load_bits(512);
        var $msg'signed_payload = in_msg~load_ref();
        var $msg'envelope_padding = in_msg;
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireEmptyEnvelopePadding($msg'envelope_padding);
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireSealed();
        tuple $userOpt = $UserState$_load_opt(__tact_dict_get_slice_cell($self'users, 267, $msg'owner_wallet));
        throw_unless(16120, (~ null?($userOpt)));
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = $UserState$_not_null($userOpt);
        throw_unless(16121, ($user'current_key_id != 0));
        throw_unless(16128, ($user'auth_pubkey != 0));
        throw_unless(16130, $global_checkSignature($Cell$_fun_hash($msg'signed_payload), $msg'signature, $user'auth_pubkey));
        slice $signedPayload = $Cell$_fun_beginParse($msg'signed_payload);
        throw_unless(16136, ($Slice$_fun_bits($signedPayload) == 864));
        throw_unless(16137, ($Slice$_fun_refs($signedPayload) == 1));
        int $signedDomain = $signedPayload~load_uint(32);
        throw_unless(16131, ($signedDomain == 1448233777));
        int $signedManifestHash = $signedPayload~load_uint(256);
        throw_unless(16132, ($signedManifestHash == $self'deployment_manifest_hash));
        int $signedVaultHash = $signedPayload~load_uint(256);
        slice $vaultAddressSlice = $Address$_fun_asSlice($global_myAddress());
        throw_unless(16133, ($vaultAddressSlice~load_uint(11) == 1024));
        throw_unless(16133, ($signedVaultHash == $vaultAddressSlice~load_uint(256)));
        int $signedOwnerHash = $signedPayload~load_uint(256);
        slice $ownerPrefix = $Address$_fun_asSlice($msg'owner_wallet);
        throw_unless(16134, ($ownerPrefix~load_uint(11) == 1024));
        throw_unless(16134, ($signedOwnerHash == $ownerPrefix~load_uint(256)));
        int $clientNonce = $signedPayload~load_uint(64);
        throw_unless(16135, ($clientNonce == $user'publish_nonce));
        int $localReserve = 2000000;
        throw_unless(16126, ($user'ton_balance >= $localReserve));
        $global_acceptMessage();
        $user'ton_balance = $user'ton_balance - $localReserve;
        $user'publish_nonce = $user'publish_nonce + 1;
        ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_writeReceipt(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), ($user'publish_nonce - 1), 3, 0, 0, 0);
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), begin_cell()));
        $global_commit();
        cell $actionPayloadCell = $signedPayload~$Slice$_fun_loadRef();
        slice $actionPayloadPreview = $Cell$_fun_beginParse($actionPayloadCell);
        ifnot (($Slice$_fun_bits($actionPayloadPreview) == 800)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($Slice$_fun_refs($actionPayloadPreview) == 1)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        int $required = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_keyRecordStorageEndowment(2);
        $required = $required + 2000000;
        int $remainingRequired = ($required - $localReserve);
        ifnot (($user'ton_balance >= $remainingRequired)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        slice $actionPayload = $Cell$_fun_beginParse($actionPayloadCell);
        int $encPubkey = $actionPayload~load_uint(256);
        int $signPubkey = $actionPayload~load_uint(256);
        int $pqKemPubkeyHash = $actionPayload~load_uint(256);
        int $pqKemPubkeyLen = $actionPayload~load_uint(16);
        cell $pqKemPubkey = $actionPayload~$Slice$_fun_loadRef();
        int $cryptoSuiteMask = $actionPayload~load_uint(16);
        ifnot (($Slice$_fun_bits($actionPayload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($Slice$_fun_refs($actionPayload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isValidKeyProfile($encPubkey, $signPubkey, $pqKemPubkeyHash, $pqKemPubkeyLen, $pqKemPubkey, $cryptoSuiteMask)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($signPubkey != $user'auth_pubkey)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        tuple $oldRecordOpt = $KeyRecord$_load_opt(__tact_dict_get_int_cell($self'key_records, 257, $user'current_key_id));
        if (null?($oldRecordOpt)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        var ($oldRecord'owner_wallet, $oldRecord'key_generation, $oldRecord'enc_pubkey, $oldRecord'sign_pubkey, $oldRecord'pq_kem_pubkey_hash, $oldRecord'pq_kem_pubkey_len, $oldRecord'pq_kem_pubkey, $oldRecord'crypto_suite_mask, $oldRecord'created_at, $oldRecord'created_lt, $oldRecord'revoked_at, $oldRecord'revoked_lt) = $KeyRecord$_not_null($oldRecordOpt);
        ifnot (( equal_slices_bits($oldRecord'owner_wallet, $msg'owner_wallet) )) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($oldRecord'revoked_lt == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($oldRecord'key_generation < 4294967295)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        int $keyGeneration = ($oldRecord'key_generation + 1);
        int $newKeyId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_computeKeyId($msg'owner_wallet, $keyGeneration, $encPubkey, $signPubkey, $pqKemPubkeyHash, $pqKemPubkeyLen, $cryptoSuiteMask);
        if (__tact_dict_exists_int($self'key_records, 257, $newKeyId)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        $self'key_records~__tact_dict_delete_int(257, $user'current_key_id);
        $self'key_records~__tact_dict_set_int_cell(257, $newKeyId, $KeyRecord$_store_cell($KeyRecord$_constructor_owner_wallet_key_generation_enc_pubkey_sign_pubkey_pq_kem_pubkey_hash_pq_kem_pubkey_len_pq_kem_pubkey_crypto_suite_mask_created_at_created_lt_revoked_at_revoked_lt($msg'owner_wallet, $keyGeneration, $encPubkey, $signPubkey, $pqKemPubkeyHash, $pqKemPubkeyLen, $pqKemPubkey, $cryptoSuiteMask, $global_now(), $global_curLt(), 0, 0), begin_cell()));
        $user'current_key_id = $newKeyId;
        $user'ton_balance = $user'ton_balance - $remainingRequired;
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), begin_cell()));
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
        return ();
    }
    
    ;; Receive PublishBatchFromVaultBalance message
    if (op == 0x7e1f5041) {
        var $msg'owner_wallet = in_msg~load_msg_addr();
        var $msg'signature = in_msg~load_bits(512);
        var $msg'signed_payload = in_msg~load_ref();
        var $msg'envelope_padding = in_msg;
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireEmptyEnvelopePadding($msg'envelope_padding);
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireSealed();
        tuple $userOpt = $UserState$_load_opt(__tact_dict_get_slice_cell($self'users, 267, $msg'owner_wallet));
        throw_unless(16451, (~ null?($userOpt)));
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = $UserState$_not_null($userOpt);
        throw_unless(16454, ($user'auth_pubkey != 0));
        throw_unless(16457, $global_checkSignature($Cell$_fun_hash($msg'signed_payload), $msg'signature, $user'auth_pubkey));
        slice $root = $Cell$_fun_beginParse($msg'signed_payload);
        $root~$Slice$_fun_skipBits(808);
        int $clientNonce = $root~load_uint(64);
        throw_unless(16453, ($clientNonce == $user'publish_nonce));
        int $maxCharge = $root~load_uint(128);
        int $partCount = $root~load_uint(8);
        throw_unless(16485, ($maxCharge >= (92700000 + (6200000 * $partCount))));
        throw_unless(16463, ($user'ton_balance >= $maxCharge));
        $global_acceptMessage();
        $user'ton_balance = $user'ton_balance - $maxCharge;
        $user'publish_nonce = $user'publish_nonce + 1;
        ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_writeReceipt(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), $clientNonce, 7, 0, 0, $partCount);
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), begin_cell()));
        $global_commit();
        slice $vroot = $Cell$_fun_beginParse($msg'signed_payload);
        slice $vAddr = $Address$_fun_asSlice($global_myAddress());
        $vAddr~load_uint(11);
        slice $oAddr = $Address$_fun_asSlice($msg'owner_wallet);
        int $structuralOk = ( (( (($vroot~load_uint(32) == 1448100402)) ? (($vroot~load_uint(256) == $self'deployment_manifest_hash)) : (false) )) ? (($vroot~load_uint(256) == $vAddr~load_uint(256))) : (false) );
        int $kind = $vroot~load_uint(8);
        $structuralOk = ( (( (( ($structuralOk) ? (( (( (( (($kind == 1)) ? (true) : (($kind == 2)) )) ? (true) : (($kind == 3)) )) ? (true) : (($kind == 4)) )) : (false) )) ? (($oAddr~load_uint(11) == 1024)) : (false) )) ? (($vroot~load_uint(256) == $oAddr~load_uint(256))) : (false) );
        $vroot~$Slice$_fun_skipBits(200);
        $structuralOk = ( (( (( ($structuralOk) ? (($vroot~load_uint(8) == 1)) : (false) )) ? (($Slice$_fun_bits($vroot) == 0)) : (false) )) ? (($Slice$_fun_refs($vroot) == 1)) : (false) );
        cell $partsRoot = $vroot~$Slice$_fun_loadRef();
        cell $cursor = $partsRoot;
        int $i = 0;
        int $rejectCode = 0;
        int $failIdx = 18446744073709551615;
        ifnot ($structuralOk) {
            $rejectCode = 23;
        } elseif (( (($partCount < 1)) ? (true) : (($partCount > 8)) )) {
            $rejectCode = 17;
        }
        int $totalFee = 0;
        int $totalHubGas = 0;
        int $totalStorage = 0;
        int $prevH0 = 0;
        int $prevH1 = 0;
        int $prevBody = 0;
        int $hasPrev = false;
        while (( (($i < $partCount)) ? (($rejectCode == 0)) : (false) )) {
            int $isLast = ($i == ($partCount - 1));
            slice $part = $Cell$_fun_beginParse($cursor);
            int $h0 = 0;
            int $h1 = 0;
            int $bh = 0;
            cell $nextCell = $partsRoot;
            if (( (( (($kind == 1)) ? (true) : (($kind == 3)) )) ? (true) : (($kind == 4)) )) {
                int $wantRefs = 3;
                ifnot ($isLast) {
                    $wantRefs = 4;
                }
                ifnot (( (($Slice$_fun_bits($part) == 784)) ? (($Slice$_fun_refs($part) == $wantRefs)) : (false) )) {
                    $rejectCode = 17;
                    $failIdx = $i;
                } else {
                    int $sizeClass = $part~load_uint(8);
                    int $cryptoSuite = $part~load_uint(8);
                    $h0 = $part~load_uint(256);
                    $h1 = $part~load_uint(256);
                    $bh = $part~load_uint(256);
                    cell $header0 = $part~$Slice$_fun_loadRef();
                    cell $header1 = $part~$Slice$_fun_loadRef();
                    cell $body = $part~$Slice$_fun_loadRef();
                    ifnot ($isLast) {
                        $nextCell = $part~$Slice$_fun_loadRef();
                    }
                    ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isPublishProfileValid($kind, $sizeClass, $cryptoSuite, $h0, $h1)) {
                        $rejectCode = 18;
                        $failIdx = $i;
                    } else {
                        int $shapeOk = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isConvCapsuleShapeValid($sizeClass, $cryptoSuite, $header0, $header1, $body);
                        if (($kind == 3)) {
                            $shapeOk = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isIntroCapsuleShapeValid($sizeClass, $cryptoSuite, $header0, $header1, $body);
                        }
                        ifnot ($shapeOk) {
                            $rejectCode = 20;
                            $failIdx = $i;
                        } elseifnot (( (( (($Cell$_fun_hash($header0) == $h0)) ? (($Cell$_fun_hash($header1) == $h1)) : (false) )) ? (($Cell$_fun_hash($body) == $bh)) : (false) )) {
                            $rejectCode = 19;
                            $failIdx = $i;
                        }
                    }
                }
            } else {
                int $wantRefs = 2;
                ifnot ($isLast) {
                    $wantRefs = 3;
                }
                ifnot (( (($Slice$_fun_bits($part) == 592)) ? (($Slice$_fun_refs($part) == $wantRefs)) : (false) )) {
                    $rejectCode = 17;
                    $failIdx = $i;
                } else {
                    int $sizeClass = $part~load_uint(8);
                    int $reserved = $part~load_uint(8);
                    $part~$Slice$_fun_skipBits(64);
                    $h0 = $part~load_uint(256);
                    $bh = $part~load_uint(256);
                    cell $header = $part~$Slice$_fun_loadRef();
                    cell $body = $part~$Slice$_fun_loadRef();
                    ifnot ($isLast) {
                        $nextCell = $part~$Slice$_fun_loadRef();
                    }
                    ifnot ((($reserved & 254) == 0)) {
                        $rejectCode = 17;
                        $failIdx = $i;
                    } elseifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isPublishProfileValid(2, $sizeClass, 0, $h0, 0)) {
                        $rejectCode = 18;
                        $failIdx = $i;
                    } elseifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isPublicCapsuleShapeValid($sizeClass, $header, $body)) {
                        $rejectCode = 20;
                        $failIdx = $i;
                    } elseifnot (( (($Cell$_fun_hash($header) == $h0)) ? (($Cell$_fun_hash($body) == $bh)) : (false) )) {
                        $rejectCode = 19;
                        $failIdx = $i;
                    }
                }
            }
            ifnot ($rejectCode) {
                if (( (( (( ($hasPrev) ? (($h0 == $prevH0)) : (false) )) ? (($h1 == $prevH1)) : (false) )) ? (($bh == $prevBody)) : (false) )) {
                    $rejectCode = 21;
                    $failIdx = $i;
                } else {
                    $totalFee = $totalFee + ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_discountedFee($msg'owner_wallet, 10000000);
                    $totalHubGas = $totalHubGas + ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_batchHubPartGas($kind);
                    $totalStorage = $totalStorage + ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_batchStorageReserve($kind);
                    $prevH0 = $h0;
                    $prevH1 = $h1;
                    $prevBody = $bh;
                    $hasPrev = true;
                    $cursor = $nextCell;
                }
            }
            $i = $i + 1;
        }
        int $publishId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_computeBatchPublishId($msg'owner_wallet, $clientNonce, $Cell$_fun_hash($partsRoot), $kind, $partCount);
        int $bounceId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_computePublishBounceId($publishId);
        ifnot ($rejectCode) {
            ifnot ($publishId) {
                $rejectCode = 24;
            } elseif (__tact_dict_exists_int($self'pending_batch_publishes, 257, $bounceId)) {
                $rejectCode = 25;
            }
        }
        int $measuredCells = 8192;
        int $measuredBits = 524280;
        try {
            var ($pd'cells, $pd'bits, $pd'refs) = $Cell$_fun_computeDataSize($msg'signed_payload, 8192);
            $measuredCells = $pd'cells;
            $measuredBits = $pd'bits;
        } catch (_, $e) {
        }
        int $measuredImport = $global_getForwardFee($measuredCells, $measuredBits, false);
        int $hubMsgCells = $measuredCells;
        int $hubMsgBits = (($measuredBits - 1016) + 924);
        if (($kind == 2)) {
            $hubMsgCells = $hubMsgCells + 1;
            $hubMsgBits = $hubMsgBits + 152;
        }
        int $hubFwd = $global_getForwardFee($hubMsgCells, $hubMsgBits, false);
        int $vaultCompute = 0;
        ifnot ($rejectCode) {
            $vaultCompute = $global_getComputeFee((50000 + (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_batchVaultPartGas($kind) * $partCount)), false);
            int $canonicalTotal = ((((((($global_getForwardFee(8192, 524280, false) + $vaultCompute) + $hubFwd) + $totalFee) + $global_getComputeFee($totalHubGas, false)) + (($totalStorage * 125) / 100)) + $global_getComputeFee(14000, false)) + 30000000);
            if (($maxCharge < $canonicalTotal)) {
                $rejectCode = 22;
            }
        }
        if ($rejectCode) {
            int $rejectFee = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_batchChargeFloor($partCount);
            int $runtimeReject = ($measuredImport + $global_getComputeFee((10000 + (90000 * $partCount)), false));
            if (($runtimeReject > $rejectFee)) {
                $rejectFee = $runtimeReject;
            }
            if (($rejectFee > $maxCharge)) {
                $rejectFee = $maxCharge;
            }
            ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_updateReceipt(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), $clientNonce, $rejectCode, $failIdx);
            $user'ton_balance = $user'ton_balance + ($maxCharge - $rejectFee);
            $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), begin_cell()));
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        int $callValue = ($maxCharge - (($measuredImport + $vaultCompute) + $hubFwd));
        cell $marketing = null();
        if (($kind == 2)) {
            $marketing = $Builder$_fun_endCell(store_uint($global_beginCell(), 2573421624129493433291659589718684717235138672, 152));
        }
        $self'pending_batch_publishes~__tact_dict_set_int_cell(257, $bounceId, $PendingBatchPublish$_store_cell($PendingBatchPublish$_constructor_owner_wallet_tombstone_refund_to_vault_nonce_publish_kind_part_count_publish_id_refundable_amount_created_at($msg'owner_wallet, false, true, $clientNonce, $kind, $partCount, $publishId, $callValue, $global_now()), begin_cell()));
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_incrementPendingPublishCount();
        $global_commit();
        $global_message($MessageParameters$_constructor_to_value_bounce_mode_body($self'capsule_hub_address, $callValue, true, 1, $PublishBatchToHub$_store_cell($PublishBatchToHub$_constructor_bounce_id_bounce_tag_publish_id_publish_kind_part_count_protocol_fee_total_author_wallet_parts_marketing($bounceId, ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_computePublishBounceTag($publishId), $publishId, $kind, $partCount, $totalFee, $msg'owner_wallet, $partsRoot, $marketing), begin_cell())));
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
        return ();
    }
    
    ;; Receive SetProfileAvatarFromVaultBalance message
    if (op == 0x7e1f5033) {
        var $msg'owner_wallet = in_msg~load_msg_addr();
        var $msg'signature = in_msg~load_bits(512);
        var $msg'signed_payload = in_msg~load_ref();
        var $msg'envelope_padding = in_msg;
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireEmptyEnvelopePadding($msg'envelope_padding);
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireSealed();
        tuple $userOpt = $UserState$_load_opt(__tact_dict_get_slice_cell($self'users, 267, $msg'owner_wallet));
        throw_unless(16601, (~ null?($userOpt)));
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = $UserState$_not_null($userOpt);
        throw_unless(16602, ($user'auth_pubkey != 0));
        throw_unless(16604, $global_checkSignature($Cell$_fun_hash($msg'signed_payload), $msg'signature, $user'auth_pubkey));
        slice $signedPayload = $Cell$_fun_beginParse($msg'signed_payload);
        int $signedDomain = $signedPayload~load_uint(32);
        throw_unless(16605, ($signedDomain == 1448100145));
        int $signedManifestHash = $signedPayload~load_uint(256);
        throw_unless(16606, ($signedManifestHash == $self'deployment_manifest_hash));
        slice $signedOwner = $signedPayload~$Slice$_fun_loadAddress();
        throw_unless(16618, ( equal_slices_bits($signedOwner, $msg'owner_wallet) ));
        int $clientNonce = $signedPayload~load_uint(64);
        throw_unless(16611, ($clientNonce == $user'publish_nonce));
        int $maxTonCharge = $signedPayload~load_uint(128);
        throw_unless(16615, ($maxTonCharge >= 6000000));
        slice $signedVaultAddress = $signedPayload~$Slice$_fun_loadAddress();
        throw_unless(16619, ( equal_slices_bits($signedVaultAddress, $global_myAddress()) ));
        throw_unless(16614, ($user'ton_balance >= 6000000));
        $global_acceptMessage();
        $user'publish_nonce = $user'publish_nonce + 1;
        ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_writeReceipt(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), ($user'publish_nonce - 1), 8, 0, 0, 0);
        $user'ton_balance = $user'ton_balance - 6000000;
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), begin_cell()));
        $global_commit();
        ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isProfileRegistryBound()) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($Slice$_fun_bits($signedPayload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($Slice$_fun_refs($signedPayload) == 1)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        cell $avatarPayloadCell = $signedPayload~$Slice$_fun_loadRef();
        slice $avatarPayloadPreview = $Cell$_fun_beginParse($avatarPayloadCell);
        ifnot (($Slice$_fun_bits($avatarPayloadPreview) == 739)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($Slice$_fun_refs($avatarPayloadPreview) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        slice $avatarPayload = $Cell$_fun_beginParse($avatarPayloadCell);
        slice $signedProfileRegistry = $avatarPayload~$Slice$_fun_loadAddress();
        int $avatarHash = $avatarPayload~load_uint(256);
        int $avatarEntryId = $avatarPayload~load_uint(64);
        int $avatarStreamId = $avatarPayload~load_uint(128);
        int $avatarPartCount = $avatarPayload~load_uint(16);
        int $mediaFormat = $avatarPayload~load_uint(8);
        ifnot (( equal_slices_bits($signedProfileRegistry, $self'profile_registry_address) )) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isValidProfileAvatarPointer($msg'owner_wallet, $avatarHash, $avatarStreamId, $avatarPartCount, $mediaFormat)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        int $tonCharge = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_profileAvatarTonCharge();
        ifnot (($maxTonCharge >= $tonCharge)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        int $remainingTonCharge = ($tonCharge - 6000000);
        ifnot (($user'ton_balance >= $remainingTonCharge)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($user'ath_balance >= 100000000000)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        int $queryId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_computeProfileAvatarPaymentId($msg'owner_wallet, $clientNonce, $avatarHash);
        if (__tact_dict_exists_int($self'pending_profile_avatar_payments, 257, $queryId)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        $user'ton_balance = $user'ton_balance - $remainingTonCharge;
        $user'ath_balance = $user'ath_balance - 100000000000;
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), begin_cell()));
        $self'pending_profile_avatar_payments~__tact_dict_set_int_cell(257, $queryId, $PendingProfileAvatarPayment$_store_cell($PendingProfileAvatarPayment$_constructor_owner_wallet_tombstone_amount_created_at($msg'owner_wallet, false, 100000000000, $global_now()), begin_cell()));
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_incrementPendingProfileAvatarPaymentCount();
        $global_commit();
        $global_message($MessageParameters$_constructor_to_value_bounce_mode_body($self'vault_ath_wallet_address, 109000000, true, 1, $ATHTransferRequestVaultProfileAvatar$_store_cell($ATHTransferRequestVaultProfileAvatar$_constructor_query_id_amount_recipient_response_destination_notify_value_owner_wallet_avatar_hash_avatar_entry_id_avatar_stream_id_avatar_part_count_media_format($queryId, 100000000000, $self'profile_registry_address, $global_myAddress(), 66000000, $msg'owner_wallet, $avatarHash, $avatarEntryId, $avatarStreamId, $avatarPartCount, $mediaFormat), begin_cell())));
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
        return ();
    }
    
    ;; Receive MintUsernameFromVaultBalance message
    if (op == 0x7e1f5034) {
        var $msg'owner_wallet = in_msg~load_msg_addr();
        var $msg'signature = in_msg~load_bits(512);
        var $msg'signed_payload = in_msg~load_ref();
        var $msg'envelope_padding = in_msg;
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireEmptyEnvelopePadding($msg'envelope_padding);
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_requireSealed();
        tuple $userOpt = $UserState$_load_opt(__tact_dict_get_slice_cell($self'users, 267, $msg'owner_wallet));
        throw_unless(16701, (~ null?($userOpt)));
        var ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = $UserState$_not_null($userOpt);
        throw_unless(16702, ($user'auth_pubkey != 0));
        throw_unless(16704, $global_checkSignature($Cell$_fun_hash($msg'signed_payload), $msg'signature, $user'auth_pubkey));
        slice $signedPayload = $Cell$_fun_beginParse($msg'signed_payload);
        int $signedDomain = $signedPayload~load_uint(32);
        throw_unless(16705, ($signedDomain == 1448431153));
        int $signedManifestHash = $signedPayload~load_uint(256);
        throw_unless(16706, ($signedManifestHash == $self'deployment_manifest_hash));
        slice $signedOwner = $signedPayload~$Slice$_fun_loadAddress();
        throw_unless(16716, ( equal_slices_bits($signedOwner, $msg'owner_wallet) ));
        int $clientNonce = $signedPayload~load_uint(64);
        throw_unless(16711, ($clientNonce == $user'publish_nonce));
        int $maxTonCharge = $signedPayload~load_uint(128);
        throw_unless(16715, ($maxTonCharge >= 6000000));
        slice $signedVaultAddress = $signedPayload~$Slice$_fun_loadAddress();
        throw_unless(16717, ( equal_slices_bits($signedVaultAddress, $global_myAddress()) ));
        throw_unless(16714, ($user'ton_balance >= 6000000));
        $global_acceptMessage();
        $user'publish_nonce = $user'publish_nonce + 1;
        ($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts) = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_writeReceipt(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), ($user'publish_nonce - 1), 9, 0, 0, 0);
        $user'ton_balance = $user'ton_balance - 6000000;
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), begin_cell()));
        $global_commit();
        ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isUsernameRegistryBound()) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($Slice$_fun_bits($signedPayload) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($Slice$_fun_refs($signedPayload) == 1)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        cell $usernamePayloadCell = $signedPayload~$Slice$_fun_loadRef();
        slice $usernamePayloadPreview = $Cell$_fun_beginParse($usernamePayloadCell);
        ifnot (($Slice$_fun_bits($usernamePayloadPreview) >= 275)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($Slice$_fun_refs($usernamePayloadPreview) == 0)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        slice $usernamePayload = $Cell$_fun_beginParse($usernamePayloadCell);
        slice $signedUsernameRegistry = $usernamePayload~$Slice$_fun_loadAddress();
        int $usernameLen = $usernamePayload~load_uint(8);
        slice $username = $usernamePayload;
        ifnot (( equal_slices_bits($signedUsernameRegistry, $self'username_registry_address) )) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_isValidUsernameBytes($usernameLen, $username)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        int $price = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_usernamePriceForLength($usernameLen);
        int $tonCharge = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_usernameMintTonCharge();
        ifnot (($maxTonCharge >= $tonCharge)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        int $remainingTonCharge = ($tonCharge - 6000000);
        ifnot (($user'ton_balance >= $remainingTonCharge)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        ifnot (($user'ath_balance >= $price)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        int $nameHash = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_computeUsernameNameHash($username);
        int $queryId = ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_computeUsernameMintPaymentId($msg'owner_wallet, $clientNonce, $nameHash);
        if (__tact_dict_exists_int($self'pending_username_mint_payments, 257, $queryId)) {
            $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
            return ();
        }
        $user'ton_balance = $user'ton_balance - $remainingTonCharge;
        $user'ath_balance = $user'ath_balance - $price;
        $self'users~__tact_dict_set_slice_cell(267, $msg'owner_wallet, $UserState$_store_cell(($user'ton_balance, $user'ath_balance, $user'current_key_id, $user'auth_pubkey, $user'publish_nonce, $user'last_active, $user'receipts), begin_cell()));
        $self'pending_username_mint_payments~__tact_dict_set_int_cell(257, $queryId, $PendingUsernameMintPayment$_store_cell($PendingUsernameMintPayment$_constructor_owner_wallet_tombstone_amount_created_at($msg'owner_wallet, false, $price, $global_now()), begin_cell()));
        ($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext)~$Vault$_fun_incrementPendingUsernameMintPaymentCount();
        $global_commit();
        $global_message($MessageParameters$_constructor_to_value_bounce_mode_body($self'vault_ath_wallet_address, 994000000, true, 1, $ATHTransferRequestVaultMintUsername$_store_cell($ATHTransferRequestVaultMintUsername$_constructor_query_id_amount_recipient_response_destination_notify_value_owner_wallet_username_len_username($queryId, $price, $self'username_registry_address, $global_myAddress(), 951000000, $msg'owner_wallet, $usernameLen, $username), begin_cell())));
        $Vault$_contract_store(($self'vault_ath_wallet_address, $self'ath_master_address, $self'capsule_hub_address, $self'profile_registry_address, $self'username_registry_address, $self'binding_flags, $self'sealed, $self'deployment_manifest_hash, $self'genesis_config_hash, $self'users, $self'key_records, $self'processed_ath_deposits, $self'pending_ath_withdrawals, $self'pending_batch_publishes, $self'pending_profile_avatar_payments, $self'pending_username_mint_payments, $self'user_count, $self'key_record_count, $self'processed_ath_deposit_count, $self'pending_ath_withdrawal_count, $self'pending_publish_count, $self'genesis_ext));
        return ();
    }
    
    ;; Throw if not handled
    throw(130);
}
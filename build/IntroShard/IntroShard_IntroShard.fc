#pragma version =0.4.6;
#pragma allow-post-modification;
#pragma compute-asm-ltr;

;; IntroShard_IntroShard.headers.fc
;;
;; Header files for IntroShard
;; NOTE: declarations are sorted for optimal order
;;

;; __tact_context_get
(int, slice, int, slice) __tact_context_get() inline;

;; __tact_context_get_sender
slice __tact_context_get_sender() inline;

;; __tact_dict_get_int_cell
cell __tact_dict_get_int_cell(cell d, int kl, int k) inline;

;; __tact_dict_set_int_cell
(cell, ()) __tact_dict_set_int_cell(cell d, int kl, int k, cell v) inline;

;; $IntroEntry$_store
builder $IntroEntry$_store(builder build_0, (int, int, int, int) v) inline;

;; $IntroEntry$_store_cell
cell $IntroEntry$_store_cell((int, int, int, int) v, builder b) inline;

;; $IntroEntry$_not_null
((int, int, int, int)) $IntroEntry$_not_null(tuple v) inline;

;; $IntroEntry$_store_opt
cell $IntroEntry$_store_opt(tuple v, builder b) inline;

;; $IntroEntry$_load
(slice, ((int, int, int, int))) $IntroEntry$_load(slice sc_0) inline;

;; $IntroEntry$_as_optional
tuple $IntroEntry$_as_optional((int, int, int, int) v) inline;

;; $IntroEntry$_load_opt
tuple $IntroEntry$_load_opt(cell cl) inline;

;; $IntroShard$_store
builder $IntroShard$_store(builder build_0, (int, int, cell, int, int, int) v) inline;

;; $IntroShard$_load
(slice, ((int, int, cell, int, int, int))) $IntroShard$_load(slice sc_0) inline;

;; $Context$_get_value
_ $Context$_get_value((int, slice, int, slice) v) inline;

;; $IntroEntry$_get_view_tag
_ $IntroEntry$_get_view_tag((int, int, int, int) v) inline;

;; $IntroEntry$_get_created_at
_ $IntroEntry$_get_created_at((int, int, int, int) v) inline;

;; $IntroEntryView$_to_external
(int, int, int, int, int) $IntroEntryView$_to_external(((int, int, int, int, int)) v) inline;

;; $IntroTagPage$_to_external
(int, int, int, int, cell) $IntroTagPage$_to_external(((int, int, int, int, cell)) v) inline;

;; $IntroShardView$_to_external
(int, int, int, int, int, int, int, int) $IntroShardView$_to_external(((int, int, int, int, int, int, int, int)) v) inline;

;; $IntroShard$init$_load
(slice, ((int, int))) $IntroShard$init$_load(slice sc_0) inline;

;; $IntroShard$_contract_init
(int, int, cell, int, int, int) $IntroShard$_contract_init(int $epoch, int $bucket) impure inline;

;; $IntroShard$_contract_load
(int, int, cell, int, int, int) $IntroShard$_contract_load() impure inline;

;; $IntroShard$_contract_store
() $IntroShard$_contract_store((int, int, cell, int, int, int) v) impure inline;

;; $IntroShard$_fun_bodyCommit
((int, int, cell, int, int, int), int) $IntroShard$_fun_bodyCommit((int, int, cell, int, int, int) $self, cell $h0, cell $body) impure inline_ref;

;; $IntroShard$_fun_packTags
((int, int, cell, int, int, int), cell) $IntroShard$_fun_packTags((int, int, cell, int, int, int) $self, int $start, int $n) impure inline_ref;

;; $IntroTagPage$_constructor_from_id_count_next_id_evict_cursor_tags
((int, int, int, int, cell)) $IntroTagPage$_constructor_from_id_count_next_id_evict_cursor_tags(int $from_id, int $count, int $next_id, int $evict_cursor, cell $tags) inline;

;; $IntroShard$_fun_get_view_tags
((int, int, cell, int, int, int), (int, int, int, int, cell)) $IntroShard$_fun_get_view_tags((int, int, cell, int, int, int) $self, int $from_id, int $max_count) impure inline_ref;

;; $IntroEntryView$_constructor_exists_r_view_tag_body_commit_created_at
((int, int, int, int, int)) $IntroEntryView$_constructor_exists_r_view_tag_body_commit_created_at(int $exists, int $r, int $view_tag, int $body_commit, int $created_at) inline;

;; $IntroShard$_fun_get_entry
((int, int, cell, int, int, int), (int, int, int, int, int)) $IntroShard$_fun_get_entry((int, int, cell, int, int, int) $self, int $entry_id) impure inline_ref;

;; $IntroShardView$_constructor_epoch_bucket_live_count_next_id_evict_cursor_retention_safe_cap_min_value
((int, int, int, int, int, int, int, int)) $IntroShardView$_constructor_epoch_bucket_live_count_next_id_evict_cursor_retention_safe_cap_min_value(int $epoch, int $bucket, int $live_count, int $next_id, int $evict_cursor, int $retention, int $safe_cap, int $min_value) inline;

;; $IntroShard$_fun_get_view
((int, int, cell, int, int, int), (int, int, int, int, int, int, int, int)) $IntroShard$_fun_get_view((int, int, cell, int, int, int) $self) impure inline_ref;

;; $IntroEntry$_constructor_r_view_tag_body_commit_created_at
((int, int, int, int)) $IntroEntry$_constructor_r_view_tag_body_commit_created_at(int $r, int $view_tag, int $body_commit, int $created_at) inline;

;; $SendParameters$_constructor_to_value_mode_bounce
((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_bounce(slice $to, int $value, int $mode, int $bounce) inline;


;; IntroShard_IntroShard.stdlib.fc
global (int, slice, int, slice) __tact_context;
global slice __tact_context_sender;
global cell __tact_child_contract_codes;
global int __tact_randomized;

(int, slice, int, slice) __tact_context_get() inline {
    return __tact_context;
}

slice __tact_context_get_sender() inline {
    return __tact_context_sender;
}

forall X0, X1, X2, X3 -> tuple __tact_tuple_create_4((X0, X1, X2, X3) v) asm """
    4 TUPLE
""";

forall X0, X1, X2, X3 -> (X0, X1, X2, X3) __tact_tuple_destroy_4(tuple v) asm """
    4 UNTUPLE
""";

cell __tact_dict_get_int_cell(cell d, int kl, int k) inline {
    var (r, ok) = idict_get_ref?(d, kl, k);
    if (ok) {
        return r;
    } else {
        return null();
    }
}

(cell, ()) __tact_dict_set_int_cell(cell d, int kl, int k, cell v) inline {
    if (null?(v)) {
        var (r, ok) = idict_delete?(d, kl, k);
        return (r, ());
    } else {
        return (idict_set_ref(d, kl, k, v), ());
    }
}

builder $global_beginCell() impure asm """
    NEWC
""";

int $global_min(int $x, int $y) impure asm """
    MIN
""";

int $global_max(int $x, int $y) impure asm """
    MAX
""";

() $global_nativeReserve(int $amount, int $mode) impure asm """
    RAWRESERVE
""";

() $global_send((int, cell, cell, cell, int, slice, int) $params) impure asm """
    NEWC
    b{01} STSLICECONST  // store tag = $0 and ihr_disabled = true
    1 STI               // store `bounce`
    b{000} STSLICECONST // store bounced = false and src = addr_none
    STSLICE             // store `to`
    SWAP
    STGRAMS             // store `value`
    105 PUSHINT         // 1 + 4 + 4 + 64 + 32
    STZEROES            // store currency_collection, ihr_fee, fwd_fee, created_lt and created_at
    // → Stack state
    // s0: Builder
    // s1: `data`
    // s2: `code`
    // s3: `body`
    // s4: `mode`

    // Group 2: Placing the Builder after code and data, then checking those for nullability
    s2 XCHG0
    DUP2
    ISNULL
    SWAP
    ISNULL
    AND
    // → Stack state
    // s0: -1 (true) if `data` and `code` are both null, 0 (false) otherwise
    // s1: `code`
    // s2: `data`
    // s3: Builder
    // s4: `body`
    // s5: `mode`

    // Group 3: Left branch of the IFELSE, executed if s0 is -1 (true)
    <{
        DROP2 // drop `data` and `code`, since either of those is null
        b{0} STSLICECONST
    }> PUSHCONT

    // Group 3: Right branch of the IFELSE, executed if s0 is 0 (false)
    <{
        // _ split_depth:(Maybe (## 5))
        //   special:(Maybe TickTock)
        //   code:(Maybe ^Cell)
        //   data:(Maybe ^Cell)
        //   library:(Maybe ^Cell)
        // = StateInit;
        ROT                // place message Builder on top
        b{10} STSLICECONST // store Maybe = true, Either = false
        // Start composing inlined StateInit
        b{00} STSLICECONST // store split_depth and special first
        STDICT             // store code
        STDICT             // store data
        b{0} STSLICECONST  // store library
    }> PUSHCONT

    // Group 3: IFELSE that does the branching shown above
    IFELSE
    // → Stack state
    // s0: Builder
    // s1: null or StateInit
    // s2: `body`
    // s3: `mode`

    // Group 4: Finalizing the message
    STDICT // store `body` as ref with an extra Maybe bit, since `body` might be null
    ENDC
    // → Stack state
    // s0: Cell
    // s1: `mode`

    // Group 5: Sending the message, with `mode` on top
    SWAP
    SENDRAWMSG // https://github.com/tact-lang/tact/issues/1558
""";

int $global_now() impure asm """
    NOW
""";

builder $Builder$_fun_storeRef(cell $cell, builder $self) impure asm """
    STREF
""";

cell $Builder$_fun_endCell(builder $self) impure asm """
    ENDC
""";

int $Cell$_fun_hash(cell $self) impure asm """
    HASHCU
""";

;; IntroShard_IntroShard.storage.fc
;;
;; Type: Context
;; TLB: _ bounceable:bool sender:address value:int257 raw:^slice = Context
;;

_ $Context$_get_value((int, slice, int, slice) v) inline {
    var (v'bounceable, v'sender, v'value, v'raw) = v;
    return v'value;
}

;;
;; Type: SendParameters
;; TLB: _ mode:int257 body:Maybe ^cell code:Maybe ^cell data:Maybe ^cell value:int257 to:address bounce:bool = SendParameters
;;

((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_bounce(slice $to, int $value, int $mode, int $bounce) inline {
    return ($mode, null(), null(), null(), $value, $to, $bounce);
}

;;
;; Type: IntroEntry
;; TLB: _ r:int257 view_tag:int257 body_commit:int257 created_at:int257 = IntroEntry
;;

builder $IntroEntry$_store(builder build_0, (int, int, int, int) v) inline {
    var (v'r, v'view_tag, v'body_commit, v'created_at) = v;
    build_0 = build_0.store_int(v'r, 257);
    build_0 = build_0.store_int(v'view_tag, 257);
    build_0 = build_0.store_int(v'body_commit, 257);
    var build_1 = begin_cell();
    build_1 = build_1.store_int(v'created_at, 257);
    build_0 = store_builder_ref(build_0, build_1);
    return build_0;
}

cell $IntroEntry$_store_cell((int, int, int, int) v, builder b) inline {
    return $IntroEntry$_store(b, v).end_cell();
}

((int, int, int, int)) $IntroEntry$_not_null(tuple v) inline {
    var (int vvv'r, int vvv'view_tag, int vvv'body_commit, int vvv'created_at) = __tact_tuple_destroy_4(v);
    return (vvv'r, vvv'view_tag, vvv'body_commit, vvv'created_at);
}

cell $IntroEntry$_store_opt(tuple v, builder b) inline {
    if (null?(v)) {
        return null();
    }
    return $IntroEntry$_store_cell($IntroEntry$_not_null(v), b);
}

(slice, ((int, int, int, int))) $IntroEntry$_load(slice sc_0) inline {
    var v'r = sc_0~load_int(257);
    var v'view_tag = sc_0~load_int(257);
    var v'body_commit = sc_0~load_int(257);
    slice sc_1 = sc_0~load_ref().begin_parse();
    var v'created_at = sc_1~load_int(257);
    return (sc_0, (v'r, v'view_tag, v'body_commit, v'created_at));
}

tuple $IntroEntry$_as_optional((int, int, int, int) v) inline {
    var (v'r, v'view_tag, v'body_commit, v'created_at) = v;
    return __tact_tuple_create_4(v'r, v'view_tag, v'body_commit, v'created_at);
}

tuple $IntroEntry$_load_opt(cell cl) inline {
    if (null?(cl)) {
        return null();
    }
    var sc = cl.begin_parse();
    return $IntroEntry$_as_optional(sc~$IntroEntry$_load());
}

_ $IntroEntry$_get_view_tag((int, int, int, int) v) inline {
    var (v'r, v'view_tag, v'body_commit, v'created_at) = v;
    return v'view_tag;
}

_ $IntroEntry$_get_created_at((int, int, int, int) v) inline {
    var (v'r, v'view_tag, v'body_commit, v'created_at) = v;
    return v'created_at;
}

((int, int, int, int)) $IntroEntry$_constructor_r_view_tag_body_commit_created_at(int $r, int $view_tag, int $body_commit, int $created_at) inline {
    return ($r, $view_tag, $body_commit, $created_at);
}

;;
;; Type: IntroEntryView
;; TLB: _ exists:bool r:int257 view_tag:int257 body_commit:int257 created_at:int257 = IntroEntryView
;;

(int, int, int, int, int) $IntroEntryView$_to_external(((int, int, int, int, int)) v) inline {
    var (v'exists, v'r, v'view_tag, v'body_commit, v'created_at) = v; 
    return (v'exists, v'r, v'view_tag, v'body_commit, v'created_at);
}

((int, int, int, int, int)) $IntroEntryView$_constructor_exists_r_view_tag_body_commit_created_at(int $exists, int $r, int $view_tag, int $body_commit, int $created_at) inline {
    return ($exists, $r, $view_tag, $body_commit, $created_at);
}

;;
;; Type: IntroTagPage
;; TLB: _ from_id:int257 count:int257 next_id:int257 evict_cursor:int257 tags:^cell = IntroTagPage
;;

(int, int, int, int, cell) $IntroTagPage$_to_external(((int, int, int, int, cell)) v) inline {
    var (v'from_id, v'count, v'next_id, v'evict_cursor, v'tags) = v; 
    return (v'from_id, v'count, v'next_id, v'evict_cursor, v'tags);
}

((int, int, int, int, cell)) $IntroTagPage$_constructor_from_id_count_next_id_evict_cursor_tags(int $from_id, int $count, int $next_id, int $evict_cursor, cell $tags) inline {
    return ($from_id, $count, $next_id, $evict_cursor, $tags);
}

;;
;; Type: IntroShardView
;; TLB: _ epoch:int257 bucket:int257 live_count:int257 next_id:int257 evict_cursor:int257 retention:int257 safe_cap:int257 min_value:int257 = IntroShardView
;;

(int, int, int, int, int, int, int, int) $IntroShardView$_to_external(((int, int, int, int, int, int, int, int)) v) inline {
    var (v'epoch, v'bucket, v'live_count, v'next_id, v'evict_cursor, v'retention, v'safe_cap, v'min_value) = v; 
    return (v'epoch, v'bucket, v'live_count, v'next_id, v'evict_cursor, v'retention, v'safe_cap, v'min_value);
}

((int, int, int, int, int, int, int, int)) $IntroShardView$_constructor_epoch_bucket_live_count_next_id_evict_cursor_retention_safe_cap_min_value(int $epoch, int $bucket, int $live_count, int $next_id, int $evict_cursor, int $retention, int $safe_cap, int $min_value) inline {
    return ($epoch, $bucket, $live_count, $next_id, $evict_cursor, $retention, $safe_cap, $min_value);
}

;;
;; Type: IntroShard
;; TLB: _ epoch:uint32 bucket:uint32 intros:dict<int, ^IntroEntry{r:int257,view_tag:int257,body_commit:int257,created_at:int257}> next_id:uint32 live_count:uint32 evict_cursor:uint32 = IntroShard
;;

builder $IntroShard$_store(builder build_0, (int, int, cell, int, int, int) v) inline {
    var (v'epoch, v'bucket, v'intros, v'next_id, v'live_count, v'evict_cursor) = v;
    build_0 = build_0.store_uint(v'epoch, 32);
    build_0 = build_0.store_uint(v'bucket, 32);
    build_0 = build_0.store_dict(v'intros);
    build_0 = build_0.store_uint(v'next_id, 32);
    build_0 = build_0.store_uint(v'live_count, 32);
    build_0 = build_0.store_uint(v'evict_cursor, 32);
    return build_0;
}

(slice, ((int, int, cell, int, int, int))) $IntroShard$_load(slice sc_0) inline {
    var v'epoch = sc_0~load_uint(32);
    var v'bucket = sc_0~load_uint(32);
    var v'intros = sc_0~load_dict();
    var v'next_id = sc_0~load_uint(32);
    var v'live_count = sc_0~load_uint(32);
    var v'evict_cursor = sc_0~load_uint(32);
    return (sc_0, (v'epoch, v'bucket, v'intros, v'next_id, v'live_count, v'evict_cursor));
}

(slice, ((int, int))) $IntroShard$init$_load(slice sc_0) inline {
    var v'epoch = sc_0~load_int(257);
    var v'bucket = sc_0~load_int(257);
    return (sc_0, (v'epoch, v'bucket));
}

(int, int, cell, int, int, int) $IntroShard$_contract_load() impure inline {
    slice $sc = get_data().begin_parse();
    int $loaded = $sc~load_int(1);
    if ($loaded) {
        return $sc~$IntroShard$_load();
    }
    else {
        (int $epoch, int $bucket) = $sc~$IntroShard$init$_load();
        $sc.end_parse();
        return $IntroShard$_contract_init($epoch, $bucket);
    }
}

() $IntroShard$_contract_store((int, int, cell, int, int, int) v) impure inline {
    builder b = begin_cell();
    b = b.store_int(true, 1);
    b = $IntroShard$_store(b, v);
    set_data(b.end_cell());
}

;;
;; Contract IntroShard functions
;;

(int, int, cell, int, int, int) $IntroShard$_contract_init(int $epoch, int $bucket) impure inline {
    var (($self'epoch, $self'bucket, $self'intros, $self'next_id, $self'live_count, $self'evict_cursor)) = (null(), null(), null(), null(), null(), null());
    $self'epoch = $epoch;
    $self'bucket = $bucket;
    $self'intros = null();
    $self'next_id = 0;
    $self'live_count = 0;
    $self'evict_cursor = 0;
    return ($self'epoch, $self'bucket, $self'intros, $self'next_id, $self'live_count, $self'evict_cursor);
}

((int, int, cell, int, int, int), int) $IntroShard$_fun_bodyCommit((int, int, cell, int, int, int) $self, cell $h0, cell $body) impure inline_ref {
    var (($self'epoch, $self'bucket, $self'intros, $self'next_id, $self'live_count, $self'evict_cursor)) = $self;
    var $fresh$ret_0 = $Cell$_fun_hash($Builder$_fun_endCell(store_uint(store_uint(store_uint($global_beginCell(), 1230193219, 32), $Cell$_fun_hash($h0), 256), $Cell$_fun_hash($body), 256)));
    return (($self'epoch, $self'bucket, $self'intros, $self'next_id, $self'live_count, $self'evict_cursor), $fresh$ret_0);
}

((int, int, cell, int, int, int), cell) $IntroShard$_fun_packTags((int, int, cell, int, int, int) $self, int $start, int $n) impure inline_ref {
    var (($self'epoch, $self'bucket, $self'intros, $self'next_id, $self'live_count, $self'evict_cursor)) = $self;
    cell $cell = $Builder$_fun_endCell($global_beginCell());
    int $isTail = true;
    int $remaining = $n;
    while (($remaining > 0)) {
        int $chunk = $global_min($remaining, 63);
        int $first = (($start + $remaining) - $chunk);
        builder $b = $global_beginCell();
        int $i = 0;
        while (($i < $chunk)) {
            tuple $e = $IntroEntry$_load_opt(__tact_dict_get_int_cell($self'intros, 257, ($first + $i)));
            $b = store_uint($b, (null?($e) ? 0 : $IntroEntry$_get_view_tag($IntroEntry$_not_null($e))), 16);
            $i = ($i + 1);
        }
        if ($isTail) {
            $cell = $Builder$_fun_endCell($b);
            $isTail = false;
        } else {
            $cell = $Builder$_fun_endCell($Builder$_fun_storeRef($cell, $b));
        }
        $remaining = ($remaining - $chunk);
    }
    var $fresh$ret_1 = $cell;
    return (($self'epoch, $self'bucket, $self'intros, $self'next_id, $self'live_count, $self'evict_cursor), $fresh$ret_1);
}

((int, int, cell, int, int, int), (int, int, int, int, cell)) $IntroShard$_fun_get_view_tags((int, int, cell, int, int, int) $self, int $from_id, int $max_count) impure inline_ref {
    var (($self'epoch, $self'bucket, $self'intros, $self'next_id, $self'live_count, $self'evict_cursor)) = $self;
    int $start = $global_max($from_id, $self'evict_cursor);
    int $available = (($self'next_id > $start) ? ($self'next_id - $start) : 0);
    int $n = $global_min($global_min($max_count, 256), $available);
    var $fresh$ret_2 = $IntroTagPage$_constructor_from_id_count_next_id_evict_cursor_tags($start, $n, $self'next_id, $self'evict_cursor, ($self'epoch, $self'bucket, $self'intros, $self'next_id, $self'live_count, $self'evict_cursor)~$IntroShard$_fun_packTags($start, $n));
    return (($self'epoch, $self'bucket, $self'intros, $self'next_id, $self'live_count, $self'evict_cursor), $fresh$ret_2);
}

((int, int, cell, int, int, int), (int, int, int, int, int)) $IntroShard$_fun_get_entry((int, int, cell, int, int, int) $self, int $entry_id) impure inline_ref {
    var (($self'epoch, $self'bucket, $self'intros, $self'next_id, $self'live_count, $self'evict_cursor)) = $self;
    tuple $e = $IntroEntry$_load_opt(__tact_dict_get_int_cell($self'intros, 257, $entry_id));
    if (null?($e)) {
        var $fresh$ret_3 = $IntroEntryView$_constructor_exists_r_view_tag_body_commit_created_at(false, 0, 0, 0, 0);
        return (($self'epoch, $self'bucket, $self'intros, $self'next_id, $self'live_count, $self'evict_cursor), $fresh$ret_3);
    }
    var ($v'r, $v'view_tag, $v'body_commit, $v'created_at) = $IntroEntry$_not_null($e);
    var $fresh$ret_4 = $IntroEntryView$_constructor_exists_r_view_tag_body_commit_created_at(true, $v'r, $v'view_tag, $v'body_commit, $v'created_at);
    return (($self'epoch, $self'bucket, $self'intros, $self'next_id, $self'live_count, $self'evict_cursor), $fresh$ret_4);
}

((int, int, cell, int, int, int), (int, int, int, int, int, int, int, int)) $IntroShard$_fun_get_view((int, int, cell, int, int, int) $self) impure inline_ref {
    var (($self'epoch, $self'bucket, $self'intros, $self'next_id, $self'live_count, $self'evict_cursor)) = $self;
    var $fresh$ret_5 = $IntroShardView$_constructor_epoch_bucket_live_count_next_id_evict_cursor_retention_safe_cap_min_value($self'epoch, $self'bucket, $self'live_count, $self'next_id, $self'evict_cursor, 604800, 8000, 2508000);
    return (($self'epoch, $self'bucket, $self'intros, $self'next_id, $self'live_count, $self'evict_cursor), $fresh$ret_5);
}

;;
;; Get methods of a Contract IntroShard
;;

_ %get_view_tags(int $from_id, int $max_count) method_id(110790) {
    int $from_id = $from_id;
    int $max_count = $max_count;
    var self = $IntroShard$_contract_load();
    var res = self~$IntroShard$_fun_get_view_tags($from_id, $max_count);
    return $IntroTagPage$_to_external(res);
}

_ %get_entry(int $entry_id) method_id(80118) {
    int $entry_id = $entry_id;
    var self = $IntroShard$_contract_load();
    var res = self~$IntroShard$_fun_get_entry($entry_id);
    return $IntroEntryView$_to_external(res);
}

_ %get_view() method_id(76114) {
    var self = $IntroShard$_contract_load();
    var res = self~$IntroShard$_fun_get_view();
    return $IntroShardView$_to_external(res);
}

;;
;; Routing of a Contract IntroShard
;;

;; message opcode reader utility: only binary receivers
;; Returns 32 bit message opcode, otherwise throws the "Invalid incoming message" exit code
(slice, int) ~load_opcode_internal(slice s) asm( -> 1 0) "32 LDUQ 130 THROWIFNOT";

() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    
    ;; Context
    var cs = in_msg_cell.begin_parse();
    cs~skip_bits(2);
    var msg_bounceable = cs~load_int(1);
    var msg_bounced = cs~load_int(1);
    slice msg_sender_addr = cs~load_msg_addr();
    __tact_context = (msg_bounceable, msg_sender_addr, msg_value, cs);
    __tact_context_sender = msg_sender_addr;
    
    ;; Load contract data
    var ($self'epoch, $self'bucket, $self'intros, $self'next_id, $self'live_count, $self'evict_cursor) = $IntroShard$_contract_load();
    
    ;; Handle bounced messages
    if (msg_bounced) { return (); }
    int op = 0;
    int in_msg_length = slice_bits(in_msg);
    if (in_msg_length >= 32) {
        op = in_msg~load_uint(32);
    }
    ;; Receive IntroPublish message
    if (op == 0x49535031) {
        var $msg'r = in_msg~load_uint(256);
        var $msg'view_tag = in_msg~load_uint(16);
        var $msg'header_0 = in_msg~load_ref();
        var $msg'body = in_msg~load_ref();
        throw_unless(13682, ($Context$_get_value(__tact_context_get()) >= 2508000));
        throw_unless(13680, ($self'live_count < 8000));
        $self'intros~__tact_dict_set_int_cell(257, $self'next_id, $IntroEntry$_store_cell($IntroEntry$_constructor_r_view_tag_body_commit_created_at($msg'r, $msg'view_tag, ($self'epoch, $self'bucket, $self'intros, $self'next_id, $self'live_count, $self'evict_cursor)~$IntroShard$_fun_bodyCommit($msg'header_0, $msg'body), $global_now()), begin_cell()));
        $self'next_id = ($self'next_id + 1);
        $self'live_count = ($self'live_count + 1);
        $global_nativeReserve((8000 + (($self'next_id == 1) ? 1000000 : 0)), 6);
        $global_send($SendParameters$_constructor_to_value_mode_bounce(__tact_context_get_sender(), 0, 130, false));
        $IntroShard$_contract_store(($self'epoch, $self'bucket, $self'intros, $self'next_id, $self'live_count, $self'evict_cursor));
        return ();
    }
    
    ;; Receive EvictIntros message
    if (op == 0x49535032) {
        var $msg'max_count = in_msg~load_uint(16);
        int $swept = 0;
        int $cursor = $self'evict_cursor;
        while (( (( (($swept < $msg'max_count)) ? (($swept < 64)) : (false) )) ? (($cursor < $self'next_id)) : (false) )) {
            tuple $e = $IntroEntry$_load_opt(__tact_dict_get_int_cell($self'intros, 257, $cursor));
            if ((~ null?($e))) {
                if ((($IntroEntry$_get_created_at($IntroEntry$_not_null($e)) + 604800) < $global_now())) {
                    $self'intros~__tact_dict_set_int_cell(257, $cursor, $IntroEntry$_store_opt(null(), begin_cell()));
                    $self'live_count = ($self'live_count - 1);
                    $cursor = ($cursor + 1);
                    $swept = ($swept + 1);
                } else {
                    $swept = $msg'max_count;
                }
            } else {
                $cursor = ($cursor + 1);
            }
        }
        $self'evict_cursor = $cursor;
        $global_nativeReserve((1000000 + ($self'live_count * 8000)), 2);
        $global_send($SendParameters$_constructor_to_value_mode_bounce(__tact_context_get_sender(), 0, 130, false));
        $IntroShard$_contract_store(($self'epoch, $self'bucket, $self'intros, $self'next_id, $self'live_count, $self'evict_cursor));
        return ();
    }
    
    ;; Receive empty message
    if ((op == 0) & (in_msg_length <= 32)) {
        $IntroShard$_contract_store(($self'epoch, $self'bucket, $self'intros, $self'next_id, $self'live_count, $self'evict_cursor));
        return ();
    }
    ;; Throw if not handled
    throw(130);
}

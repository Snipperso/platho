#pragma version =0.4.6;
#pragma allow-post-modification;
#pragma compute-asm-ltr;

;; IntroShard_IntroShard.headers.fc
;;
;; Header files for IntroShard
;; NOTE: declarations are sorted for optimal order
;;

;; __tact_context_get
(int, slice, int, slice) __tact_context_get() inline;

;; __tact_context_get_sender
slice __tact_context_get_sender() inline;

;; __tact_dict_get_int_cell
cell __tact_dict_get_int_cell(cell d, int kl, int k) inline;

;; __tact_dict_set_int_cell
(cell, ()) __tact_dict_set_int_cell(cell d, int kl, int k, cell v) inline;

;; $DepositCapsuleFee$_store
builder $DepositCapsuleFee$_store(builder build_0, (int, int, int, int, slice) v) inline;

;; $DepositCapsuleFee$_store_cell
cell $DepositCapsuleFee$_store_cell((int, int, int, int, slice) v, builder b) inline;

;; $DepositCapsuleFee$_load_bounced
(slice, ((int, int))) $DepositCapsuleFee$_load_bounced(slice sc_0) inline;

;; $IntroEntry$_store
builder $IntroEntry$_store(builder build_0, (int, int, int, int) v) inline;

;; $IntroEntry$_store_cell
cell $IntroEntry$_store_cell((int, int, int, int) v, builder b) inline;

;; $IntroEntry$_load
(slice, ((int, int, int, int))) $IntroEntry$_load(slice sc_0) inline;

;; $IntroEntry$_as_optional
tuple $IntroEntry$_as_optional((int, int, int, int) v) inline;

;; $IntroEntry$_load_opt
tuple $IntroEntry$_load_opt(cell cl) inline;

;; $IntroShard$_store
builder $IntroShard$_store(builder build_0, (int, int, cell, int) v) inline;

;; $IntroShard$_load
(slice, ((int, int, cell, int))) $IntroShard$_load(slice sc_0) inline;

;; $Context$_get_value
_ $Context$_get_value((int, slice, int, slice) v) inline;

;; $IntroEntry$_get_r
_ $IntroEntry$_get_r((int, int, int, int) v) inline;

;; $IntroEntry$_get_view_tag
_ $IntroEntry$_get_view_tag((int, int, int, int) v) inline;

;; $IntroEntry$_not_null
((int, int, int, int)) $IntroEntry$_not_null(tuple v) inline;

;; $IntroEntryView$_to_external
(int, int, int, int, int) $IntroEntryView$_to_external(((int, int, int, int, int)) v) inline;

;; $IntroScanPage$_to_external
(int, int, int, cell) $IntroScanPage$_to_external(((int, int, int, cell)) v) inline;

;; $IntroShardView$_to_external
(int, int, int, int, int, int, int, int, int, slice) $IntroShardView$_to_external(((int, int, int, int, int, int, int, int, int, slice)) v) inline;

;; $IntroShard$init$_load
(slice, ((int, int))) $IntroShard$init$_load(slice sc_0) inline;

;; $IntroShard$_contract_init
(int, int, cell, int) $IntroShard$_contract_init(int $epoch, int $bucket) impure inline;

;; $IntroShard$_contract_load
(int, int, cell, int) $IntroShard$_contract_load() impure inline;

;; $IntroShard$_contract_store
() $IntroShard$_contract_store((int, int, cell, int) v) impure inline;

;; $IntroShard$_fun_bodyCommit
((int, int, cell, int), int) $IntroShard$_fun_bodyCommit((int, int, cell, int) $self, cell $h0, cell $body) impure inline_ref;

;; $IntroShard$_fun_packPairs
((int, int, cell, int), cell) $IntroShard$_fun_packPairs((int, int, cell, int) $self, int $start, int $n) impure inline_ref;

;; $IntroScanPage$_constructor_from_id_count_next_id_pairs
((int, int, int, cell)) $IntroScanPage$_constructor_from_id_count_next_id_pairs(int $from_id, int $count, int $next_id, cell $pairs) inline;

;; $IntroShard$_fun_get_scan_page
((int, int, cell, int), (int, int, int, cell)) $IntroShard$_fun_get_scan_page((int, int, cell, int) $self, int $from_id, int $max_count) impure inline_ref;

;; $IntroEntryView$_constructor_exists_r_view_tag_body_commit_created_at
((int, int, int, int, int)) $IntroEntryView$_constructor_exists_r_view_tag_body_commit_created_at(int $exists, int $r, int $view_tag, int $body_commit, int $created_at) inline;

;; $IntroShard$_fun_get_entry
((int, int, cell, int), (int, int, int, int, int)) $IntroShard$_fun_get_entry((int, int, cell, int) $self, int $entry_id) impure inline_ref;

;; $IntroShardView$_constructor_epoch_bucket_next_id_retention_safe_cap_min_value_deploy_min_value_retire_at_protocol_fee_fee_sink
((int, int, int, int, int, int, int, int, int, slice)) $IntroShardView$_constructor_epoch_bucket_next_id_retention_safe_cap_min_value_deploy_min_value_retire_at_protocol_fee_fee_sink(int $epoch, int $bucket, int $next_id, int $retention, int $safe_cap, int $min_value, int $deploy_min_value, int $retire_at, int $protocol_fee, slice $fee_sink) inline;

;; $IntroShard$_fun_get_view
((int, int, cell, int), (int, int, int, int, int, int, int, int, int, slice)) $IntroShard$_fun_get_view((int, int, cell, int) $self) impure inline_ref;

;; $IntroEntry$_constructor_r_view_tag_body_commit_created_at
((int, int, int, int)) $IntroEntry$_constructor_r_view_tag_body_commit_created_at(int $r, int $view_tag, int $body_commit, int $created_at) inline;

;; $MessageParameters$_constructor_to_value_bounce_mode_body
((int, cell, int, slice, int)) $MessageParameters$_constructor_to_value_bounce_mode_body(slice $to, int $value, int $bounce, int $mode, cell $body) inline;

;; $DepositCapsuleFee$_constructor_amount_lane_init_arg0_init_arg1_publisher
((int, int, int, int, slice)) $DepositCapsuleFee$_constructor_amount_lane_init_arg0_init_arg1_publisher(int $amount, int $lane, int $init_arg0, int $init_arg1, slice $publisher) inline;

;; $SendParameters$_constructor_to_value_mode_bounce
((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_bounce(slice $to, int $value, int $mode, int $bounce) inline;


;; IntroShard_IntroShard.stdlib.fc
global (int, slice, int, slice) __tact_context;
global slice __tact_context_sender;
global cell __tact_child_contract_codes;
global int __tact_randomized;

(int, slice, int, slice) __tact_context_get() inline {
    return __tact_context;
}

slice __tact_context_get_sender() inline {
    return __tact_context_sender;
}

forall X0, X1, X2, X3 -> tuple __tact_tuple_create_4((X0, X1, X2, X3) v) asm """
    4 TUPLE
""";

forall X0, X1, X2, X3 -> (X0, X1, X2, X3) __tact_tuple_destroy_4(tuple v) asm """
    4 UNTUPLE
""";

cell __tact_dict_get_int_cell(cell d, int kl, int k) inline {
    var (r, ok) = idict_get_ref?(d, kl, k);
    if (ok) {
        return r;
    } else {
        return null();
    }
}

(cell, ()) __tact_dict_set_int_cell(cell d, int kl, int k, cell v) inline {
    if (null?(v)) {
        var (r, ok) = idict_delete?(d, kl, k);
        return (r, ());
    } else {
        return (idict_set_ref(d, kl, k, v), ());
    }
}

builder $global_beginCell() impure asm """
    NEWC
""";

int $global_min(int $x, int $y) impure asm """
    MIN
""";

int $global_max(int $x, int $y) impure asm """
    MAX
""";

() $global_nativeReserve(int $amount, int $mode) impure asm """
    RAWRESERVE
""";

() $global_message((int, cell, int, slice, int) $params) impure asm """
    NEWC
    b{01} STSLICECONST  // store tag = $0 and ihr_disabled = true
    1 STI               // store `bounce`
    b{000} STSLICECONST // store bounced = false and src = addr_none
    STSLICE             // store `to`
    SWAP
    STGRAMS             // store `value`
    106 PUSHINT         // 1 + 4 + 4 + 64 + 32 + 1
    STZEROES
    // → Stack state
    // s0: Builder
    // s1: `body`
    // s2: `mode`
    STDICT
    ENDC
    SWAP
    SENDRAWMSG
""";

() $global_send((int, cell, cell, cell, int, slice, int) $params) impure asm """
    NEWC
    b{01} STSLICECONST  // store tag = $0 and ihr_disabled = true
    1 STI               // store `bounce`
    b{000} STSLICECONST // store bounced = false and src = addr_none
    STSLICE             // store `to`
    SWAP
    STGRAMS             // store `value`
    105 PUSHINT         // 1 + 4 + 4 + 64 + 32
    STZEROES            // store currency_collection, ihr_fee, fwd_fee, created_lt and created_at
    // → Stack state
    // s0: Builder
    // s1: `data`
    // s2: `code`
    // s3: `body`
    // s4: `mode`

    // Group 2: Placing the Builder after code and data, then checking those for nullability
    s2 XCHG0
    DUP2
    ISNULL
    SWAP
    ISNULL
    AND
    // → Stack state
    // s0: -1 (true) if `data` and `code` are both null, 0 (false) otherwise
    // s1: `code`
    // s2: `data`
    // s3: Builder
    // s4: `body`
    // s5: `mode`

    // Group 3: Left branch of the IFELSE, executed if s0 is -1 (true)
    <{
        DROP2 // drop `data` and `code`, since either of those is null
        b{0} STSLICECONST
    }> PUSHCONT

    // Group 3: Right branch of the IFELSE, executed if s0 is 0 (false)
    <{
        // _ split_depth:(Maybe (## 5))
        //   special:(Maybe TickTock)
        //   code:(Maybe ^Cell)
        //   data:(Maybe ^Cell)
        //   library:(Maybe ^Cell)
        // = StateInit;
        ROT                // place message Builder on top
        b{10} STSLICECONST // store Maybe = true, Either = false
        // Start composing inlined StateInit
        b{00} STSLICECONST // store split_depth and special first
        STDICT             // store code
        STDICT             // store data
        b{0} STSLICECONST  // store library
    }> PUSHCONT

    // Group 3: IFELSE that does the branching shown above
    IFELSE
    // → Stack state
    // s0: Builder
    // s1: null or StateInit
    // s2: `body`
    // s3: `mode`

    // Group 4: Finalizing the message
    STDICT // store `body` as ref with an extra Maybe bit, since `body` might be null
    ENDC
    // → Stack state
    // s0: Cell
    // s1: `mode`

    // Group 5: Sending the message, with `mode` on top
    SWAP
    SENDRAWMSG // https://github.com/tact-lang/tact/issues/1558
""";

int $global_now() impure asm """
    NOW
""";

builder $Builder$_fun_storeRef(cell $cell, builder $self) impure asm """
    STREF
""";

cell $Builder$_fun_endCell(builder $self) impure asm """
    ENDC
""";

int $Cell$_fun_hash(cell $self) impure asm """
    HASHCU
""";

;; IntroShard_IntroShard.constants.fc
;; EQBOSbFHf8Iqe390MhsuN8RywBimRbzTwq8dtnN9fN4MyZOP
slice __gen_slice_address_41b863b406c5ea17786b7f82c5e9c0824560899aa0ba92e8319e5b87fd569c47() asm """
    B{b5ee9c724101010100240000438009c93628eff8454f6fee864365c6f88e580314c8b79a7855e3b6ce6faf9bc199304986eec0} B>boc <s PUSHSLICE
""";

;; IntroShard_IntroShard.storage.fc
;;
;; Type: Context
;; TLB: _ bounceable:bool sender:address value:int257 raw:^slice = Context
;;

_ $Context$_get_value((int, slice, int, slice) v) inline {
    var (v'bounceable, v'sender, v'value, v'raw) = v;
    return v'value;
}

;;
;; Type: SendParameters
;; TLB: _ mode:int257 body:Maybe ^cell code:Maybe ^cell data:Maybe ^cell value:int257 to:address bounce:bool = SendParameters
;;

((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_bounce(slice $to, int $value, int $mode, int $bounce) inline {
    return ($mode, null(), null(), null(), $value, $to, $bounce);
}

;;
;; Type: MessageParameters
;; TLB: _ mode:int257 body:Maybe ^cell value:int257 to:address bounce:bool = MessageParameters
;;

((int, cell, int, slice, int)) $MessageParameters$_constructor_to_value_bounce_mode_body(slice $to, int $value, int $bounce, int $mode, cell $body) inline {
    return ($mode, $body, $value, $to, $bounce);
}

;;
;; Type: DepositCapsuleFee
;; Header: 0x52535046
;; TLB: deposit_capsule_fee#52535046 amount:uint128 lane:uint8 init_arg0:int257 init_arg1:int257 publisher:address = DepositCapsuleFee
;;

builder $DepositCapsuleFee$_store(builder build_0, (int, int, int, int, slice) v) inline {
    var (v'amount, v'lane, v'init_arg0, v'init_arg1, v'publisher) = v;
    build_0 = store_uint(build_0, 1381191750, 32);
    build_0 = build_0.store_uint(v'amount, 128);
    build_0 = build_0.store_uint(v'lane, 8);
    build_0 = build_0.store_int(v'init_arg0, 257);
    build_0 = build_0.store_int(v'init_arg1, 257);
    build_0 = build_0.store_slice(v'publisher);
    return build_0;
}

cell $DepositCapsuleFee$_store_cell((int, int, int, int, slice) v, builder b) inline {
    return $DepositCapsuleFee$_store(b, v).end_cell();
}

(slice, ((int, int))) $DepositCapsuleFee$_load_bounced(slice sc_0) inline {
    var v'amount = sc_0~load_uint(128);
    var v'lane = sc_0~load_uint(8);
    return (sc_0, (v'amount, v'lane));
}

((int, int, int, int, slice)) $DepositCapsuleFee$_constructor_amount_lane_init_arg0_init_arg1_publisher(int $amount, int $lane, int $init_arg0, int $init_arg1, slice $publisher) inline {
    return ($amount, $lane, $init_arg0, $init_arg1, $publisher);
}

;;
;; Type: IntroEntry
;; TLB: _ r:int257 view_tag:int257 body_commit:int257 created_at:int257 = IntroEntry
;;

builder $IntroEntry$_store(builder build_0, (int, int, int, int) v) inline {
    var (v'r, v'view_tag, v'body_commit, v'created_at) = v;
    build_0 = build_0.store_int(v'r, 257);
    build_0 = build_0.store_int(v'view_tag, 257);
    build_0 = build_0.store_int(v'body_commit, 257);
    var build_1 = begin_cell();
    build_1 = build_1.store_int(v'created_at, 257);
    build_0 = store_builder_ref(build_0, build_1);
    return build_0;
}

cell $IntroEntry$_store_cell((int, int, int, int) v, builder b) inline {
    return $IntroEntry$_store(b, v).end_cell();
}

(slice, ((int, int, int, int))) $IntroEntry$_load(slice sc_0) inline {
    var v'r = sc_0~load_int(257);
    var v'view_tag = sc_0~load_int(257);
    var v'body_commit = sc_0~load_int(257);
    slice sc_1 = sc_0~load_ref().begin_parse();
    var v'created_at = sc_1~load_int(257);
    return (sc_0, (v'r, v'view_tag, v'body_commit, v'created_at));
}

tuple $IntroEntry$_as_optional((int, int, int, int) v) inline {
    var (v'r, v'view_tag, v'body_commit, v'created_at) = v;
    return __tact_tuple_create_4(v'r, v'view_tag, v'body_commit, v'created_at);
}

tuple $IntroEntry$_load_opt(cell cl) inline {
    if (null?(cl)) {
        return null();
    }
    var sc = cl.begin_parse();
    return $IntroEntry$_as_optional(sc~$IntroEntry$_load());
}

_ $IntroEntry$_get_r((int, int, int, int) v) inline {
    var (v'r, v'view_tag, v'body_commit, v'created_at) = v;
    return v'r;
}

_ $IntroEntry$_get_view_tag((int, int, int, int) v) inline {
    var (v'r, v'view_tag, v'body_commit, v'created_at) = v;
    return v'view_tag;
}

((int, int, int, int)) $IntroEntry$_not_null(tuple v) inline {
    var (int vvv'r, int vvv'view_tag, int vvv'body_commit, int vvv'created_at) = __tact_tuple_destroy_4(v);
    return (vvv'r, vvv'view_tag, vvv'body_commit, vvv'created_at);
}

((int, int, int, int)) $IntroEntry$_constructor_r_view_tag_body_commit_created_at(int $r, int $view_tag, int $body_commit, int $created_at) inline {
    return ($r, $view_tag, $body_commit, $created_at);
}

;;
;; Type: IntroEntryView
;; TLB: _ exists:bool r:int257 view_tag:int257 body_commit:int257 created_at:int257 = IntroEntryView
;;

(int, int, int, int, int) $IntroEntryView$_to_external(((int, int, int, int, int)) v) inline {
    var (v'exists, v'r, v'view_tag, v'body_commit, v'created_at) = v; 
    return (v'exists, v'r, v'view_tag, v'body_commit, v'created_at);
}

((int, int, int, int, int)) $IntroEntryView$_constructor_exists_r_view_tag_body_commit_created_at(int $exists, int $r, int $view_tag, int $body_commit, int $created_at) inline {
    return ($exists, $r, $view_tag, $body_commit, $created_at);
}

;;
;; Type: IntroScanPage
;; TLB: _ from_id:int257 count:int257 next_id:int257 pairs:^cell = IntroScanPage
;;

(int, int, int, cell) $IntroScanPage$_to_external(((int, int, int, cell)) v) inline {
    var (v'from_id, v'count, v'next_id, v'pairs) = v; 
    return (v'from_id, v'count, v'next_id, v'pairs);
}

((int, int, int, cell)) $IntroScanPage$_constructor_from_id_count_next_id_pairs(int $from_id, int $count, int $next_id, cell $pairs) inline {
    return ($from_id, $count, $next_id, $pairs);
}

;;
;; Type: IntroShardView
;; TLB: _ epoch:int257 bucket:int257 next_id:int257 retention:int257 safe_cap:int257 min_value:int257 deploy_min_value:int257 protocol_fee:int257 retire_at:int257 fee_sink:address = IntroShardView
;;

(int, int, int, int, int, int, int, int, int, slice) $IntroShardView$_to_external(((int, int, int, int, int, int, int, int, int, slice)) v) inline {
    var (v'epoch, v'bucket, v'next_id, v'retention, v'safe_cap, v'min_value, v'deploy_min_value, v'protocol_fee, v'retire_at, v'fee_sink) = v; 
    return (v'epoch, v'bucket, v'next_id, v'retention, v'safe_cap, v'min_value, v'deploy_min_value, v'protocol_fee, v'retire_at, v'fee_sink);
}

((int, int, int, int, int, int, int, int, int, slice)) $IntroShardView$_constructor_epoch_bucket_next_id_retention_safe_cap_min_value_deploy_min_value_retire_at_protocol_fee_fee_sink(int $epoch, int $bucket, int $next_id, int $retention, int $safe_cap, int $min_value, int $deploy_min_value, int $retire_at, int $protocol_fee, slice $fee_sink) inline {
    return ($epoch, $bucket, $next_id, $retention, $safe_cap, $min_value, $deploy_min_value, $protocol_fee, $retire_at, $fee_sink);
}

;;
;; Type: IntroShard
;; TLB: _ epoch:uint32 bucket:uint32 intros:dict<int, ^IntroEntry{r:int257,view_tag:int257,body_commit:int257,created_at:int257}> next_id:uint32 = IntroShard
;;

builder $IntroShard$_store(builder build_0, (int, int, cell, int) v) inline {
    var (v'epoch, v'bucket, v'intros, v'next_id) = v;
    build_0 = build_0.store_uint(v'epoch, 32);
    build_0 = build_0.store_uint(v'bucket, 32);
    build_0 = build_0.store_dict(v'intros);
    build_0 = build_0.store_uint(v'next_id, 32);
    return build_0;
}

(slice, ((int, int, cell, int))) $IntroShard$_load(slice sc_0) inline {
    var v'epoch = sc_0~load_uint(32);
    var v'bucket = sc_0~load_uint(32);
    var v'intros = sc_0~load_dict();
    var v'next_id = sc_0~load_uint(32);
    return (sc_0, (v'epoch, v'bucket, v'intros, v'next_id));
}

(slice, ((int, int))) $IntroShard$init$_load(slice sc_0) inline {
    var v'epoch = sc_0~load_int(257);
    var v'bucket = sc_0~load_int(257);
    return (sc_0, (v'epoch, v'bucket));
}

(int, int, cell, int) $IntroShard$_contract_load() impure inline {
    slice $sc = get_data().begin_parse();
    int $loaded = $sc~load_int(1);
    if ($loaded) {
        return $sc~$IntroShard$_load();
    }
    else {
        (int $epoch, int $bucket) = $sc~$IntroShard$init$_load();
        $sc.end_parse();
        return $IntroShard$_contract_init($epoch, $bucket);
    }
}

() $IntroShard$_contract_store((int, int, cell, int) v) impure inline {
    builder b = begin_cell();
    b = b.store_int(true, 1);
    b = $IntroShard$_store(b, v);
    set_data(b.end_cell());
}

;;
;; Contract IntroShard functions
;;

(int, int, cell, int) $IntroShard$_contract_init(int $epoch, int $bucket) impure inline {
    var (($self'epoch, $self'bucket, $self'intros, $self'next_id)) = (null(), null(), null(), null());
    $self'epoch = $epoch;
    $self'bucket = $bucket;
    $self'intros = null();
    $self'next_id = 0;
    return ($self'epoch, $self'bucket, $self'intros, $self'next_id);
}

((int, int, cell, int), int) $IntroShard$_fun_bodyCommit((int, int, cell, int) $self, cell $h0, cell $body) impure inline_ref {
    var (($self'epoch, $self'bucket, $self'intros, $self'next_id)) = $self;
    var $fresh$ret_0 = $Cell$_fun_hash($Builder$_fun_endCell(store_uint(store_uint(store_uint($global_beginCell(), 1230193219, 32), $Cell$_fun_hash($h0), 256), $Cell$_fun_hash($body), 256)));
    return (($self'epoch, $self'bucket, $self'intros, $self'next_id), $fresh$ret_0);
}

((int, int, cell, int), cell) $IntroShard$_fun_packPairs((int, int, cell, int) $self, int $start, int $n) impure inline_ref {
    var (($self'epoch, $self'bucket, $self'intros, $self'next_id)) = $self;
    cell $cell = $Builder$_fun_endCell($global_beginCell());
    int $isTail = true;
    int $remaining = $n;
    while (($remaining > 0)) {
        int $chunk = $global_min($remaining, 3);
        int $first = (($start + $remaining) - $chunk);
        builder $b = $global_beginCell();
        int $i = 0;
        while (($i < $chunk)) {
            tuple $e = $IntroEntry$_load_opt(__tact_dict_get_int_cell($self'intros, 257, ($first + $i)));
            $b = store_uint(store_uint($b, (null?($e) ? 0 : $IntroEntry$_get_r($IntroEntry$_not_null($e))), 256), (null?($e) ? 0 : $IntroEntry$_get_view_tag($IntroEntry$_not_null($e))), 16);
            $i = ($i + 1);
        }
        if ($isTail) {
            $cell = $Builder$_fun_endCell($b);
            $isTail = false;
        } else {
            $cell = $Builder$_fun_endCell($Builder$_fun_storeRef($cell, $b));
        }
        $remaining = ($remaining - $chunk);
    }
    var $fresh$ret_1 = $cell;
    return (($self'epoch, $self'bucket, $self'intros, $self'next_id), $fresh$ret_1);
}

((int, int, cell, int), (int, int, int, cell)) $IntroShard$_fun_get_scan_page((int, int, cell, int) $self, int $from_id, int $max_count) impure inline_ref {
    var (($self'epoch, $self'bucket, $self'intros, $self'next_id)) = $self;
    int $start = $global_max($from_id, 0);
    int $available = (($self'next_id > $start) ? ($self'next_id - $start) : 0);
    int $n = $global_max(0, $global_min($global_min($max_count, 256), $available));
    var $fresh$ret_2 = $IntroScanPage$_constructor_from_id_count_next_id_pairs($start, $n, $self'next_id, ($self'epoch, $self'bucket, $self'intros, $self'next_id)~$IntroShard$_fun_packPairs($start, $n));
    return (($self'epoch, $self'bucket, $self'intros, $self'next_id), $fresh$ret_2);
}

((int, int, cell, int), (int, int, int, int, int)) $IntroShard$_fun_get_entry((int, int, cell, int) $self, int $entry_id) impure inline_ref {
    var (($self'epoch, $self'bucket, $self'intros, $self'next_id)) = $self;
    tuple $e = $IntroEntry$_load_opt(__tact_dict_get_int_cell($self'intros, 257, $entry_id));
    if (null?($e)) {
        var $fresh$ret_3 = $IntroEntryView$_constructor_exists_r_view_tag_body_commit_created_at(false, 0, 0, 0, 0);
        return (($self'epoch, $self'bucket, $self'intros, $self'next_id), $fresh$ret_3);
    }
    var ($v'r, $v'view_tag, $v'body_commit, $v'created_at) = $IntroEntry$_not_null($e);
    var $fresh$ret_4 = $IntroEntryView$_constructor_exists_r_view_tag_body_commit_created_at(true, $v'r, $v'view_tag, $v'body_commit, $v'created_at);
    return (($self'epoch, $self'bucket, $self'intros, $self'next_id), $fresh$ret_4);
}

((int, int, cell, int), (int, int, int, int, int, int, int, int, int, slice)) $IntroShard$_fun_get_view((int, int, cell, int) $self) impure inline_ref {
    var (($self'epoch, $self'bucket, $self'intros, $self'next_id)) = $self;
    var $fresh$ret_5 = $IntroShardView$_constructor_epoch_bucket_next_id_retention_safe_cap_min_value_deploy_min_value_retire_at_protocol_fee_fee_sink($self'epoch, $self'bucket, $self'next_id, 604800, 8000, 15310000, 17810000, (((($self'epoch + 2) * 86400) + 604800) + 345600), 10000000, __gen_slice_address_41b863b406c5ea17786b7f82c5e9c0824560899aa0ba92e8319e5b87fd569c47());
    return (($self'epoch, $self'bucket, $self'intros, $self'next_id), $fresh$ret_5);
}

;;
;; Get methods of a Contract IntroShard
;;

_ %get_scan_page(int $from_id, int $max_count) method_id(97744) {
    int $from_id = $from_id;
    int $max_count = $max_count;
    var self = $IntroShard$_contract_load();
    var res = self~$IntroShard$_fun_get_scan_page($from_id, $max_count);
    return $IntroScanPage$_to_external(res);
}

_ %get_entry(int $entry_id) method_id(80118) {
    int $entry_id = $entry_id;
    var self = $IntroShard$_contract_load();
    var res = self~$IntroShard$_fun_get_entry($entry_id);
    return $IntroEntryView$_to_external(res);
}

_ %get_view() method_id(76114) {
    var self = $IntroShard$_contract_load();
    var res = self~$IntroShard$_fun_get_view();
    return $IntroShardView$_to_external(res);
}

;;
;; Routing of a Contract IntroShard
;;

;; message opcode reader utility: only binary receivers
;; Returns 32 bit message opcode, otherwise throws the "Invalid incoming message" exit code
(slice, int) ~load_opcode_internal(slice s) asm( -> 1 0) "32 LDUQ 130 THROWIFNOT";

() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    
    ;; Context
    var cs = in_msg_cell.begin_parse();
    cs~skip_bits(2);
    var msg_bounceable = cs~load_int(1);
    var msg_bounced = cs~load_int(1);
    slice msg_sender_addr = cs~load_msg_addr();
    __tact_context = (msg_bounceable, msg_sender_addr, msg_value, cs);
    __tact_context_sender = msg_sender_addr;
    
    ;; Load contract data
    var ($self'epoch, $self'bucket, $self'intros, $self'next_id) = $IntroShard$_contract_load();
    
    ;; Handle bounced messages
    if (msg_bounced) {
        ;; Skip 0xFFFFFFFF prefix of a bounced message
        in_msg~skip_bits(32);
        int op = 0;
        if (slice_bits(in_msg) >= 32) {
            op = in_msg~load_uint(32);
        }
        ;; Bounced handler for DepositCapsuleFee message
        if (op == 0x52535046) {
            var ($msg'amount, $msg'lane) = in_msg~$DepositCapsuleFee$_load_bounced();
            throw_unless(13685, ( equal_slices_bits(__tact_context_get_sender(), __gen_slice_address_41b863b406c5ea17786b7f82c5e9c0824560899aa0ba92e8319e5b87fd569c47()) ));
            throw_unless(13686, ($msg'amount > 0));
            $IntroShard$_contract_store(($self'epoch, $self'bucket, $self'intros, $self'next_id));
            return ();
        }
        
        return ();
    }
    int op = 0;
    int in_msg_length = slice_bits(in_msg);
    if (in_msg_length >= 32) {
        op = in_msg~load_uint(32);
    }
    ;; Receive IntroPublish message
    if (op == 0x49535031) {
        var $msg'r = in_msg~load_uint(256);
        var $msg'view_tag = in_msg~load_uint(16);
        var $msg'header_0 = in_msg~load_ref();
        var $msg'body = in_msg~load_ref();
        int $nowEpoch = ($global_now() / 86400);
        throw_unless(13684, ( (($nowEpoch >= ($self'epoch - 1))) ? (($nowEpoch <= ($self'epoch + 1))) : (false) ));
        throw_unless(13682, ($Context$_get_value(__tact_context_get()) >= (($self'next_id == 0) ? 17810000 : 15310000)));
        throw_unless(13680, ($self'next_id < 8000));
        $self'intros~__tact_dict_set_int_cell(257, $self'next_id, $IntroEntry$_store_cell($IntroEntry$_constructor_r_view_tag_body_commit_created_at($msg'r, $msg'view_tag, ($self'epoch, $self'bucket, $self'intros, $self'next_id)~$IntroShard$_fun_bodyCommit($msg'header_0, $msg'body), $global_now()), begin_cell()));
        $self'next_id = ($self'next_id + 1);
        $global_message($MessageParameters$_constructor_to_value_bounce_mode_body(__gen_slice_address_41b863b406c5ea17786b7f82c5e9c0824560899aa0ba92e8319e5b87fd569c47(), 12600000, true, 1, $DepositCapsuleFee$_store_cell($DepositCapsuleFee$_constructor_amount_lane_init_arg0_init_arg1_publisher(10000000, 1, $self'epoch, $self'bucket, __tact_context_get_sender()), begin_cell())));
        $global_nativeReserve((10000 + (($self'next_id == 1) ? 2500000 : 0)), 6);
        $global_send($SendParameters$_constructor_to_value_mode_bounce(__tact_context_get_sender(), 0, 130, false));
        $IntroShard$_contract_store(($self'epoch, $self'bucket, $self'intros, $self'next_id));
        return ();
    }
    
    ;; Receive RetireShard message
    if (op == 0x49535033) {
        var $msg = empty_tuple();
        throw_unless(13683, ($global_now() > (((($self'epoch + 2) * 86400) + 604800) + 345600)));
        $self'intros = null();
        $self'next_id = 0;
        $global_send($SendParameters$_constructor_to_value_mode_bounce(__tact_context_get_sender(), 0, 162, false));
        $IntroShard$_contract_store(($self'epoch, $self'bucket, $self'intros, $self'next_id));
        return ();
    }
    
    ;; Receive empty message
    if ((op == 0) & (in_msg_length <= 32)) {
        $IntroShard$_contract_store(($self'epoch, $self'bucket, $self'intros, $self'next_id));
        return ();
    }
    ;; Throw if not handled
    throw(130);
}

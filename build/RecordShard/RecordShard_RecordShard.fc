#pragma version =0.4.6;
#pragma allow-post-modification;
#pragma compute-asm-ltr;

;; RecordShard_RecordShard.headers.fc
;;
;; Header files for RecordShard
;; NOTE: declarations are sorted for optimal order
;;

;; __tact_context_get
(int, slice, int, slice) __tact_context_get() inline;

;; __tact_context_get_sender
slice __tact_context_get_sender() inline;

;; __tact_dict_get_int_cell
cell __tact_dict_get_int_cell(cell d, int kl, int k) inline;

;; __tact_dict_set_int_cell
(cell, ()) __tact_dict_set_int_cell(cell d, int kl, int k, cell v) inline;

;; $DepositCapsuleFee$_store
builder $DepositCapsuleFee$_store(builder build_0, (int, int, int, int, slice) v) inline;

;; $DepositCapsuleFee$_store_cell
cell $DepositCapsuleFee$_store_cell((int, int, int, int, slice) v, builder b) inline;

;; $DepositCapsuleFee$_load_bounced
(slice, ((int, int))) $DepositCapsuleFee$_load_bounced(slice sc_0) inline;

;; $RecordEntry$_store
builder $RecordEntry$_store(builder build_0, (int, int) v) inline;

;; $RecordEntry$_store_cell
cell $RecordEntry$_store_cell((int, int) v, builder b) inline;

;; $RecordEntry$_load
(slice, ((int, int))) $RecordEntry$_load(slice sc_0) inline;

;; $RecordEntry$_as_optional
tuple $RecordEntry$_as_optional((int, int) v) inline;

;; $RecordEntry$_load_opt
tuple $RecordEntry$_load_opt(cell cl) inline;

;; $RecordShard$_store
builder $RecordShard$_store(builder build_0, (int, int, int, cell, int) v) inline;

;; $RecordShard$_load
(slice, ((int, int, int, cell, int))) $RecordShard$_load(slice sc_0) inline;

;; $Context$_get_value
_ $Context$_get_value((int, slice, int, slice) v) inline;

;; $RecordEntry$_not_null
((int, int)) $RecordEntry$_not_null(tuple v) inline;

;; $CapsuleRecordView$_to_external
(int, int, int) $CapsuleRecordView$_to_external(((int, int, int)) v) inline;

;; $RecordShardView$_to_external
(int, int, int, int, int, int, int, int, int, int, slice) $RecordShardView$_to_external(((int, int, int, int, int, int, int, int, int, int, slice)) v) inline;

;; $RecordShard$init$_load
(slice, ((int, int))) $RecordShard$init$_load(slice sc_0) inline;

;; $RecordShard$_contract_init
(int, int, int, cell, int) $RecordShard$_contract_init(int $write_pubkey, int $epoch) impure inline;

;; $RecordShard$_contract_load
(int, int, int, cell, int) $RecordShard$_contract_load() impure inline;

;; $RecordShard$_contract_store
() $RecordShard$_contract_store((int, int, int, cell, int) v) impure inline;

;; $RecordShard$_fun_frameCommit
((int, int, int, cell, int), int) $RecordShard$_fun_frameCommit((int, int, int, cell, int) $self, cell $h0, cell $h1, cell $body) impure inline_ref;

;; $CapsuleRecordView$_constructor_exists_frame_commit_created_at
((int, int, int)) $CapsuleRecordView$_constructor_exists_frame_commit_created_at(int $exists, int $frame_commit, int $created_at) inline;

;; $RecordShard$_fun_get_record
((int, int, int, cell, int), (int, int, int)) $RecordShard$_fun_get_record((int, int, int, cell, int) $self, int $entry_id) impure inline_ref;

;; $RecordShardView$_constructor_write_pubkey_epoch_last_seq_record_count_safe_cap_retention_min_value_deploy_min_value_retire_at_protocol_fee_fee_sink
((int, int, int, int, int, int, int, int, int, int, slice)) $RecordShardView$_constructor_write_pubkey_epoch_last_seq_record_count_safe_cap_retention_min_value_deploy_min_value_retire_at_protocol_fee_fee_sink(int $write_pubkey, int $epoch, int $last_seq, int $record_count, int $safe_cap, int $retention, int $min_value, int $deploy_min_value, int $retire_at, int $protocol_fee, slice $fee_sink) inline;

;; $RecordShard$_fun_get_view
((int, int, int, cell, int), (int, int, int, int, int, int, int, int, int, int, slice)) $RecordShard$_fun_get_view((int, int, int, cell, int) $self) impure inline_ref;

;; $RecordEntry$_constructor_frame_commit_created_at
((int, int)) $RecordEntry$_constructor_frame_commit_created_at(int $frame_commit, int $created_at) inline;

;; $MessageParameters$_constructor_to_value_bounce_mode_body
((int, cell, int, slice, int)) $MessageParameters$_constructor_to_value_bounce_mode_body(slice $to, int $value, int $bounce, int $mode, cell $body) inline;

;; $DepositCapsuleFee$_constructor_amount_lane_init_arg0_init_arg1_publisher
((int, int, int, int, slice)) $DepositCapsuleFee$_constructor_amount_lane_init_arg0_init_arg1_publisher(int $amount, int $lane, int $init_arg0, int $init_arg1, slice $publisher) inline;

;; $SendParameters$_constructor_to_value_mode_bounce
((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_bounce(slice $to, int $value, int $mode, int $bounce) inline;


;; RecordShard_RecordShard.stdlib.fc
global (int, slice, int, slice) __tact_context;
global slice __tact_context_sender;
global cell __tact_child_contract_codes;
global int __tact_randomized;

(int, slice, int, slice) __tact_context_get() inline {
    return __tact_context;
}

slice __tact_context_get_sender() inline {
    return __tact_context_sender;
}

forall X0, X1 -> tuple __tact_tuple_create_2((X0, X1) v) asm """
    2 TUPLE
""";

forall X0, X1 -> (X0, X1) __tact_tuple_destroy_2(tuple v) asm """
    2 UNTUPLE
""";

cell __tact_dict_get_int_cell(cell d, int kl, int k) inline {
    var (r, ok) = idict_get_ref?(d, kl, k);
    if (ok) {
        return r;
    } else {
        return null();
    }
}

(cell, ()) __tact_dict_set_int_cell(cell d, int kl, int k, cell v) inline {
    if (null?(v)) {
        var (r, ok) = idict_delete?(d, kl, k);
        return (r, ());
    } else {
        return (idict_set_ref(d, kl, k, v), ());
    }
}

builder $global_beginCell() impure asm """
    NEWC
""";

int $global_checkSignature(int $hash, slice $signature, int $publicKey) impure asm """
    CHKSIGNU
""";

() $global_nativeReserve(int $amount, int $mode) impure asm """
    RAWRESERVE
""";

() $global_message((int, cell, int, slice, int) $params) impure asm """
    NEWC
    b{01} STSLICECONST  // store tag = $0 and ihr_disabled = true
    1 STI               // store `bounce`
    b{000} STSLICECONST // store bounced = false and src = addr_none
    STSLICE             // store `to`
    SWAP
    STGRAMS             // store `value`
    106 PUSHINT         // 1 + 4 + 4 + 64 + 32 + 1
    STZEROES
    // → Stack state
    // s0: Builder
    // s1: `body`
    // s2: `mode`
    STDICT
    ENDC
    SWAP
    SENDRAWMSG
""";

() $global_send((int, cell, cell, cell, int, slice, int) $params) impure asm """
    NEWC
    b{01} STSLICECONST  // store tag = $0 and ihr_disabled = true
    1 STI               // store `bounce`
    b{000} STSLICECONST // store bounced = false and src = addr_none
    STSLICE             // store `to`
    SWAP
    STGRAMS             // store `value`
    105 PUSHINT         // 1 + 4 + 4 + 64 + 32
    STZEROES            // store currency_collection, ihr_fee, fwd_fee, created_lt and created_at
    // → Stack state
    // s0: Builder
    // s1: `data`
    // s2: `code`
    // s3: `body`
    // s4: `mode`

    // Group 2: Placing the Builder after code and data, then checking those for nullability
    s2 XCHG0
    DUP2
    ISNULL
    SWAP
    ISNULL
    AND
    // → Stack state
    // s0: -1 (true) if `data` and `code` are both null, 0 (false) otherwise
    // s1: `code`
    // s2: `data`
    // s3: Builder
    // s4: `body`
    // s5: `mode`

    // Group 3: Left branch of the IFELSE, executed if s0 is -1 (true)
    <{
        DROP2 // drop `data` and `code`, since either of those is null
        b{0} STSLICECONST
    }> PUSHCONT

    // Group 3: Right branch of the IFELSE, executed if s0 is 0 (false)
    <{
        // _ split_depth:(Maybe (## 5))
        //   special:(Maybe TickTock)
        //   code:(Maybe ^Cell)
        //   data:(Maybe ^Cell)
        //   library:(Maybe ^Cell)
        // = StateInit;
        ROT                // place message Builder on top
        b{10} STSLICECONST // store Maybe = true, Either = false
        // Start composing inlined StateInit
        b{00} STSLICECONST // store split_depth and special first
        STDICT             // store code
        STDICT             // store data
        b{0} STSLICECONST  // store library
    }> PUSHCONT

    // Group 3: IFELSE that does the branching shown above
    IFELSE
    // → Stack state
    // s0: Builder
    // s1: null or StateInit
    // s2: `body`
    // s3: `mode`

    // Group 4: Finalizing the message
    STDICT // store `body` as ref with an extra Maybe bit, since `body` might be null
    ENDC
    // → Stack state
    // s0: Cell
    // s1: `mode`

    // Group 5: Sending the message, with `mode` on top
    SWAP
    SENDRAWMSG // https://github.com/tact-lang/tact/issues/1558
""";

int $global_now() impure asm """
    NOW
""";

cell $Builder$_fun_endCell(builder $self) impure asm """
    ENDC
""";

slice $Cell$_fun_beginParse(cell $self) impure asm """
    CTOS
""";

int $Cell$_fun_hash(cell $self) impure asm """
    HASHCU
""";

;; RecordShard_RecordShard.constants.fc
;; EQAsK2s3Dv0Gr1tj8Ycznk_75eCWTy7paF3yFhgpL1dzGdLH
slice __gen_slice_address_d5c3503e45ebaa622eb24a99cd82f286d3042aaa65a54e47a6884db1859ecc06() asm """
    B{b5ee9c724101010100240000438005856d66e1dfa0d5eb6c7e30e673c9ff7cbc12c9e5dd2d0bbe42c30525eaee633056937326} B>boc <s PUSHSLICE
""";

;; RecordShard_RecordShard.storage.fc
;;
;; Type: Context
;; TLB: _ bounceable:bool sender:address value:int257 raw:^slice = Context
;;

_ $Context$_get_value((int, slice, int, slice) v) inline {
    var (v'bounceable, v'sender, v'value, v'raw) = v;
    return v'value;
}

;;
;; Type: SendParameters
;; TLB: _ mode:int257 body:Maybe ^cell code:Maybe ^cell data:Maybe ^cell value:int257 to:address bounce:bool = SendParameters
;;

((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_bounce(slice $to, int $value, int $mode, int $bounce) inline {
    return ($mode, null(), null(), null(), $value, $to, $bounce);
}

;;
;; Type: MessageParameters
;; TLB: _ mode:int257 body:Maybe ^cell value:int257 to:address bounce:bool = MessageParameters
;;

((int, cell, int, slice, int)) $MessageParameters$_constructor_to_value_bounce_mode_body(slice $to, int $value, int $bounce, int $mode, cell $body) inline {
    return ($mode, $body, $value, $to, $bounce);
}

;;
;; Type: DepositCapsuleFee
;; Header: 0x52535046
;; TLB: deposit_capsule_fee#52535046 amount:uint128 lane:uint8 init_arg0:int257 init_arg1:int257 publisher:address = DepositCapsuleFee
;;

builder $DepositCapsuleFee$_store(builder build_0, (int, int, int, int, slice) v) inline {
    var (v'amount, v'lane, v'init_arg0, v'init_arg1, v'publisher) = v;
    build_0 = store_uint(build_0, 1381191750, 32);
    build_0 = build_0.store_uint(v'amount, 128);
    build_0 = build_0.store_uint(v'lane, 8);
    build_0 = build_0.store_int(v'init_arg0, 257);
    build_0 = build_0.store_int(v'init_arg1, 257);
    build_0 = build_0.store_slice(v'publisher);
    return build_0;
}

cell $DepositCapsuleFee$_store_cell((int, int, int, int, slice) v, builder b) inline {
    return $DepositCapsuleFee$_store(b, v).end_cell();
}

(slice, ((int, int))) $DepositCapsuleFee$_load_bounced(slice sc_0) inline {
    var v'amount = sc_0~load_uint(128);
    var v'lane = sc_0~load_uint(8);
    return (sc_0, (v'amount, v'lane));
}

((int, int, int, int, slice)) $DepositCapsuleFee$_constructor_amount_lane_init_arg0_init_arg1_publisher(int $amount, int $lane, int $init_arg0, int $init_arg1, slice $publisher) inline {
    return ($amount, $lane, $init_arg0, $init_arg1, $publisher);
}

;;
;; Type: RecordEntry
;; TLB: _ frame_commit:int257 created_at:int257 = RecordEntry
;;

builder $RecordEntry$_store(builder build_0, (int, int) v) inline {
    var (v'frame_commit, v'created_at) = v;
    build_0 = build_0.store_int(v'frame_commit, 257);
    build_0 = build_0.store_int(v'created_at, 257);
    return build_0;
}

cell $RecordEntry$_store_cell((int, int) v, builder b) inline {
    return $RecordEntry$_store(b, v).end_cell();
}

(slice, ((int, int))) $RecordEntry$_load(slice sc_0) inline {
    var v'frame_commit = sc_0~load_int(257);
    var v'created_at = sc_0~load_int(257);
    return (sc_0, (v'frame_commit, v'created_at));
}

tuple $RecordEntry$_as_optional((int, int) v) inline {
    var (v'frame_commit, v'created_at) = v;
    return __tact_tuple_create_2(v'frame_commit, v'created_at);
}

tuple $RecordEntry$_load_opt(cell cl) inline {
    if (null?(cl)) {
        return null();
    }
    var sc = cl.begin_parse();
    return $RecordEntry$_as_optional(sc~$RecordEntry$_load());
}

((int, int)) $RecordEntry$_not_null(tuple v) inline {
    var (int vvv'frame_commit, int vvv'created_at) = __tact_tuple_destroy_2(v);
    return (vvv'frame_commit, vvv'created_at);
}

((int, int)) $RecordEntry$_constructor_frame_commit_created_at(int $frame_commit, int $created_at) inline {
    return ($frame_commit, $created_at);
}

;;
;; Type: CapsuleRecordView
;; TLB: _ exists:bool frame_commit:int257 created_at:int257 = CapsuleRecordView
;;

(int, int, int) $CapsuleRecordView$_to_external(((int, int, int)) v) inline {
    var (v'exists, v'frame_commit, v'created_at) = v; 
    return (v'exists, v'frame_commit, v'created_at);
}

((int, int, int)) $CapsuleRecordView$_constructor_exists_frame_commit_created_at(int $exists, int $frame_commit, int $created_at) inline {
    return ($exists, $frame_commit, $created_at);
}

;;
;; Type: RecordShardView
;; TLB: _ write_pubkey:int257 epoch:int257 last_seq:int257 record_count:int257 safe_cap:int257 retention:int257 min_value:int257 deploy_min_value:int257 protocol_fee:int257 retire_at:int257 fee_sink:address = RecordShardView
;;

(int, int, int, int, int, int, int, int, int, int, slice) $RecordShardView$_to_external(((int, int, int, int, int, int, int, int, int, int, slice)) v) inline {
    var (v'write_pubkey, v'epoch, v'last_seq, v'record_count, v'safe_cap, v'retention, v'min_value, v'deploy_min_value, v'protocol_fee, v'retire_at, v'fee_sink) = v; 
    return (v'write_pubkey, v'epoch, v'last_seq, v'record_count, v'safe_cap, v'retention, v'min_value, v'deploy_min_value, v'protocol_fee, v'retire_at, v'fee_sink);
}

((int, int, int, int, int, int, int, int, int, int, slice)) $RecordShardView$_constructor_write_pubkey_epoch_last_seq_record_count_safe_cap_retention_min_value_deploy_min_value_retire_at_protocol_fee_fee_sink(int $write_pubkey, int $epoch, int $last_seq, int $record_count, int $safe_cap, int $retention, int $min_value, int $deploy_min_value, int $retire_at, int $protocol_fee, slice $fee_sink) inline {
    return ($write_pubkey, $epoch, $last_seq, $record_count, $safe_cap, $retention, $min_value, $deploy_min_value, $protocol_fee, $retire_at, $fee_sink);
}

;;
;; Type: RecordShard
;; TLB: _ write_pubkey:uint256 epoch:uint32 last_seq:uint64 records:dict<int, ^RecordEntry{frame_commit:int257,created_at:int257}> record_count:uint32 = RecordShard
;;

builder $RecordShard$_store(builder build_0, (int, int, int, cell, int) v) inline {
    var (v'write_pubkey, v'epoch, v'last_seq, v'records, v'record_count) = v;
    build_0 = build_0.store_uint(v'write_pubkey, 256);
    build_0 = build_0.store_uint(v'epoch, 32);
    build_0 = build_0.store_uint(v'last_seq, 64);
    build_0 = build_0.store_dict(v'records);
    build_0 = build_0.store_uint(v'record_count, 32);
    return build_0;
}

(slice, ((int, int, int, cell, int))) $RecordShard$_load(slice sc_0) inline {
    var v'write_pubkey = sc_0~load_uint(256);
    var v'epoch = sc_0~load_uint(32);
    var v'last_seq = sc_0~load_uint(64);
    var v'records = sc_0~load_dict();
    var v'record_count = sc_0~load_uint(32);
    return (sc_0, (v'write_pubkey, v'epoch, v'last_seq, v'records, v'record_count));
}

(slice, ((int, int))) $RecordShard$init$_load(slice sc_0) inline {
    var v'write_pubkey = sc_0~load_int(257);
    var v'epoch = sc_0~load_int(257);
    return (sc_0, (v'write_pubkey, v'epoch));
}

(int, int, int, cell, int) $RecordShard$_contract_load() impure inline {
    slice $sc = get_data().begin_parse();
    int $loaded = $sc~load_int(1);
    if ($loaded) {
        return $sc~$RecordShard$_load();
    }
    else {
        (int $write_pubkey, int $epoch) = $sc~$RecordShard$init$_load();
        $sc.end_parse();
        return $RecordShard$_contract_init($write_pubkey, $epoch);
    }
}

() $RecordShard$_contract_store((int, int, int, cell, int) v) impure inline {
    builder b = begin_cell();
    b = b.store_int(true, 1);
    b = $RecordShard$_store(b, v);
    set_data(b.end_cell());
}

;;
;; Contract RecordShard functions
;;

(int, int, int, cell, int) $RecordShard$_contract_init(int $write_pubkey, int $epoch) impure inline {
    var (($self'write_pubkey, $self'epoch, $self'last_seq, $self'records, $self'record_count)) = (null(), null(), null(), null(), null());
    $self'write_pubkey = $write_pubkey;
    $self'epoch = $epoch;
    $self'last_seq = 0;
    $self'records = null();
    $self'record_count = 0;
    return ($self'write_pubkey, $self'epoch, $self'last_seq, $self'records, $self'record_count);
}

((int, int, int, cell, int), int) $RecordShard$_fun_frameCommit((int, int, int, cell, int) $self, cell $h0, cell $h1, cell $body) impure inline_ref {
    var (($self'write_pubkey, $self'epoch, $self'last_seq, $self'records, $self'record_count)) = $self;
    var $fresh$ret_988 = $Cell$_fun_hash($Builder$_fun_endCell(store_uint(store_uint(store_uint(store_uint($global_beginCell(), 1381189187, 32), $Cell$_fun_hash($h0), 256), $Cell$_fun_hash($h1), 256), $Cell$_fun_hash($body), 256)));
    return (($self'write_pubkey, $self'epoch, $self'last_seq, $self'records, $self'record_count), $fresh$ret_988);
}

((int, int, int, cell, int), (int, int, int)) $RecordShard$_fun_get_record((int, int, int, cell, int) $self, int $entry_id) impure inline_ref {
    var (($self'write_pubkey, $self'epoch, $self'last_seq, $self'records, $self'record_count)) = $self;
    tuple $e = $RecordEntry$_load_opt(__tact_dict_get_int_cell($self'records, 257, $entry_id));
    if (null?($e)) {
        var $fresh$ret_989 = $CapsuleRecordView$_constructor_exists_frame_commit_created_at(false, 0, 0);
        return (($self'write_pubkey, $self'epoch, $self'last_seq, $self'records, $self'record_count), $fresh$ret_989);
    }
    var ($v'frame_commit, $v'created_at) = $RecordEntry$_not_null($e);
    var $fresh$ret_990 = $CapsuleRecordView$_constructor_exists_frame_commit_created_at(true, $v'frame_commit, $v'created_at);
    return (($self'write_pubkey, $self'epoch, $self'last_seq, $self'records, $self'record_count), $fresh$ret_990);
}

((int, int, int, cell, int), (int, int, int, int, int, int, int, int, int, int, slice)) $RecordShard$_fun_get_view((int, int, int, cell, int) $self) impure inline_ref {
    var (($self'write_pubkey, $self'epoch, $self'last_seq, $self'records, $self'record_count)) = $self;
    var $fresh$ret_991 = $RecordShardView$_constructor_write_pubkey_epoch_last_seq_record_count_safe_cap_retention_min_value_deploy_min_value_retire_at_protocol_fee_fee_sink($self'write_pubkey, $self'epoch, $self'last_seq, $self'record_count, 4096, 31536000, 15600000, 19100000, ((($self'epoch + 2) * 86400) + 31536000), 10000000, __gen_slice_address_d5c3503e45ebaa622eb24a99cd82f286d3042aaa65a54e47a6884db1859ecc06());
    return (($self'write_pubkey, $self'epoch, $self'last_seq, $self'records, $self'record_count), $fresh$ret_991);
}

;;
;; Get methods of a Contract RecordShard
;;

_ %get_record(int $entry_id) method_id(115713) {
    int $entry_id = $entry_id;
    var self = $RecordShard$_contract_load();
    var res = self~$RecordShard$_fun_get_record($entry_id);
    return $CapsuleRecordView$_to_external(res);
}

_ %get_view() method_id(76114) {
    var self = $RecordShard$_contract_load();
    var res = self~$RecordShard$_fun_get_view();
    return $RecordShardView$_to_external(res);
}

;;
;; Routing of a Contract RecordShard
;;

;; message opcode reader utility: only binary receivers
;; Returns 32 bit message opcode, otherwise throws the "Invalid incoming message" exit code
(slice, int) ~load_opcode_internal(slice s) asm( -> 1 0) "32 LDUQ 130 THROWIFNOT";

() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    
    ;; Context
    var cs = in_msg_cell.begin_parse();
    cs~skip_bits(2);
    var msg_bounceable = cs~load_int(1);
    var msg_bounced = cs~load_int(1);
    slice msg_sender_addr = cs~load_msg_addr();
    __tact_context = (msg_bounceable, msg_sender_addr, msg_value, cs);
    __tact_context_sender = msg_sender_addr;
    
    ;; Load contract data
    var ($self'write_pubkey, $self'epoch, $self'last_seq, $self'records, $self'record_count) = $RecordShard$_contract_load();
    
    ;; Handle bounced messages
    if (msg_bounced) {
        ;; Skip 0xFFFFFFFF prefix of a bounced message
        in_msg~skip_bits(32);
        int op = 0;
        if (slice_bits(in_msg) >= 32) {
            op = in_msg~load_uint(32);
        }
        ;; Bounced handler for DepositCapsuleFee message
        if (op == 0x52535046) {
            var ($msg'amount, $msg'lane) = in_msg~$DepositCapsuleFee$_load_bounced();
            throw_unless(13657, ( equal_slices_bits(__tact_context_get_sender(), __gen_slice_address_d5c3503e45ebaa622eb24a99cd82f286d3042aaa65a54e47a6884db1859ecc06()) ));
            throw_unless(13658, ($msg'amount > 0));
            $RecordShard$_contract_store(($self'write_pubkey, $self'epoch, $self'last_seq, $self'records, $self'record_count));
            return ();
        }
        
        return ();
    }
    int op = 0;
    int in_msg_length = slice_bits(in_msg);
    if (in_msg_length >= 32) {
        op = in_msg~load_uint(32);
    }
    ;; Receive CapsulePublish message
    if (op == 0x52535031) {
        var $msg'seq = in_msg~load_uint(64);
        var $msg'header_0 = in_msg~load_ref();
        var $msg'header_1 = in_msg~load_ref();
        slice sc_1 = in_msg~load_ref().begin_parse();
        var $msg'body = sc_1~load_ref();
        var $msg'sig = sc_1~load_ref();
        int $nowEpoch = ($global_now() / 86400);
        throw_unless(13656, ( (($nowEpoch >= ($self'epoch - 1))) ? (($nowEpoch <= ($self'epoch + 1))) : (false) ));
        throw_unless(13652, ($Context$_get_value(__tact_context_get()) >= (($self'record_count == 0) ? 19100000 : 15600000)));
        throw_unless(13651, ($self'record_count < 4096));
        throw_unless(13653, ($msg'seq > $self'last_seq));
        int $commit = ($self'write_pubkey, $self'epoch, $self'last_seq, $self'records, $self'record_count)~$RecordShard$_fun_frameCommit($msg'header_0, $msg'header_1, $msg'body);
        int $digest = $Cell$_fun_hash($Builder$_fun_endCell(store_uint(store_uint(store_uint($global_beginCell(), 1381193540, 32), $msg'seq, 64), $commit, 256)));
        throw_unless(13654, $global_checkSignature($digest, $Cell$_fun_beginParse($msg'sig), $self'write_pubkey));
        $self'last_seq = $msg'seq;
        $self'records~__tact_dict_set_int_cell(257, $self'record_count, $RecordEntry$_store_cell($RecordEntry$_constructor_frame_commit_created_at($commit, $global_now()), begin_cell()));
        $self'record_count = ($self'record_count + 1);
        $global_message($MessageParameters$_constructor_to_value_bounce_mode_body(__gen_slice_address_d5c3503e45ebaa622eb24a99cd82f286d3042aaa65a54e47a6884db1859ecc06(), 12600000, true, 1, $DepositCapsuleFee$_store_cell($DepositCapsuleFee$_constructor_amount_lane_init_arg0_init_arg1_publisher(10000000, 0, $self'write_pubkey, $self'epoch, __tact_context_get_sender()), begin_cell())));
        $global_nativeReserve((300000 + (($self'record_count == 1) ? 3500000 : 0)), 6);
        $global_send($SendParameters$_constructor_to_value_mode_bounce(__tact_context_get_sender(), 0, 130, false));
        $RecordShard$_contract_store(($self'write_pubkey, $self'epoch, $self'last_seq, $self'records, $self'record_count));
        return ();
    }
    
    ;; Receive RetireShard message
    if (op == 0x52535033) {
        var $msg = empty_tuple();
        throw_unless(13655, ($global_now() > ((($self'epoch + 2) * 86400) + 31536000)));
        $self'records = null();
        $self'record_count = 0;
        $global_send($SendParameters$_constructor_to_value_mode_bounce(__tact_context_get_sender(), 0, 162, false));
        $RecordShard$_contract_store(($self'write_pubkey, $self'epoch, $self'last_seq, $self'records, $self'record_count));
        return ();
    }
    
    ;; Receive empty message
    if ((op == 0) & (in_msg_length <= 32)) {
        $RecordShard$_contract_store(($self'write_pubkey, $self'epoch, $self'last_seq, $self'records, $self'record_count));
        return ();
    }
    ;; Throw if not handled
    throw(130);
}

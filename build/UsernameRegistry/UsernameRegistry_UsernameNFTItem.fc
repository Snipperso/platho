#pragma version =0.4.6;
#pragma allow-post-modification;
#pragma compute-asm-ltr;

;; UsernameRegistry_UsernameNFTItem.headers.fc
;;
;; Header files for UsernameNFTItem
;; NOTE: declarations are sorted for optimal order
;;

;; __tact_context_get
(int, slice, int, slice) __tact_context_get() inline;

;; __tact_context_get_sender
slice __tact_context_get_sender() inline;

;; __tact_string_builder_start
tuple __tact_string_builder_start(builder b) inline;

;; __tact_string_builder_start_tail_string
tuple __tact_string_builder_start_tail_string() inline;

;; __tact_string_builder_end
cell __tact_string_builder_end(tuple builders) inline;

;; __tact_string_builder_append
((tuple), ()) __tact_string_builder_append(tuple builders, slice sc) inline_ref;

;; __tact_dict_set_uint_cell
(cell, ()) __tact_dict_set_uint_cell(cell d, int kl, int k, cell v) inline;

;; $UsernameOwnershipProof$_store
builder $UsernameOwnershipProof$_store(builder build_0, (int, int, slice, int, slice) v) inline;

;; $UsernameOwnershipProof$_store_cell
cell $UsernameOwnershipProof$_store_cell((int, int, slice, int, slice) v, builder b) inline;

;; $UsernameItemDeployedAck$_store
builder $UsernameItemDeployedAck$_store(builder build_0, (int, slice) v) inline;

;; $UsernameItemDeployedAck$_store_cell
cell $UsernameItemDeployedAck$_store_cell((int, slice) v, builder b) inline;

;; $NftOwnershipAssigned$_store
builder $NftOwnershipAssigned$_store(builder build_0, (int, slice, slice) v) inline;

;; $NftOwnershipAssigned$_store_cell
cell $NftOwnershipAssigned$_store_cell((int, slice, slice) v, builder b) inline;

;; $NftExcesses$_store
builder $NftExcesses$_store(builder build_0, (int) v) inline;

;; $NftExcesses$_store_cell
cell $NftExcesses$_store_cell((int) v, builder b) inline;

;; $UsernameNftOnchainContent$_store
builder $UsernameNftOnchainContent$_store(builder build_0, (int, cell) v) inline;

;; $UsernameNftOnchainContent$_store_cell
cell $UsernameNftOnchainContent$_store_cell((int, cell) v, builder b) inline;

;; $UsernameNFTItem$_store
builder $UsernameNFTItem$_store(builder build_0, (int, slice, slice, int, int, cell) v) inline;

;; $UsernameNFTItem$_load
(slice, ((int, slice, slice, int, int, cell))) $UsernameNFTItem$_load(slice sc_0) inline;

;; $Context$_get_value
_ $Context$_get_value((int, slice, int, slice) v) inline;

;; $UsernameNFTItemStateView$_to_external
(int, slice, slice, int, int, cell, int) $UsernameNFTItemStateView$_to_external(((int, slice, slice, int, int, cell, int)) v) inline;

;; $UsernameNftDataView$_to_external
(int, int, slice, slice, cell) $UsernameNftDataView$_to_external(((int, int, slice, slice, cell)) v) inline;

;; $UsernameNFTItem$init$_load
(slice, ((slice, int))) $UsernameNFTItem$init$_load(slice sc_0) inline;

;; $UsernameNFTItem$_contract_init
(int, slice, slice, int, int, cell) $UsernameNFTItem$_contract_init(slice $username_registry_address, int $name_hash) impure inline;

;; $UsernameNFTItem$_contract_load
(int, slice, slice, int, int, cell) $UsernameNFTItem$_contract_load() impure inline;

;; $UsernameNFTItem$_contract_store
() $UsernameNFTItem$_contract_store((int, slice, slice, int, int, cell) v) impure inline;

;; $Cell$_fun_asSlice
slice $Cell$_fun_asSlice(cell $self) impure inline;

;; $UsernameNFTItem$_fun_isBasechainAddress
((int, slice, slice, int, int, cell), int) $UsernameNFTItem$_fun_isBasechainAddress((int, slice, slice, int, int, cell) $self, slice $address) impure inline_ref;

;; $UsernameNFTItem$_fun_isValidNameLength
((int, slice, slice, int, int, cell), int) $UsernameNFTItem$_fun_isValidNameLength((int, slice, slice, int, int, cell) $self, int $name_len) impure inline_ref;

;; $UsernameNFTItem$_fun_isValidUsernameBytes
((int, slice, slice, int, int, cell), int) $UsernameNFTItem$_fun_isValidUsernameBytes((int, slice, slice, int, int, cell) $self, int $name_len, slice $username) impure inline_ref;

;; $UsernameNFTItem$_fun_computeNameHash
((int, slice, slice, int, int, cell), int) $UsernameNFTItem$_fun_computeNameHash((int, slice, slice, int, int, cell) $self, slice $username) impure inline_ref;

;; $UsernameNFTItem$_fun_tier
((int, slice, slice, int, int, cell), int) $UsernameNFTItem$_fun_tier((int, slice, slice, int, int, cell) $self) impure inline_ref;

;; $UsernameNFTItem$_fun_snakeNameCell
((int, slice, slice, int, int, cell), cell) $UsernameNFTItem$_fun_snakeNameCell((int, slice, slice, int, int, cell) $self) impure inline_ref;

;; $UsernameNFTItem$_fun_snakeDescriptionCell
((int, slice, slice, int, int, cell), cell) $UsernameNFTItem$_fun_snakeDescriptionCell((int, slice, slice, int, int, cell) $self) impure inline_ref;

;; $UsernameNftOnchainContent$_constructor_marker_metadata
((int, cell)) $UsernameNftOnchainContent$_constructor_marker_metadata(int $marker, cell $metadata) inline;

;; $UsernameNFTItem$_fun_individualContent
((int, slice, slice, int, int, cell), cell) $UsernameNFTItem$_fun_individualContent((int, slice, slice, int, int, cell) $self) impure inline_ref;

;; $UsernameNFTItem$_fun_normalizedForwardPayload
((int, slice, slice, int, int, cell), slice) $UsernameNFTItem$_fun_normalizedForwardPayload((int, slice, slice, int, int, cell) $self, slice $payload) impure inline_ref;

;; $MessageParameters$_constructor_to_value_bounce_mode_body
((int, cell, int, slice, int)) $MessageParameters$_constructor_to_value_bounce_mode_body(slice $to, int $value, int $bounce, int $mode, cell $body) inline;

;; $UsernameItemDeployedAck$_constructor_name_hash_owner_wallet
((int, slice)) $UsernameItemDeployedAck$_constructor_name_hash_owner_wallet(int $name_hash, slice $owner_wallet) inline;

;; $UsernameNFTItem$_fun_sendDeployedAck
((int, slice, slice, int, int, cell), ()) $UsernameNFTItem$_fun_sendDeployedAck((int, slice, slice, int, int, cell) $self) impure inline_ref;

;; $UsernameNFTItemStateView$_constructor_initialized_owner_wallet_username_registry_address_name_hash_username_len_username_tier
((int, slice, slice, int, int, cell, int)) $UsernameNFTItemStateView$_constructor_initialized_owner_wallet_username_registry_address_name_hash_username_len_username_tier(int $initialized, slice $owner_wallet, slice $username_registry_address, int $name_hash, int $username_len, cell $username, int $tier) inline;

;; $UsernameNFTItem$_fun_get_state
((int, slice, slice, int, int, cell), (int, slice, slice, int, int, cell, int)) $UsernameNFTItem$_fun_get_state((int, slice, slice, int, int, cell) $self) impure inline_ref;

;; $UsernameNftDataView$_constructor_initialized_index_collection_address_owner_address_individual_content
((int, int, slice, slice, cell)) $UsernameNftDataView$_constructor_initialized_index_collection_address_owner_address_individual_content(int $initialized, int $index, slice $collection_address, slice $owner_address, cell $individual_content) inline;

;; $UsernameNFTItem$_fun_get_nft_data
((int, slice, slice, int, int, cell), (int, int, slice, slice, cell)) $UsernameNFTItem$_fun_get_nft_data((int, slice, slice, int, int, cell) $self) impure inline_ref;

;; $MessageParameters$_constructor_to_value_mode_bounce_body
((int, cell, int, slice, int)) $MessageParameters$_constructor_to_value_mode_bounce_body(slice $to, int $value, int $mode, int $bounce, cell $body) inline;

;; $UsernameOwnershipProof$_constructor_query_id_name_hash_owner_wallet_username_len_username
((int, int, slice, int, slice)) $UsernameOwnershipProof$_constructor_query_id_name_hash_owner_wallet_username_len_username(int $query_id, int $name_hash, slice $owner_wallet, int $username_len, slice $username) inline;

;; $NftOwnershipAssigned$_constructor_query_id_previous_owner_forward_payload
((int, slice, slice)) $NftOwnershipAssigned$_constructor_query_id_previous_owner_forward_payload(int $query_id, slice $previous_owner, slice $forward_payload) inline;

;; $NftExcesses$_constructor_query_id
((int)) $NftExcesses$_constructor_query_id(int $query_id) inline;


;; UsernameRegistry_UsernameNFTItem.stdlib.fc
global (int, slice, int, slice) __tact_context;
global slice __tact_context_sender;
global cell __tact_child_contract_codes;
global int __tact_randomized;

(int, slice, int, slice) __tact_context_get() inline {
    return __tact_context;
}

slice __tact_context_get_sender() inline {
    return __tact_context_sender;
}

tuple __tact_string_builder_start(builder b) inline {
    return tpush(tpush(empty_tuple(), b), null());
}

tuple __tact_string_builder_start_tail_string() inline {
    return __tact_string_builder_start(begin_cell().store_uint(0, 8));
}

cell __tact_string_builder_end(tuple builders) inline {
    (builder b, tuple tail) = uncons(builders);
    cell c = b.end_cell();
    while(~ null?(tail)) {
        (b, tail) = uncons(tail);
        c = b.store_ref(c).end_cell();
    }
    return c;
}

((tuple), ()) __tact_string_builder_append(tuple builders, slice sc) inline_ref {
    int sliceRefs = slice_refs(sc);
    int sliceBits = slice_bits(sc);

    while((sliceBits > 0) | (sliceRefs > 0)) {

        ;; Load the current builder
        (builder b, tuple tail) = uncons(builders);
        int remBytes = 127 - (builder_bits(b) / 8);
        int exBytes = sliceBits / 8;

        ;; Append bits
        int amount = min(remBytes, exBytes);
        if (amount > 0) {
            slice read = sc~load_bits(amount * 8);
            b = b.store_slice(read);
        }

        ;; Update builders
        builders = cons(b, tail);

        ;; Check if we need to add a new cell and continue
        if (exBytes - amount > 0) {
            var bb = begin_cell();
            builders = cons(bb, builders);
            sliceBits = (exBytes - amount) * 8;
        } elseif (sliceRefs > 0) {
            sc = sc~load_ref().begin_parse();
            sliceRefs = slice_refs(sc);
            sliceBits = slice_bits(sc);
        } else {
            sliceBits = 0;
            sliceRefs = 0;
        }
    }

    return ((builders), ());
}

(cell, ()) __tact_dict_set_uint_cell(cell d, int kl, int k, cell v) inline {
    if (null?(v)) {
        var (r, ok) = udict_delete?(d, kl, k);
        return (r, ());
    } else {
        return (udict_set_ref(d, kl, k, v), ());
    }
}

cell $Builder$_fun_endCell(builder $self) impure asm """
    ENDC
""";

builder $global_beginCell() impure asm """
    NEWC
""";

int $global_max(int $x, int $y) impure asm """
    MAX
""";

() $global_message((int, cell, int, slice, int) $params) impure asm """
    NEWC
    b{01} STSLICECONST  // store tag = $0 and ihr_disabled = true
    1 STI               // store `bounce`
    b{000} STSLICECONST // store bounced = false and src = addr_none
    STSLICE             // store `to`
    SWAP
    STGRAMS             // store `value`
    106 PUSHINT         // 1 + 4 + 4 + 64 + 32 + 1
    STZEROES
    // → Stack state
    // s0: Builder
    // s1: `body`
    // s2: `mode`
    STDICT
    ENDC
    SWAP
    SENDRAWMSG
""";

(int, int) $global_parseStdAddress(slice $slice) impure asm """
    REWRITESTDADDR
""";

builder $Builder$_fun_storeBit(int $value, builder $self) impure asm """
    1 STI
""";

builder $Builder$_fun_storeSlice(builder $self, slice $slice) impure asm """
    STSLICER
""";

(slice, cell) $Slice$_fun_loadRef(slice $self) impure asm( -> 1 0) """
    LDREF
""";

(slice, int) $Slice$_fun_loadBool(slice $self) impure asm( -> 1 0) """
    1 LDI
""";

(slice, int) $Slice$_fun_loadCoins(slice $self) impure asm( -> 1 0) """
    LDVARUINT16
""";

(slice, ()) $Slice$_fun_skipBits(slice $self, int $l) impure asm """
    SDSKIPFIRST
""";

int $Slice$_fun_bits(slice $self) impure asm """
    SBITS
""";

int $Slice$_fun_empty(slice $self) impure asm """
    SEMPTY
""";

int $Slice$_fun_refsEmpty(slice $self) impure asm """
    SREMPTY
""";

(slice, slice) $Slice$_fun_loadAddress(slice $self) impure asm( -> 1 0) """
    LDMSGADDR
""";

slice $Cell$_fun_beginParse(cell $self) impure asm """
    CTOS
""";

slice $Cell$_fun_asSlice(cell $self) impure inline {
    var ($self) = $self;
    return $Cell$_fun_beginParse($self);
}

int $Cell$_fun_hash(cell $self) impure asm """
    HASHCU
""";

slice $Address$_fun_asSlice(slice $self) impure asm "NOP";

int $Context$_fun_readForwardFee((int, slice, int, slice) $self) impure asm """
    LDMSGADDR   // load dest:MsgAddressInt
    LDGRAMS     // load value:CurrencyCollection
    ONE
    SDSKIPFIRST // skip extra currency collection
    LDGRAMS     // load ihr_fee
    LDGRAMS     // load fwd_fee, we'll be using this!
    DROP        // drop remaining Slice (with created_lt and created_at)

    // There are 7 entries on the stack — first 3 fields of Context plus 4 loaded ones.
    // The topmost is fwd_fee, which is the only one we're after, so let's drop 6 entries below:
    6 1 BLKDROP2 // drop the loaded values as well as the first 3 fields of Context

    ZERO              // not masterchain
    GETORIGINALFWDFEE // floor(fwd_fee * 2^16 / (2^16 - first_frac)), where
                      // first_frac is a value listed in config param 25
                      // of the blockchain: https://tonviewer.com/config#25
                      // this instruction effectively multiplies the fwd_fee by 1.5,
                      // at least for the current value of first_frac, which is 21845
""";

;; UsernameRegistry_UsernameNFTItem.constants.fc
;; String "Private correspondence is a right, not a privilege. This is a username NFT for Platho — the anonymous, post-quantum-encrypted, uncensorable messenger. A permanent, transferable, fully on-chain handle: the name others reach you by, held by no one but you. platho.app"
slice __gen_slice_string_772226ccf1356301686fba4dcc4c81332c0c7470c4e5732318ac44d730bce72a() asm """
    B{} B>boc <s PUSHSLICE
""";

;; UsernameRegistry_UsernameNFTItem.storage.fc
;;
;; Type: Context
;; TLB: _ bounceable:bool sender:address value:int257 raw:^slice = Context
;;

_ $Context$_get_value((int, slice, int, slice) v) inline {
    var (v'bounceable, v'sender, v'value, v'raw) = v;
    return v'value;
}

;;
;; Type: MessageParameters
;; TLB: _ mode:int257 body:Maybe ^cell value:int257 to:address bounce:bool = MessageParameters
;;

((int, cell, int, slice, int)) $MessageParameters$_constructor_to_value_bounce_mode_body(slice $to, int $value, int $bounce, int $mode, cell $body) inline {
    return ($mode, $body, $value, $to, $bounce);
}

((int, cell, int, slice, int)) $MessageParameters$_constructor_to_value_mode_bounce_body(slice $to, int $value, int $mode, int $bounce, cell $body) inline {
    return ($mode, $body, $value, $to, $bounce);
}

;;
;; Type: UsernameOwnershipProof
;; Header: 0x554e504f
;; TLB: username_ownership_proof#554e504f query_id:uint64 name_hash:uint256 owner_wallet:address username_len:uint8 username:remainder<slice> = UsernameOwnershipProof
;;

builder $UsernameOwnershipProof$_store(builder build_0, (int, int, slice, int, slice) v) inline {
    var (v'query_id, v'name_hash, v'owner_wallet, v'username_len, v'username) = v;
    build_0 = store_uint(build_0, 1431195727, 32);
    build_0 = build_0.store_uint(v'query_id, 64);
    build_0 = build_0.store_uint(v'name_hash, 256);
    build_0 = build_0.store_slice(v'owner_wallet);
    build_0 = build_0.store_uint(v'username_len, 8);
    build_0 = build_0.store_slice(v'username);
    return build_0;
}

cell $UsernameOwnershipProof$_store_cell((int, int, slice, int, slice) v, builder b) inline {
    return $UsernameOwnershipProof$_store(b, v).end_cell();
}

((int, int, slice, int, slice)) $UsernameOwnershipProof$_constructor_query_id_name_hash_owner_wallet_username_len_username(int $query_id, int $name_hash, slice $owner_wallet, int $username_len, slice $username) inline {
    return ($query_id, $name_hash, $owner_wallet, $username_len, $username);
}

;;
;; Type: UsernameItemDeployedAck
;; Header: 0xbba3ec19
;; TLB: username_item_deployed_ack#bba3ec19 name_hash:uint256 owner_wallet:address = UsernameItemDeployedAck
;;

builder $UsernameItemDeployedAck$_store(builder build_0, (int, slice) v) inline {
    var (v'name_hash, v'owner_wallet) = v;
    build_0 = store_uint(build_0, 3148082201, 32);
    build_0 = build_0.store_uint(v'name_hash, 256);
    build_0 = build_0.store_slice(v'owner_wallet);
    return build_0;
}

cell $UsernameItemDeployedAck$_store_cell((int, slice) v, builder b) inline {
    return $UsernameItemDeployedAck$_store(b, v).end_cell();
}

((int, slice)) $UsernameItemDeployedAck$_constructor_name_hash_owner_wallet(int $name_hash, slice $owner_wallet) inline {
    return ($name_hash, $owner_wallet);
}

;;
;; Type: NftOwnershipAssigned
;; Header: 0x05138d91
;; TLB: nft_ownership_assigned#05138d91 query_id:uint64 previous_owner:address forward_payload:remainder<slice> = NftOwnershipAssigned
;;

builder $NftOwnershipAssigned$_store(builder build_0, (int, slice, slice) v) inline {
    var (v'query_id, v'previous_owner, v'forward_payload) = v;
    build_0 = store_uint(build_0, 85167505, 32);
    build_0 = build_0.store_uint(v'query_id, 64);
    build_0 = build_0.store_slice(v'previous_owner);
    build_0 = build_0.store_slice(v'forward_payload);
    return build_0;
}

cell $NftOwnershipAssigned$_store_cell((int, slice, slice) v, builder b) inline {
    return $NftOwnershipAssigned$_store(b, v).end_cell();
}

((int, slice, slice)) $NftOwnershipAssigned$_constructor_query_id_previous_owner_forward_payload(int $query_id, slice $previous_owner, slice $forward_payload) inline {
    return ($query_id, $previous_owner, $forward_payload);
}

;;
;; Type: NftExcesses
;; Header: 0xd53276db
;; TLB: nft_excesses#d53276db query_id:uint64 = NftExcesses
;;

builder $NftExcesses$_store(builder build_0, (int) v) inline {
    var (v'query_id) = v;
    build_0 = store_uint(build_0, 3576854235, 32);
    build_0 = build_0.store_uint(v'query_id, 64);
    return build_0;
}

cell $NftExcesses$_store_cell((int) v, builder b) inline {
    return $NftExcesses$_store(b, v).end_cell();
}

((int)) $NftExcesses$_constructor_query_id(int $query_id) inline {
    return ($query_id);
}

;;
;; Type: UsernameNFTItemStateView
;; TLB: _ initialized:bool owner_wallet:address username_registry_address:address name_hash:int257 username_len:int257 username:^cell tier:int257 = UsernameNFTItemStateView
;;

(int, slice, slice, int, int, cell, int) $UsernameNFTItemStateView$_to_external(((int, slice, slice, int, int, cell, int)) v) inline {
    var (v'initialized, v'owner_wallet, v'username_registry_address, v'name_hash, v'username_len, v'username, v'tier) = v; 
    return (v'initialized, v'owner_wallet, v'username_registry_address, v'name_hash, v'username_len, v'username, v'tier);
}

((int, slice, slice, int, int, cell, int)) $UsernameNFTItemStateView$_constructor_initialized_owner_wallet_username_registry_address_name_hash_username_len_username_tier(int $initialized, slice $owner_wallet, slice $username_registry_address, int $name_hash, int $username_len, cell $username, int $tier) inline {
    return ($initialized, $owner_wallet, $username_registry_address, $name_hash, $username_len, $username, $tier);
}

;;
;; Type: UsernameNftDataView
;; TLB: _ initialized:bool index:int257 collection_address:address owner_address:address individual_content:^cell = UsernameNftDataView
;;

(int, int, slice, slice, cell) $UsernameNftDataView$_to_external(((int, int, slice, slice, cell)) v) inline {
    var (v'initialized, v'index, v'collection_address, v'owner_address, v'individual_content) = v; 
    return (v'initialized, v'index, v'collection_address, v'owner_address, v'individual_content);
}

((int, int, slice, slice, cell)) $UsernameNftDataView$_constructor_initialized_index_collection_address_owner_address_individual_content(int $initialized, int $index, slice $collection_address, slice $owner_address, cell $individual_content) inline {
    return ($initialized, $index, $collection_address, $owner_address, $individual_content);
}

;;
;; Type: UsernameNftOnchainContent
;; TLB: _ marker:uint8 metadata:dict<uint256, ^cell> = UsernameNftOnchainContent
;;

builder $UsernameNftOnchainContent$_store(builder build_0, (int, cell) v) inline {
    var (v'marker, v'metadata) = v;
    build_0 = build_0.store_uint(v'marker, 8);
    build_0 = build_0.store_dict(v'metadata);
    return build_0;
}

cell $UsernameNftOnchainContent$_store_cell((int, cell) v, builder b) inline {
    return $UsernameNftOnchainContent$_store(b, v).end_cell();
}

((int, cell)) $UsernameNftOnchainContent$_constructor_marker_metadata(int $marker, cell $metadata) inline {
    return ($marker, $metadata);
}

;;
;; Type: UsernameNFTItem
;; TLB: _ initialized:bool owner_wallet:address username_registry_address:address name_hash:uint256 username_len:uint8 username:^cell = UsernameNFTItem
;;

builder $UsernameNFTItem$_store(builder build_0, (int, slice, slice, int, int, cell) v) inline {
    var (v'initialized, v'owner_wallet, v'username_registry_address, v'name_hash, v'username_len, v'username) = v;
    build_0 = build_0.store_int(v'initialized, 1);
    build_0 = build_0.store_slice(v'owner_wallet);
    build_0 = build_0.store_slice(v'username_registry_address);
    build_0 = build_0.store_uint(v'name_hash, 256);
    build_0 = build_0.store_uint(v'username_len, 8);
    build_0 = build_0.store_ref(v'username);
    return build_0;
}

(slice, ((int, slice, slice, int, int, cell))) $UsernameNFTItem$_load(slice sc_0) inline {
    var v'initialized = sc_0~load_int(1);
    var v'owner_wallet = sc_0~load_msg_addr();
    var v'username_registry_address = sc_0~load_msg_addr();
    var v'name_hash = sc_0~load_uint(256);
    var v'username_len = sc_0~load_uint(8);
    var v'username = sc_0~load_ref();
    return (sc_0, (v'initialized, v'owner_wallet, v'username_registry_address, v'name_hash, v'username_len, v'username));
}

(slice, ((slice, int))) $UsernameNFTItem$init$_load(slice sc_0) inline {
    var v'username_registry_address = sc_0~load_msg_addr();
    var v'name_hash = sc_0~load_int(257);
    return (sc_0, (v'username_registry_address, v'name_hash));
}

(int, slice, slice, int, int, cell) $UsernameNFTItem$_contract_load() impure inline {
    slice $sc = get_data().begin_parse();
    int $loaded = $sc~load_int(1);
    if ($loaded) {
        return $sc~$UsernameNFTItem$_load();
    }
    else {
        (slice $username_registry_address, int $name_hash) = $sc~$UsernameNFTItem$init$_load();
        $sc.end_parse();
        return $UsernameNFTItem$_contract_init($username_registry_address, $name_hash);
    }
}

() $UsernameNFTItem$_contract_store((int, slice, slice, int, int, cell) v) impure inline {
    builder b = begin_cell();
    b = b.store_int(true, 1);
    b = $UsernameNFTItem$_store(b, v);
    set_data(b.end_cell());
}

;;
;; Contract UsernameNFTItem functions
;;

(int, slice, slice, int, int, cell) $UsernameNFTItem$_contract_init(slice $username_registry_address, int $name_hash) impure inline {
    var (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)) = (null(), null(), null(), null(), null(), null());
    throw_unless(18001, ($name_hash != 0));
    $self'initialized = false;
    $self'owner_wallet = $username_registry_address;
    $self'username_registry_address = $username_registry_address;
    $self'name_hash = $name_hash;
    $self'username_len = 0;
    $self'username = $Builder$_fun_endCell($global_beginCell());
    return ($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username);
}

((int, slice, slice, int, int, cell), int) $UsernameNFTItem$_fun_isBasechainAddress((int, slice, slice, int, int, cell) $self, slice $address) impure inline_ref {
    var (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)) = $self;
    var ($std'workchain, $std'address) = $global_parseStdAddress($Address$_fun_asSlice($address));
    var $fresh$ret_0 = ($std'workchain == 0);
    return (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username), $fresh$ret_0);
}

((int, slice, slice, int, int, cell), int) $UsernameNFTItem$_fun_isValidNameLength((int, slice, slice, int, int, cell) $self, int $name_len) impure inline_ref {
    var (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)) = $self;
    var $fresh$ret_1 = ( (($name_len >= 4)) ? (($name_len <= 16)) : (false) );
    return (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username), $fresh$ret_1);
}

((int, slice, slice, int, int, cell), int) $UsernameNFTItem$_fun_isValidUsernameBytes((int, slice, slice, int, int, cell) $self, int $name_len, slice $username) impure inline_ref {
    var (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)) = $self;
    ifnot (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)~$UsernameNFTItem$_fun_isValidNameLength($name_len)) {
        var $fresh$ret_2 = false;
        return (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username), $fresh$ret_2);
    }
    if (($Slice$_fun_bits($username) != ($name_len * 8))) {
        var $fresh$ret_3 = false;
        return (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username), $fresh$ret_3);
    }
    ifnot ($Slice$_fun_refsEmpty($username)) {
        var $fresh$ret_4 = false;
        return (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username), $fresh$ret_4);
    }
    slice $cursor = $username;
    int $i = 0;
    while (($i < $name_len)) {
        int $ch = $cursor~load_uint(8);
        int $lower = ( (($ch >= 97)) ? (($ch <= 122)) : (false) );
        int $digit = ( (($ch >= 48)) ? (($ch <= 57)) : (false) );
        int $separator = ( (($ch == 45)) ? (true) : (($ch == 95)) );
        ifnot (( (( ($lower) ? (true) : ($digit) )) ? (true) : ($separator) )) {
            var $fresh$ret_5 = false;
            return (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username), $fresh$ret_5);
        }
        $i = $i + 1;
    }
    var $fresh$ret_6 = $Slice$_fun_empty($cursor);
    return (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username), $fresh$ret_6);
}

((int, slice, slice, int, int, cell), int) $UsernameNFTItem$_fun_computeNameHash((int, slice, slice, int, int, cell) $self, slice $username) impure inline_ref {
    var (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)) = $self;
    var $fresh$ret_7 = $Cell$_fun_hash($Builder$_fun_endCell($Builder$_fun_storeSlice(store_uint($global_beginCell(), 3318512854, 32), $username)));
    return (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username), $fresh$ret_7);
}

((int, slice, slice, int, int, cell), int) $UsernameNFTItem$_fun_tier((int, slice, slice, int, int, cell) $self) impure inline_ref {
    var (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)) = $self;
    if (($self'username_len == 4)) {
        var $fresh$ret_8 = 1;
        return (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username), $fresh$ret_8);
    }
    if (($self'username_len == 5)) {
        var $fresh$ret_9 = 2;
        return (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username), $fresh$ret_9);
    }
    var $fresh$ret_10 = 3;
    return (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username), $fresh$ret_10);
}

((int, slice, slice, int, int, cell), cell) $UsernameNFTItem$_fun_snakeNameCell((int, slice, slice, int, int, cell) $self) impure inline_ref {
    var (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)) = $self;
    var $fresh$ret_11 = $Builder$_fun_endCell(store_uint($Builder$_fun_storeSlice(store_uint($global_beginCell(), 0, 8), $Cell$_fun_beginParse($self'username)), 778138728, 32));
    return (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username), $fresh$ret_11);
}

((int, slice, slice, int, int, cell), cell) $UsernameNFTItem$_fun_snakeDescriptionCell((int, slice, slice, int, int, cell) $self) impure inline_ref {
    var (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)) = $self;
    tuple $b = __tact_string_builder_start_tail_string();
    $b~__tact_string_builder_append(__gen_slice_string_772226ccf1356301686fba4dcc4c81332c0c7470c4e5732318ac44d730bce72a());
    var $fresh$ret_12 = __tact_string_builder_end($b);
    return (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username), $fresh$ret_12);
}

((int, slice, slice, int, int, cell), cell) $UsernameNFTItem$_fun_individualContent((int, slice, slice, int, int, cell) $self) impure inline_ref {
    var (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)) = $self;
    cell $metadata = null();
    $metadata~__tact_dict_set_uint_cell(256, 59089242681608890680090686026688704441792375738894456860693970539822503415433, ($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)~$UsernameNFTItem$_fun_snakeNameCell());
    $metadata~__tact_dict_set_uint_cell(256, 90922719342317012409671596374183159143637506542604000676488204638996496437508, ($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)~$UsernameNFTItem$_fun_snakeDescriptionCell());
    var $fresh$ret_13 = $UsernameNftOnchainContent$_store_cell($UsernameNftOnchainContent$_constructor_marker_metadata(0, $metadata), begin_cell());
    return (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username), $fresh$ret_13);
}

((int, slice, slice, int, int, cell), slice) $UsernameNFTItem$_fun_normalizedForwardPayload((int, slice, slice, int, int, cell) $self, slice $payload) impure inline_ref {
    var (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)) = $self;
    if ($Slice$_fun_empty($payload)) {
        var $fresh$ret_14 = $Cell$_fun_beginParse($Builder$_fun_endCell($Builder$_fun_storeBit(false, $global_beginCell())));
        return (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username), $fresh$ret_14);
    }
    var $fresh$ret_15 = $payload;
    return (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username), $fresh$ret_15);
}

((int, slice, slice, int, int, cell), ()) $UsernameNFTItem$_fun_sendDeployedAck((int, slice, slice, int, int, cell) $self) impure inline_ref {
    var (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)) = $self;
    $global_message($MessageParameters$_constructor_to_value_bounce_mode_body($self'username_registry_address, 3000000, true, 1, $UsernameItemDeployedAck$_store_cell($UsernameItemDeployedAck$_constructor_name_hash_owner_wallet($self'name_hash, $self'owner_wallet), begin_cell())));
    return (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username), ());
}

((int, slice, slice, int, int, cell), (int, slice, slice, int, int, cell, int)) $UsernameNFTItem$_fun_get_state((int, slice, slice, int, int, cell) $self) impure inline_ref {
    var (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)) = $self;
    var $fresh$ret_16 = $UsernameNFTItemStateView$_constructor_initialized_owner_wallet_username_registry_address_name_hash_username_len_username_tier($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username, ($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)~$UsernameNFTItem$_fun_tier());
    return (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username), $fresh$ret_16);
}

((int, slice, slice, int, int, cell), (int, int, slice, slice, cell)) $UsernameNFTItem$_fun_get_nft_data((int, slice, slice, int, int, cell) $self) impure inline_ref {
    var (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)) = $self;
    var $fresh$ret_17 = $UsernameNftDataView$_constructor_initialized_index_collection_address_owner_address_individual_content($self'initialized, $self'name_hash, $self'username_registry_address, $self'owner_wallet, ($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)~$UsernameNFTItem$_fun_individualContent());
    return (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username), $fresh$ret_17);
}

;;
;; Get methods of a Contract UsernameNFTItem
;;

_ %get_state() method_id(86957) {
    var self = $UsernameNFTItem$_contract_load();
    var res = self~$UsernameNFTItem$_fun_get_state();
    return $UsernameNFTItemStateView$_to_external(res);
}

_ %get_nft_data() method_id(102351) {
    var self = $UsernameNFTItem$_contract_load();
    var res = self~$UsernameNFTItem$_fun_get_nft_data();
    return $UsernameNftDataView$_to_external(res);
}

;;
;; Routing of a Contract UsernameNFTItem
;;

;; message opcode reader utility: only binary receivers
;; Returns 32 bit message opcode, otherwise throws the "Invalid incoming message" exit code
(slice, int) ~load_opcode_internal(slice s) asm( -> 1 0) "32 LDUQ 130 THROWIFNOT";

() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    
    ;; Context
    var cs = in_msg_cell.begin_parse();
    cs~skip_bits(2);
    var msg_bounceable = cs~load_int(1);
    var msg_bounced = cs~load_int(1);
    slice msg_sender_addr = cs~load_msg_addr();
    __tact_context = (msg_bounceable, msg_sender_addr, msg_value, cs);
    __tact_context_sender = msg_sender_addr;
    
    ;; Load contract data
    var ($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username) = $UsernameNFTItem$_contract_load();
    
    ;; Handle bounced messages
    if (msg_bounced) { return (); }
    int op = 0;
    int in_msg_length = slice_bits(in_msg);
    if (in_msg_length >= 32) {
        op = in_msg~load_uint(32);
    }
    ;; Receive InitializeUsernameItem message
    if (op == 0x554e494e) {
        var $msg'owner_wallet = in_msg~load_msg_addr();
        var $msg'username_len = in_msg~load_uint(8);
        var $msg'username = in_msg;
        throw_unless(18010, ( equal_slices_bits(__tact_context_get_sender(), $self'username_registry_address) ));
        throw_unless(18011, (~ $self'initialized));
        throw_unless(18012, ($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)~$UsernameNFTItem$_fun_isBasechainAddress($msg'owner_wallet));
        throw_unless(18013, ($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)~$UsernameNFTItem$_fun_isValidUsernameBytes($msg'username_len, $msg'username));
        throw_unless(18014, (($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)~$UsernameNFTItem$_fun_computeNameHash($msg'username) == $self'name_hash));
        throw_unless(18015, ($Context$_get_value(__tact_context_get()) >= 4000000));
        $self'initialized = true;
        $self'owner_wallet = $msg'owner_wallet;
        $self'username_len = $msg'username_len;
        $self'username = $Builder$_fun_endCell($Builder$_fun_storeSlice($global_beginCell(), $msg'username));
        ($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)~$UsernameNFTItem$_fun_sendDeployedAck();
        $UsernameNFTItem$_contract_store(($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username));
        return ();
    }
    
    ;; Receive ResendDeployedAck message
    if (op == 0x639cfc6c) {
        var $msg = empty_tuple();
        throw_unless(18020, $self'initialized);
        throw_unless(18021, ($Context$_get_value(__tact_context_get()) >= 4000000));
        throw_unless(18022, ($Context$_get_value(__tact_context_get()) <= 20000000));
        ($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)~$UsernameNFTItem$_fun_sendDeployedAck();
        $UsernameNFTItem$_contract_store(($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username));
        return ();
    }
    
    ;; Receive ProveUsernameOwnership message
    if (op == 0x554e5052) {
        var $msg'query_id = in_msg~load_uint(64);
        var $msg'to = in_msg~load_msg_addr();
        throw_unless(18040, $self'initialized);
        throw_unless(18041, ($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)~$UsernameNFTItem$_fun_isBasechainAddress($msg'to));
        throw_unless(18042, ($Context$_get_value(__tact_context_get()) >= 6000000));
        throw_unless(18043, ($Context$_get_value(__tact_context_get()) <= 20000000));
        $global_message($MessageParameters$_constructor_to_value_mode_bounce_body($msg'to, 5000000, 1, true, $UsernameOwnershipProof$_store_cell($UsernameOwnershipProof$_constructor_query_id_name_hash_owner_wallet_username_len_username($msg'query_id, $self'name_hash, $self'owner_wallet, $self'username_len, $Cell$_fun_asSlice($self'username)), begin_cell())));
        $UsernameNFTItem$_contract_store(($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username));
        return ();
    }
    
    ;; Receive NftTransfer message
    if (op == 0x5fcc3d14) {
        var $msg'query_id = in_msg~load_uint(64);
        var $msg'payload = in_msg;
        throw_unless(18030, $self'initialized);
        throw_unless(18031, ( equal_slices_bits(__tact_context_get_sender(), $self'owner_wallet) ));
        slice $body = $msg'payload;
        slice $newOwner = $body~$Slice$_fun_loadAddress();
        throw_unless(18032, ($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)~$UsernameNFTItem$_fun_isBasechainAddress($newOwner));
        int $hasResponseDestination = (preload_uint($body, 2) != 0);
        slice $responseDestination = null();
        if ($hasResponseDestination) {
            $responseDestination = $body~$Slice$_fun_loadAddress();
            throw_unless(18033, ($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)~$UsernameNFTItem$_fun_isBasechainAddress($responseDestination));
        } else {
            $body~$Slice$_fun_skipBits(2);
        }
        if ($body~$Slice$_fun_loadBool()) {
            $body~$Slice$_fun_loadRef();
        }
        int $forwardAmount = $body~$Slice$_fun_loadCoins();
        int $fwdFeeAllowance = $global_max(10000000, ($Context$_fun_readForwardFee(__tact_context_get()) * 2));
        int $restAmount = ((($Context$_get_value(__tact_context_get()) - 2000000) - $forwardAmount) - $fwdFeeAllowance);
        throw_unless(18035, ($restAmount >= 0));
        slice $previousOwner = $self'owner_wallet;
        $self'owner_wallet = $newOwner;
        if (($forwardAmount > 0)) {
            $global_message($MessageParameters$_constructor_to_value_mode_bounce_body($newOwner, $forwardAmount, 1, false, $NftOwnershipAssigned$_store_cell($NftOwnershipAssigned$_constructor_query_id_previous_owner_forward_payload($msg'query_id, $previousOwner, ($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username)~$UsernameNFTItem$_fun_normalizedForwardPayload($body)), begin_cell())));
        }
        if (( ($hasResponseDestination) ? (($restAmount >= 100000)) : (false) )) {
            $global_message($MessageParameters$_constructor_to_value_mode_bounce_body($responseDestination, $restAmount, 1, false, $NftExcesses$_store_cell($NftExcesses$_constructor_query_id($msg'query_id), begin_cell())));
        }
        $UsernameNFTItem$_contract_store(($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username));
        return ();
    }
    
    ;; Receive TopUpStorageReserve message
    if (op == 0x27acdf8b) {
        var $msg = empty_tuple();
        $UsernameNFTItem$_contract_store(($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username));
        return ();
    }
    
    ;; Receive empty message
    if ((op == 0) & (in_msg_length <= 32)) {
        throw(18099);
        $UsernameNFTItem$_contract_store(($self'initialized, $self'owner_wallet, $self'username_registry_address, $self'name_hash, $self'username_len, $self'username));
        return ();
    }
    ;; Throw if not handled
    throw(130);
}

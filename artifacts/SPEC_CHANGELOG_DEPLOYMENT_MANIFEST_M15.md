# Spec changelog — M15 Deployment Manifest Implemented-Subset Profile

Accepted a canonical cell-hash manifest profile for the currently implemented Platho v1 subset.

This is deliberately not final genesis. It carries explicit blockers for BuybackBurn / STON.fi route values and official tooling must reject it as final genesis until blockers are empty.

No contract behavior changed in M15.

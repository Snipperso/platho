# PLATHO clean-17 — БРИФ ДЛЯ ВНЕШНЕГО КРИПТО-АУДИТА

Назначение: точная спецификация того, ЧТО обязан проверить внешний криптограф ДО immutable seal.

> **ОБНОВЛЕНО 2026-07-18:** свойств было ДВА; свойство 1 (unlinkability + ROS слепой выдачи) СНЯТО вместе с удалением
> всего слоя слепой оплаты. Остаётся ОДИН seal-блокер — свойство 2 (ANO-CCA/key-privacy ML-KEM-конверта), и он
> несущий: от него зависит деанон ПОЛУЧАТЕЛЯ, то есть ровно та угроза, ради которой затеян редизайн.

> ⚠️ Прозаические части — по-русски для владельца (approve объём). **Перед передачей аудиторам перевести на
> английский.** Крипто-конструкции даны в универсальной нотации — перевода не требуют.

---

## ~~Свойство 1 — UNLINKABILITY + ROS слепой выдачи~~ — СНЯТО 2026-07-18, предмета аудита БОЛЬШЕ НЕТ

Слой слепой выдачи удалён целиком (коммит `aaa9a1aa`): вместе с ним ушли `NullifierShard`, `CreditSale`,
`web/crypto/blind-issuance.mjs`, CAC-корни и ежегодная церемония.

**Причина — архитектурная, а не крипто.** Владелец: «вся внешняя инфраструктура запрещена в проекте с первого дня».
Слепая выдача требует онлайнового эмитента, и — ключевое — **окупается только в паре с реле**: наблюдатель цепи
читает `src` транзакции траты независимо от того, насколько слепо подписан жетон, так что слепота защищала лишь от
самого эмитента. Без эмитента защищать не от кого. Публикация теперь **оплачивается напрямую**: право записи в
CONV-бакет — это знание `bucket_key = HKDF(K_epoch‖dir‖epoch)`, выводимого только двумя участниками беседы;
анти-спам — сама рента.

**Что это значит для аудита:** из двух seal-блокирующих свойств остаётся ОДНО — свойство 2 ниже. Утечка графа
«кто-кому», ради которой затевался редизайн, закрывается непрозрачным `bucket_key`, то есть чистой клиентской
криптой без единого сервиса. Принятый остаток, который надо назвать вслух: `src` транзакции публикации виден, то
есть виден ОТПРАВИТЕЛЬ конкретного бакета (но не его собеседник — направления беседы выводятся в разные,
несвязываемые бакеты).

## Свойство 2 — ANO-CCA / KEY-PRIVACY гибридного KEM-конверта тела (K_root)

### Конструкция (из закрытого спека §9)
Установление K_root при первом контакте (INTRO), генуинная ML-KEM:
```
DH_ss   = X25519(A.enc_sec, B.enc_pub)
mlkem_ss = ML-KEM-768.decaps(ct_root)   // ct_root 1088Б, СВЕЖАЯ encapsulate(B.mlKem768_pub), отдельная секция тела
K_root  = HKDF(DH_ss ‖ mlkem_ss, salt='PLATHO.CONV.ROOT.SALT.V1', info='PLATHO.CONV.ROOT.V1' ‖ lo_keyId ‖ hi_keyId)
K_epoch = HKDF(K_root, salt='PLATHO.CONV.RATCHET.SALT.V1', info='PLATHO.CONV.RATCHET.V1' ‖ epoch)   // прямая по-эпоховая
bucketKey = HKDF(K_epoch, ... 'PLATHO.CONV.BUCKET.V1' ‖ dir ‖ epoch)[:32]
```
Реализация ML-KEM — **noble-ml-kem** (нужно подтвердить версию/параметры в `web/crypto/platho-crypto.mjs`).

**НЕ ДОКАЗАНО — что требуется от аудитора:**
1. **KEY-PRIVACY (ANO-CCA) конверта тела.** `body_KEM_ct` передаётся В КЛИРТЕКСТЕ на цепи (в теле INTRO/CONV).
   Opacity bucketKey НЕОБХОДИМА, но НЕ ДОСТАТОЧНА: если KEM-ct раскрывает, к КАКОМУ публичному ключу он
   инкапсулирован (получателя), — это утечка получателя, обходящая всю stealth/bucket-приватность. **noble-ML-KEM
   НЕ ЗАЯВЛЯЕТ key-privacy.** Проверить: обладает ли ML-KEM-768 (и конкретная реализация) свойством ANO-CCA
   (ciphertext не раскрывает pubkey получателя); то же для X25519-части.
2. **Гибридная композиция.** Корректно ли `HKDF(DH_ss ‖ mlkem_ss)` даёт PQ-стойкость (утечка X25519 не роняет
   K_root ниже ML-KEM, и наоборот)? Отклонённый §5-вариант (coin=HKDF(DH_ss)) коллапсировал ML-KEM к X25519 —
   подтвердить, что текущая генуинная схема этого не делает.
3. **Транскрипт-подпись INTRO** (§10): ed25519 A над `'PLATHO.INTRO.TRANSCRIPT.V1' ‖ keyId_A ‖ keyId_B ‖ R ‖
   sha256(body_KEM_ct) ‖ sha256(ct_root) ‖ view_tag ‖ introNonce`, verify ПОСЛЕ decrypt. Проверить полноту
   транскрипта (нет malleability/reflection).

**Митигация при провале (заложить бюджет ДО immutable):** если ML-KEM-ct не key-private — обёртка key-blinding над
KEM-ct (шифровать ct симметрично на ключе, выводимом из stealth-секрета), чтобы ct не раскрывал получателя. Версионный
хук (suite@7) под PQ-discovery уже согласован — позволяет обновить без редеплоя.

**Последствие провала:** прямой деанон ПОЛУЧАТЕЛЯ — ровно та угроза (recipient-index), ради которой затеян весь
redesign. Несущий гейт.

---

## Что аудитор может ПРИНЯТЬ как данность (внутренне проверено/принято владельцем)
Принятые остатки: тайминг-корреляция диалога; RPC-видимость `{IP → bucketKey}`; out-degree отправителя; квантовый
ретро-граф знакомств (view_tag классический X25519, harvest-now, локализован ребром первого контакта); INTRO
real-time-наблюдение в момент контакта. Это НЕ предмет аудита — owner-подтверждённые остатки в границе угрозы.

## Ссылки на код
Слепая выдача: **`web/crypto/blind-issuance.mjs`** (боевой разбитый протокол — ГЛАВНЫЙ объект свойства 1) ·
`tests/blind-issuance.test.ts` (BLIND-ISSUE-01..04, сквозной) · `tests/blind-schnorr-checksig.test.ts` (прототип VERIFY).
Отправной путь: `web/publish-builder.mjs` (сборка сообщения, подпись `spend_sig` над рамкой).
Гибрид/KEM: `web/crypto/platho-crypto.mjs` (ML-KEM, HKDF, stealth view_tag) · `web/crypto/conv-routing.mjs`
(K_root/K_epoch/bucketKey, recoveryOwner-ключ) · `web/crypto/intro-handshake.mjs` (транскрипт INTRO) ·
`tests/compact-mlkem-option.test.ts`.
Контракты: `contracts/NullifierShard.tact` (CAC 13601/13630-13636 + **13605 привязка рамки**, `frameBindingDigest`) ·
`contracts/RecoveryShard.tact` (**13575** `slotKeyForOwner`, 13574 owner_sig).
Спеки: `artifacts/PLATHO_CLEAN16_HYBRID_CLOSED_SPEC.md` §9-11 (деривации) · `artifacts/PLATHO_CLEAN17_SHARDED_SPEC.md` §4 (CAC).

## Итог для владельца
**Внешний крипто-аудит теперь ОДИН, а не два.** Остался ANO-CCA / key-privacy ML-KEM-конверта — и он несущий: если
KEM-шифротекст раскрывает, к какому публичному ключу инкапсулирован, это прямой деанон ПОЛУЧАТЕЛЯ в обход всей
stealth/bucket-приватности, то есть ровно та угроза, ради которой затеян редизайн. Бюджет и время на
key-blinding-обёртку заложить заранее: если аудит отрицательный, её надо вшить ДО компиляции финального кода.

Свойство 1 снято не потому, что доказано, а потому, что **исчез сам механизм**: слой слепой оплаты удалён как
требующий внешней инфраструктуры (эмитент + реле), запрещённой в проекте. Побочно ушли ежегодная церемония корней и
генерация 2-из-3 холодных ключей — этой операционной нагрузки на владельце больше нет.

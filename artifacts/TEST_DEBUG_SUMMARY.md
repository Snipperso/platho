# Platho test debug summary

Debug date: 2026-05-15

## Finding

The contract test files themselves are not the source of a deterministic hang.

A full Vitest run with per-test timestamp instrumentation completed successfully:

```text
Test Files: 10 passed
Tests: 59 passed
Duration: ~50s
```

The earlier apparent hang occurred in a custom Node sequential `npx vitest ...` debug runner, not in a specific test file. Running suspect files directly passed.

## Added tooling

```text
vitest.all.config.ts       sequential full-suite config
vitest.debug.config.ts     sequential full-suite config with setup timestamp logging
tests/debug-setup.ts       beforeEach/afterEach test timestamp logger
scripts/debug-test-files.sh per-file bash runner using system timeout
```

## Package scripts

```text
npm test                  -> full suite via vitest.all.config.ts
npm run test:all          -> full suite, verbose
npm run test:debug        -> full suite with per-test timestamp log
npm run test:file -- <f>  -> single file helper
npm run test:binding      -> deployment binding only
npm run test:debug:files  -> per-file bash timeout runner
```

## Debug files

```text
vitest-full-debug.log     generated by npm run test:debug
test-debug-bash.log       generated by npm run test:debug:files
test-debug-bash.jsonl     machine-readable per-file results
```

## Interpretation

Use `npm run test:debug` when the full suite appears to hang. The last `TEST_START` without matching `TEST_END` identifies the stuck test.

Use `npm run test:debug:files` when the issue appears to be per-file startup/teardown or process orchestration.
